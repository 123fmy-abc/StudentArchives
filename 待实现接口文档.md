# 待实现接口设计文档

- **日期**：2026-09-11（2026-09-15 更新：两个接口均已实现）
- **来源**：前端《2026-09-11-后端问题清单》P1-3 —— 前端已在调用、后端尚不存在的 2 个接口
- **状态**：**已实现**。正式接口契约见《学生端接口文档》9.8（重命名会话）与 4.1.7（多维度能力画像）；
  本文档保留为设计依据与实现口径说明。`/archive/dimensions` 的「待确认」小节已按 2026-09-15 结论收敛。
- **核对方式**：对后端 47 条 `@RequestMapping` 路由与前端调用做了全量交叉核对，除下列 2 条外其余 243 条均匹配。

---

## 一、`PUT /ai/conversations/{conversationId}`

- **接口中文名**：重命名对话会话
- **请求方式**：`PUT`
- **路径**：`/api/v1/ai/conversations/{conversationId}`
- **鉴权**：需登录（JWT）。会话所属人必须是当前登录用户，否则返回 30001。
- **权限码**：无（与 `/ai/**` 其余接口一致，仅要求登录 + 归属校验）
- **前端调用位置**：`src/shared/api/ai-chat.ts:272`
- **前端现状**：后端未实现时前端 catch 吞掉异常并乐观更新本地标题 —— 即「明知未实现，靠乐观更新掩盖」。
  实现后前端应移除该兜底，改为按真实响应确认标题。

### Path 参数

| 字段           | 类型 | 必填 | 说明       |
| -------------- | ---- | ---- | ---------- |
| conversationId | long | 是   | 会话主键 ID |

### Body 参数

| 字段  | 类型   | 必填 | 说明                     |
| ----- | ------ | ---- | ------------------------ |
| title | string | 是   | 新标题，去除首尾空格后 1~100 字符 |

- **请求参数示例**

```json
{
    "title": "计算机保研规划"
}
```

### 响应参数

| 字段           | 类型   | 说明                  |
| -------------- | ------ | --------------------- |
| conversationId | long   | 会话 ID               |
| title          | string | 重命名后的标题        |
| updatedAt      | string | 更新时间，ISO 8601 带时区 |

- **响应示例**

```json
{
    "code": 0,
    "message": "重命名成功",
    "data": {
        "conversationId": 12,
        "title": "计算机保研规划",
        "updatedAt": "2026-09-11T16:20:00+08:00"
    }
}
```

### 错误码

| code  | HTTP | 场景                                    |
| ----- | ---- | --------------------------------------- |
| 0     | 200  | 成功                                    |
| 10001 | 400  | title 为空或超过 100 字符               |
| 20001 | 401  | 未登录                                  |
| 30001 | 404  | 会话不存在，或不属于当前登录用户        |

### 实现要点（可复用现有代码）

- 实体字段已存在：`entity/ai/AiConversation.java:24` —— `@Column(name = "title", length = 255) private String title;`
  无需改表结构。
- 归属校验可直接复用 `service/Lzw/AiConversationService.java` 的
  `loadOwnedConversation(conversationId, userId)`（约 422 行，内部以
  `userId.equals(conv.getUserId())` 判定）。
- **建议实现位置**：`controller/Fmy/` 下新建控制器（与 `AiConversationController` 同为
  `@RequestMapping("/ai")`，方法级路径 `PUT /conversations/{conversationId}` 不冲突），
  以免改动 `Lzw` 包的既有文件。

---

## 二、`GET /archive/dimensions`

- **接口中文名**：学生多维度能力画像
- **请求方式**：`GET`
- **路径**：`/api/v1/archive/dimensions`
- **鉴权**：需登录（JWT），返回**当前登录学生本人**的画像。
- **权限码**：无（学生端个人数据；仅登录 + 本人数据）
- **前端调用位置**：`src/shared/api/archive.ts:210`（调用方 `src/app/stores/archive.ts:167`）
- **前端注释**：指向 `docs/api.md 3.4` —— **该文档不在本仓库，后端无法据此实现，需前端提供**。

### Query 参数

| 字段       | 类型 | 必填 | 说明                                                   |
| ---------- | ---- | ---- | ------------------------------------------------------ |
| semesterId | long | 否   | 学期 ID；不传=全部学期累计（与 `GET /messages/unified` 的 archive 参数口径一致） |

### 响应参数（建议契约）

`data` 为对象，包含画像汇总与各维度得分：

| 字段              | 类型   | 说明                                       |
| ----------------- | ------ | ------------------------------------------ |
| userId            | long   | 学生用户 ID                                |
| semesterId        | long   | 生效的学期筛选，为 null 表示全部学期       |
| totalScore        | number | 各维度得分之和                             |
| dimensions        | array  | 维度得分列表，见下表                       |
| dimensions[].dimensionCode | string | 维度编码（`ability_dimensions.dimension_code`） |
| dimensions[].dimensionName | string | 维度名称（`ability_dimensions.dimension_name`） |
| dimensions[].description   | string | 维度说明（可为空）                         |
| dimensions[].score         | number | 该维度累计得分                             |
| dimensions[].ratio         | number | 该维度占总分比例，保留 4 位小数；总分为 0 时为 0 |
| dimensions[].sort          | int    | 维度排序号，与维度字典一致                 |

- **响应示例**

```json
{
    "code": 0,
    "message": "success",
    "data": {
        "userId": 1,
        "semesterId": null,
        "totalScore": 86.5,
        "dimensions": [
            { "dimensionCode": "academic", "dimensionName": "学业能力", "description": "课程成绩与学业表现", "score": 45.0, "ratio": 0.5202, "sort": 1 },
            { "dimensionCode": "practice", "dimensionName": "实践能力", "description": "竞赛、项目与实践经历", "score": 26.5, "ratio": 0.3064, "sort": 2 },
            { "dimensionCode": "innovation", "dimensionName": "创新能力", "description": "科研与创新成果", "score": 15.0, "ratio": 0.1734, "sort": 3 }
        ]
    }
}
```

### 错误码

| code  | HTTP | 场景             |
| ----- | ---- | ---------------- |
| 0     | 200  | 成功             |
| 20001 | 401  | 未登录           |

> 维度字典为空或该生暂无得分时，返回 `dimensions: []`、`totalScore: 0`，**不报错** ——
> 前端应正常渲染空态（与本次 P0-3 的教训一致：不要用异常表达"没数据"）。

### 实现要点（数据模型已具备，无需改表）

| 用途         | 表 / 实体                                                              | 关键字段                                             |
| ------------ | ---------------------------------------------------------------------- | ---------------------------------------------------- |
| 维度字典     | `ability_dimensions`（`entity/foundation/AbilityDimension.java`）       | `dimension_code`, `dimension_name`, `description`, `sort`, `status=1` |
| 学生成长事件 | `growth_timelines`                                                     | `user_id`（学生ID）, `semester_id`, `status`（2=已通过） |
| 维度得分明细 | `growth_timeline_abilities`                                            | `timeline_id`, `dimension_code`, `score`             |
| 指标→维度    | `evaluation_indicators.dimension_code`                                 | 关联 `ability_dimensions.dimension_code`             |

- **推荐取数**：以学生 `growth_timelines`（`status=2` 已通过、可选 `semester_id` 过滤）
  JOIN `growth_timeline_abilities`，按 `dimension_code` 聚合 `SUM(score)`，
  再 LEFT JOIN `ability_dimensions` 补名称/说明/排序。
- 用 LEFT JOIN 保留「字典里有、该生尚无得分」的维度，避免画像缺角。

### 待确认（2026-09-15 已裁定，实现按此口径）

1. `score` 的口径：**累计原始分**（`SUM(growth_timeline_abilities.score)`，不做归一化）—— 已确认。
2. 是否按 `event_type` 拆分：**不拆分**，只输出各能力维度的累计分。
3. 等级标签 / 同年级横向对比：**不做**。后续如需，另开接口，不改本接口结构。
4. `docs/api.md 3.4` 的原始定义 —— 该文档不在本仓库；本接口结构以前端已确认的上表为准。
5. 是否提供 `GET /archive/dimensions/{userId}` 供教师/管理员查看学生画像：**本次不做**，仅覆盖学生本人。
   若后续需要，教师端应走独立路径并按 `role_scopes` 做范围校验，不复用本接口。

---

## 三、后端回复口径（对应问题清单第三部分）

| 清单问题                     | 回复                                                                                   |
| ---------------------------- | -------------------------------------------------------------------------------------- |
| 1. 线上库权限/范围条数       | 见《后端问题清单》修复说明；根因是这两张表的种子只在手工脚本中，未纳入 Flyway 迁移       |
| 2. P0-2 / P0-4 / P0-5 修复时间点 | 本次一并修复：V35 迁移补授权与范围（含教师 `log:view`）；HTTP 层收紧三个前缀与兜底；委托模块补权限校验 |
| 3. P1-1 / P1-2 权限码裁定    | P1-1 的 7 个码已在种子中定义并授予 admin；P1-2 中 4 个码实际已校验，剩余 2 个是菜单码，维持现状 |
| 4. P1-3 两个缺失接口         | **2026-09-15 已实现**：`PUT /ai/conversations/{conversationId}`（学生端文档 9.8）、`GET /archive/dimensions`（学生端文档 4.1.7）。`/archive/dimensions` **无替代接口**；口径按本文档「待确认」小节 2026-09-15 结论 |
| 5. 补充：委托页被委托人下拉   | **2026-09-15 新增** `GET /teacher/delegations/candidates`（教师端文档 15.4），教师调用不再 403 |
| 6. 补充：`/public/statistics` | P0-5 收紧兜底时把 `PUBLIC_AUTH_PATHS` 里的 `/public` 改成 `/public/**`，导致 JWT 过滤器不再跳过该路径、登录页统计接口返回 401。**已于 2026-09-15 修复**（过滤器改用 Ant 通配匹配） |

---

## 四、前端需要配合的改动

1. **移除「日志查看」菜单上的通配码 `*`** —— 前置条件已满足：V35 为 teacher / counselor 授予了 `log:view`。
2. **移除 AI 会话重命名的乐观更新兜底** —— `PUT /ai/conversations/{conversationId}` 已于 2026-09-15 实现，改为按真实响应确认标题。
3. **审批委托页「被委托人」下拉改调教师端接口** —— 后端已提供 `GET /teacher/delegations/candidates`（教师端文档 15.4），
   不要再调管理端 `GET /admin/users`（教师调用必然 403）。该模块保持教师专属，管理员不在该模块指派审核人。
4. `/archive/dimensions` **已于 2026-09-15 实现**，响应结构见学生端文档 4.1.7，可直接联调。
   注意：本接口对无效 `semesterId` 返回 `10001`，请勿把该错误当作「无数据」处理。
