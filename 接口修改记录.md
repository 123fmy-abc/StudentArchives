# 举例：

**操作：修改   操作人：###   操作时间：2026-4-12**

# `POST /auth/register`

- **接口中文名**：用户注册
- **请求体**：`RegisterRequest`

**Body 参数**

| 字段                  | 类型   | 必填 | 说明                                                        |
| --------------------- | ------ | ---- | ----------------------------------------------------------- |
| account               | string | 是   | 账号（学号/工号），为11位数字                               |
| name                  | string | 是   | 用户名                                                      |
| email                 | string | 是   | 邮箱                                                        |
| password              | string | 是   | 密码，至少8位，必须以英文字母开头，且只能包含英文字母和数字 |
| password_confirmation | string | 是   | 确认密码                                                    |
| code                  | string | 是   | 验证码，必须是6位                                           |

- **请求参数示例**

```json
{
    "account":"25010420551",
    "name":"伏明月1",
    "email": "3596976474@qq.com",
    "password": "fmy111111",
    "password_confirmation":"fmy111111",
    "code":"230699"
}
```

- **响应 `data`**：用户注册时信息

| 字段    | 类型   | 说明        |
| ------- | ------ | ----------- |
| id      | string | 注册用户 ID |
| account | string | 账号        |
| name    | number | 用户名      |
| email   | string | 邮箱        |
| role    | string | 角色        |

- **错误码**

| status | message                      | 条件               |
| ------ | ---------------------------- | ------------------ |
| 422    | 该账号已被注册，邮箱已被注册 | 账号和邮箱已被注册 |
| 400    | 验证码错误或已过期           | 验证码错误或过期   |

------

**操作：新增   操作人：fmy   操作时间：2026-08-20**

### 教师端数据导出模块：复用管理端导出引擎，补齐任务列表/删除

- **背景**：教师端数据导出模块（《教师端接口文档》十二、数据导出模块 12.1~12.4）此前仅存在于文档为契约，无代码实现。代码库中管理端已具备可复用的「一键导出学生档案」引擎（`AdminExportService.submitArchiveExport`，异步导出 pdf/xlsx，`filter_conditions` 快照）、`export_templates` 模板与 `export_jobs`/`export_operation_logs`/`anonymization_maps` 实体与仓储；但**任务列表与任务删除两个能力任何端均未实现**（`ExportJobRepository` 原为空仓储）。
- **变更**：
  - 新增 `TeacherExportController`（`/teacher/exports`）— 四个端点，权限口径均为「管理员放行或持有 `export:execute`」：
    - `GET /teacher/exports/templates`（12.1）：复用 `export_templates`，仅返回本校启用模板（`status=1`）
    - `POST /teacher/exports`（12.2）：复用 `AdminExportService.submitArchiveExportByTeacher`（管理端「一键导出学生档案」引擎，契约同管理端 5.11 `ArchiveExportRequest`）
    - `GET /teacher/exports`（12.3）：**新增**，`export_jobs` 按 `operator_id` 分页查询 + 模板名/下载链接解析
    - `DELETE /teacher/exports/{jobId}`（12.4）：**新增**，软删除 `export_jobs` + 写 `export_operation_logs` 删除审计（action=3），仅本人或管理员可删
  - 新增 `TeacherExportService` — 模板列表/任务列表/删除直接复用仓储；提交导出委托引擎（权限与 `role_scopes` 范围校验在引擎内完成）
  - 新增响应 DTO：`TeacherExportTemplateResponse` / `TeacherExportJobItem` / `TeacherExportDeleteResponse`
  - `AdminExportService` — 抽取 `doSubmitArchiveExport` 共用提交流程；新增教师入口 `submitArchiveExportByTeacher`（权限 `export:execute` + 教师 `role_scopes` 范围校验，学校级授权覆盖校内全部范围、学院/专业/班级按同类型匹配、年级粗粒度，越权 `20005`）；`buildDownloadUrl` 改为包可见供教师端复用
  - `ExportJobRepository` — 新增 `findByOperatorId`（分页）+ `softDeleteById`（native 软删除，模式同 `AdminExportTemplateRepository.softDelete`）
  - `教师端接口文档.md` — 版本 V5.10 → V5.11；十二、数据导出模块逐节对齐真实契约：12.2 请求参数对齐 `ArchiveExportRequest`（新增 `fileType` 必填/`grade`/`sections`/`archiveStatus`/`purpose`/`includeMetadata`，移除 `filterConditions`/`isAnonymized`，响应 `exportJobId` → `jobId`）；12.3/12.4 补充请求参数、响应示例与实现说明；附录 A 权限对照表补充三行
- **涉及文件**：
  - `TeacherExportController.java`、`TeacherExportService.java` — 新增
  - `TeacherExportTemplateResponse.java`、`TeacherExportJobItem.java`、`TeacherExportDeleteResponse.java` — 新增
  - `AdminExportService.java` — 抽取共用流程 + 教师入口 + 教师范围校验 + `buildDownloadUrl` 包可见
  - `ExportJobRepository.java` — 分页查询 + 软删除
  - `教师端接口文档.md`（本节）
- **验证**：`mvn compile -q` 通过。
- **说明**：无数据库结构变更（`export_jobs`/`export_templates`/`export_operation_logs`/`role_scopes` 均已存在），`数据库修改记录.md` 无需新增记录。`isAnonymized` 匿名化研究导出仍仅限管理端 `POST /admin/exports/research`，教师端不开放；任务删除为软删除，不物理删除 OSS 文件（下载链接本身 7 天过期）。

------

**操作：新增   操作人：fmy   操作时间：2026-08-20**

### 教师端消息中心模块：复用学生端 MessageService 实现

- **背景**：教师端消息中心模块此前仅存在于《教师端接口文档》（十三、消息中心模块）为文档契约，无代码实现。学生端 `MessageService` / `MessageController` 完全按 `user_messages.user_id` 归属（教师/学生同为 `users` 记录），无学生特有逻辑，与 14.2 联系信息更新同构，可直接复用。
- **变更**：
  - 新增 `TeacherMessageController`（`/teacher/messages`）— 三个端点直接委托 `MessageService` 同名方法，仅 URL 前缀不同：
    - `GET /teacher/messages`（复用学生端 5.1 消息列表）
    - `PUT /teacher/messages/{messageId}/read`（复用学生端 5.2 标记已读）
    - `PUT /teacher/messages/{messageId}/archive`（复用学生端 5.4 归档）
  - 请求/响应 DTO（`MessageListResponse` / `MessageArchiveResponse` / `PageParam`）与参数校验完全复用，无独立服务与 DTO。
  - **不直接让前端调用 `/messages` 充当教师端接口**：教师端统一走 `/teacher/*` 前缀（安全配置按前缀做教师角色校验），且十三只列 3 个端点；若后续需要取消归档/删除/通知设置，可同样复用学生端 5.5/5.6/5.7/5.9/5.10 服务。
- **涉及文件**：
  - `TeacherMessageController.java` — 新增，教师端消息中心控制器
  - `教师端接口文档.md` — 版本 V5.9 → V5.10；十三、消息中心模块补充实现说明
  - `接口修改记录.md`（本节）
- **验证**：`mvn compile -q` 通过。
- **说明**：无数据库结构变更，`数据库修改记录.md` 无需新增记录。

------

**操作：新增   操作人：fmy   操作时间：2026-08-20**

### 教师端个人中心 14.1 获取教师个人信息：复用 /auth/me 逻辑实现

- **背景**：14.1（`GET /teacher/profile`）此前仅存在于《教师端接口文档》为文档契约，无代码实现。接口需返回 11 个字段，其中 9 个（`userId`/`userNo`/`name`/`email`/`phone`/`gender`/`genderLabel`/`roles`/`scopes`/`avatar`）与 `GET /auth/me`（`AuthService.getCurrentUser`）来源一致可复用；仅 `collegeName`、`title` 为教师特有字段（来源 `teacher_profiles`），`/auth/me` 不读取该表。
- **变更**：
  - 新增 `TeacherProfileResponse`（`dto/Fmy/profile/response/`）— 14.1 响应 DTO，字段与文档契约精确对齐；`email`/`phone` 沿用 `@Sensitive` 脱敏；`scopes` 复用 `UserScopeInfoResponse`。
  - 新增 `TeacherProfileService.getTeacherProfile(userId)` — 复用 `AuthService.getCurrentUser` 组装 9 个公共字段，再补充 `collegeName`（`teacher_profiles.college_id` → `colleges.name`）与 `title`（`teacher_profiles.title`）；教师档案缺失时两字段返回 `null`。
  - `TeacherProfileController` 新增 `GET /teacher/profile` 端点（14.1）。
  - **不直接让前端调用 `/auth/me` 充当 14.1**：`/auth/me` 为教师/学生/管理员三端共用契约，且缺 `collegeName`（`/auth/me` 返回的是学校 `schoolName`，非学院）与 `title`；向其添加教师档案字段会污染学生/管理员响应。改为独立端点 + 复用底层查询逻辑，契约与文档精确对齐。
- **涉及文件**：
  - `TeacherProfileResponse.java` — 新增
  - `TeacherProfileService.java` — 新增
  - `TeacherProfileController.java` — 新增 `GET /teacher/profile`
  - `教师端接口文档.md` — 版本 V5.8 → V5.9；14.1 补充实现说明
  - `接口修改记录.md`（本节）
- **验证**：`mvn compile -q` 通过。
- **说明**：无数据库结构变更，`数据库修改记录.md` 无需新增记录；14.3（改密，复用 `/auth/password`）仍为文档契约，未在本节实现范围内。

------

**操作：新增   操作人：fmy   操作时间：2026-08-20**

### 教师端个人中心 14.2 更新个人联系信息：复用学生端实现

- **背景**：教师端个人中心模块此前仅存在于《教师端接口文档》（十四、个人中心模块）无代码实现；14.2 原契约与学生端 4.1.1 存在两处不一致——请求体含 `avatar` 字段、且字段均为可选（部分更新语义），无法直接复用学生端实现。
- **变更**：
  - 新增 `TeacherProfileController`（`PUT /teacher/profile/contact`），直接复用 `ProfileService.updateContact` 与 `ContactUpdateRequest` / `ContactUpdateResponse` DTO——数据表同为 `user_contact_infos`（按 `user_id` 区分，无学生特有逻辑），仅 URL 前缀不同。
  - 14.2 契约对齐学生端：请求体**移除 `avatar` 字段**，头像更新复用通用上传头像接口 `POST /common/upload/avatar`（已写入 `user_contact_infos.avatar`）；`email`/`phone`/`address` 改为**必填（全量更新语义）**，空串表示清空。
- **涉及文件**：
  - `TeacherProfileController.java` — 新增，教师端个人中心控制器（当前仅 14.2 联系信息更新）
  - `教师端接口文档.md` — 版本 V5.7 → V5.8；14.2 请求参数表移除 `avatar`、字段改必填，补充全量更新语义、头像复用与实现说明
  - `接口修改记录.md`（本节）
- **验证**：`mvn compile -q` 通过。
- **说明**：无数据库结构变更，`数据库修改记录.md` 无需新增记录；14.1（`GET /teacher/profile`）、14.3（改密，复用 `/auth/password`）仍为文档契约，未在本节实现范围内。

------

**操作：修改   操作人：fmy   操作时间：2026-08-16**

### 修复 `GET /profile/career-plans/{planId}/download`（4.5）/`preview`（4.5.1）`purpose` 参数无效

- **背景**：`purpose` 区分 internal（带屏幕水印并缓存）/ external（无水印、不缓存），但 internal 与 external 共用同一 `file_uploads` 附件行（`biz_type='career_plan_export'`）+ `career_plans.file_id` 缓存槽。external 生成时会复用该行、把 OSS 里的带水印对象替换成无水印对象，internal 兜底复用（`findExportRelation`）也不区分用途——只要先下载过一次 external，之后 internal 下载复用的就是无水印文件，两种用途拿到完全相同的 PDF，`purpose` 看似无效。
- **变更**：
  - external 改用独立业务类型 `career_plan_export_external`（新增 `AttachmentBizTypeEnum.CAREER_PLAN_EXPORT_EXTERNAL`），不再复用/覆盖 internal 的 `career_plan_export` 行，也不写入 `career_plans.file_id`。
  - `generateCareerPlanFile()` 按用途选择 `bizType`，仅复用同用途既有附件行（internal 优先 `file_id` 主文件，external 只查外部投递行）。
  - `findExportRelation(planId, userId, bizType)` 增加 `bizType` 参数；internal 兜底（`file_id` 丢失）只查 `career_plan_export`。
- **涉及文件**：
  - `AttachmentBizTypeEnum.java` — 新增 `CAREER_PLAN_EXPORT_EXTERNAL("career_plan_export_external", "职业规划外部投递导出文件")`
  - `ProfileCareerPlanService.java` — `resolveExportFile` / `generateCareerPlanFile` / `findExportRelation` 按用途隔离附件行
- **历史数据说明**：修复前被 external 覆盖的旧 `career_plan_export` 行仍指向无水印文件，需清理后下次 internal 下载才会重新生成带水印文件（一次性清理 SQL 见对话，仅在开发库执行）。

------

**操作：修改   操作人：fmy   操作时间：2026-08-16**

### 职业规划下载/预览 `purpose` 默认值改为 external（无水印）

- **背景**：职业规划下载（4.5）/预览（4.5.1）默认 `purpose=internal`（屏幕水印+打印隐藏），与简历（4.19）、档案导出（4.17）默认 `external`（无水印）不一致；学生下载规划常直接转发给实习公司/HR/导师，默认带水印显得不正式。
- **变更**：职业规划下载/预览 `purpose` 默认值由 `internal` 改为 `external`——不传 `purpose` 时默认无水印；需要内部屏幕水印时显式传 `purpose=internal`（水印文件写入 `career_plans.file_id` 缓存复用，无水印文件每次重新生成、不写缓存）。
- **涉及文件**：
  - `ProfileCareerPlanController.java` — download / preview 的 `@RequestParam defaultValue` 改为 `external`
  - `ProfileCareerPlanService.java` — `resolveExportFile` 空值兜底、`getCareerPlanPreviewUrl` 返回的 `purpose` 默认改为 `external`
  - `学生端接口文档.md` — 4.5 / 4.5.1 的 `purpose` 默认值说明与响应示例同步更新

------

**操作：修改与补全   操作人：fmy   操作时间：2026-07-28**

本次根据认证模块实际代码（`AuthController`、`AuthService` 及相关 DTO）对 `学生端接口文档.md` 中“一、认证模块”进行校对与补全，主要差异与调整如下：

### 1. `POST /auth/login`
- **新增请求字段**：`rememberMe`（Boolean，非必填，默认 `false`）。为 `true` 时返回 `refreshToken` 且访问令牌有效期更长。
- **调整错误码**：账号不存在、密码错误、账号被禁用统一返回 `20008`；新增 `6`（请求过于频繁，登录失败次数过多）。

### 2. `GET /auth/captcha`
- **修正验证码字符集说明**：由“4 位大写字母+数字”调整为“4 位大小写英文字母+数字”。

### 3. `POST /auth/logout`
- **调整 `all` 字段说明**：当前后端忽略该字段，调用即吊销用户全部已签发 Token。

### 4. `PUT /auth/password`
- **新增密码强度规则说明**：长度 6-32 位，必须同时包含大写字母、小写字母、数字和特殊字符。

### 5. `POST /auth/password/reset`
- **修正错误码**：邮件发送失败由 `50007` 修正为 `50004`。
- **新增错误码**：`20006` 账号被禁用、`6` 请求过于频繁（同一邮箱 60 秒内限发）。

### 6. `POST /auth/password/reset/confirm`
- **新增密码强度规则说明**：同 `PUT /auth/password`。
- **移除错误码**：`50006` 验证码错误次数超限（当前实现无此独立错误码）。

### 7. 新增接口 `POST /auth/refresh`
- **用途**：使用 `refreshToken` 换取新的 `accessToken` 与 `refreshToken`（Token Rotation）。
- **请求字段**：`refreshToken`（String，必填）。
- **响应字段**：`accessToken`、`refreshToken`、`tokenType`、`expiresIn`。
- **错误码**：`20004` 刷新令牌已过期、`20003` 刷新令牌无效、`20002` Token 已失效、`20006` 账号被禁用、`6` 并发刷新。

------

**操作：修改   操作人：fmy  操作时间：2026-07-29**

### 1. `POST /auth/login` 登录锁定策略调整
- **锁定时间**：由 15 分钟调整为 2 分钟。
- **错误提示**：触发锁定时，错误消息返回剩余锁定秒数，例如 `登录失败次数过多，请 87 秒后重试`。
- **涉及文件**：`LoginAttemptLimiter.java`、`AuthService.java`。

------

**操作：新增   操作人：fmy   操作时间：2026-07-30**

### 1. 新增头像上传接口 `POST /common/upload/avatar`

- **用途**：独立头像上传，不走临时存储流程，直接上传至 OSS `avatar/{uuid}.{ext}` 正式目录，并更新 `user_contact_infos.avatar` 字段。
- **请求参数（multipart/form-data）**：`file`（MultipartFile，必填，仅支持 jpg/png/gif/webp/bmp，最大 2MB）。
- **响应字段**：`avatarUrl`（头像完整访问 URL，带签名 30 天有效）、`objectKey`（OSS 对象路径）。
- **涉及文件**：
  - `OssFileService.java` — 新增 `uploadAvatar()` 方法
  - `CommonService.java` — 新增 `uploadAvatar()` 方法
  - `CommonController.java` — 新增 `POST /common/upload/avatar` 端点
  - `AvatarUploadResponse.java` — 新增 DTO
  - `学生端接口文档.md` — 新增 2.1.4 节
- **错误码**：`10006` 文件格式错误、`10007` 文件过大、`10008` 上传失败
- **文档说明**：已在 `学生端接口文档.md` 通用接口中新增 2.1.4 节，注明头像不走临时存储、签名 URL 30 天有效期、生产环境建议开启公共读。

------

**操作：校对与补全   操作人：fmy   操作时间：2026-08-01**

根据个人中心模块实际代码（`ProfileController`、`ProfileCareerPlanController`、`ProfileService`、`ProfileCareerPlanService`、`ProfileExportService`）对 `学生端接口文档.md` 四「个人中心模块」进行校对，主要差异与调整如下：

### 1. `GET /profile/info`（4.1）
- **新增响应字段**：`academicInfo.politicalStatusLabel`（政治面貌展示名称）。`academicInfo.politicalStatus` 仍返回 `student_profiles.political_status` 的**字典编码**（如 `league_member`），`politicalStatusLabel` 为后端按字典表 `political_status` 解析出的展示名称（如“共青团员”），前端直接展示，无需再调用 `GET /common/dict` 映射。
- **涉及文件**：
  - `ProfileService.java` — `getProfileInfo()` 新增 `resolveDictLabel()` 按字典类型+编码解析展示名称
  - `ProfileInfoResponse.java` — `AcademicInfo` 新增 `politicalStatusLabel` 字段
  - `学生端接口文档.md` — 4.1 响应示例补充 `politicalStatusLabel`，说明由“前端映射”调整为“后端已返回”

### 2. `GET /profile/growth-timeline`（4.2）
- **新增 `viewType=tree` / `viewType=ring` 实现**：
  - `tree`：按 **学年 → 学期 → 事件** 三级分组返回树形结构，学年由学期名称推导（如 `2023-2024-1` → `2023-2024`），无学期归属事件归入 `"其他"` 分组。
  - `ring`：按事件日期所在**自然年**聚合，返回年度事件数（`eventCount`）与事件类型分布（`typeDistribution`），供环形时间轴组件渲染。
- **文档**：`学生端接口文档.md` 4.2 补充了 tree / ring 两种视图的响应结构示例。
- **涉及文件**：
  - `ProfileService.java` — `getGrowthTimeline` 新增 `viewType` 分支，新增 `buildTree` / `buildRing` / `academicYearOf` / `yearOf` 方法
  - `GrowthTimelineResponse.java` — 新增 `tree` / `ring` 字段与 `YearGroup` / `SemesterGroup` / `RingYear` 嵌套结构
  - `ProfileController.java` — `viewType` 参数透传给服务层

### 3. `POST /profile/export`（4.17）
- **调整 `fileType` 参数**：由 `pdf` / `word` 调整为仅支持 `pdf`（`word` 为后续版本），默认 `pdf`。
- **调整响应结构**：由 `status=0(待处理)/downloadUrl=null` 调整为同步导出结果——`status=2(已完成)`、新增 `fileId`（`file_uploads.id`）、`downloadUrl`（签名链接，默认 30 分钟有效）、`expireAt`（业务有效期 7 天）；响应 `message` 由“导出任务已创建”改为“导出成功”。
- **调整业务逻辑说明**：导出由“后台任务”调整为同步生成；`fileId` 在 7 天有效期内可通过 `GET /common/files/{fileId}/download` 重新获取下载链接。
- **涉及文件**：
  - `ProfileExportService.java` — `submitExport()` 同步完成并返回真实状态与下载链接；导出完成消息 `category` 改为文档 5.1 支持的 `system_notice`；`getExportPreview()` 的 `dataVersion` 改为按当前学期评分规则版本实时计算
  - `ExportSubmitResponse.java` — 新增 `fileId` 字段
  - `ProfileController.java` — `getExportPreview` 增加当前用户参数

### 4. `PUT /profile/career-plans/{planId}/actions/{actionId}/status`（4.12）
- **实现补齐**：对齐文档要求，`status=2` 时同步写入 `career_actions.completed_at`（新增数据库列，见 `V18__career_action_completed_at.sql`）与 `completion_rate=100`。

------

**操作：修改   操作人：fmy   操作时间：2026-08-01**

### 5. `GET /profile/info`（4.1）短板分析项新增关联字段
- **新增响应字段**：`weaknessAnalysis[]` 新增可选字段 `relatedType`（关联模型：`archive` / `award_application` / `career_plan`）、`relatedId`（关联记录 ID），用于标识短板关联的具体业务记录。无具体来源时返回 `null`，配合 `@JsonInclude(NON_NULL)` 空值不输出，现有前端无感知。
- **涉及文件**：
  - `ProfileInfoResponse.java` — `WeaknessItem` 新增 `relatedType` / `relatedId` 字段，并补充 `@JsonInclude(JsonInclude.Include.NON_NULL)`（与项目其他响应 DTO 保持一致）
  - `ProfileService.java` — `toWeaknessItem()` 补充 `relatedType` / `relatedId` 映射
  - `学生端接口文档.md` — 4.1 响应说明补充可选字段说明

------

**操作：修改   操作人：fmy   操作时间：2026-08-01**

### 6. `PUT /profile/contact`（4.1.1）移除 `avatar` 字段，头像统一走专用上传接口

- **背景**：`user_contact_infos.avatar` 存在两个写入入口（`PUT /profile/contact` 与 `POST /common/upload/avatar`），且 contact 接口对 `avatar` 为纯字符串直写、无任何校验，可绕过上传接口的图片格式/大小校验，同时造成单一字段写入路径混乱。头像是画像/媒体字段，语义上不属于「联系信息」。
- **变更**：
  - `ContactUpdateRequest.java` — 移除 `avatar` 字段（请求体仅保留 `email` / `phone` / `address`，全字段可选）
  - `ProfileService.java` — `updateContact()` 移除 `contact.setAvatar(request.getAvatar())` 分支
- **未变更（保持只读展示）**：`ContactUpdateResponse.avatar`、`ProfileInfoResponse.ContactInfo.avatar` 仍返回头像 URL，仅供前端展示；`POST /common/upload/avatar` 仍是头像唯一写入入口，上传成功后直接更新 `user_contact_infos.avatar`。
- **前端调用调整**：更新头像仅需调用 `POST /common/upload/avatar`，无需再通过 `PUT /profile/contact` 提交头像。
- **涉及文件**：
  - `ContactUpdateRequest.java`
  - `ProfileService.java`
  - `学生端接口文档.md` — 4.1.1 请求参数表移除 `avatar` 行并补充头像维护说明；2.1.4 注明本接口为头像唯一写入入口

------

**操作：修改   操作人：fmy   操作时间：2026-08-01**

### 7. `PUT /profile/contact`（4.1.1）与 `PUT /profile/basic-info`（4.1.5）改为全量更新语义

- **背景**：原实现为「部分更新」——请求字段为空（`null`）时静默跳过，全空请求体也会返回「更新成功」但不做任何变更，造成误导性成功且字段设置后无法清空。
- **变更**：
  - `ContactUpdateRequest.java` — `email` / `phone` / `address` 全部改为必填（`@NotNull`），缺失任一字段返回参数校验错误（`PARAM_ERROR`）；传空字符串表示清空对应字段
  - `BasicInfoUpdateRequest.java` — `politicalStatus` 改为必填（`@NotBlank`），缺失返回 `PARAM_ERROR`
  - `ProfileService.java` — `updateContact()` 移除 `null` 判断，全量写入三字段；`updateBasicInfo()` 移除「为空保留旧值」逻辑，改为校验字典编码存在后直接写入
- **前端调用调整**：两个接口均需在表单提交时携带全部字段；无需修改的字段以当前值回传，需清空的字段传空字符串。
- **涉及文件**：
  - `ContactUpdateRequest.java`
  - `BasicInfoUpdateRequest.java`
  - `ProfileService.java`
  - `学生端接口文档.md` — 4.1.1 / 4.1.5 请求参数表必填列调整为「是」，并补充全量更新语义说明

------

**操作：新增   操作人：fmy   操作时间：2026-08-02**

### 新增删除个人兴趣标签接口 `DELETE /profile/interests/{id}`

- **背景**：`PUT /profile/interests`（4.11）仅支持新增与更新，学生误加或不再需要的兴趣标签无法删除，个人兴趣标签缺少删除能力；项目内文件等资源已有 `DELETE` 端点，补全删除以保持资源操作完整性（增删改查）。
- **用途**：删除当前登录学生自己的单个兴趣标签。
- **请求**：`DELETE /profile/interests/{id}`，路径参数 `id`（`user_interests.id`）。
- **业务规则**：
  - 标签不存在返回 `30001 兴趣标签不存在`；因 `@SQLRestriction("deleted_at IS NULL")` 自动过滤已软删除记录，已删除标签重复删除同样返回 `30001`。
  - 归属校验：非本人标签返回 `5 无访问权限`（与 `updateInterests` 的越权口径一致）。
  - 软删除：仅置 `deleted_at` 为当前时间（`user_interests` 表无 `deleted_by` 列），配合生成列 `is_deleted_null` 与条件唯一索引 `uk_ui_user_tag`，删除后可重新添加同名标签。
- **涉及文件**：
  - `UserInterestRepository.java` — 新增 `softDeleteById()`（native query 绕过 `deleted_at updatable=false` 限制）
  - `ProfileService.java` — 新增 `deleteInterest()` 方法
  - `ProfileController.java` — 新增 `DELETE /profile/interests/{id}` 端点
  - `学生端接口文档.md` — 新增 4.11.1 节

------

**操作：修改   操作人：fmy   操作时间：2026-08-05**

### 1. `GET /profile/career-plans/{planId}/download`（4.5）职业规划文件独立生成

- **背景**：此前 4.5 下载**从不生成职业规划文档**——`career_plans.file_id`（"生成的主文件ID"）全代码库无人写入，接口仅返回绑定在规划上的上传佐证（`biz_type='career_plan'`），学生实际拿到的往往是档案导出 PDF，与「学生成长档案」文件逐字相同。
- **变更**：
  - 4.5 改为**惰性生成 + 缓存**：首次下载按规划详情渲染 PDF → 上传 OSS → 写入 `career_plans.file_id`；再次下载复用主文件，不重复生成。
  - 下载优先级改为：`file_id` 主文件 → `career_plan_export` 类型绑定文件（`file_id` 丢失时兜底并补写）→ 惰性生成。
  - 带**过期感知**：规划被编辑/审批状态变化（`plan.updatedAt` 晚于生成文件更新时间）后自动重新生成，替换 OSS 旧对象，保证文档反映最新状态。
  - 上传佐证（`biz_type='career_plan'`）不再作为「职业规划文件」返回，仍可作佐证材料经通用文件接口（2.1/2.1.1/2.1.2）访问。
- **文档内容**：封面（姓名/学号/学期/标题）+ 六个固定章节：一、规划信息（标题/学期/整体进度/审批状态/提交时间/审核时间/审核教师/退回原因）；二、要求与目标；三、规划内容；四、目标与行动（目标→行动→里程碑嵌套）；五、阶段反思；六、教师反馈。空数据章节统一显示「暂无」，保持章节编号稳定。
- **模板**：`export_templates` 表新增 `career_plan` 类型默认模板（页眉「职业规划」、水印学生姓名），与学生成长档案（`student_archive`）模板相互独立；模板未播种时用内存兜底模板（同一套默认配置）保证单渲染路径。
- **涉及文件**：
  - `ProfileCareerPlanService.java` — 重写 `getCareerPlanDownloadUrl`（只读→可写事务），新增 `generateCareerPlanFile` / `buildPlanDocumentContext` / `buildFallbackTemplate` / `resolveOwnedRelation` / `findExportRelation` / `isStale` / `downloadUrl` / `formatTimeRange` 方法
  - `DefaultTemplateHtml.java` — 新增 `CAREER_PLAN` HTML 模板常量，共享默认配置（页眉/页脚/边距/字体/水印/纸张）提升为包内静态常量
  - `ExportDefaultTemplateSeeder.java` — 单模板种子逻辑泛化为播种 `student_archive_default` 与 `career_plan_default` 两条默认模板
  - `AttachmentBizTypeEnum.java` — 新增 `CAREER_PLAN_EXPORT("career_plan_export", "职业规划导出文件")`
  - `学生端接口文档.md` — 4.5 节由占位补全为独立文档说明
- **错误码**：`4` 操作失败（文件生成失败）、`50005` OSS上传失败、`30001` 数据不存在（规划文件缺失）、`5` 无访问权限（非本人规划）。

------

**操作：新增   操作人：fmy   操作时间：2026-08-05**

### 新增个人简历导出接口 `GET /profile/resume/export/preview`、`POST /profile/resume/export`

- **用途**：学生端个人中心新增个人简历导出能力，支持选择导出栏目并同步生成 PDF。
- **新增接口**：
  - `GET /profile/resume/export/preview` — 返回简历可导出栏目：`education`（教育背景，必选）、`awards`（获奖情况）、`skills`（技能与兴趣）、`practices`（实践经历）、`certificates`（证书）、`selfEvaluation`（自我评价）。
  - `POST /profile/resume/export` — 同步生成个人简历 PDF 并上传 OSS，返回 `exportJobId`、`status=2`、`fileId`、`downloadUrl`、`expireAt`。
- **数据模型**：
  - 新增数据库列 `student_profiles.self_evaluation`（TEXT，自我评价），见 `V20__resume_export_support.sql`。
  - 新增 Repository：`AwardApplicationRepository`、`AwardCompetitionStarRepository`、`ArchiveCertificateRepository`。
  - 新增附件业务类型：`AttachmentBizTypeEnum.RESUME_EXPORT("resume_export", "个人简历导出")`。
- **模板配置**：
  - `export_templates` 表新增 `resume_default` / `export_type='resume'` 默认模板，字段配置覆盖基础信息、教育背景、获奖情况、技能与兴趣、实践经历、证书、自我评价。
  - `DefaultTemplateHtml.java` 新增 `RESUME` HTML 模板常量，样式按设计稿实现（姓名/副标题/联系方式头栏、教育背景及 GPA 芯片、获奖列表及级别徽章、技能分类芯片、实践经历、证书绿色芯片、自我评价）。
- **涉及文件**：
  - `ResumeExportService.java` — 新增服务，负责简历数据聚合、模板渲染、OSS 上传、审计日志与消息通知
  - `ProfileController.java` — 新增上述两个端点
  - `ExportDefaultTemplateSeeder.java` — 注册 `resume_default` 默认模板种子
  - `DefaultTemplateHtml.java`、`StudentProfile.java`、3 个 Repository、迁移脚本、枚举
  - `学生端接口文档.md` — 新增 4.18 / 4.19 节
- **错误码**：`10000` 参数错误（section 不存在或格式不支持）、`4` 操作失败（未配置模板或渲染失败）、`50005` OSS 上传失败。

------

**操作：新增   操作人：fmy   操作时间：2026-08-05**

### 新增/合并自我评价接口

- **用途**：为个人简历导出提供自我评价内容的维护入口，支持学生在个人中心维护简历中的「自我评价」模块。
- **最终接口设计（合并后）**：
  - `GET /profile/info` — 在响应中新增 `selfEvaluation` 字段，未填写时返回 `null`，不再单独提供 `GET /profile/self-evaluation`。
  - `PUT /profile/self-evaluation` — 全量更新自我评价，允许传空字符串清空，内容最长 2000 字；无独立 DELETE 接口。
- **数据模型**：依赖 `V20__resume_export_support.sql` 新增的 `student_profiles.self_evaluation`（TEXT）字段。
- **新增 DTO**：
  - `SelfEvaluationUpdateRequest.java` — 更新请求体
  - `SelfEvaluationResponse.java` — 更新响应体
- **涉及文件**：
  - `ProfileInfoResponse.java` — 新增 `selfEvaluation` 字段
  - `ProfileService.java` — `getProfileInfo()` 补充 `selfEvaluation` 返回；新增 `updateSelfEvaluation()` 方法；移除独立的 `getSelfEvaluation()` / `deleteSelfEvaluation()`
  - `ProfileController.java` — 仅保留 `PUT /profile/self-evaluation`（4.1.6），移除独立的 GET / DELETE 端点
  - `学生端接口文档.md` — 4.1 响应示例补充 `selfEvaluation`；4.1.6 节保留为更新接口
- **错误码**：`10000` 参数错误（内容超过 2000 字）。

------

**操作：修改   操作人：fmy   操作时间：2026-08-05**

### 导出接口支持 `purpose` 用途参数，简历/职业规划外部投递可无水印

- **背景**：简历、职业规划文件学生常转发给实习公司/HR/导师，屏幕上带水印显得不正式；学生成长档案属于内部文件，仍需强制水印。
- **变更**：
  1. `ExportRequest.java` — 新增 `purpose` 字段，默认 `internal`，可选 `internal` / `external`。
  2. `ExportTemplateRenderService.java` — `renderTemplate` 新增 `watermarkEnabled` 参数，控制是否添加屏幕可见、打印隐藏的 PDFBox OCG 水印。
  3. `POST /profile/resume/export`（4.19）— 支持 `purpose=external`，此时屏幕和打印均不添加水印；`purpose=internal`（默认）保持屏幕水印+打印隐藏；非法 `purpose` 返回 `10000` 参数错误。
  4. `GET /profile/career-plans/{planId}/download`（4.5）— 新增 `purpose` Query 参数；`external` 时无水印且不写入 `career_plans.file_id` 缓存，每次重新生成，避免污染内部有水印缓存；`internal` 保持原有缓存复用逻辑。
  5. `POST /profile/export`（4.17）— 档案导出固定 `purpose=internal`，强制屏幕水印+打印隐藏，不响应 `external`。
- **涉及文件**：
  - `ExportRequest.java`
  - `ExportTemplateRenderService.java`
  - `ResumeExportService.java`
  - `ProfileCareerPlanController.java`
  - `ProfileCareerPlanService.java`
  - `ProfileExportService.java`
  - `学生端接口文档.md` — 4.5 / 4.17 / 4.19 节补充 `purpose` 参数与水印策略说明
- **审计**：`export_jobs.filter_conditions` 与 `export_operation_logs.filter_conditions` 中均会记录实际使用的 `purpose`。

------

**操作：新增   操作人：fmy   操作时间：2026-08-06**

### 新增管理端导出模板管理接口

- **背景**：`export_templates` 表及默认模板播种、渲染服务均已就绪，但管理端缺少对导出模板的维护入口，管理员无法自定义学校维度的导出模板（如水印、页眉页脚、字段配置等）。
- **新增接口**：
  - `GET /admin/export-templates`（4.3）— 获取导出模板列表，支持按 `schoolId`、`exportType`、`status` 分页筛选。
  - `GET /admin/export-templates/{templateId}`（4.4）— 获取导出模板详情，返回完整字段配置、模板内容、水印与字体配置等。
  - `POST /admin/export-templates`（4.5）— 创建自定义导出模板，`templateMode` 固定为 `2=自定义`，`version` 初始为 `1`。
  - `PUT /admin/export-templates/{templateId}`（4.6）— 更新自定义模板，更新后 `version` 自动 +1；系统内置模板（`templateMode=1`）仅允许修改状态、水印等有限字段。
  - `DELETE /admin/export-templates/{templateId}`（4.7）— 软删除自定义模板；系统内置模板和当前默认模板不允许删除。
  - `PUT /admin/export-templates/{templateId}/default`（4.8）— 将指定启用模板设为某学校、某导出类型的默认模板，并取消同类型其他模板的默认状态。
- **权限控制**：新增权限码 `export:template:manage`，对应 `/admin/export-templates/*`。
- **业务规则**：
  - 支持导出类型：`student_archive`（学生成长档案）、`career_plan`（职业规划）、`resume`（个人简历）。
  - `templateCode` 在同一学校内唯一，重复返回 `10002 参数错误`。
  - 被设为默认的模板必须处于启用状态（`status=1`），否则返回 `4 操作失败`。
- **涉及文件**：
  - `管理端接口文档.md` — 第四章新增 4.3-4.8 节，并在权限码表中补充 `export:template:manage`。
- **错误码**：`10000/10001/10002` 参数错误、`30001` 模板不存在、`20005` 无访问权限（系统内置模板不可变更核心配置）、`4` 操作失败（默认模板/禁用状态限制）。

------

**操作：新增与修改   操作人：fmy   操作时间：2026-08-06**

### 新增学历层次与学生状态字段，简历副标题与教育背景展示同步调整

- **背景**：简历模板需要展示学生的学历层次（本科/专科/研究生/博士/博士后）与学生状态（应届毕业生/在校生/已毕业），用于副标题和教育背景行；之前缺少对应存储字段。
- **数据模型变更**：
  1. `majors` 表新增 `degree_type`（`varchar(30)`）— 学历层次，由专业决定，学生只读。
  2. `student_profiles` 表新增 `student_status`（`varchar(30)`）— 学生状态，学生可自行维护。
  3. 新增枚举 `DegreeTypeEnum`（`associate`/`undergraduate`/`master`/`doctor`/`postdoctor`）和 `StudentStatusEnum`（`current`/`fresh_graduate`/`graduated`）。
- **接口变更**：
  1. `GET /profile/info`（4.1）— `academicInfo` 新增 `degreeType`、`degreeTypeLabel`、`studentStatus`、`studentStatusLabel`。
  2. 新增 `PUT /profile/student-status`（4.1.5.1）— 学生更新自己的状态，请求 `studentStatus`，响应 `studentStatus` + `studentStatusLabel`。
- **简历导出变更**：
  1. `ResumeExportService.java` — 简历副标题 `title` 由 `studentStatusLabel + " · " + major` 拼接；上下文新增 `degreeType` / `degreeTypeLabel`。
  2. `DefaultTemplateHtml.java` — 教育背景行由 `学校 · 专业` 扩展为 `学校 · 专业 · 学历层次`。
- **涉及文件**：
  - `Major.java`、`StudentProfile.java`
  - `DegreeTypeEnum.java`、`StudentStatusEnum.java`
  - `ProfileInfoResponse.java`、`ProfileService.java`
  - `StudentStatusUpdateRequest.java`、`StudentStatusUpdateResponse.java`
  - `ProfileController.java`
  - `ResumeExportService.java`、`DefaultTemplateHtml.java`
  - `学生档案系统表.md`、`数据库修改记录.md`、`学生端接口文档.md`

------

**操作：修改   操作人：fmy   操作时间：2026-08-06**

### 修复导出/下载问题：中文文件名乱码、职业规划下载失败、档案导出支持无水印、栏目校验诊断化

- **背景**：QA 实测发现档案导出报 `10001 导出栏目不存在`、职业规划文件（internal 默认）无法下载、档案/职业规划下载文件名乱码；同时希望档案导出也能像简历/职业规划一样选择是否带水印。
- **变更**：
  1. **中文文件名乱码**：`OssFileService.buildContentDisposition()` 原先把原始 UTF-8 中文放进 `filename="..."`（HTTP 头按 ISO-8859-1 传输会乱码）。改为 `filename` 仅保留 ASCII 兜底（非 ASCII → `_`），完整中文名走 RFC 5987 的 `filename*=UTF-8''` 通道。一处修复覆盖档案/简历/职业规划/通用下载全部路径。
  2. **职业规划下载失败**：`ExportTemplateRenderService.loadWatermarkFont()` 用 `PDType0Font.load(doc, File)` 加载不了 `.ttc`（Docker `font-noto-cjk`、Windows 微软雅黑均为 .ttc），回退 Helvetica 后中文水印 `showText` 抛异常导致导出失败。改为对 `.ttc` 用 `TrueTypeCollection`（`getFontByName` → `processAllFonts` 取首个字体）加载 `PDType0Font.load(doc, ttf, true)`；TTC 在 `doc.save()` 后统一关闭。并做**优雅降级**：无可用中文字体或水印绘制失败时跳过水印返回原 PDF，不再阻断导出。
  3. **职业规划下载健壮性**：`ProfileCareerPlanService.getCareerPlanDownloadUrl()` 缓存复用前校验 OSS 对象存在，对象已清理时自动重新生成；`downloadUrl()` 显式携带 `originalName`（response-content-disposition 覆盖），不依赖 OSS 对象上可能旧乱码的元数据。
  4. **档案导出支持无水印**：`POST /profile/export`（4.17）由固定 `purpose=internal` 放开为支持 `internal`/`external`，与简历（4.19）、职业规划下载（4.5）对齐；`external` 时屏幕与打印均无水印，`filterConditions` 仍记录实际用途。
  5. **栏目校验诊断化**：档案/简历导出栏目校验失败时，错误信息带出具体非法值与支持的 code 列表，并 `log.warn` 打印收到的完整 sections，便于前端定位提交了哪个未支持的栏目。
- **涉及文件**：
  - `OssFileService.java`
  - `ExportTemplateRenderService.java`
  - `ProfileCareerPlanService.java`
  - `ProfileExportService.java`、`ResumeExportService.java`
  - `学生端接口文档.md` — 4.17 补充 `purpose` 参数说明与请求示例
- **水印打印行为**（机制确认，无需改动）：档案/职业规划/简历 internal 文件均通过 OCG `Usage/Print PrintState=OFF` 实现屏幕显示、打印隐藏，符合「打印后水印消失」；Adobe Acrobat/Foxit/macOS 预览均遵循，Chrome/Edge（PDFium）大部分版本遵循。仅档案导出无模板时回退的 OpenPDF 渲染路径不含水印（当前学校必建模板，极少触发）。

------

**操作：新增与修改   操作人：fmy   操作时间：2026-08-06**

### 新增职业规划文件预览接口，职业规划下载文件名固定化

- **背景**：职业规划下载（4.5）直接重定向下载，学生无法先查看成稿版式；下载文件名原为 `职业规划-日期.pdf`，随日期变化，且已缓存的旧文件在复用时不生效，需一并修正。
- **新增接口**：`GET /profile/career-plans/{planId}/preview`（4.5.1）— 返回职业规划 PDF 的 OSS 签名在线预览 URL（`response-content-disposition=inline`，浏览器内嵌渲染、不触发下载），与下载共用同一份生成/缓存文件，预览即所见即所得；支持 `purpose=internal/external`（external 无水印版，便于投递前确认版式）。
- **文件名固定**：职业规划导出文件名统一为 `职业规划文件.pdf`（不再含日期）。复用缓存文件时通过 `refreshExportFileName` 同步刷新附件行 `originalName`，使旧缓存文件的下载/预览名也变为新名称，无需重新生成 PDF。
- **实现说明**：
  1. `OssFileService.generatePresignedUrl` 增加 4 参重载（`disposition` / `filename`），3 参旧版保持兼容；新增 `getPreviewUrl` 显式覆盖 `response-content-disposition=inline`——OSS 对象上传元数据自带 `attachment`（见 `uploadBytes`），不覆盖会触发下载而非预览。
  2. `ProfileCareerPlanService` 将下载/预览共用的「解析缓存 → 惰性生成」逻辑抽取为 `resolveExportFile`；`generateCareerPlanFile` 改为返回绑定后的附件关系（原返回下载 URL），由下载/预览各自生成对应 URL。
- **涉及文件**：
  - `OssFileService.java`
  - `ProfileCareerPlanService.java`
  - `ProfileCareerPlanController.java`
  - `CareerPlanPreviewResponse.java`（新增）
  - `学生端接口文档.md` — 4.5 补充文件名说明，新增 4.5.1 预览接口

------

**操作：修改   操作人：fmy   操作时间：2026-08-06**

### 档案/简历导出支持默认参数：sections 不传默认全栏目、默认无水印（external）

- **背景**：前端学生端个人简历/档案导出目前只有导出按钮、尚无栏目选择 UI，但后端 `sections` 必填导致不传时直接报参数错误；同时希望默认导出不带水印。
- **变更**：
  1. **`sections` 放开必填**：`ExportRequest.sections` 移除 `@NotEmpty`。`POST /profile/export`（4.17）与 `POST /profile/resume/export`（4.19）在 `sections` 不传或为空时，默认按全栏目导出（档案：`academicInfo`/`dimensionScores`/`awards`/`practices`/`careerPlans`；简历：`education`/`awards`/`skills`/`practices`/`certificates`/`selfEvaluation`，顺序与导出预览一致）；传了 `sections` 仍按传入栏目导出，未支持的 code 仍返回参数错误。
  2. **默认不带水印**：`purpose` 默认值由 `internal` 改为 `external`（`ExportRequest` 字段默认值与两个 Service 的空值兜底同步调整），即不传 `purpose` 时默认无水印；传 `internal` 仍带屏幕水印。`filterConditions` 快照记录实际生效的 `sections`/`purpose`。
  3. **档案导出预览默认全选**：`GET /profile/export/preview`（4.16）中 `careerPlans` 由 `selected=false` 调整为 `selected=true`，与接口「不传 sections 默认全栏目导出」口径一致（默认全栏目包含 `careerPlans`），避免前端按预览 selected 勾选时与后端默认行为不一致。
- **涉及文件**：
  - `ExportRequest.java`
  - `ProfileExportService.java`、`ResumeExportService.java`
  - `学生端接口文档.md` — 4.16 预览示例 `careerPlans` 默认选中；4.17/4.19 的 `sections` 改为非必填并说明默认全栏目，`purpose` 默认值改为 `external`

------

**操作：修改   操作人：fmy   操作时间：2026-08-06**

### 修复职业规划预览/下载文件名与 OSS 默认域名强制下载问题

- **背景**：QA 实测发现 4.5.1 预览接口的 `previewUrl` 在浏览器打开后下载的文件名不是 `职业规划文件.pdf`（回退为 OSS 对象路径上的 UUID）；4.5 下载接口在 Apifox 中下载名仍为 `____-20260806.pdf`（ASCII 兜底，中文被替换为下划线）。
- **根因**：
  1. **下载名只走 `filename*` 通道**：旧 `buildContentDisposition()` 的 `filename="..."` 仅放 ASCII 兜底，中文只放 `filename*=UTF-8''`。浏览器等规范客户端优先取 `filename*` 正常，但 Apifox 等只读 `filename=` 的工具拿到的是 `______.pdf` 这类下划线兜底名。
  2. **OSS 默认域名强制下载**：阿里云 OSS 对新建 Bucket 的默认域名访问强制返回 `Content-Disposition: attachment`（`x-oss-force-download`），`response-content-disposition=inline` 覆盖会被 OSS 归一化为裸 `attachment` 并**丢弃文件名**，因此预览 URL 打开即下载且名为对象路径 UUID，无法内嵌渲染。
- **变更**：
  1. `OssFileService.buildContentDisposition(String mode, String filename)`（新增 2 参重载，响应头用）：`filename="..."` 放**原始 UTF-8 中文名**，`filename*=UTF-8''` 放 RFC 5987 百分号编码，双通道兼容——浏览器优先 `filename*`，Apifox 等只读 `filename=` 的工具也能直接拿到正确中文名（经 OSS 签名 URL percent-encoding 往返，UTF-8 字节原样到达，已实测验证）。存储态元数据仍用 1 参 ASCII 兜底版本，避免 PUT 时 ISO-8859-1 重编码损坏。
  2. `ProfileCareerPlanService.getCareerPlanPreviewUrl()`：预览 URL 改用 `getFileUrl(filePath, originalName)`（`attachment; filename=...` 覆盖）——受 OSS 默认域名强制下载约束，预览 URL 等价于带正确中文名的下载；`fileName` 保持 `职业规划文件.pdf`。
  3. 已缓存的旧文件名 `职业规划-20260806.pdf` 经 `refreshExportFileName` 复用刷新为 `职业规划文件.pdf`（file_uploads.id=10 已更新，无需重新生成 PDF）。
- **涉及文件**：
  - `OssFileService.java`
  - `ProfileCareerPlanService.java`
  - `学生端接口文档.md` — 4.5.1 预览说明更正：OSS 默认域名下 `previewUrl` 触发下载并携带正确文件名；真正内嵌渲染需自定义域名或后端流式返回。
- **遗留/说明**：真正内嵌渲染 PDF 需①Bucket 绑定已备案自定义域名，或②后端代理流式返回 PDF（`Content-Disposition: inline`）。若需 ②，可新增预览流式接口，当前接口保持返回 URL 不破坏前端契约。

------

**操作：新增   操作人：fmy   操作时间：2026-08-06**

### 新增职业规划目标/行动/里程碑的更新与删除接口

- **背景**：职业规划嵌套结构（目标→行动→里程碑）此前只有「添加」（4.8~4.10）与「更新行动状态」（4.12），缺少内容更新与删除能力；「复制上一学期计划后再修改」（To Do List P1 #7）工作流无法调整复制的目标/行动/里程碑；里程碑达成状态（`career_milestones.is_achieved`）此前无任何写入入口。
- **新增接口**（`学生端接口文档.md` 新增 4.8.1 / 4.8.2 / 4.9.1 / 4.9.2 / 4.10.1 / 4.10.2 六节）：
  - `PUT /profile/career-plans/{planId}/goals/{goalId}`（4.8.1）— 更新目标：`goalTitle` 必填，`goalDesc`/`targetDate`/`sort` 不传保留原值、传空字符串清空。
  - `PUT /profile/career-plans/{planId}/goals/{goalId}/actions/{actionId}`（4.9.1）— 更新行动：`actionTitle` 必填，其余不传保留原值；状态仍走 4.12。
  - `PUT /profile/career-plans/{planId}/actions/{actionId}/milestones/{milestoneId}`（4.10.1）— 更新里程碑：`milestoneTitle` 必填；`isAchieved=1` 写 `achieved_at`、回退 0 清空（里程碑达成状态唯一写入入口）。
  - `DELETE /profile/career-plans/{planId}/goals/{goalId}`（4.8.2）— 删除目标，级联软删行动、里程碑与行动成果文件，重算规划进度。
  - `DELETE /profile/career-plans/{planId}/goals/{goalId}/actions/{actionId}`（4.9.2）— 删除行动，级联软删里程碑与成果文件，重算目标状态与规划进度。
  - `DELETE /profile/career-plans/{planId}/actions/{actionId}/milestones/{milestoneId}`（4.10.2）— 删除里程碑。
- **删除行动成果**：复用通用附件删除接口 `DELETE /common/files/{fileId}`（2.1.3），未新增专用接口（4.13 补充删除说明）。
- **业务规则**：
  - 所有接口沿用 `ensureEditable`：仅 `status=0(草稿)` / `status=3(已退回)` 可改删，已通过（`status=2`）禁止。
  - 归属校验走 目标→规划、行动→目标→规划、里程碑→行动→规划 链路。
  - 删除采用软删除（native query 置 `deleted_at`，绕过 `updatable=false` 限制，与 `UserInterestRepository.softDeleteById` 同款模式）；级联删除行动成果时软删 `file_uploads` 记录并删 OSS 物理文件（与 2.1.3 口径一致）。
- **涉及文件**：
  - 新增 DTO：`CareerGoalUpdateRequest.java`、`CareerActionUpdateRequest.java`、`CareerMilestoneUpdateRequest.java`
  - 仓储新增软删方法：`CareerGoalRepository`、`CareerActionRepository`、`CareerMilestoneRepository`（`softDeleteById`）
  - `ProfileCareerPlanService.java` — 新增 `updateGoal` / `updateAction` / `updateMilestone` / `deleteGoal` / `deleteAction` / `deleteMilestone` 及级联删除辅助方法 `deleteActionCascade` / `deleteActionFiles`
  - `ProfileCareerPlanController.java` — 新增 6 个端点
  - `学生端接口文档.md` — 新增 4.8.1~4.10.2 六节，4.13 补充删除行动成果说明

------

**操作：修改   操作人：fmy   操作时间：2026-08-06**

### 修复职业规划下载文件名乱码（`filename=` 原始 UTF-8 → 百分号编码）

- **背景**：上一轮修复把原始 UTF-8 中文放进 `filename="..."` 后，实测下载名变成 `è_ä_è§åæä»¶.pdf`（乱码）。解码可得：`è_ä_è§åæä»¶` 正是「职业规划文件」的 UTF-8 字节（`E8 81 8C E4 B8 9A ...`）被按 **ISO-8859-1/Latin-1** 解读、控制字符（`0x81/0x8C/0xB8/0x9A...`）被客户端替换为下划线。
- **根因**：RFC 7230/6266 规定 HTTP 头字段值按 ISO-8859-1 传输，`filename=` 只能放 ASCII。OSS 虽能逐字节原样返回 UTF-8（字节级实测通过），但客户端读取响应头时按 Latin-1 解码，任何 ≥0x80 的字节必然乱码；Apifox 等只读 `filename=` 的工具无法规避。
- **变更**：
  - `OssFileService.buildContentDisposition(mode, filename)`：`filename="..."` 由「原始 UTF-8」改为「**百分号编码后的 UTF-8**」（纯 ASCII，RFC 3986，空格 `%20`），`filename*=UTF-8''` 保持同值。规范浏览器优先取 `filename*` 解码出 `职业规划文件.pdf`；Firefox/Chrome 也会解码 `filename=` 的 `%XX`；Apifox 按百分号编码解码同样得到中文名，且不再出现乱码（若某客户端不解码，仅显示 `%E8%81%8C...pdf` 原始编码串，无乱码）。编码串中引号/反斜杠均被 `%22/%5C` 替代，不破坏 quoted-string 语法。
  - 1 参存储态 `buildContentDisposition(filename)` 改为委托 2 参版本（同为百分号编码，纯 ASCII，PUT 到 OSS 时不会被 ISO-8859-1 重编码损坏）。
  - 数据库校验：`file_uploads.id=10` 的 `original_name` 为正确 UTF-8 `职业规划文件.pdf`（HEX `E8818CE4B89AE8A784E58892E69687E4BBB62E706466`），无需修正。
- **验证**：反射调用真实 `buildContentDisposition("attachment","职业规划文件.pdf")` → 生成 OSS 签名 URL → HTTP GET 200，OSS 返回的 `Content-Disposition` 与代码生成值**逐字节一致**（纯 ASCII 百分号编码）。
- **涉及文件**：
  - `OssFileService.java`
  - `学生端接口文档.md` — 4.5.1 预览说明更新为「文件名以百分号编码 UTF-8 放入 `filename` 与 `filename*` 双通道」。

------

**操作：修改   操作人：fmy   操作时间：2026-08-06**

### 职业规划目标/行动/里程碑接口路径扁平化（移除冗余中间路径参数）

- **背景**：目标→行动→里程碑是一级连一级的嵌套结构，但中间路径参数（`goalId`、`actionId`）是冗余的——所有 ID 均为全局唯一自增主键，且 Service 已通过 `requireActionInPlan`（行动→目标→规划 链路）做归属校验，路径里的祖先 ID 不参与身份识别与安全校验。原设计还不一致：行动挂在 `goals/{goalId}/actions` 下，里程碑却挂在 `actions/{actionId}/milestones` 下。
- **变更**（breaking change，接口地址变更）：
  - `POST /profile/career-plans/{planId}/goals/{goalId}/actions` → `POST /profile/career-plans/{planId}/actions`，`goalId` 移入请求体（必填）
  - `POST /profile/career-plans/{planId}/actions/{actionId}/milestones` → `POST /profile/career-plans/{planId}/milestones`，`actionId` 移入请求体（必填）
  - `PUT /profile/career-plans/{planId}/goals/{goalId}/actions/{actionId}` → `PUT /profile/career-plans/{planId}/actions/{actionId}`（去掉 `goalId`）
  - `DELETE /profile/career-plans/{planId}/goals/{goalId}/actions/{actionId}` → `DELETE /profile/career-plans/{planId}/actions/{actionId}`（去掉 `goalId`）
  - `PUT /profile/career-plans/{planId}/actions/{actionId}/milestones/{milestoneId}` → `PUT /profile/career-plans/{planId}/milestones/{milestoneId}`（去掉 `actionId`）
  - `DELETE /profile/career-plans/{planId}/actions/{actionId}/milestones/{milestoneId}` → `DELETE /profile/career-plans/{planId}/milestones/{milestoneId}`（去掉 `actionId`）
  - 规则统一为：**创建时父级 ID 放请求体，操作叶子时路径只留 `planId` + 叶子自身 ID**；4.12 状态 / 4.13 文件接口路径不变。
- **归属校验强度不变**：`updateAction`/`deleteAction` 由 `requireActionInPlan` 的 行动→目标→规划 链路校验替代原「目标+行动」双重校验（等价且少一次查询）；`updateMilestone`/`deleteMilestone` 改为先取里程碑、再对 `milestone.action_id` 走 里程碑→行动→目标→规划 链路校验。
- **涉及文件**：
  - `CareerActionAddRequest.java` — 新增必填 `goalId`
  - `CareerMilestoneAddRequest.java` — 新增必填 `actionId`
  - `ProfileCareerPlanService.java` — 六个方法签名与归属校验调整
  - `ProfileCareerPlanController.java` — 六个端点路径调整
  - `学生端接口文档.md` — 4.9 / 4.9.1 / 4.9.2 / 4.10 / 4.10.1 / 4.10.2 接口地址与参数表更新

**操作：修改   操作人：fmy   操作时间：2026-08-07**

### 职业规划目标/行动/里程碑：更新接口允许空串清空日期、里程碑成果证明支持、里程碑→行动进展传导

- **背景**：接口文档约定更新接口日期字段「传空字符串清空」，但 `@Pattern` 正则不允许空串导致校验拦截；`career_milestones.proof_file_id`（成果证明）在表结构中定义但无任何写入/返回路径；表文档（V5.6）约定 `career_actions.completion_rate` 由里程碑达成占比回填，但代码仅按行动状态写 0/100，里程碑达成与行动进度完全脱钩。
- **变更**：
  1. **空串清空日期**：`CareerGoalUpdateRequest.targetDate`、`CareerActionUpdateRequest.startDate/endDate`、`CareerMilestoneUpdateRequest.milestoneDate` 的正则由 `^\d{4}-\d{2}-\d{2}$` 放宽为 `^$|^\d{4}-\d{2}-\d{2}$`，允许传空字符串清空（与文档一致；代码 `parseDate` 本就支持）。
  2. **里程碑成果证明**（4.10.1）：新增可选参数 `proofFileId`（关联 `file_uploads.id`）——不传保留原值，传 `0` 清空解绑（软删原证明 + 删除 OSS 文件），传具体 fileId 绑定（校验归属当前用户且可绑定，先释放旧证明再绑定新文件）。详情响应（4.6）里程碑项新增 `proofFileId` / `proofFileName` / `proofFileUrl`。
  3. **里程碑→行动进展传导**（对齐表文档 V5.6）：新增 `syncActionProgress`——行动存在里程碑时，完成率 = 达成数/总数回填，状态按达成情况推导（全部达成→2、部分达成→1、均未达成→0），`status=2` 写 `completed_at`，并联动重算目标状态与规划进度。在添加（4.10）、更新（4.10.1）、删除（4.10.2）里程碑后触发；删除里程碑同时清理其成果证明。
  4. **行动状态边界**（4.12 联动调整）：行动存在里程碑时，手动回退状态（0/1）的 `completion_rate` 按里程碑占比回填，不丢失已积累进度；`status=2` 仍强制 100。
  5. **目标状态判定修正**：`updateGoalStatus` 在存在已完成行动但未全部完成时判为「进行中(1)」，避免「1个完成+1个未开始」误判为「未开始」。
- **涉及文件**：
  - `CareerGoalUpdateRequest.java` / `CareerActionUpdateRequest.java` / `CareerMilestoneUpdateRequest.java` — 日期正则放宽；`CareerMilestoneUpdateRequest` 新增 `proofFileId`
  - `AttachmentBizTypeEnum.java` — 新增 `CAREER_MILESTONE("career_milestone", "职业规划行动里程碑")`
  - `ProfileCareerPlanService.java` — 新增 `syncActionProgress` / `unbindMilestoneProof` / `collectMilestoneProofFiles`；`addMilestone` / `updateMilestone` / `deleteMilestone` / `updateActionStatus` / `updateGoalStatus` / `getPlanDetail` 调整
  - `CareerPlanDetailResponse.java` — `MilestoneItem` 新增 `proofFileId` / `proofFileName` / `proofFileUrl`
  - `学生端接口文档.md` — 4.6 / 4.10 / 4.10.1 / 4.10.2 / 4.12 更新

------

**操作：修改   操作人：fmy   操作时间：2026-08-07**

### 添加行动 / 添加里程碑的 `goalId` / `actionId` 移回路径参数

- **背景**：4.9 添加行动、4.10 添加里程碑的父级 ID（`goalId` / `actionId`）此前随 08-06 路径扁平化移入请求体。为与资源嵌套语义一致、便于前端按目标/行动维度组织请求，将父级 ID 移回路径参数；归属校验逻辑不变（目标必须属于当前规划、行动必须属于当前规划）。
- **变更**（breaking change，接口地址变更）：
  - `POST /profile/career-plans/{planId}/actions` → `POST /profile/career-plans/{planId}/goals/{goalId}/actions`，`goalId` 由请求体移入路径参数
  - `POST /profile/career-plans/{planId}/milestones` → `POST /profile/career-plans/{planId}/actions/{actionId}/milestones`，`actionId` 由请求体移入路径参数
  - 归属校验：4.9 仍走 目标→规划 链路（`findByIdAndCareerPlanId`），4.10 仍走 行动→目标→规划 链路（`requireActionInPlan`），强度不变。
- **涉及文件**：
  - `CareerActionAddRequest.java` — 移除 `goalId` 字段
  - `CareerMilestoneAddRequest.java` — 移除 `actionId` 字段
  - `ProfileCareerPlanService.java` — `addAction` / `addMilestone` 增加路径参数入参，去掉从请求体取值
  - `ProfileCareerPlanController.java` — 4.9 / 4.10 端点路径调整，新增 `@PathVariable` 参数
  - `学生端接口文档.md` — 4.9 / 4.10 接口地址、路径参数表、请求参数表与请求示例更新

------

# **操作：新增   操作人：fmy   操作时间：2026-08-09**

### 1.消息中心新增批量已读与删除接口

- **背景**：消息中心此前仅支持「单个已读」（5.2）与「全部已读」（5.3），缺少**选择多个消息已读**、**删除单条消息**、**选择多个消息删除**三个能力；`user_messages` 表已有软删除字段（`deleted_at`）但无任何删除入口。
- **新增接口**（`学生端接口文档.md` 新增 5.8 / 5.9 / 5.10 三节）：
  - `PUT /messages/batch-read`（5.8）— 按 ID 列表批量标记已读，请求体 `messageIds`（1~100 个），返回 `markedCount`（本次实际「未读→已读」条数）。
  - `DELETE /messages/{messageId}`（5.9）— 删除单条消息，软删除；消息不存在返回 `30001`，非本人返回 `20005`。
  - `DELETE /messages/batch`（5.10）— 按 ID 列表批量删除，请求体 `messageIds`，返回 `deletedCount`。
- **业务规则**：
  - 删除统一为**软删除**（置 `deleted_at`），配合 `UserMessage` 的 `@SQLRestriction("deleted_at IS NULL")` 自动从列表过滤，不做物理删除。
  - 批量操作按 `user_id` 过滤当前用户消息：非本人、已软删的 ID 静默跳过，返回实际处理条数（幂等）；单条删除沿用 `getOwnedMessage` 归属/存在校验。
  - 批量 `messageIds` 必填且最多 100 个，超限返回 `PARAM_ERROR`。
- **涉及文件**：
  - `MessageBatchIdsRequest.java`（新增）— 批量请求体，已读/删除共用
  - `MessageBatchDeleteResponse.java`（新增）— 批量删除响应体（`deletedCount`）
  - `UserMessageRepository.java` — 新增 `markReadByIds`（JPQL，显式附加 `deletedAt IS NULL`）、`softDeleteById` / `softDeleteByIds`（native query，绕过 `deleted_at updatable=false`，与 `UserInterestRepository.softDeleteById` 同款模式）
  - `MessageService.java` — 新增 `markBatchRead` / `deleteMessage` / `deleteMessages`
  - `MessageController.java` — 新增 `PUT /messages/batch-read`、`DELETE /messages/{messageId}`、`DELETE /messages/batch` 三个端点（`DELETE /messages/batch` 字面量优先于 `/{messageId}`，Spring MVC 自动处理）
  - `学生端接口文档.md` — 版本升至 V5.8，新增 5.8 / 5.9 / 5.10 三节

------

# **操作：新增   操作人：fmy   操作时间：2026-08-11**

### 1. 管理端指标配置模块（《管理端接口文档》一、1.1~1.6）

- **背景**：管理端缺少指标树配置能力——学校级指标树此前仅由种子 SQL 初始化，无任何维护入口；`indicator_rule_versions` / `indicator_versions` 版本表已存在但无发布路径。本次按文档实现指标配置全流程：树查询、创建、更新、删除、发布规则版本、版本列表。
- **新增接口**（`AdminIndicatorController`，前缀 `/admin/indicators`）：
  - `GET /admin/indicators/tree`（1.1）— 返回学校下完整三级指标树，节点携带权重/状态/计分规则/当前版本，顶部返回规则版本元数据（`versionId`/`version`/`versionName`/`effectiveAt`）；支持 `status` 过滤、按学期过滤（`semesterId` 不传取当前学期；该学期已发布过版本则返回其 `tree_snapshot` 快照，未发布过则返回当前草稿树）。
  - `POST /admin/indicators`（1.2）— 创建一/二/三级指标；创建三级指标必须传 `scoringRule`；指标编码学校内唯一；`sort` 缺省取同级末位 +1；`path`/`dimensionCode` 自动维护（`dimensionCode` 从父级继承）。
  - `PUT /admin/indicators/{indicatorId}`（1.3）— 更新名称/权重/说明/计分规则（仅三级可改）/排序/状态；禁用时若存在启用子级先拦截；修改仅作用于当前草稿树，不影响历史评分（发布时形成新版本快照）。
  - `DELETE /admin/indicators/{indicatorId}`（1.4）— 软删除；存在子指标返回 `41003`。
  - `POST /admin/indicators/publish`（1.5）— 将当前草稿树打包为新的规则版本：版本名称不可重复（重复返回 `41002`）、权重校验失败返回 `41004`；发布版本记录归属学期（`semesterId` 可选，不传取当前学期，V26 迁移新增 `indicator_rule_versions.semester_id`）；发布后生成 `indicator_versions` 单指标快照并全校推进 `evaluation_indicators.version`，历史评分不受影响。
  - `GET /admin/indicators/rule-versions`（1.6）— 分页查询历史规则版本，按版本号倒序。
- **权限控制**：所有接口先校验 `admin` 角色或 `indicator:manage` 权限码（`AdminIndicatorService.requireIndicatorPermission`），越权返回 `20005 无访问权限`；`SecurityConfig` 增加 `.requestMatchers("/admin/**").authenticated()`。
- **计分规则校验**：新增 `ScoringRuleValidator` 统一按 type（AVG/SUM/MAX/WEIGHTED/THRESHOLD/COUNT）校验 `scoring_rule` JSON，失败返回 `10001` 且错误信息带字段路径；WEIGHTED 额外校验各维度权重之和为 1、维度编码须在 `ability_dimensions` 中存在。
- **权重口径**：权重刻度 0-1（`decimal(5,4)`）。校验规则与种子数据一致——同一父节点下子级权重之和 = 父节点权重，一级节点权重之和 = 1；创建/更新/发布均校验。
- **版本化设计**：`evaluation_indicators.version` 与 `indicator_rule_versions.version` 保持一致（与现有 `findActiveByVersion` 读路径对齐）。发布 = ①新规则版本（version = 当前最大 +1）②全校指标当前 weight/scoringRule 写入 `indicator_versions`（(indicator_id, version) 唯一键）③bulk 推进全校指标 version。
- **涉及文件**：
  - `AdminIndicatorController.java`（新增）— 6 个端点，写操作带 `@AuditLog(module="indicator")`
  - `AdminIndicatorService.java`（新增）— 权限校验、树构建、创建/更新/删除、发布、版本列表及私有辅助方法
  - `ScoringRuleValidator.java`（新增）— 计分规则 JSON 校验器
  - `dto/Fmy/indicator/`（新增）— `IndicatorCreateRequest` / `IndicatorUpdateRequest` / `IndicatorPublishRequest` / `AdminIndicatorTreeResponse` / `IndicatorCreateResponse` / `IndicatorPublishResponse` / `IndicatorRuleVersionItem`
  - `IndicatorVersionRepository.java`（新增）、`EvaluationIndicatorRepository.java` / `IndicatorRuleVersionRepository.java`（扩展管理端查询）
  - `ResultCode.java` — 补充 `INDICATOR_*` 错误码（`41001`/`41002`/`41003`/`41004`/`41005`）
  - `EvaluationIndicator.java` — 新增 `description` 字段（对应 V24 迁移）
  - `SecurityConfig.java` — `/admin/**` 需认证
  - `V24__add_indicator_description.sql`（新增）— `evaluation_indicators.description`
  - `管理端接口文档.md`、`学生档案系统表.md`、`数据库修改记录.md` — 同步
- **错误码**：`41001` 指标版本冲突、`41002` 规则版本已发布（版本名称重复）、`41003` 存在子指标、`41004` 权重合计不为 1、`41005` 评分重算任务执行中、`20005` 无访问权限。注：管理端错误码与学生端共用体系，`20006`/`50004`/`50005` 已被学生端占用（账号被禁用/邮件发送失败/OSS 上传失败）；指标相关错误码原分配于 `50000-59999` 段（`50001`/`50002`/`50003`/`50006`/`50007`），后因与第三方错误码数值重复，统一重编号至专属子段 `41001-41005`（见下方"错误码重编号"记录）。

### 2. 指标历史版本查询修复：发布快照机制（`tree_snapshot`）

- **背景**：学生端文档 2.4 承诺可按历史版本查询指标树，但旧实现 `evaluation_indicators.version` 只是"当前版本号戳记"（发布时 bulk 覆盖所有行），`indicator_versions` 又仅快照 weight/scoring_rule，无法按旧版本还原完整指标树（结构字段 name/code/parent_id/level/sort/dimension 全缺失）——历史查询会返回空或还原出错误结构。
- **方案**：发布时把完整指标树（`AdminIndicatorTreeResponse.IndicatorNode` 序列化，含全部结构字段）写入 `indicator_rule_versions.tree_snapshot`（JSON 列，V25 迁移）。读路径（`GET /common/indicators`）优先读快照——快照存在则直接反序列化并裁剪为学生节点，否则回退旧逻辑 `findActiveByVersion`。副作用修正：公共端不再看到发布后的草稿改动（此前会暴露未发布内容）。
- **涉及文件**：
  - `V25__add_indicator_rule_version_tree_snapshot.sql`（新增）— `indicator_rule_versions.tree_snapshot`
  - `IndicatorRuleVersion.java` — 新增 `treeSnapshot` 字段
  - `AdminIndicatorService.java` — publish 时 `buildTreeSnapshot` 生成并写入快照
  - `CommonService.java` — `getIndicators` 优先读快照 + `readSnapshot`/`toStudentNodes`/`resolveRuleVersion` 辅助方法
  - `管理端接口文档.md`（1.1/1.5）、`学生端接口文档.md`（2.4）、`学生档案系统表.md`（`indicator_rule_versions`）、`数据库修改记录.md`（记录 7）— 同步
- **兼容性**：`tree_snapshot` 落地前已发布的旧版本该列为 `NULL`，读路径自动回退按 `evaluation_indicators.version` 查询，旧数据不受影响。

### 3. 指标错误码重编号：`50001/50002/50003/50006/50007` → `41001-41005`

- **背景**：`INDICATOR_*` 业务错误码原占用 `50000-59999` 段（`50001` 指标版本冲突、`50002` 规则版本已发布、`50003` 存在子指标、`50006` 权重合计不为 1、`50007` 评分重算任务执行中），与同一枚举中第三方错误码（`THIRD_*`）数值重复，调用方无法区分 `50001` 是指标版本冲突还是第三方错误。
- **方案**：将 5 个 `INDICATOR_*` 错误码重编号至 `41000-41999` 业务专属子段的 `41001`-`41005`（顺序不变），`message`/`httpStatus` 不变。`THIRD_*`（`50001`-`50008`）维持原值，`50000-59999` 段整体预留给第三方/OSS 错误码。
- **影响**：**破坏性变更**。已对接管理端指标模块的前端需同步更新错误码判断；`41001`-`41005` 在 `40000-49999` 业务段内为专属子段，不与任何学生端错误码重复。
- **涉及文件**：
  - `ResultCode.java` — 5 个枚举值改数值
  - `AdminIndicatorService.java` / `AdminIndicatorController.java` — Javadoc 数字同步
  - `管理端接口文档.md`（错误码表、1.1/1.4/1.5、测试用例）、`接口修改记录.md`（本节）
- **验证**：`./mvnw -o compile -DskipTests` 通过。

------

# **操作：新增与修改   操作人：fmy   操作时间：2026-08-11**

### 管理端接口文档 V5.7：补齐档案查看 / 统计看板 / 表单模板等需求缺口接口契约

- **背景**：需求对齐检查发现《管理端接口文档》存在 7 处缺口——档案查看、学校整体档案汇总/多维可视化、成果热力图、表单自定义、一键导出学生档案、学期批量导入均无对应接口；日志查询（3.1）缺少按年级/学院/专业筛选维度。本次为**纯文档同步，不涉及代码实现**，新增接口统一标注「状态：规划中（本版本未实现，接口契约保留）」，前端可先行按此契约对接。
- **变更明细**：
  1. **数据导出模块（四）新增 4.9 `POST /admin/exports/archives`** — 一键导出学生档案：按组织范围（学校/学院/专业/班级/年级）批量导出学生成长档案与基本信息，`fileType` 支持 `pdf`（复用 `export_templates` 的 `student_archive` 模板）/ `xlsx`；异步写入 `export_jobs`，审计走 `export_operation_logs`；权限码 `archive:export`。
  2. **学期管理模块（八）新增 8.6 `POST /admin/semesters/import`、8.7 `GET /admin/semesters/import-template`** — 批量导入学期：Excel/CSV 逐行校验（名称唯一、日期合法、不重叠）后写入 `semesters`，同步返回成功/失败明细；权限码 `semester:import`。学期为低频小数据量，不走任务表。
  3. **新增十四、档案管理模块（14.1-14.3）** — `GET /admin/archives`（按年级/学院/专业/班级/状态分页查询学生档案）、`GET /admin/archives/{archiveId}`（档案详情，含对应类型扩展字段）、`GET /admin/archives/overview`（组织维度档案汇总）；权限码 `archive:view`。
  4. **新增十五、统计看板与可视化模块（15.1-15.3）** — `GET /admin/statistics/dashboard`（学校整体档案汇总 KPI）、`GET /admin/statistics/overview`（组织下钻多维汇总：成绩/奖项/实践/爱好）、`GET /admin/statistics/heatmap`（成果热力图矩阵数据，0-100 归一化）；数据源为 `org_archive_summaries` + `statistics_cache`，沿用两级缓存与降级策略；权限码 `statistics:view`。
  5. **新增十六、表单自定义模板模块（16.1-16.6）** — `form_templates` 增删改查：列表/详情/创建/更新（`version` +1）/删除/设置默认；字段配置 `fields` 支持 `input`/`textarea`/`number`/`select`/`radio`/`checkbox`/`date`/`upload`/`switch` 等组件类型与字典/接口数据源、校验规则、联动逻辑；权限码 `form:template:manage`。
  6. **3.1 `GET /admin/logs/system` 新增组织维度筛选** — 请求参数补充 `grade`/`collegeId`/`majorId`/`classId`，按**被操作业务对象**的组织归属过滤；解析优先级 `classId` → `majorId` → `collegeId` → `grade`，`grade` 按 `classes.grade` 匹配（与表文档约定一致）。
  7. **权限码表补充 5 个新权限码**：`archive:view`、`statistics:view`、`archive:export`、`form:template:manage`、`semester:import`。
  8. **附录与测试清单同步**：附录接口关系表补充新增接口行；测试清单新增 18.7（档案/统计/表单模板/档案导出/学期导入用例），原 15.x 子节随章节重编号为 18.x；文档版本 V5.6 → V5.7，更新日期 2026-08-11。
- **章节重编号**：因在「定时任务管理模块」后插入三个新模块，原「十四、附录」重编号为「十七、附录」，原「十五、接口测试交付清单」重编号为「十八、接口测试交付清单」。
- **涉及文件**：
  - `管理端接口文档.md` — 版本 V5.7；3.1 参数表；4.9；8.6/8.7；新增十四/十五/十六章；附录表；18.7 测试用例
  - `接口修改记录.md`（本节）
- **说明**：本次无数据库结构变更，`数据库修改记录.md` 无需新增记录；相关表结构（`org_archive_summaries`、`statistics_cache`、`form_templates`、`export_jobs`、`system_logs`）均已存在。

------

# **操作：新增   操作人：fmy   操作时间：2026-08-11**

### 教师端接口文档 V5.7：补齐成果热力图 / 系统日志查询需求缺口接口契约

- **背景**：需求对齐检查发现《教师端接口文档》存在 2 处缺口——「成果热力图」与「日志查看」在课任教师/审核员角色界面均出现，但文档无对应接口：热力图需以班级或更高范围为单位、按 8 个学期节点（大一上→大四下）聚合展示奖项/实践/成绩密度；日志查看需支持「选择时间 → 查看记录」的查询交互。本次为**纯文档同步，不涉及代码实现**，新增接口统一标注 V5.7 说明，前端可先行按此契约对接。
- **变更明细**：
  1. **新增六、操作日志查询模块（6.1 `GET /teacher/logs`）** — 按操作人/操作类型/模块/时间（`startTime`/`endTime`）分页查询 `system_logs`；数据范围取「当前教师自身操作 + `role_scopes` 授权范围内学生相关操作」，越权返回 `20005`；权限码 `log:view`。
  2. **十一、统计看板模块新增 11.4 `GET /teacher/statistics/heatmap`** — 班级/专业级成果热力图：以授权范围内班级（或专业/学院）为行、该年级 8 个学期节点为列，按 `metric`（`gpa`/`award`/`practice`/`archive`）聚合展示成果密度，`values` 归一化 0-100 并返回 `rawValues`/`total` 供 Tooltip；`stageLabel` 提供「大一上~大四下」列头；缓存与降级策略同 11.1；权限码 `statistics:view`。
  3. **章节重编号** — 因在「五、审核历史模块」后插入操作日志模块，原「六~十四」重编号为「七~十五」：六学生管理→七、七职业规划反馈→八、八AI辅助评价→九、九短板识别→十、十统计看板→十一、十一数据导出→十二、十二消息中心→十三、十三个人中心→十四、十四审批委托→十五。
  4. **权限码表补充**：`log:view`、`statistics:view`（第 6 节关键权限码表 + 附录 A 权限对照表）。
  5. **附录 C 测试清单**：新增 C.5 操作日志查询用例（L-001~L-003）、C.6 成果热力图用例（H-001~H-003）。
  6. **文档版本** V5.6 → V5.7，更新日期 2026-08-11。
- **涉及文件**：
  - `教师端接口文档.md` — 版本 V5.7；新增六章 6.1；11.4；章节重编号；权限码表；附录 A / C
  - `接口修改记录.md`（本节）
- **说明**：本次无数据库结构变更，`数据库修改记录.md` 无需新增记录；相关表结构（`system_logs`、`org_archive_summaries`、`statistics_cache`、`semester_gpa_summaries`、`portrait_evaluation_scores`）均已存在。

------

# **操作：修改   操作人：fmy   操作时间：2026-08-11**

### `GET /auth/me`（共用接口）响应新增 `scopes`（授权数据范围）

- **背景**：`GET /auth/me` 为教师/学生/管理员共用接口（契约见《学生端接口文档》1.3）。教师端登录后需展示其可管理/审核的数据范围（班级、专业等），原响应缺少该字段，本次补充。
- **变更明细**：
  1. **新增 DTO** `UserScopeInfoResponse`（`dto/Fmy/auth/response/`）— 字段：`scopeType`（Integer）、`scopeTypeLabel`（String）、`scopeId`（Long）、`scopeName`（String）、`semesterId`（Long，可空）。
  2. **`UserInfoResponse` 新增字段** `scopes`（`List<UserScopeInfoResponse>`）。
  3. **`AuthService.getCurrentUser` 补充 scopes 解析** — 数据来源 `role_scopes`（`user_id` + `status=1`）；`scopeName` 按 `scopeType` 批量关联 `schools`（1）/`colleges`（2）/`majors`（3）/`classes`（4），`scopeTypeLabel` 复用 `ScopeTypeEnum`，课程（5）/年级（6）暂无关联表 `scopeName` 返回 `null`。
- **影响**：教师/辅导员角色返回 `scopes` 数组（授权范围）；学生/管理员 `role_scopes` 无记录，返回空数组，契约兼容。`email`/`phone` 维持现有 `@Sensitive` 脱敏，不在此次变更范围。
- **涉及文件**：
  - `AuthService.java` — 新增 `resolveUserScopes`/`scopeIds`/`idToName`/`scopeName`/`scopeTypeLabel` 辅助方法及对应 Repository 注入
  - `UserInfoResponse.java` — 新增 `scopes` 字段
  - `UserScopeInfoResponse.java` — 新增
  - `学生端接口文档.md` — 1.3 响应示例与说明补充 `scopes` 字段表
  - `教师端接口文档.md` — 认证模块补充共用接口说明
  - `接口修改记录.md`（本节）
- **说明**：本次无数据库结构变更，`数据库修改记录.md` 无需新增记录；`role_scopes` 表结构已存在。

------

# **操作：新增与修改   操作人：fmy   操作时间：2026-08-12**

### 管理端接口文档 V5.8：指标体系草稿隔离、历史版本快照修补与基于历史版本发布

- **背景**：当前 `evaluation_indicators` 是学校维度唯一活草稿树，管理端默认直接返回草稿，导致彻底重组指标体系时可能看到半成品；`indicator_rule_versions.tree_snapshot` 与 `indicator_versions` 一经发布即为只读，发现历史版本笔误无法单独修补；`POST /admin/indicators/publish` 只能基于当前全校草稿打包，会连带引入草稿中其他未预期改动。本次通过「默认展示已发布快照 + 显式草稿编辑模式 + 历史版本快照修补 + 基于历史版本发布」四层能力解决上述问题。
- **变更明细**：
  1. **修改 `GET /admin/indicators/tree`** — 新增请求参数 `draft`（Boolean，非必填，默认 `false`）。默认行为改为：学期已发布 → 返回该学期权威快照；学期未发布 → 回退全校当前生效/最新已发布版本快照；全校尚未发布过任何版本时才回退当前草稿树。`draft=true` 时强制返回草稿树（供发布前编辑确认）。解决重组期间管理端看到半成品草稿的问题。
  2. **修改 `POST /admin/indicators/publish`** — 请求体新增 `sourceVersionId`（Long，非必填）。传入时从指定历史版本的 `tree_snapshot` 深拷贝生成新版本的完整指标树快照，并深拷贝对应 `indicator_versions` 记录；不传时保持原行为，基于当前草稿树发布。基于历史版本发布时跳过权重校验（源版本已校验且快照不可变更权重/计分规则），但要求源版本存在、属于该学校且 `tree_snapshot` 非空。解决只能基于当前草稿发布、连带引入未预期改动的问题。
  3. **新增 `PATCH /admin/indicators/rule-versions/{versionId}/snapshot`** — 定向修补指定历史版本快照中某指标的元数据字段（`indicatorName`、`description`、`indicatorCode`），用于修正发布后发现的名字/说明/编码笔误。至少需指定一项待修改字段；修改 `indicatorCode` 时需保证同一份快照内编码唯一；禁止修改 `weight`、`scoringRule`、`status`、`parentId`、`level` 等影响评分或树结构的字段。修补后该历史版本立即生效，已产生的评分记录不受影响。解决历史版本快照无法单独修补的问题。
- **新增/修改 DTO**：
  - `IndicatorPublishRequest.java` — 新增 `sourceVersionId` 字段。
  - `IndicatorRuleVersionSnapshotPatchRequest.java` — 新增（字段：`indicatorCode`、`indicatorName`、`description`、`newIndicatorCode`）。
- **涉及文件**：
  - `AdminIndicatorService.java` — `getTree` 新增 `draft` 参数与默认快照回退逻辑；`publish` 新增 `sourceVersionId` 分支；新增 `patchRuleVersionSnapshot` 及 `cloneSnapshotWithVersion`/`refreshNodeVersion`/`findNodeByCode` 辅助方法。
  - `AdminIndicatorController.java` — `getTree` 增加 `draft` 参数；`publish` Javadoc 更新；新增 `patchRuleVersionSnapshot` 接口。
  - `IndicatorVersionRepository.java` — 新增 `findByVersion` 查询。
  - `管理端接口文档.md` — 版本 V5.8；1.1 节增加 `draft` 参数与快照回退说明；1.5 节增加 `sourceVersionId` 参数与校验说明；新增 1.7 节快照修补接口文档。
  - `接口修改记录.md`（本节）
- **验证**：`mvn compile -q` 通过。
- **说明**：本次无数据库结构变更，`数据库修改记录.md` 无需新增记录；`indicator_rule_versions`/`indicator_versions` 表结构已存在。

------

# **操作：新增与修改   操作人：fmy   操作时间：2026-08-13**

### 管理端接口文档：新增指标状态变更接口（单条/批量）与启用权重校验口径调整

- **背景**：原指标启用/禁用只能通过 `PUT /admin/indicators/{id}` 携带 `status` 字段完成，且启用校验在"同级全部启用"时强制 `启用子级权重和 = 父权重`（`validateSiblingWeightsAfterChange`），而创建/更新权重仅校验"不超过"，导致子级全禁用时可任意配权、一旦子级权重和 < 父权重便永远无法启用全部子级（`41004` 死锁）。状态切换还无法批量操作。
- **变更明细**：
  1. **新增 `PATCH /admin/indicators/{indicatorId}/status`（文档 1.8）** — 单独修改某指标启用/禁用状态。禁用时自动级联禁用其所有后代节点；启用时校验启用同级权重之和不超过父权重（一级之和不超过 1），超过返回 `41004`。
  2. **新增 `PATCH /admin/indicators/status`（文档 1.9）** — 批量修改一批指标（1~100 个）的启用/禁用状态。整体一个事务，任一指标校验失败（指标不存在、跨学校混合、启用后同级权重超限）整批不生效（fail-fast），报错信息含具体指标。
  3. **启用校验口径调整（方案 A）** — `validateSiblingWeightsAfterChange` 删除"同级全部启用时强制 = 父权重"分支，编辑期仅校验"不超过"；严格"等于"由发布（1.5）`validateWeightsForPublish` 统一校验。`PUT /admin/indicators/{id}` 携带 `status` 的逻辑同步受益，死锁根治。
- **新增/修改 DTO**：
  - `IndicatorStatusUpdateRequest.java` — 新增（字段：`status`）。
  - `IndicatorStatusBatchRequest.java` — 新增（字段：`indicatorIds`、`status`）。
  - `IndicatorStatusChangeResponse.java` — 新增（字段：`indicatorId`、`status`、`affectedCount`、`descendantCount`）。
- **涉及文件**：
  - `AdminIndicatorController.java` — 新增 1.8/1.9 两个 PATCH 端点，类 Javadoc 更新为 1.1~1.9。
  - `AdminIndicatorService.java` — 新增 `updateIndicatorStatus`/`updateIndicatorStatusBatch` 及私有 `applyStatusChange`/`collectDescendantIds`/`validateEnableSiblingSumsNotExceed`；放宽 `validateSiblingWeightsAfterChange`；类注释权重口径说明更新。
  - `管理端接口文档.md` — 新增 1.8/1.9 节；1.3 约束修正（禁用自动级联、启用"≤"口径）；1.1 权重说明补充编辑期口径。
  - `接口修改记录.md`（本节）。
- **验证**：`mvn compile -q` 通过。
- **说明**：本次无数据库结构变更，`数据库修改记录.md` 无需新增记录；`evaluation_indicators.status` 列已存在。

------

# **操作：修改   操作人：fmy   操作时间：2026-08-13**

### 管理端接口文档：1.3 更新指标接口取消 `status` 字段修改

- **背景**：状态变更已由 1.8（单个）/1.9（批量）专用接口承载，1.3 仍保留 `status` 字段形成第二条修改状态的路径。两条路径逻辑重复且易发散——历史死锁 bug（`validateSiblingWeightsAfterChange` 强制"启用同级和 = 父权重"）正是由 1.3 内嵌状态逻辑与创建/更新权重口径不一致引起。故取消 1.3 对 `status` 的支持，收敛为单一状态变更入口。
- **变更明细**：
  1. **1.3 不再接受 `status` 字段** — `updateIndicator` 顶部新增校验：请求携带 `status`（无论 0/1）直接返回 `10001 参数错误`，错误信息指引改用 1.8/1.9。`IndicatorUpdateRequest.status` 保留字段并标注废弃（传值即报错，避免 Spring 默认静默忽略未知字段导致"200 成功但状态未变更"的陷阱）。
  2. **移除 1.3 内嵌状态逻辑** — 删除原"禁用级联禁用后代"、"从禁用恢复启用时校验"、"置 status"三处处理；`updateIndicator` 现在只更新基础字段（名称/权重/说明/计分规则/维度/排序）。
  3. **权重校验收窄** — `validateSiblingWeightsAfterChange(indicator, request)` 重命名为 `validateWeightAgainstEnabledSiblings(indicator, newWeight)`，去除对 `request.status` 的依赖（仅 `weightChanged` 触发），校验语义不变（启用同级之和 + 新权重 ≤ 父权重 / 一级 ≤ 1）。
  4. **清理死代码** — 删除仅被 1.3 级联禁用块引用的 `findAllDescendants`/`collectDescendants`；1.8/1.9 使用的 `collectDescendantIds` 保留。
- **涉及文件**：
  - `AdminIndicatorService.java` — `updateIndicator` 增加 status 拒绝校验、移除级联禁用/置状态逻辑；`validateSiblingWeightsAfterChange` → `validateWeightAgainstEnabledSiblings`；删除 `findAllDescendants`/`collectDescendants`。
  - `IndicatorUpdateRequest.java` — `status` 字段标注废弃。
  - `AdminIndicatorController.java` — 1.3 Javadoc 可更新字段去掉"状态"，指向 1.8/1.9。
  - `管理端接口文档.md` — 1.3 说明与请求参数表移除 `status`；约束改为"不再支持 status，传值返回 10001"。
  - `接口修改记录.md`（本节）。
- **验证**：`mvn compile -q` 通过。
- **说明**：本次无数据库结构变更，`数据库修改记录.md` 无需新增记录；不破坏既有草稿树状态（仅移除 1.3 的变更入口，`evaluation_indicators.status` 值不受影响）。

------

# **操作：修改   操作人：fmy   操作时间：2026-08-14**

### 管理端指标状态接口：1.8 修复 `indicatorId` 返回为空、1.9 批量响应省略 `indicatorId` 字段

- **背景**：`IndicatorStatusChangeResponse` 由单个（1.8）/批量（1.9）共用 `applyStatusChange` 构建，builder 未设置 `indicatorId`，导致单个接口（契约应返回目标指标 ID）实际恒返回 `null`；批量接口本不应返回 `indicatorId`，但响应中会序列化出 `"indicatorId": null` 冗余字段。
- **变更明细**：
  1. **1.8 修复** — `AdminIndicatorService.updateIndicatorStatus` 在 `applyStatusChange` 返回后补 `response.setIndicatorId(indicatorId)`，单个接口现正常返回目标指标 ID。
  2. **1.9 省略字段** — `IndicatorStatusChangeResponse.indicatorId` 增加 `@JsonInclude(JsonInclude.Include.NON_NULL)`：批量接口（`indicatorId` 恒为 null）整个省略该字段，不再出现 `"indicatorId": null`。
- **涉及文件**：
  - `AdminIndicatorService.java` — `updateIndicatorStatus` 补充 `setIndicatorId`
  - `IndicatorStatusChangeResponse.java` — `indicatorId` 字段标注 `@JsonInclude(JsonInclude.Include.NON_NULL)`
  - `管理端接口文档.md` — 1.8/1.9 补充响应 `data` 字段表，明确 `indicatorId` 仅单条接口返回、批量整字段省略
  - `接口修改记录.md`（本节）
- **验证**：`mvn compile -q` 通过。
- **说明**：本次无数据库结构变更，`数据库修改记录.md` 无需新增记录；不改变接口契约——1.8 从「应返回却返回 null」修复为「返回目标 ID」，1.9 响应体更干净。

# **操作：修改   操作人：fmy   操作时间：2026-08-14**

### 管理端评分重算模块：3.1/3.2 响应补充 `message`、`statusLabel` 字段并同步文档

- **背景**：管理端接口文档 3.1 响应示例仅列 `taskId`/`status`/`createdAt`，未体现 `statusLabel` 与 `message`；3.2 响应示例同样缺 `statusLabel`。而代码返回的 `data` 中 `statusLabel` 已存在，只是文档未同步，`message` 字段缺失。
- **变更明细**：
  1. **3.1 触发评分重算** — `ScoreRecalculateResponse` 新增 `message` 字段，返回固定值「评分重算任务已创建」；`statusLabel` 原有字段保留并补齐文档。
  2. **3.2 查询任务进度** — `ScoreRecalculationTaskResponse` 新增 `message` 字段，按任务状态返回提示文案（排队中/执行中/完成摘要「评分重算完成（成功 X 条，失败 Y 条）」/失败原因）；`statusLabel` 原有字段保留并补齐文档。
  3. **文档同步** — `管理端接口文档.md` 3.1/3.2 响应示例补齐 `statusLabel`、`message`（3.2 同时补齐 `totalCount`），并新增响应 `data` 字段说明表。
- **涉及文件**：
  - `ScoreRecalculateResponse.java` — 新增 `message` 字段
  - `ScoreRecalculationTaskResponse.java` — 新增 `message` 字段
  - `AdminScoreService.java` — 触发响应设置 `message`；`toTaskResponse` 经 `taskMessage(task)` 生成任务状态提示
  - `管理端接口文档.md` — 3.1/3.2 响应示例与字段表同步
  - `接口修改记录.md`（本节）
- **验证**：`mvn compile -q` 通过。
- **说明**：本次仅增加响应字段（向后兼容，原有字段含义不变），无数据库结构变更，`数据库修改记录.md` 无需新增记录。

# **操作：修改   操作人：fmy   操作时间：2026-08-14**

### 管理端评分重算模块 3.2：per-student 失败原因持久化到任务 `error_message`

- **背景**：3.2 查询任务进度时，`status=2(完成)` 且 `failCount>0` 的情况下 `errorMessage` 恒为 `null`，前端无法得知具体学生失败原因（仅服务端日志可见）。
- **变更明细**：
  1. **持久化** — `AdminScoreService.executeAsync` 收集每条失败学生（`userId` + 异常原因），任务完成时若 `failCount>0` 将明细序列化为 JSON 数组 `[{"userId":1,"message":"..."}]` 写入 `error_message`；无失败仍为 `null`（与旧行为一致，不改变完成路径语义）。
  2. **响应扩展** — `ScoreRecalculationTaskResponse` 新增 `failures` 列表字段（元素 `{userId, message}`），`toTaskResponse` 从 `error_message` 中的 JSON 解析；任务级失败（纯文本）或空时不解析、返回 `null`。
  3. **文档同步** — `管理端接口文档.md` 3.2 字段表更新 `errorMessage` 说明并新增 `failures` 字段。
- **涉及文件**：
  - `ScoreRecalculationTaskResponse.java` — 新增 `failures` 字段与嵌套类 `StudentFailureItem`
  - `AdminScoreService.java` — 失败明细收集、`serializeFailures`/`parseFailures`/`errorText` 工具方法
  - `管理端接口文档.md` — 3.2 字段表
  - `接口修改记录.md`（本节）
- **验证**：`mvn compile -q` 通过。
- **说明**：无数据库结构变更（复用 `score_recalculation_tasks.error_message` TEXT 列），`数据库修改记录.md` 无需新增记录；仅新增响应字段，向后兼容。
