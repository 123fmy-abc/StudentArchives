# 学生档案管理系统 - 学生端接口文档（V5.13）

> 基于数据库表结构 V5.6 整理，仅保留学生端接口。
> 本文档同时作为系统全局规范、认证接口、通用接口、消息中心等共享内容的主文档，教师端与管理端文档引用此处定义。
> 更新日期：2026-09-01

------

## 基础信息




| 项目         | 说明                                         |
| :----------- | :------------------------------------------- |
| 基础URL      | `http://111.228.56.207:8089/api/v1`          |
| 请求格式     | JSON                                         |
| 认证方式     | Bearer Token (JWT)                           |
| API 版本     | `/api/v1/`，未来升级时递增主版本号，向后兼容 |
| 统一响应格式 | `{code: 0, message: "success", data: {...}, trace_id: "...", timestamp: "2026-07-04T14:30:25+08:00"}` |

------

## 全局规范

### 1. 响应体统一结构


```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

### 2. 分页参数（通用）

| 参数         | 类型   | 必填 | 说明                            |
| :----------- | :----- | :--- | :------------------------------ |
| `page`       | int    | 否   | 页码，默认 `1`                  |
| `per_page`   | int    | 否   | 每页条数，默认 `20`，最大 `100` |
| `sort_by`    | string | 否   | 排序字段                        |
| `sort_order` | string | 否   | `asc` / `desc`，默认 `desc`     |

### 3. 分页响应（通用）


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 156,
      "total_pages": 8
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

### 4. 幂等性与防重复提交

- 所有写操作接口（POST/PUT/DELETE）建议携带幂等键 `idempotency-key`，服务端 24 小时内同一 key 重复请求直接返回首次结果。
- 提交类接口需前端按钮防重复点击，服务端通过 `submit_count` 和状态机校验避免重复提交。
- 文件上传支持显示上传进度，失败时返回明确错误码（`10008` 上传失败 / `10007` 文件过大 / `10006` 文件格式错误）。

### 5. 全局错误码

#### 5.1 通用响应码

| Code | 含义         | 说明               |
| :--- | :----------- | :----------------- |
| 0    | 成功         | 接口执行成功       |
| 1    | 系统异常     | 未知异常           |
| 2    | 数据不存在   | 查询结果为空       |
| 3    | 数据已存在   | 唯一性校验失败     |
| 4    | 操作失败     | 通用失败           |
| 5    | 禁止操作     | 当前状态不允许执行 |
| 6    | 请求过于频繁 | 限流               |
| 7    | 服务暂不可用 | 系统维护           |
| 8    | 网络异常     | 第三方服务异常     |
| 9    | 并发冲突     | 乐观锁冲突         |

#### 5.2 参数相关（10000段）

| Code  | 含义         |
| :---- | :----------- |
| 10001 | 参数错误     |
| 10002 | 必填参数缺失 |
| 10003 | 参数格式错误 |
| 10004 | 参数超出范围 |
| 10005 | 非法参数     |
| 10006 | 文件格式错误 |
| 10007 | 文件过大     |
| 10008 | 上传失败     |

#### 5.3 认证授权（20000段）

| Code  | 含义       |
| :---- | :--------- |
| 20001 | 未登录     |
| 20002 | Token失效  |
| 20003 | Token错误  |
| 20004 | 登录已过期 |
| 20005 | 无访问权限 |
| 20006 | 账号被禁用 |
| 20007 | 账号被冻结 |
| 20008 | 密码错误   |

> **HTTP 状态码约定：** 20005（无访问权限）接口返回 HTTP `403 Forbidden`，响应体仍保持统一格式；20001/20002/20004 等登录态异常返回 HTTP `401 Unauthorized`。

#### 5.4 数据相关（30000段）

| Code  | 含义         |
| :---- | :----------- |
| 30001 | 数据不存在   |
| 30002 | 数据已删除   |
| 30003 | 数据重复     |
| 30004 | 数据状态异常 |
| 30005 | 数据已锁定   |
| 30006 | 数据关联存在 |
| 30007 | 数据校验失败 |
| 30008 | 数据版本冲突 |
| 30009 | 文件不存在或已失效 |

#### 5.5 业务相关（40000段）




| Code  | 含义             |
| :---- | :--------------- |
| 40001 | 操作失败         |
| 40002 | 当前状态不可操作 |
| 40003 | 审批未通过       |
| 40004 | 库存不足         |
| 40005 | 金额超限         |
| 40006 | 超出配额         |
| 40007 | 已提交审核       |
| 40008 | 已完成不可修改   |
| 40009 | 重复提交         |
| 40010 | 超出业务规则限制 |

### 6. 接口与参数命名规范

#### 6.1 URL 规范

| 规范项 | 说明 | 示例 |
| :----- | :--- | :--- |
| 资源路径统一使用复数 | 表示资源集合 | `/applications`、`/career-plans` |
| 操作使用 HTTP 方法表达 | 避免在 URL 中使用动词 | `POST /applications`（创建）、`PUT /applications/{id}`（更新） |
| 子资源置于父资源之后 | 体现资源层级 | `/applications/{id}/attachments`、`/profile/career-plans/{id}/download` |
| 文件相关接口统一置于 `/common/files` 下 | 与上传接口保持一致 | `GET /common/files/{fileId}/preview` |

#### 6.2 请求参数规范

| 规范项 | 说明 | 示例 |
| :----- | :--- | :--- |
| 统一采用小驼峰命名 | 字段首单词小写，后续单词首字母大写 | `oldPassword`、`captchaKey`、`confirmPassword` |
| 布尔值使用小写 | 统一规范 | `true` / `false` |
| 时间统一使用 ISO 8601 | 带时区 | `2026-07-04T14:30:25+08:00` |

#### 6.3 状态枚举规范

> 本文档中不同模块的 `status` 字段语义不同，调用时请务必按所属模块的枚举表解析，禁止混用。

| 枚举组 | 适用表/字段 | 说明 |
| :----- | :---------- | :--- |
| **账号状态** | `users.status`、`roles.status` | `0=禁用，1=正常`；与通用开关语义一致 |
| **通用开关** | `dictionaries.status`、`evaluation_indicators.status` 等 | `0=禁用，1=启用` |
| **业务审批状态** | `archives.status`、`award_applications.status`、`career_plans.status` | `0草稿→1待审批→2通过/3已退回/4已撤销` |

> **与需求口径对齐说明：** 已严格对齐 To Do List 中的“草稿、待审核、已退回、已通过、已撤销”五种状态。`3=已退回`表示教师退回修改，学生可编辑后重新提交；`4=已撤销`表示学生主动撤销、管理员撤销，或更正流程中原记录被替代后失效。

### 6.4 动态指标与维度说明

> 本文档中所有维度名称（如“学业成绩”“竞赛实践”）、指标名称、权重、目标分数均由后端 `evaluation_indicators`、`ability_dimensions` 等表动态配置。以下接口示例中出现的维度名称仅用于说明响应结构，**前端禁止写死**。首页雷达图、档案概览、成长分析等页面必须统一调用后端接口获取当前生效指标。

### 6.5 资源访问与文件下载权限

> 所有涉及学生个人数据的查询/操作接口（如 `/profile/*`、`/applications/{archiveId}`、`/activities/{activityId}`、`/career-plans/{id}` 等），后端必须校验当前登录用户是否为资源所有者。学生只能访问自己的档案、奖项、职业规划、消息和附件；越权访问统一返回 `20005 无访问权限`。
>
> 文件下载接口（`GET /common/files/{fileId}/download`、`GET /common/files/{fileId}/preview`）必须校验当前用户对该文件的访问权限：学生只能下载/预览自己申报的佐证材料，教师只能下载其授权班级或专业范围内学生的材料。禁止通过拼接 URL 直接访问他人文件。
>
> **数据权限扩展规则（V5.2 新增）：** `data_permission_rules` 表提供超出基础角色范围的补充规则，适用于学生互评可见范围、临时跨班查看、跨专业小组项目等场景。规则评估在基础权限校验通过后执行，两者取交集。具体实现由后端权限中间件统一解析，前端无需关心。

------

## 一、认证模块

### 1.1 获取图形验证码

**接口地址：** `GET /auth/captcha`

**请求参数：** 无

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "key": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "image": "data:image/png;base64,iVBORw0KGgo..."
  },
  "trace_id": "req-20260710-103000-abc123",
  "timestamp": "2026-07-10T10:30:00+08:00"
}
```

**说明：**

- 验证码有效期为 5 分钟
- 验证码使用后立即失效（不可重复使用）
- 图片包含干扰线和噪点，字符为 4 位大小写英文字母+数字（排除易混淆字符）
- 首次调用无需登录，属于公开接口

------

### 1.2 用户登录（获取JWT Token）

**接口地址：** `POST /auth/login`

**请求参数：**

| 参数名      | 类型    | 必填 | 说明                                                     |
| :---------- | :------ | :--- | :------------------------------------------------------- |
| userNo      | String  | 是   | 学号/工号（对应 `users.user_no`）                        |
| password    | String  | 是   | 密码（明文传输，后端与 `users.password` Bcrypt哈希校验） |
| captchaKey  | String  | 是   | 验证码标识（对应 `/auth/captcha` 返回的 `key`）          |
| captchaCode | String  | 是   | 验证码内容                                               |
| rememberMe  | Boolean | 否   | 是否记住我，默认 `false`。为 `true` 时返回 `refreshToken` 且令牌有效期更长 |

**请求示例：**


```json
{
  "userNo": "2023001001",
  "password": "123456",
  "captchaKey": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "captchaCode": "A7B3"
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIs...",
    "tokenType": "Bearer",
    "expiresIn": 86400,
    "refreshToken": "eyJhbGciOiJIUzI1NiIs...",
    "user": {
      "userId": 5,
      "userNo": "2023001001",
      "name": "赵小明",
      "email": "zhaoxm@demo.edu.cn",
      "gender": 1,
      "genderLabel": "男",
      "schoolId": 1,
      "schoolName": "示例大学",
      "roles": ["student"],
      "roleNames": ["学生"],
      "avatar": null
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> `user` 中基础信息来自 `users`；`email`、`avatar` 来自 `user_contact_infos`。
> `refreshToken` 仅在 `rememberMe=true` 时返回；`expiresIn` 在记住我状态下与常规状态的时长不同。

**错误码：**

| Code  | 说明                                                         |
| :---- | :----------------------------------------------------------- |
| 20008 | 账号或密码错误（账号不存在、密码错误、账号被禁用均返回此码） |
| 6     | 请求过于频繁（同一账号连续失败 5 次后锁定 2 分钟，响应消息包含剩余秒数） |

> **安全说明：** 为避免账号枚举，账号不存在、密码错误、账号被禁用等场景统一返回 `20008`。

------

### 1.3 获取当前用户信息

**接口地址：** `GET /auth/me`

**请求头：** `Authorization: Bearer {accessToken}`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userId": 5,
    "userNo": "2023001001",
    "name": "赵小明",
    "email": "zhaoxm@demo.edu.cn",
    "phone": "13800000005",
    "gender": 1,
    "genderLabel": "男",
    "schoolId": 1,
    "schoolName": "示例大学",
    "roles": ["student"],
    "roleNames": ["学生"],
    "permissions": ["student:archive:view", "student:archive:create", "student:archive:edit", "student:profile:view"],
    "scopes": [],
    "avatar": null
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T10:30:00+08:00"
}
```

> 用户基础信息来自 `users`；`email`、`phone`、`avatar` 来自 `user_contact_infos`。
>
> 本接口为学生/教师/管理员共用。`scopes` 为授权数据范围（来源 `role_scopes`，`status=1`）：教师/辅导员角色返回其授权范围（如班级、专业等），学生/管理员无范围，返回空数组。`scopes` 元素字段：
>
> | 字段            | 类型    | 说明                                                         |
> | :-------------- | :------ | :----------------------------------------------------------- |
> | `scopeType`     | Integer | 范围类型：1=学校 2=学院 3=专业 4=班级 5=课程 6=年级          |
> | `scopeTypeLabel`| String  | 范围类型标签（如“班级”“专业”）                              |
> | `scopeId`       | Long    | 范围 ID                                                      |
> | `scopeName`     | String  | 范围名称（按 `scopeType` 关联 `schools`/`colleges`/`majors`/`classes` 表，课程/年级暂为空） |
> | `semesterId`    | Long    | 学期 ID（可空）                                              |

------

### 1.4 退出登录

**接口地址：** `POST /auth/logout`

**请求头：** `Authorization: Bearer {accessToken}`

**请求参数：**




| 参数名 | 类型    | 必填 | 说明                                                         |
| :----- | :------ | :--- | :----------------------------------------------------------- |
| all    | Boolean | 否   | 兼容字段，当前后端忽略该值。调用本接口即吊销当前用户全部已签发 Token |

**请求示例：**

```json
{}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "退出成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

------

### 1.5 修改密码

**接口地址：** `PUT /auth/password`

**请求头：** `Authorization: Bearer {accessToken}`

**请求参数：**

| 参数名          | 类型   | 必填 | 说明                                  |
| :-------------- | :----- | :--- | :------------------------------------ |
| oldPassword     | String | 是   | 原密码（与 `users.password` 校验）    |
| newPassword     | String | 是   | 新密码（后端Bcrypt哈希后更新密码）    |
| confirmPassword | String | 是   | 确认新密码（需与 `newPassword` 一致） |

> **密码强度规则：** 长度 6-32 位，必须同时包含大写字母、小写字母、数字和特殊字符。

**请求示例：**


```json
{
  "oldPassword": "123456",
  "newPassword": "NewPass@888",
  "confirmPassword": "NewPass@888"
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "密码修改成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**错误码：**




| Code  | 说明       |
| :---- | :--------- |
| 20008 | 原密码错误 |

------

### 1.6 请求重置密码（发送邮件）

**接口地址：** `POST /auth/password/reset`

**说明：** 向注册邮箱发送 6 位随机数字验证码，验证码有效期为 5 分钟。

**请求参数：**

| 参数名 | 类型   | 必填 | 说明                      |
| :----- | :----- | :--- | :------------------------ |
| email  | String | 是   | 注册邮箱（`user_contact_infos.email`） |

**请求示例：**


```json
{
  "email": "zhaoxm@demo.edu.cn"
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "验证码已发送至您的邮箱，请查收",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**错误码：**

| Code  | 说明         |
| :---- | :----------- |
| 30001 | 邮箱未注册   |
| 20006 | 账号被禁用   |
| 50004 | 邮件发送失败 |
| 6     | 请求过于频繁（同一邮箱 60 秒内只能发送一次） |

> **安全说明：** 验证码为 6 位纯数字，5 分钟内有效且使用后立即失效。同一邮箱 60 秒内只能发送一次，防止接口被滥用。服务端存储验证码哈希值而非明文，邮箱与验证码哈希值配对校验。

------

### 1.7 确认重置密码

**接口地址：** `POST /auth/password/reset/confirm`

**请求参数：**

| 参数名            | 类型   | 必填 | 说明                              |
| :---------------- | :----- | :--- | :-------------------------------- |
| verificationCode  | String | 是   | 邮件中的 6 位数字验证码           |
| email           | String | 是   | 注册邮箱                          |
| newPassword     | String | 是   | 新密码（Bcrypt哈希加密）          |
| confirmPassword | String | 是   | 确认新密码（需与 `newPassword` 一致） |

> **密码强度规则：** 长度 6-32 位，必须同时包含大写字母、小写字母、数字和特殊字符。

**请求示例：**


```json
{
  "verificationCode": "837251",
  "email": "zhaoxm@demo.edu.cn",
  "newPassword": "NewPass@888",
  "confirmPassword": "NewPass@888"
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "密码重置成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**错误码：**

| Code  | 说明                 |
| :---- | :------------------- |
| 20003 | 验证码无效或已过期   |
| 30001 | 邮箱未注册           |

------

### 1.8 刷新访问令牌

**接口地址：** `POST /auth/refresh`

**说明：** 使用未过期的 `refreshToken` 换取新的 `accessToken` 与 `refreshToken`（Token Rotation）。调用后旧 `refreshToken` 立即失效。

**请求参数：**

| 参数名       | 类型   | 必填 | 说明                      |
| :----------- | :----- | :--- | :------------------------ |
| refreshToken | String | 是   | 登录或上一次刷新返回的刷新令牌 |

**请求示例：**

```json
{
  "refreshToken": "eyJhbGciOiJIUzI1NiIs..."
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIs...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIs...",
    "tokenType": "Bearer",
    "expiresIn": 86400
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**错误码：**

| Code  | 说明                      |
| :---- | :------------------------ |
| 20004 | 刷新令牌已过期            |
| 20003 | 刷新令牌无效              |
| 20002 | Token 已失效（版本不匹配或被复用） |
| 20006 | 账号被禁用                |
| 6     | 并发刷新（请使用最新刷新令牌） |

------

## 二、通用接口

### 2.1 文件上传接口

**接口地址：** `POST /common/upload`

**请求参数：**




| 参数名 | 类型   | 必填 | 说明                                                         |
| :----- | :----- | :--- | :----------------------------------------------------------- |
| file   | File   | 是   | 文件对象                                                     |
| `type`   | String | 是   | 文件类型，映射到 `file_uploads.file_category`：evidence→proof、certificate→certificate、plan→other（头像请使用专用接口 `POST /common/upload/avatar`，见 2.1.4；封面图请使用专用接口 `POST /common/upload/cover`，见 2.1.5） |
| module | String | 是   | 所属模块：competition、scholarship、practice、research、career_plan 等 |

**响应示例：**


```json
{
  "code": 0,
  "message": "上传成功",
  "data": {
    "fileId": 10001,
    "fileName": "certificate.jpg",
    "fileUrl": "https://student-archives.oss-cn-beijing.aliyuncs.com/competition/proof/2026-07/a1b2c3d4e5f6.jpg?OSSAccessKeyId=...&Expires=...&Signature=...",
    "objectKey": "competition/proof/2026-07/a1b2c3d4e5f6.jpg",
    "fileSize": 204800,
    "fileType": "image/jpeg"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> **文件存储说明：**
> - 文件统一存储于阿里云 OSS。首次上传写入 `file_uploads` 表（`file_status=1` 暂存，`temp_expire_at=NOW()+24H`），文件先存至 OSS 临时目录 `temp/{yyyy-MM}/{uuid}.{ext}`；关联业务提交时自动拷贝至正式目录 `{bizType}/{category}/{yyyy-MM}/{uuid}.{ext}` 并删除临时文件，同时更新 `file_uploads` 表置 `file_status=2`（已关联）、清空 `temp_expire_at`、更新 `biz_type`/`biz_id`
> - 响应中 `objectKey` 为 OSS 对象路径，`fileUrl` 为带签名的临时访问 URL（默认 30 分钟有效期，由 `oss.url-expire-minutes` 配置）
> - 响应字段 `fileType` 对应表字段 `mime_type`
>
> **后端校验：** 根据 `attachment_limits` 表中对应 `school_id` + `archive_type` 的配置校验文件类型、大小和数量。
>
> 校验规则（来源于 `attachment_limits` 表）：
> - 允许的文件扩展名：由 `allowed_extensions` 控制，如 `["pdf","jpg","png"]`
> - 单个文件最大大小：由 `max_file_size` 控制，默认 10MB
> - 文件最大数量：由 `max_files` 控制，默认 5 个
> - 校验失败时分别返回对应错误码：`10006 文件格式错误` / `10007 文件过大` / `10004 参数超出范围`

------

### 2.1.1 文件预览接口

**接口地址：** `GET /common/files/{fileId}/preview`

**响应：**

- 图片/PDF：直接返回文件流或在线预览 URL
- 其他类型：返回文件元数据

**响应示例（PDF/图片）：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "fileId": 10001,
    "fileName": "获奖证书.pdf",
    "fileUrl": "https://student-archives.oss-cn-beijing.aliyuncs.com/competition/proof/2026-07/abc123def456.pdf?OSSAccessKeyId=...&Expires=...&Signature=...",
    "objectKey": "competition/proof/2026-07/abc123def456.pdf",
    "fileType": "application/pdf",
    "previewUrl": "https://student-archives.oss-cn-beijing.aliyuncs.com/competition/proof/2026-07/abc123def456.pdf?OSSAccessKeyId=...&Expires=...&Signature=...",
    "canPreview": true
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 权限校验：学生只能预览自己的附件；教师可预览负责范围内学生的附件。
>
> **说明：** 预览通过 OSS 签名 URL 实现，后端校验权限后返回带签名的临时访问 URL（有效期由 `oss.url-expire-minutes` 配置），前端可直接用于 `<img>`、`<iframe>` 等标签的 `src` 属性。

------

### 2.1.2 文件下载接口

**接口地址：** `GET /common/files/{fileId}/download`

**响应：** 重定向至 OSS 签名 URL，返回文件流（Content-Disposition: attachment）；前端无需额外处理，浏览器自动下载

**错误码：**

| Code  | 说明                       |
| :---- | :------------------------- |
| 20005 | 无访问权限（非本人/非负责范围） |
| 30001 | 文件不存在                 |
| 30005 | 文件下载链接已过期         |
| 30009 | 文件不存在或已失效（OSS 物理文件已被清理/缺失） |

> 权限校验：与预览接口一致，学生只能下载自己的附件；教师可下载负责范围内学生的附件。下载时后端生成 OSS 签名 URL（默认 30 分钟有效，由 `oss.url-expire-minutes` 配置），重定向至该 URL 实现文件流下载，不直接暴露 OSS 真实密钥。导出文件、临时下载链接需校验 `file_uploads.download_expire_at`。

------

### 2.1.3 删除未提交附件接口

**接口地址：** `DELETE /common/files/{fileId}`

**路径参数：**

| 参数名 | 类型    | 必填 | 说明       |
| :----- | :------ | :--- | :--------- |
| fileId | Integer | 是   | 附件文件ID |

**响应示例：**

```json
{
  "code": 0,
  "message": "附件已删除",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：仅允许删除当前登录用户上传、且关联业务记录处于草稿/已退回状态或未关联业务记录的附件。已通过记录禁止删除附件。删除时对 `file_uploads` 记录做软删除（`deleted_at` 置为当前时间），同时删除 OSS 上的物理文件（调用 `OssFileService.deleteFile()`）。

------

### 2.1.4 上传头像接口

**接口地址：** `POST /common/upload/avatar`

**请求参数（multipart/form-data）：**

| 参数名 | 类型 | 必填 | 说明 |
| :----- | :--- | :--- | :--- |
| file   | File | 是   | 头像文件，仅支持 jpg/png/gif/webp/bmp，最大 2MB |

**响应示例：**

```json
{
  "code": 0,
  "message": "头像上传成功",
  "data": {
    "avatarUrl": "https://student-archives.oss-cn-beijing.aliyuncs.com/avatar/a1b2c3d4e5f6.jpg?OSSAccessKeyId=...&Expires=...&Signature=...",
    "objectKey": "avatar/a1b2c3d4e5f6.jpg"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> **说明：**
> - 与普通文件上传不同，头像上传**不走临时存储流程**：直接上传至 OSS `avatar/{uuid}.{ext}` 正式目录，并立即更新 `user_contact_infos.avatar` 字段。
> - 上传成功后返回的 `avatarUrl` 为 OSS 签名 URL（**30 天有效**），前端可直接用于 `<img>` 标签的 `src` 属性。
> - 生产环境建议 OSS 头像目录开启公共读，改为返回不带签名的永久 URL。
> - 更新头像后，登录返回和 `GET /auth/me` 返回的用户信息中的 `avatar` 字段将同步更新。
> - **本接口是头像字段的唯一写入入口**：`PUT /profile/contact`（4.1.1）不接收 `avatar` 字段，`GET /profile/info` 响应中的 `avatar` 为只读展示。

**错误码：**

| Code  | 说明                             |
| :---- | :------------------------------- |
| 10006 | 文件格式错误（非图片格式）       |
| 10007 | 文件过大（超过 2MB）             |
| 10008 | 上传失败（OSS 异常等）           |

------

### 2.1.5 上传封面图接口

**接口地址：** `POST /common/upload/cover`

**请求参数（multipart/form-data）：**

| 参数名 | 类型 | 必填 | 说明 |
| :----- | :--- | :--- | :--- |
| file   | File | 是   | 封面图片文件，仅支持 jpg/jpeg/png/gif/webp/bmp，最大 5MB |

**响应示例：**

```json
{
  "code": 0,
  "message": "封面上传成功",
  "data": {
    "coverUrl": "https://student-archives.oss-cn-beijing.aliyuncs.com/cover/photo/2026-08/a1b2c3d4e5f6.jpg?OSSAccessKeyId=...&Expires=...&Signature=...",
    "objectKey": "cover/photo/2026-08/a1b2c3d4e5f6.jpg"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> **说明：**
> - 与普通文件上传（2.1）不同，封面上传**不走临时存储流程**：直接上传至 OSS `cover/photo/{yyyy-MM}/{uuid}.{ext}` 正式目录，**不写入 `file_uploads` 表**。
> - 上传成功后返回的 `coverUrl` 为 OSS 签名 URL（**30 天有效**），前端将其作为 `coverImage` 字段传入成长时间轴新增（4.2.1）/ 修改（4.2.3）等业务接口；过期后需重新上传刷新。
> - 生产环境建议 OSS cover 目录开启公共读，改为返回不带签名的永久 URL。

**错误码：**

| Code  | 说明                             |
| :---- | :------------------------------- |
| 10001 | 参数错误（上传文件为空）         |
| 10006 | 文件格式错误（非图片格式）       |
| 10007 | 文件过大（超过 5MB）             |
| 10008 | 上传失败（OSS 异常等）           |

------

### 2.2 学期下拉选项接口

**接口地址：** `GET /common/semesters`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": [
    {"value": 1, "label": "2022-2023第一学期", "name": "2022-2023-1", "isCurrent": 0},
    {"value": 2, "label": "2022-2023第二学期", "name": "2022-2023-2", "isCurrent": 0},
    {"value": 3, "label": "2023-2024第一学期", "name": "2023-2024-1", "isCurrent": 1}
  ],
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`semesters` 表，`status=1` 且 `deleted_at IS NULL`，按 `start_date` 倒序。`name` 取 `semesters.name`，`label` 由 `name` 按规则生成（如"2022-2023第一学期"）。

------

### 2.3 字典数据接口（下拉选项）

**接口地址：** `GET /common/dict`

**请求参数：**




| 参数名   | 类型   | 必填 | 说明     |
| :------- | :----- | :--- | :------- |
| dictType | String | 是   | 字典类型 |

**支持的字典类型（对应 `dictionaries.dict_type`）：**

- `competition_level` - 竞赛级别
- `award_level` - 获奖等级
- `scholarship_level` - 奖学金级别
- `scholarship_award_level` - 奖学金获奖等级
- `company_type` - 公司类型
- `industry_type` - 行业类型
- `research_type` - 研究类型
- `project_level` - 项目级别
- `organization_level` - 组织级别
- `archive_category` - 档案分类（数据来源 `archive_type_configs` 表，而非 `dictionaries` 表）
- `message_category` - 消息分类
- `political_status` - 政治面貌
- `competition_type` - 竞赛类型

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": [
    {"value": "national", "label": "国家级", "sort": 1},
    {"value": "provincial", "label": "省部级", "sort": 2},
    {"value": "school", "label": "校级", "sort": 3},
    {"value": "college", "label": "院级", "sort": 4}
  ],
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`dictionaries` 表（`archive_category` 除外，数据来源为 `archive_type_configs` 表），`status=1` 且 `deleted_at IS NULL`，按 `sort` 正序。
>
> **字段映射：** `value` 对应 `dictionaries.dict_code`，`label` 对应 `dictionaries.dict_name`，`sort` 对应 `dictionaries.sort`。

------

### 2.4 指标树查询接口

**接口地址：** `GET /common/indicators`

**请求参数：**

| 参数名    | 类型    | 必填 | 说明                                    |
| :-------- | :------ | :--- | :-------------------------------------- |
| versionId | Integer | 否   | 指定指标版本ID，对应 `indicator_rule_versions.id`，不传则返回当前生效版本；传历史版本ID时返回该版本发布时点的完整指标树快照（`indicator_rule_versions.tree_snapshot`） |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "versionId": 3,
    "versionName": "2025-2026学年评价体系",
    "effectiveAt": "2025-09-01T00:00:00+08:00",
    "indicators": [
      {
        "indicatorId": 1,
        "indicatorCode": "academic",
        "indicatorName": "学业成绩",
        "level": 1,
        "weight": 0.35,
        "dimensionCode": "academic",
        "dimensionName": "学业成绩",
        "children": [
          {
            "indicatorId": 2,
            "indicatorCode": "academic.gpa",
            "indicatorName": "课程绩点",
            "level": 2,
            "weight": 0.7,
            "children": [
              {
                "indicatorId": 3,
                "indicatorCode": "academic.gpa.required",
                "indicatorName": "必修课绩点",
                "level": 3,
                "weight": 1.0
              }
            ]
          }
        ]
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`evaluation_indicators`、`indicator_rule_versions`、`ability_dimensions` 表，仅返回 `status=1` 且 `deleted_at IS NULL` 的指标。
>
> **版本快照说明：** 管理端发布指标规则版本（`POST /admin/indicators/publish`）时，后端会将完整指标树 JSON 快照写入 `indicator_rule_versions.tree_snapshot`。`versionId` 查询即读取对应版本快照，可精确回溯历史版本；发布后未发布的草稿改动对公共端不可见。快照落地前已发布的旧版本（`tree_snapshot` 为 `NULL`）回退按 `evaluation_indicators.version` 查询。
>
> **投影说明：** 本接口返回同一棵 `evaluation_indicators` 指标树的**轻量展示投影**（`indicatorId` 为 `evaluation_indicators.id` 的别名、仅返回展示字段）；管理端 `GET /admin/indicators/tree`（管理端文档 1.1）返回**管理模型**（节点字段 `id`、`weight`、`status`、`scoringRule`、`version`、`description` 等，编码为指标编码）。两者同源、真实数据一致，示例中编码 `academic` 与响应字段差异仅为示意，响应字段不同是有意设计。

------

## 三、首页模块

### 3.1 首页数据概览

**接口地址：** `GET /home/dashboard`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "studentName": "张三",
    "studentNo": "2024060001",
    "major": "计算机科学与技术",
    "className": "计科2401班",
    "grade": "2024级",
    "currentDate": "2026年7月7日星期一",
    "applicationTotal": 12,
    "approvedCount": 8,
    "pendingCount": 3,
    "rejectedCount": 1,
    "currentGpa": 3.82,
    "totalCredits": 86.5,
    "rankInClass": 5,
    "rankInMajor": 12,
    "indicators": [
      {"dimensionCode": "academic", "dimensionName": "学业成绩", "score": 88, "trend": "+7", "targetScore": 92, "gap": 4, "unit": "分", "comparedSemesterId": 2, "comparedSemesterName": "2022-2023-2", "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"},
      {"dimensionCode": "competition", "dimensionName": "竞赛实践", "score": 75, "trend": "+7", "targetScore": 90, "gap": 15, "unit": "分", "comparedSemesterId": 2, "comparedSemesterName": "2022-2023-2", "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"},
      {"dimensionCode": "research", "dimensionName": "科研创新", "score": 60, "trend": "+5", "targetScore": 80, "gap": 20, "unit": "分", "comparedSemesterId": 2, "comparedSemesterName": "2022-2023-2", "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"},
      {"dimensionCode": "socialWork", "dimensionName": "社会工作", "score": 85, "trend": "+3", "targetScore": 90, "gap": 5, "unit": "分", "comparedSemesterId": 2, "comparedSemesterName": "2022-2023-2", "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"},
      {"dimensionCode": "comprehensive", "dimensionName": "综合素质", "score": 80, "trend": "+4", "targetScore": 88, "gap": 8, "unit": "分", "comparedSemesterId": 2, "comparedSemesterName": "2022-2023-2", "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"}
    ],
    "radarChart": {
      "dimensions": [
        {"code": "academic", "name": "学业成绩"},
        {"code": "competition", "name": "竞赛实践"},
        {"code": "research", "name": "科研创新"},
        {"code": "socialWork", "name": "社会工作"},
        {"code": "comprehensive", "name": "综合素质"}
      ],
      "current": [88, 75, 60, 85, 80],
      "target": [92, 90, 80, 90, 88],
      "previous": [81, 68, 55, 82, 76]
    },
    "dataCompleteness": {"rate": 78, "missingItems": ["科研创新-项目经历", "社会实践-志愿时长"]},
    "quickEntries": [
      {"name": "成长时间轴", "icon": "timeline", "path": "/growth-timeline", "recent": true},
      {"name": "奖学金", "icon": "scholarship", "path": "/applications?tab=scholarship", "recent": true},
      {"name": "社会实践", "icon": "practice", "path": "/applications?tab=practice", "recent": true},
      {"name": "学科竞赛", "icon": "competition", "path": "/applications?tab=competition", "recent": false}
    ],
    "recentActivities": [
      {
        "id": 1,
        "title": "学科竞赛申报已提交",
        "time": "2026-06-28 14:30",
        "type": "submitted",
        "archiveType": "academic_competition",
        "status": 1
      }
    ],
    "unreadMessageCount": 5
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`users`、`student_profiles`、`classes`、`majors`、`semester_gpa_summaries`、`portrait_evaluation_scores`、`archives`、`user_messages` 等表聚合。
>
> **重要：** 本接口所有数值（含 `indicators`、`radarChart`、`dataCompleteness`）必须从上述真实数据表计算/查询返回，**禁止返回写死的演示数据**。

------

## 四、个人中心模块

### 4.1 获取个人档案信息

**接口地址：** `GET /profile/info`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "academicInfo": {
      "userId": 1001,
      "name": "张三",
      "studentNo": "2024060001",
      "grade": "2024级",
      "major": "计算机科学与技术",
      "degreeType": "undergraduate",
      "degreeTypeLabel": "本科",
      "className": "计科2401班",
      "collegeName": "信息工程学院",
      "gender": 1,
      "genderLabel": "男",
      "politicalStatus": "league_member",
      "politicalStatusLabel": "共青团员",
      "studentStatus": "fresh_graduate",
      "studentStatusLabel": "应届毕业生",
      "birthDate": "2005-03-15"
    },
    "contactInfo": {
      "email": "zhangsan@edu.cn",
      "phone": "13800138000",
      "avatar": "/uploads/avatar/xxx.jpg",
      "address": "北京市海淀区中关村大街xx号",
      "emergencyName": "张建国",
      "emergencyRelation": "父亲",
      "emergencyPhone": "13900139000"
    },
    "totalVolunteerHours": 48.5,
    "dimensionProfile": [
      {"dimensionCode": "academic", "dimensionName": "学业成绩", "score": 88, "targetScore": 92, "gap": 4, "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"},
      {"dimensionCode": "competition", "dimensionName": "竞赛实践", "score": 65, "targetScore": 90, "gap": 25, "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"},
      {"dimensionCode": "research", "dimensionName": "科研创新", "score": 60, "targetScore": 80, "gap": 20, "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"},
      {"dimensionCode": "socialWork", "dimensionName": "社会工作", "score": 85, "targetScore": 90, "gap": 5, "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"},
      {"dimensionCode": "comprehensive", "dimensionName": "综合素质", "score": 80, "targetScore": 88, "gap": 8, "calculationId": 101, "ruleVersion": 3, "calculatedAt": "2026-07-01T10:00:00+08:00"}
    ],
    "interests": [
      {
        "id": 1,
        "tagName": "编程开发",
        "detailContent": "Web 前端开发、人工智能应用",
        "proficiencyLevel": 3,
        "proficiencyLabel": "熟练",
        "weight": 15,
        "isDetail": 1
      },
      {
        "id": 2,
        "tagName": "语言能力",
        "detailContent": "英语（CET-6）、日语（N3）",
        "proficiencyLevel": 2,
        "proficiencyLabel": "良好",
        "weight": 8,
        "isDetail": 1
      }
    ],
    "semesterGrades": [
      {
        "semesterId": 1,
        "semester": "2022-2023-1",
        "semesterName": "2022-2023第一学期",
        "courseCount": 8,
        "totalCredit": 22.5,
        "gpa": 3.20,
        "averageScore": 85.6,
        "rankInClass": 15,
        "rankInMajor": 42
      },
      {
        "semesterId": 2,
        "semester": "2022-2023-2",
        "semesterName": "2022-2023第二学期",
        "courseCount": 9,
        "totalCredit": 24.0,
        "gpa": 3.50,
        "averageScore": 87.2,
        "rankInClass": 8,
        "rankInMajor": 25
      }
    ],
    "personalAwards": [
      {
        "category": "学科竞赛",
        "totalCount": 3,
        "maxLevel": "省级",
        "latestTime": "2025-09-01"
      },
      {
        "category": "奖学金",
        "totalCount": 2,
        "maxLevel": "校级",
        "latestTime": "2025-09-01"
      }
    ],
    "weaknessAnalysis": [
      {
        "id": 1,
        "weaknessType": "科研创新",
        "weaknessDesc": "科研项目经历较少，缺乏论文发表",
        "severityLevel": 3,
        "isRead": 1,
        "createdAt": "2026-06-01T10:00:00+08:00"
      }
    ],
    "selfEvaluation": "本人热爱计算机技术，具备扎实的编程基础与良好的团队协作能力。在校期间积极参与学科竞赛与实践项目，善于学习新技术并快速应用到实际场景中。"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`users`、`student_profiles`、`classes`、`majors`、`colleges`、`semester_gpa_summaries`、`portrait_evaluation_scores`、`user_interests`、`award_summaries`、`weakness_analyses`、`user_contact_infos` 等表。其中 `academicInfo` 为学籍同步信息，除 `politicalStatus`（可通过 `PUT /profile/political-status` 修改）、`studentStatus`（可通过 `PUT /profile/student-status` 修改）外均只读；`contactInfo` 为个人可修改联系信息，来自 `user_contact_infos`；`totalVolunteerHours` 由社会实践申报中的志愿时长累计汇总；`selfEvaluation` 来自 `student_profiles.self_evaluation`，未填写时返回 `null`，可通过 `PUT /profile/self-evaluation` 更新。
>
> **说明：**
> - `weaknessAnalysis[].relatedType` / `relatedId` 为**可选字段**，标识短板关联的具体业务记录（`archive` / `award_application` / `career_plan` 及对应记录 ID），无具体来源时返回 `null`（未命中则不返回）。
> - `academicInfo.politicalStatus` 返回 `student_profiles.political_status` 的**字典编码**（如 `league_member`）；`politicalStatusLabel` 为后端按字典表 `political_status` 解析出的**展示名称**（如“共青团员”）。
> - `academicInfo.studentStatus` 返回 `student_profiles.student_status` 的**枚举编码**（`current` / `fresh_graduate` / `graduated`）；`studentStatusLabel` 为后端解析出的**展示名称**（如“应届毕业生”）。
> - `academicInfo.degreeType` 返回 `majors.degree_type` 的**枚举编码**（`associate` / `undergraduate` / `master` / `doctor` / `postdoctor`）；`degreeTypeLabel` 为后端解析出的**展示名称**（如“本科”）。学历层次随专业只读，学生不可自行修改。

------

### 4.1.1 更新个人联系信息

**接口地址：** `PUT /profile/contact`

**请求参数：**

| 参数名            | 类型   | 必填 | 说明                                                             |
| :---------------- | :----- | :--- | :--------------------------------------------------------------- |
| email             | String | 是   | 邮箱（对应 `user_contact_infos.email`，空串表示清空）            |
| phone             | String | 是   | 手机号（对应 `user_contact_infos.phone`，空串表示清空）          |
| address           | String | 是   | 通讯地址（对应 `user_contact_infos.address`，空串表示清空）      |
| emergencyName     | String | 是   | 紧急联系人姓名（对应 `user_contact_infos.emergency_name`，空串表示清空） |
| emergencyRelation | String | 是   | 与紧急联系人关系（对应 `user_contact_infos.emergency_relation`，空串表示清空） |
| emergencyPhone    | String | 是   | 紧急联系人电话（对应 `user_contact_infos.emergency_phone`，空串表示清空） |

> **全量更新语义**：六个字段均必填，缺失任一字段返回参数校验错误；传空字符串表示清空对应字段；无需修改的字段以当前值回传。

**响应示例：**


```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "email": "zhangsan_new@edu.cn",
    "phone": "13800138000",
    "avatar": "/uploads/avatar/xxx.jpg",
    "address": "北京市海淀区中关村大街xx号",
    "emergencyName": "张建国",
    "emergencyRelation": "父亲",
    "emergencyPhone": "13900139000"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`user_contact_infos` 表；`users` 表中的 `email`、`phone`、`avatar` 不再作为学生端修改入口。紧急联系人字段（`emergency_name` / `emergency_relation` / `emergency_phone`）同样写入本表。
>
> **头像维护说明：** 本接口不包含 `avatar` 字段。头像只能通过专用上传接口 `POST /common/upload/avatar`（见 2.1.4）更新，该接口上传成功后直接写入 `user_contact_infos.avatar`，前端更新头像后刷新 `GET /profile/info` 即可；本接口返回的 `data.avatar` 为只读展示字段。

------

### 4.1.2 获取画像分数列表

**接口地址：** `GET /profile/scores`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明                       |
| :--------- | :------ | :--- | :------------------------- |
| semesterId | Integer | 否   | 学期ID，不传则返回当前学期 |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "semesterId": 3,
    "semesterName": "2023-2024-1",
    "calculatedAt": "2026-07-01T10:00:00+08:00",
    "ruleVersion": 3,
    "calculationId": 101,
    "list": [
      {"dimensionCode": "academic", "dimensionName": "学业成绩", "score": 88, "targetScore": 92, "gap": 4, "change": "+7", "comparedSemesterId": 2, "comparedSemesterName": "2022-2023-2", "unit": "分"}
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`portrait_evaluation_scores`、`ability_dimensions`、`indicator_rule_versions`、`semesters` 表。
>
> **版本说明：** `ruleVersion` 对应 `indicator_rule_versions.version`，即本次画像分数计算时所使用的指标规则全局快照版本。  
> **时间字段说明：** `calculatedAt` 对应 `portrait_evaluation_scores.evaluated_at`。

------

### 4.1.3 获取分数计算说明

**接口地址：** `GET /profile/scores/{calculationId}/details`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "calculationId": 101,
    "calculatedAt": "2026-07-01T10:00:00+08:00",
    "ruleVersion": 3,
    "dataSource": "已通过档案 + 学期成绩",
    "details": [
      {
        "indicatorId": 12,
        "indicatorName": "学科竞赛获奖",
        "dimensionCode": "competition",
        "dimensionName": "竞赛实践",
        "weight": 0.35,
        "rawScore": 85,
        "weightedScore": 29.75,
        "sourceArchiveIds": [1001, 1002],
        "sourceArchiveTitles": ["全国大学生数学建模竞赛", "全国大学生英语竞赛"]
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`score_calculations`、`score_calculation_details`、`evaluation_indicators`、`indicator_rule_versions`、`archives` 表。
>
> **版本说明：** `ruleVersion` 对应 `indicator_rule_versions.version`，即计算时生效的指标规则全局快照版本，可用于追溯本次评分使用了哪一套指标配置。  
> **时间字段说明：** `calculatedAt` 对应 `score_calculations.calculated_at`。

------

### 4.1.4 获取数据完整度

**接口地址：** `GET /profile/data-completeness`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明                       |
| :--------- | :------ | :--- | :------------------------- |
| semesterId | Integer | 否   | 学期ID，不传则返回当前学期 |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "semesterId": 3,
    "overallRate": 78,
    "dimensions": [
      {"dimensionCode": "academic", "dimensionName": "学业成绩", "rate": 95, "missingItems": []},
      {"dimensionCode": "research", "dimensionName": "科研创新", "rate": 45, "missingItems": ["科研项目", "论文发表"]}
    ],
    "updatedAt": "2026-07-01T10:00:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`data_completeness` 表。

------

### 4.1.5 更新政治面貌

**接口地址：** `PUT /profile/political-status`

**说明：** 更新学生的政治面貌。`politicalStatus` **必填**（缺失返回 `PARAM_ERROR`，如「政治面貌不能为空」）。学号、姓名、年级、专业、出生日期等学籍信息由教务系统同步，应用层只读，不支持在此修改。

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| politicalStatus | String  | 是   | 政治面貌，字典编码（`student_profiles.political_status`），字典类型 `political_status` |

**请求示例：**

```json
{
  "politicalStatus": "party_member"
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "politicalStatus": "party_member",
    "politicalStatusLabel": "中共党员"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`student_profiles` 表。
> **出生日期说明：** `birth_date` 字段存储在 `users` 表，由外部教务系统同步，应用层只读。如有生日信息错误需联系教务处更正，不在本接口处理范围内。

-------

### 4.1.5.1 更新学生状态

**接口地址：** `PUT /profile/student-status`

**说明：** 更新学生的状态。`studentStatus` **必填**（缺失返回 `PARAM_ERROR`，如「学生状态不能为空」），仅允许以下枚举编码：

| 编码            | 展示名称   |
| :-------------- | :--------- |
| `current`       | 在校生     |
| `fresh_graduate`| 应届毕业生 |
| `graduated`     | 已毕业     |

**请求参数：**

| 参数名        | 类型   | 必填 | 说明                                                         |
| :------------ | :----- | :--- | :----------------------------------------------------------- |
| studentStatus | String | 是   | 学生状态编码（`student_profiles.student_status`）            |

**请求示例：**

```json
{
  "studentStatus": "fresh_graduate"
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "studentStatus": "fresh_graduate",
    "studentStatusLabel": "应届毕业生"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`student_profiles` 表。

-------

### 4.1.6 更新自我评价

**接口地址：** `PUT /profile/self-evaluation`

**说明：** 全量更新学生的自我评价内容。`selfEvaluation` 字段可传空字符串以清空内容，但请求体不能缺失该字段；内容最长 2000 字。填写后可在 `GET /profile/info` 的 `data.selfEvaluation` 中读取。

**请求参数：**

| 参数名         | 类型   | 必填 | 说明                                |
| :------------- | :----- | :--- | :---------------------------------- |
| selfEvaluation | String | 否   | 自我评价内容，最大长度 2000，传空字符串表示清空 |

**请求示例：**

```json
{
  "selfEvaluation": "本人热爱计算机技术，具备扎实的编程基础与良好的团队协作能力。"
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "selfEvaluation": "本人热爱计算机技术，具备扎实的编程基础与良好的团队协作能力。"
  },
  "trace_id": "req-20260805-193000-abc123",
  "timestamp": "2026-08-05T19:30:00+08:00"
}
```

> **业务逻辑：** 写入 `student_profiles.self_evaluation`。若当前登录学生暂无 `student_profiles` 记录，则自动创建一条仅含 `user_id` 与 `self_evaluation` 的空档案。创建审计字段由 `@EntityListeners` 自动填充。清空操作可通过传 `""` 或调用本接口实现，无需独立的 DELETE 接口。

------

### 4.2 获取成长时间轴

**接口地址：** `GET /profile/growth-timeline`

**请求参数：**




| 参数名     | 类型    | 必填 | 说明                                                         |
| :--------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId | Integer | 否   | 学期ID筛选                                                   |
| eventType  | Integer | 否   | 事件类型：1=奖项 2=成绩 3=实践 4=职业规划 5=短板改进 6=能力提升 |
| status     | Integer | 否   | 审核状态筛选：0=草稿 1=待审核 2=已通过 3=已退回 4=已撤销     |
| viewType   | String  | 否   | 视图类型：`list`（默认）/ `tree` / `ring`，决定返回结构       |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "summary": {
      "experiences": 3,
      "skills": 4,
      "averageGrowth": "7%",
      "potential": "12%"
    },
    "timeline": [
      {
        "id": 1,
        "semesterId": 4,
        "semesterName": "2023-2024-2",
        "eventAt": "2025-07-05",
        "eventName": "暑期社会实践",
        "content": "参与乡村支教项目，学会了与不同背景的人沟通。",
        "eventType": 3,
        "eventTypeLabel": "实践",
        "status": 2,
        "statusLabel": "已通过",
        "coverImage": "/uploads/cover/xxx.jpg",
        "sourceId": 100,
        "sourceType": "archive_social_practices",
        "abilityData": [
          {"dimensionCode": "communication", "dimensionName": "沟通能力", "score": 80},
          {"dimensionCode": "responsibility", "dimensionName": "责任心", "score": 85}
        ],
        "tags": ["实践", "公益", "支教"]
      },
      {
        "id": 2,
        "semesterId": 3,
        "semesterName": "2023-2024-1",
        "eventAt": "2024-11-20",
        "eventName": "全国大学生数学建模竞赛获奖",
        "content": "获得省级二等奖",
        "eventType": 1,
        "eventTypeLabel": "奖项",
        "status": 2,
        "statusLabel": "已通过",
        "coverImage": null,
        "sourceId": 50,
        "sourceType": "archive_competitions",
        "abilityData": [
          {"dimensionCode": "innovation", "dimensionName": "创新能力", "score": 75},
          {"dimensionCode": "teamwork", "dimensionName": "团队协作", "score": 70}
        ],
        "tags": ["竞赛", "数学建模", "省级"]
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`growth_timelines`、`growth_timeline_abilities`、`growth_timeline_tags` 表。
>
> **视图类型说明：**
> - `viewType=list`（默认）：返回扁平时间线数组（如上例）。
> - `viewType=tree`：按 **学年 → 学期 → 事件** 分组返回树形结构，便于年轮/环形视图渲染。学年由学期名称推导（如 `2023-2024-1` → `2023-2024`）；无学期归属的事件归入 `"其他"` 学年分组。
> - `viewType=ring`：按事件日期所在**自然年**聚合，返回年度事件数与事件类型分布（`eventType` → 数量），供前端环形时间轴组件按年度扇区与类型着色。
> - 手机端默认使用 `list`。
>
> **响应示例（viewType=tree）：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "summary": {
      "experiences": 3,
      "skills": 4,
      "averageGrowth": "7%",
      "potential": "12%"
    },
    "tree": [
      {
        "academicYear": "2023-2024",
        "semesters": [
          {
            "semesterId": 4,
            "semesterName": "2023-2024-2",
            "events": [
              {
                "id": 1,
                "semesterId": 4,
                "semesterName": "2023-2024-2",
                "eventAt": "2025-07-05",
                "eventName": "暑期社会实践",
                "content": "参与乡村支教项目，学会了与不同背景的人沟通。",
                "eventType": 3,
                "eventTypeLabel": "实践",
                "status": 2,
                "statusLabel": "已通过",
                "coverImage": "/uploads/cover/xxx.jpg",
                "sourceId": 100,
                "sourceType": "archive_social_practices",
                "abilityData": [],
                "tags": ["实践", "公益", "支教"]
              }
            ]
          }
        ]
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> **响应示例（viewType=ring）：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "summary": {
      "experiences": 3,
      "skills": 4,
      "averageGrowth": "7%",
      "potential": "12%"
    },
    "ring": [
      {
        "year": "2025",
        "eventCount": 2,
        "typeDistribution": {"3": 1, "1": 1},
        "events": [
          {
            "id": 1,
            "semesterId": 4,
            "semesterName": "2023-2024-2",
            "eventAt": "2025-07-05",
            "eventName": "暑期社会实践",
            "content": "参与乡村支教项目，学会了与不同背景的人沟通。",
            "eventType": 3,
            "eventTypeLabel": "实践",
            "status": 2,
            "statusLabel": "已通过",
            "coverImage": "/uploads/cover/xxx.jpg",
            "sourceId": 100,
            "sourceType": "archive_social_practices",
            "abilityData": [],
            "tags": ["实践", "公益", "支教"]
          }
        ]
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：
> - `timeline` / `tree` / `ring` → `growth_timelines`、`growth_timeline_abilities`、`growth_timeline_tags`
> - `summary.experiences` → 时间轴经历总数；`summary.skills` → 时间轴涉及的能力维度去重数
> - `summary.averageGrowth` → `portrait_evaluation_scores` 最近一次评估各维度 `change / 上阶段分数` 均值百分比
> - `summary.potential` → `portrait_evaluation_scores` 最近一次评估各维度 `gap / 目标分` 均值百分比（提升空间）
>   无画像数据时 `averageGrowth` / `potential` 返回 `"0%"`，不返回虚构占位值。

------

### 4.2.1 新增成长时间轴事件

**接口地址：** `POST /profile/growth-timeline`

**请求头：** `Authorization: Bearer {accessToken}`

**请求参数：**

| 参数名      | 类型    | 必填 | 说明                                                         |
| :---------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId  | Integer | 否   | 学期ID（`growth_timelines.semester_id`），需为有效学期        |
| eventType   | Integer | 是   | 事件类型：1=奖项 2=成绩 3=实践 4=职业规划 5=短板改进 6=能力提升 |
| eventName   | String  | 是   | 事件名称（`growth_timelines.event_name`），最长 255 字符      |
| content     | String  | 否   | 事件详细描述/富文本（`growth_timelines.content`）             |
| coverImage  | String  | 否   | 封面图片 URL（`growth_timelines.cover_image`），最长 500 字符；先通过 `POST /common/upload/cover`（见 2.1.5）上传获取 |
| eventAt     | String  | 是   | 发生时间，格式 `YYYY-MM-DD`（`growth_timelines.event_at`）    |
| status      | Integer | 否   | 审核状态（0-4）。**不需要审核**，新增时后端强制置为 `2=已通过`（直接生效），本字段可不传 |
| sourceId    | Long    | **是** | 来源记录 ID（与 `sourceType` 配对）。**必须关联一条已有来源记录，否则报错** |
| sourceType  | String  | **是** | 来源模型类型（与 `sourceId` 配对），支持值见下方说明，不支持的来源类型返回 `10005` |
| eventKey    | String  | 否   | 业务去重键（`growth_timelines.event_key`），同用户下唯一       |
| abilityData | Array   | 否   | 能力维度得分列表，见下表                                     |
| tags        | Array   | 否   | 事件标签列表（`growth_timeline_tags`），自动去重              |

**abilityData 子对象：**

| 参数名        | 类型    | 必填 | 说明                                                         |
| :------------ | :------ | :--- | :----------------------------------------------------------- |
| dimensionCode | String  | 是   | 能力维度编码（对应 `ability_dimensions.dimension_code`），最长 50 字符 |
| score         | Decimal | 否   | 该事件带来的维度得分变化，0-100，默认 0                      |

**请求示例：**

```json
{
  "semesterId": 4,
  "eventType": 3,
  "eventName": "暑期社会实践",
  "content": "参与乡村支教项目，学会了与不同背景的人沟通。",
  "coverImage": "/uploads/cover/xxx.jpg",
  "eventAt": "2025-07-05",
  "status": 0,
  "abilityData": [
    {"dimensionCode": "communication", "score": 80},
    {"dimensionCode": "responsibility", "score": 85}
  ],
  "tags": ["实践", "公益", "支教"]
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "新增成功",
  "data": {
    "id": 16,
    "semesterId": 4,
    "semesterName": "2023-2024-2",
    "eventAt": "2025-07-05",
    "eventName": "暑期社会实践",
    "content": "参与乡村支教项目，学会了与不同背景的人沟通。",
    "eventType": 3,
    "eventTypeLabel": "实践",
    "status": 0,
    "statusLabel": "草稿",
    "coverImage": "/uploads/cover/xxx.jpg",
    "sourceId": null,
    "sourceType": null,
    "eventKey": null,
    "abilityData": [
      {"dimensionCode": "communication", "dimensionName": "沟通能力", "score": 80},
      {"dimensionCode": "responsibility", "dimensionName": "责任心", "score": 85}
    ],
    "tags": ["实践", "公益", "支教"],
    "createdAt": "2026-08-31T15:00:00+08:00",
    "updatedAt": "2026-08-31T15:00:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**说明：**

- 新增成功后自动生成主记录及 `abilityData` / `tags` 子记录。**不需要审核**：新增即直接生效，`status` 固定为 `2=已通过`。
- **来源记录必填**：`sourceType` + `sourceId` 必须关联一条当前学生已有、未软删除的来源记录，否则返回 `30001 数据不存在`（来源不存在或非本人）。
- 来源类型支持值（对应 `source_type`，来自《学生档案系统表》H 节）：
  - 奖项：`archive_competitions`、`award_competition_stars`
  - 成绩：`semester_gpa_summaries`、`gpa_records`
  - 实践：`archive_social_practices`、`archive_internships`、`archive_organizations`
  - 职业规划：`career_plans`、`career_plan_feedbacks`
  - 短板改进：`weakness_analyses`、`improvement_suggestions`
  - 能力提升：`portrait_evaluation_scores`
  - 另兼容 `archives` / `award_applications` 及别名 `archive` / `award_application`
- 事件去重键 `eventKey` 与来源（`sourceType` + `sourceId`）均受条件唯一索引约束（`uk_gt_event_key` / `uk_gt_source`），重复返回 `30003 数据重复`（同一条来源记录只能生成一个时间轴节点）。
- `sourceId` 与 `sourceType` 需成对出现，只传其一返回 `10001 参数错误`。
- `eventType` 非法（非 1-6）返回 `10005 非法参数`。
- 数据权限：后端校验当前登录学生是否为记录归属者，仅本人可新增（新增时归属自动为当前登录学生）。

**错误码：**

| Code  | 说明                                   |
| :---- | :------------------------------------- |
| 10001 | 参数错误（来源ID与来源类型未配对）     |
| 10003 | 参数格式错误（日期格式错误）           |
| 10005 | 非法参数（事件类型非 1-6 / 不支持的来源类型） |
| 30001 | 数据不存在（学期不存在 / 来源记录不存在或非本人） |
| 30003 | 数据重复（事件去重键或来源唯一约束冲突） |

------

### 4.2.2 获取成长时间轴事件详情

**接口地址：** `GET /profile/growth-timeline/{id}`

**请求头：** `Authorization: Bearer {accessToken}`

**路径参数：**

| 参数名 | 类型    | 必填 | 说明               |
| :----- | :------ | :--- | :----------------- |
| id     | Integer | 是   | 时间轴节点 ID      |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "semesterId": 4,
    "semesterName": "2023-2024-2",
    "eventAt": "2025-07-05",
    "eventName": "暑期社会实践",
    "content": "参与乡村支教项目，学会了与不同背景的人沟通。",
    "eventType": 3,
    "eventTypeLabel": "实践",
    "status": 2,
    "statusLabel": "已通过",
    "coverImage": "/uploads/cover/xxx.jpg",
    "sourceId": 100,
    "sourceType": "archive_social_practices",
    "eventKey": "zs-2025-practice",
    "abilityData": [
      {"dimensionCode": "communication", "dimensionName": "沟通能力", "score": 80}
    ],
    "tags": ["实践", "公益", "支教"],
    "createdAt": "2025-07-05T10:00:00+08:00",
    "updatedAt": "2025-07-05T10:00:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**说明：**

- 数据来源：`growth_timelines`、`growth_timeline_abilities`、`growth_timeline_tags` 表。
- 数据权限：后端校验当前登录学生是否为记录归属者，非本人返回 `20005 无访问权限`。

**错误码：**

| Code  | 说明                             |
| :---- | :------------------------------- |
| 20005 | 无访问权限（非本人事件）         |
| 30001 | 数据不存在（事件不存在或已删除） |

------

### 4.2.3 修改成长时间轴事件

**接口地址：** `PUT /profile/growth-timeline/{id}`

**请求头：** `Authorization: Bearer {accessToken}`

**路径参数：**

| 参数名 | 类型    | 必填 | 说明               |
| :----- | :------ | :--- | :----------------- |
| id     | Integer | 是   | 时间轴节点 ID      |

**请求参数：** 与 4.2.1 同名字段含义一致，**全部字段可选**，仅更新传入的非空字段。

| 参数名      | 类型    | 必填 | 说明                                                         |
| :---------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId  | Integer | 否   | 学期ID，不传保留原值                                         |
| eventType   | Integer | 否   | 事件类型（1-6），不传保留原值                                |
| eventName   | String  | 否   | 事件名称，不传保留原值                                       |
| content     | String  | 否   | 事件详细描述，不传保留原值，传空字符串清空                   |
| coverImage  | String  | 否   | 封面图片 URL，不传保留原值，传空字符串清空；新图先通过 `POST /common/upload/cover`（见 2.1.5）上传获取 |
| eventAt     | String  | 否   | 发生时间（`YYYY-MM-DD`），不传保留原值                       |
| status      | Integer | 否   | 审核状态（0-4），不传保留原值                                |
| sourceId    | Long    | 否   | 来源记录 ID，不传保留原值；传入时须关联已有来源记录          |
| sourceType  | String  | 否   | 来源模型类型，不传保留原值；传空字符串报错（来源记录必填，不允许清空） |
| eventKey    | String  | 否   | 业务去重键，不传保留原值，传空字符串清空                     |
| abilityData | Array   | 否   | 能力维度得分列表，传入（含空数组）时整体替换                 |
| tags        | Array   | 否   | 事件标签列表，传入（含空数组）时整体替换                     |

**请求示例：**

```json
{
  "eventName": "暑期乡村支教社会实践",
  "content": "参与乡村支教项目，学会了与不同背景的人沟通，获校级优秀团队。",
  "tags": ["实践", "公益", "支教", "优秀团队"]
}
```

**响应示例：** 与 4.2.2 相同（返回更新后详情），`message` 为 `更新成功`。

**说明：**

- **不需要审核**：任意状态（含 `status=2(已通过)`）均可直接修改，不再返回 `40002`。
- 来源记录必填且不允许清空：传空字符串 `sourceType` 返回 `10001 参数错误`；传入新来源时须关联已有来源记录（未软删除且归属当前学生），否则返回 `30001 数据不存在`。
- `abilityData` / `tags` 传入时整体替换子表数据（旧子记录软删除后重建）；传空数组表示清空该子表。
- 其余校验（来源配对、唯一键、日期格式、归属校验）与 4.2.1 一致。

**错误码：**

| Code  | 说明                                         |
| :---- | :------------------------------------------- |
| 10001 | 参数错误（来源ID与来源类型未配对 / 来源不能清空） |
| 10003 | 参数格式错误（日期格式错误）                 |
| 10005 | 非法参数（事件类型非 1-6 / 不支持的来源类型） |
| 20005 | 无访问权限（非本人事件）                     |
| 30001 | 数据不存在（事件不存在 / 来源记录不存在或非本人） |
| 30003 | 数据重复（事件去重键或来源唯一约束冲突）     |

------

### 4.2.4 删除成长时间轴事件

**接口地址：** `DELETE /profile/growth-timeline/{id}`

**请求头：** `Authorization: Bearer {accessToken}`

**路径参数：**

| 参数名 | 类型    | 必填 | 说明               |
| :----- | :------ | :--- | :----------------- |
| id     | Integer | 是   | 时间轴节点 ID      |

**响应示例：**

```json
{
  "code": 0,
  "message": "删除成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**说明：**

- 软删除：对 `growth_timelines` 主记录及其 `growth_timeline_abilities`、`growth_timeline_tags` 子记录置 `deleted_at`，删除后列表与详情接口不再返回。

**错误码：**

| Code  | 说明                             |
| :---- | :------------------------------- |
| 20005 | 无访问权限（非本人事件）         |
| 30001 | 数据不存在（事件不存在或已删除） |

------

### 4.3 获取职业规划列表

**接口地址：** `GET /profile/career-plans`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明                       |
| :--------- | :------ | :--- | :------------------------- |
| semesterId | Integer | 否   | 学期ID，不传返回全部学期   |
| page       | Integer | 否   | 页码，默认1                |
| per_page   | Integer | 否   | 每页条数，默认10           |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "semesterId": 3,
        "semesterName": "2023-2024-1",
        "title": "大二学年成长规划",
        "progressRate": 65,
        "status": 2,
        "statusLabel": "已通过",
        "submittedAt": "2025-09-15T10:00:00+08:00",
        "auditedAt": "2025-09-20T14:00:00+08:00",
        "currentVersion": 2,
        "submitCount": 2,
        "rejectedReason": null,
        "auditorName": "王老师"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 10,
      "total": 1,
      "total_pages": 1
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`career_plans` 表。

------

### 4.4 新增/提交职业规划

**接口地址：** `POST /profile/career-plans`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID（`career_plans.semester_id`）                         |
| title           | String  | 是   | 规划标题（`career_plans.title`）                             |
| content         | String  | 否   | 规划总述（`career_plans.content`）                           |
| requirement     | String  | 否   | 要求/目标（`career_plans.requirement`）                      |
| isDraft         | Integer | 否   | 0=提交（status=1待审批） 1=保存草稿（status=0），默认0       |
| goals           | Array   | 否   | 目标列表，见下表                                             |
| evidenceFileIds | Array   | 否   | 规划文件附件ID列表（先通过文件上传接口上传，再传入返回的 fileId） |

**goals 子对象：**

| 参数名      | 类型    | 必填 | 说明                                          |
| :---------- | :------ | :--- | :-------------------------------------------- |
| goalTitle   | String  | 是   | 目标标题（`career_goals.goal_title`）         |
| goalDesc    | String  | 否   | 目标描述（`career_goals.goal_desc`）          |
| targetDate  | String  | 否   | 目标日期，格式 `YYYY-MM-DD`                   |
| actions     | Array   | 否   | 行动列表，见下表                              |

**actions 子对象：**

| 参数名      | 类型    | 必填 | 说明                                          |
| :---------- | :------ | :--- | :-------------------------------------------- |
| actionTitle | String  | 是   | 行动标题（`career_actions.action_title`）     |
| actionDesc  | String  | 否   | 行动描述（`career_actions.action_desc`）      |
| startDate   | String  | 否   | 开始日期，格式 `YYYY-MM-DD`                   |
| endDate     | String  | 否   | 结束日期，格式 `YYYY-MM-DD`                   |
| milestones  | Array   | 否   | 里程碑列表，见下表                            |

**milestones 子对象：**

| 参数名         | 类型   | 必填 | 说明                                         |
| :------------- | :----- | :--- | :------------------------------------------- |
| milestoneTitle | String | 是   | 里程碑标题（`career_milestones.milestone_title`） |
| milestoneDate  | String | 否   | 预计达成日期，格式 `YYYY-MM-DD`              |

**请求示例：**


```json
{
  "semesterId": 3,
  "title": "大二学年成长规划",
  "content": "本学年重点提升科研创新能力",
  "requirement": "GPA达到3.5以上，参与至少一项科研项目",
  "isDraft": 0,
  "evidenceFileIds": [10001, 10002],
  "goals": [
    {
      "goalTitle": "参与一项科研项目",
      "goalDesc": "加入导师课题组",
      "targetDate": "2025-12-31",
      "actions": [
        {
          "actionTitle": "联系导师",
          "actionDesc": "发送邮件并面谈",
          "startDate": "2025-09-01",
          "endDate": "2025-09-15",
          "milestones": [
            {"milestoneTitle": "确定导师意向", "milestoneDate": "2025-09-10"}
          ]
        }
      ]
    }
  ]
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "提交成功",
  "data": {
    "planId": 3,
    "status": 1,
    "statusLabel": "待审批",
    "currentVersion": 1,
    "submitCount": 1
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：创建 `career_plans` 主记录，同时级联创建 `career_goals`、`career_actions`、`career_milestones`，并写入 `model_versions` 版本记录（`model_type='career_plan'`），`submit_count` +1。传入的 `evidenceFileIds` 需校验每个文件 `file_status=1`（防重复绑定），通过后更新 `file_uploads` 表的 `biz_type='career_plan'`，`biz_id` 指向新创建的规划 ID，置 `file_status=2`，清空 `temp_expire_at`。

------

### 4.5 下载职业规划文件

**接口地址：** `GET /profile/career-plans/{planId}/download`

**请求参数：**

| 参数名  | 类型   | 必填 | 说明                                                         |
| :------ | :----- | :--- | :----------------------------------------------------------- |
| purpose | String | 否   | 导出用途：`internal`（内部查看，屏幕水印+打印隐藏）/ `external`（外部投递，**默认**，屏幕和打印均无水印） |

**响应：** 重定向至 OSS 签名 URL，文件流（PDF），浏览器自动下载

**说明：**

- **独立文档**：本接口按单条职业规划详情生成**独立 PDF 文档**，不再返回上传佐证文件或档案导出 PDF。文档内容：
  - 封面：学生姓名、学号、所属学期、规划标题；
  - 一、规划信息：标题、学期、整体进度、审批状态、提交时间、审核时间、审核教师、退回原因；
  - 二、要求与目标；三、规划内容；
  - 四、目标与行动：按「目标 → 行动 → 里程碑」嵌套展示，含行动起止时间、完成度、里程碑达成状态；
  - 五、阶段反思；六、教师反馈。
  - 空数据章节统一显示「暂无」，保持章节编号稳定。
- **水印策略**：
  - `purpose=external`（默认）：屏幕和打印均不添加水印，用于投递给企业/导师等外部场景；为保证文件干净，**不写入 `career_plans.file_id` 缓存**，每次调用都重新生成。
  - `purpose=internal`：屏幕显示水印，打印时自动隐藏；生成文件写入 `career_plans.file_id` 缓存，再次下载直接复用。
- **过期感知**：规划被编辑或审批状态变化（`career_plans.updated_at` 晚于生成文件更新时间）后，自动重新生成并替换旧文件，保证文档反映最新状态。
- 上传佐证文件（`biz_type='career_plan'`）仍可经 `GET /common/files/{fileId}/download`（2.1.2）访问，不作为本接口的「职业规划文件」返回。
- 模板来自 `export_templates` 表 `career_plan` 类型默认模板（页眉「职业规划」、水印为学生姓名），与学生成长档案导出（4.17）模板相互独立。
- **文件名**：固定为 `职业规划文件.pdf`（不含日期），下载时经 `response-content-disposition` 携带中文名；已缓存的旧版日期文件名（如 `职业规划-20260806.pdf`）会在复用文件时自动刷新为新名称，无需重新生成 PDF。

**错误码：**

| Code   | 说明                                   |
| :----- | :------------------------------------- |
| 5      | 无访问权限（非本人规划）               |
| 30001  | 数据不存在（规划文件缺失/生成失败）    |
| 4      | 操作失败（文件生成失败）               |
| 50005  | OSS上传失败（文件上传失败）            |

------

### 4.5.1 预览职业规划文件

**接口地址：** `GET /profile/career-plans/{planId}/preview`

**请求参数：**

| 参数名  | 类型   | 必填 | 说明                                                         |
| :------ | :----- | :--- | :----------------------------------------------------------- |
| purpose | String | 否   | 导出用途：`internal`（内部预览，屏幕水印+打印隐藏）/ `external`（外部投递，**默认**，屏幕和打印均无水印） |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "previewUrl": "https://student-archives.oss-cn-chengdu.aliyuncs.com/career_plan_export/pdf/2026-08/xxxx.pdf?OSSAccessKeyId=...&Expires=...&Signature=...&response-content-disposition=attachment%3B%20filename%3D%22%E8%81%8C%E4%B8%9A%E8%A7%84%E5%88%92%E6%96%87%E4%BB%B6.pdf%22%3B%20filename%2A%3DUTF-8%27%27...",
    "fileName": "职业规划文件.pdf",
    "purpose": "external",
    "generatedAt": "2026-08-06T10:00:00+08:00"
  }
}
```

**说明：**

- 与下载（4.5）共用同一份生成/缓存文件：复用未过期缓存或惰性生成，保证「预览即所见即所得」。
- 返回的 `previewUrl` 为 OSS 签名 URL，`response-content-disposition` 覆盖为 `attachment`，文件名以**百分号编码后的 UTF-8** 同时放入 `filename` 与 `filename*`（RFC 5987 双通道，纯 ASCII）。浏览器优先采用 `filename*` 解码出 `职业规划文件.pdf`；仅读 `filename` 的工具（如 Apifox）能解码 `%E8%81%8C...` 得到中文名，且不会出现原始 UTF-8 被按 ISO-8859-1 解读的乱码。
- **内嵌渲染说明**：阿里云 OSS 默认域名安全策略对新建 Bucket 强制返回 `Content-Disposition: attachment`（`x-oss-force-download`），本 Bucket 当前使用默认域名，因此 `previewUrl` 在浏览器中会触发**下载**而非内嵌渲染。如需真正内嵌渲染 PDF，需为 Bucket 绑定已备案自定义域名，或在后端代理流式返回 PDF（`Content-Disposition: inline`）。当前接口保证下载命名正确，前端可在新标签页打开 `previewUrl` 查看/保存。
- `purpose=external` 返回无水印版本，便于投递前确认最终版式。
- 有效期与下载一致：`oss.url-expire-minutes` 配置，默认 30 分钟。

**错误码：**

| Code   | 说明                                   |
| :----- | :------------------------------------- |
| 5      | 无访问权限（非本人规划）               |
| 30001  | 数据不存在（规划文件缺失/生成失败）    |
| 4      | 操作失败（文件生成失败）               |
| 50005  | OSS上传失败（文件上传失败）            |

------

### 4.6 获取职业规划详情

**接口地址：** `GET /profile/career-plans/{planId}`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "semesterId": 3,
    "semesterName": "2023-2024-1",
    "title": "大二学年成长规划",
    "content": "本学年重点提升科研创新能力",
    "requirement": "GPA达到3.5以上，参与至少一项科研项目",
    "progressRate": 65,
    "status": 2,
    "statusLabel": "已通过",
    "submittedAt": "2025-09-15T10:00:00+08:00",
    "auditedAt": "2025-09-20T14:00:00+08:00",
    "auditorName": "王老师",
    "rejectedReason": null,
    "copyFromId": null,
    "source": 1,
    "sourceLabel": "手动创建",
    "aiSuggestionId": null,
    "requireConfirm": 0,
    "goals": [
      {
        "id": 10,
        "goalTitle": "参与一项科研项目",
        "goalDesc": "加入导师课题组",
        "targetDate": "2025-12-31",
        "status": 1,
        "statusLabel": "进行中",
        "actions": [
          {
            "id": 20,
            "actionTitle": "联系导师",
            "actionDesc": "发送邮件并面谈",
            "status": 2,
            "statusLabel": "已完成",
            "startDate": "2025-09-01",
            "endDate": "2025-09-15",
            "completionRate": 100,
            "files": [
              {"fileId": 1001, "fileName": "导师确认邮件.pdf", "fileUrl": "/uploads/..."}
            ],
            "milestones": [
              {"id": 30, "milestoneTitle": "确定导师意向", "milestoneDate": "2025-09-10", "isAchieved": 1, "proofFileId": 2001, "proofFileName": "导师确认邮件.pdf", "proofFileUrl": "/uploads/..."}
            ]
          }
        ]
      }
    ],
    "reflections": [
      {"id": 1, "reflectionContent": "本阶段科研能力有所提升", "createdAt": "2025-10-01T10:00:00+08:00"}
    ],
    "feedbacks": [
      {"id": 1, "teacherName": "王老师", "feedbackContent": "目标明确，建议增加论文阅读计划", "createdAt": "2025-09-25T10:00:00+08:00"}
    ],
    "versionHistory": [
      {"version": 1, "status": 1, "statusLabel": "待审批", "createdAt": "2025-09-15T10:00:00+08:00"},
      {"version": 2, "status": 2, "statusLabel": "已通过", "createdAt": "2025-09-20T14:00:00+08:00"}
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`career_plans`、`career_goals`、`career_actions`、`career_milestones`、`file_uploads`（`biz_type='career_plan'` 规划附件、`biz_type='career_action'` 行动成果、`biz_type='career_milestone'` 里程碑证明）、`career_reflections`、`career_plan_feedbacks`、`model_versions` 等表。

------

### 4.7 复制上一学期计划

**接口地址：** `POST /profile/career-plans/copy`

**请求参数：**

| 参数名           | 类型    | 必填 | 说明                       |
| :--------------- | :------ | :--- | :------------------------- |
| sourceSemesterId | Integer | 是   | 源学期ID                   |
| targetSemesterId | Integer | 是   | 目标学期ID                 |
| title            | String  | 否   | 新规划标题，不传则自动生成 |

**响应示例：**


```json
{
  "code": 0,
  "message": "复制成功",
  "data": {
    "planId": 5,
    "copyFromId": 1,
    "status": 0,
    "statusLabel": "草稿"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：复制源学期的 `career_plans` 及其 `career_goals`、`career_actions`、`career_milestones`，`copy_from_id` 指向源规划，状态重置为草稿。

------

### 4.8 添加目标

**接口地址：** `POST /profile/career-plans/{planId}/goals`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明                        |
| :--------- | :------ | :--- | :-------------------------- |
| goalTitle  | String  | 是   | 目标标题                    |
| goalDesc   | String  | 否   | 目标描述                    |
| targetDate | String  | 否   | 目标日期，格式 `YYYY-MM-DD` |
| sort       | Integer | 否   | 排序，默认0                 |

**响应示例：**


```json
{
  "code": 0,
  "message": "添加成功",
  "data": {"goalId": 11},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 限制：仅 `status=0(草稿)` 或 `status=3(已退回)` 时可编辑目标。

------

### 4.8.1 更新目标

**接口地址：** `PUT /profile/career-plans/{planId}/goals/{goalId}`

**路径参数：**

| 参数名 | 类型    | 必填 | 说明     |
| :----- | :------ | :--- | :------- |
| planId | Integer | 是   | 规划 ID  |
| goalId | Integer | 是   | 目标 ID  |

**请求参数：**

| 参数名     | 类型    | 必填 | 说明                                                        |
| :--------- | :------ | :--- | :---------------------------------------------------------- |
| goalTitle  | String  | 是   | 目标标题                                                    |
| goalDesc   | String  | 否   | 目标描述，不传保留原值，传空字符串清空                      |
| targetDate | String  | 否   | 目标日期，格式 `YYYY-MM-DD`；不传保留原值，传空字符串清空   |
| sort       | Integer | 否   | 排序，不传保留原值                                          |

**请求示例：**


```json
{
  "goalTitle": "参与一项科研项目",
  "goalDesc": "加入导师课题组并完成结题",
  "targetDate": "2025-12-31",
  "sort": 0
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "更新成功",
  "data": {"goalId": 11},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 限制：仅 `status=0(草稿)` 或 `status=3(已退回)` 时可编辑目标。目标不存在返回 `30001`，非本人规划返回 `5`。

------

### 4.8.2 删除目标

**接口地址：** `DELETE /profile/career-plans/{planId}/goals/{goalId}`

**路径参数：**

| 参数名 | 类型    | 必填 | 说明     |
| :----- | :------ | :--- | :------- |
| planId | Integer | 是   | 规划 ID  |
| goalId | Integer | 是   | 目标 ID  |

**响应示例：**


```json
{
  "code": 0,
  "message": "删除成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：级联软删该目标下的行动、里程碑及行动已绑定的成果文件（软删 `file_uploads` 记录并删除 OSS 物理文件，与 2.1.3 删除附件口径一致），并重算规划进度。限制：仅草稿/已退回可删除，已通过（`status=2`）禁止删除。

------

### 4.9 添加行动

**接口地址：** `POST /profile/career-plans/{planId}/goals/{goalId}/actions`

**路径参数：**

| 参数名 | 类型    | 必填 | 说明                       |
| :----- | :------ | :--- | :------------------------- |
| planId | Integer | 是   | 规划 ID                    |
| goalId | Integer | 是   | 目标 ID（行动归属的目标）  |

**请求参数：**

| 参数名      | 类型    | 必填 | 说明                        |
| :---------- | :------ | :--- | :-------------------------- |
| actionTitle | String  | 是   | 行动标题                    |
| actionDesc  | String  | 否   | 行动描述                    |
| startDate   | String  | 否   | 开始日期，格式 `YYYY-MM-DD` |
| endDate     | String  | 否   | 结束日期，格式 `YYYY-MM-DD` |
| sort        | Integer | 否   | 排序，默认0                 |

**请求示例：**


```json
{
  "actionTitle": "联系导师",
  "actionDesc": "发送邮件并面谈，确认研究方向",
  "startDate": "2025-09-01",
  "endDate": "2025-09-20",
  "sort": 0
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "添加成功",
  "data": {"actionId": 21},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`career_actions` 表，关联 `career_goals.id`。目标不存在返回 `30001`，非本人规划返回 `5`。

------

### 4.9.1 更新行动

**接口地址：** `PUT /profile/career-plans/{planId}/actions/{actionId}`

**路径参数：**

| 参数名   | 类型    | 必填 | 说明     |
| :------- | :------ | :--- | :------- |
| planId   | Integer | 是   | 规划 ID  |
| actionId | Integer | 是   | 行动 ID  |

**请求参数：**

| 参数名      | 类型    | 必填 | 说明                                                            |
| :---------- | :------ | :--- | :-------------------------------------------------------------- |
| actionTitle | String  | 是   | 行动标题                                                        |
| actionDesc  | String  | 否   | 行动描述，不传保留原值，传空字符串清空                          |
| startDate   | String  | 否   | 开始日期，格式 `YYYY-MM-DD`；不传保留原值，传空字符串清空       |
| endDate     | String  | 否   | 结束日期，格式 `YYYY-MM-DD`；不传保留原值，传空字符串清空       |
| sort        | Integer | 否   | 排序，不传保留原值                                              |

**请求示例：**


```json
{
  "actionTitle": "联系导师",
  "actionDesc": "发送邮件并面谈，确认研究方向",
  "startDate": "2025-09-01",
  "endDate": "2025-09-20",
  "sort": 0
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "更新成功",
  "data": {"actionId": 21},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 说明：状态字段不走本接口，行动状态仍通过 4.12 更新行动状态接口维护。限制：仅 `status=0(草稿)` 或 `status=3(已退回)` 时可编辑行动。

------

### 4.9.2 删除行动

**接口地址：** `DELETE /profile/career-plans/{planId}/actions/{actionId}`

**路径参数：**

| 参数名   | 类型    | 必填 | 说明     |
| :------- | :------ | :--- | :------- |
| planId   | Integer | 是   | 规划 ID  |
| actionId | Integer | 是   | 行动 ID  |

**响应示例：**


```json
{
  "code": 0,
  "message": "删除成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：级联软删该行动下的里程碑及已绑定的成果文件（软删 `file_uploads` 记录并删除 OSS 物理文件），并重算目标状态与规划进度。限制：仅草稿/已退回可删除，已通过（`status=2`）禁止删除。

------

### 4.10 添加里程碑

**接口地址：** `POST /profile/career-plans/{planId}/actions/{actionId}/milestones`

**路径参数：**

| 参数名   | 类型    | 必填 | 说明                       |
| :------- | :------ | :--- | :------------------------- |
| planId   | Integer | 是   | 规划 ID                    |
| actionId | Integer | 是   | 行动 ID（里程碑归属的行动） |

**请求参数：**

| 参数名         | 类型    | 必填 | 说明                           |
| :------------- | :------ | :--- | :----------------------------- |
| milestoneTitle | String  | 是   | 里程碑标题                     |
| milestoneDate  | String  | 否   | 预计达成日期，格式 `YYYY-MM-DD` |

**请求示例：**


```json
{
  "milestoneTitle": "确定导师意向",
  "milestoneDate": "2025-09-10"
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "添加成功",
  "data": {"milestoneId": 31},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`career_milestones` 表，关联 `career_actions.id`。行动不存在返回 `30001`，非本人规划返回 `5`。
>
> 业务逻辑：新增里程碑后，若行动存在里程碑，则按里程碑达成占比自动回填行动完成率并推导行动状态（全部达成→2已完成、部分达成→1进行中、均未达成→0未开始），联动重算目标状态与规划进度。限制：仅 `status=0(草稿)` 或 `status=3(已退回)` 时可添加里程碑。

------

### 4.10.1 更新里程碑

**接口地址：** `PUT /profile/career-plans/{planId}/milestones/{milestoneId}`

**路径参数：**

| 参数名      | 类型    | 必填 | 说明      |
| :---------- | :------ | :--- | :-------- |
| planId      | Integer | 是   | 规划 ID   |
| milestoneId | Integer | 是   | 里程碑 ID |

**请求参数：**

| 参数名         | 类型    | 必填 | 说明                                                          |
| :------------- | :------ | :--- | :------------------------------------------------------------ |
| milestoneTitle | String  | 是   | 里程碑标题                                                    |
| milestoneDate  | String  | 否   | 预计达成日期，格式 `YYYY-MM-DD`；不传保留原值，传空字符串清空 |
| isAchieved     | Integer | 否   | 达成状态 0=未完成 1=已完成；不传保留原值；置 1 时写入 `achieved_at`，回退 0 时清空 |
| proofFileId    | Integer | 否   | 成果证明材料（关联 `file_uploads.id`）；不传保留原值，传 0 清空解绑（并清理原证明文件），传具体 fileId 重新绑定 |

**请求示例：**


```json
{
  "milestoneTitle": "确定导师意向",
  "milestoneDate": "2025-09-10",
  "isAchieved": 1,
  "proofFileId": 2001
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "更新成功",
  "data": {"milestoneId": 31},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 说明：本接口是里程碑达成状态（`career_milestones.is_achieved`）与成果证明（`career_milestones.proof_file_id`）的唯一写入入口。达成状态或里程碑结构变更后，若行动存在里程碑，将按达成占比自动回填 `career_actions.completion_rate` 并推导行动状态（全部达成→2、部分达成→1、均未达成→0），联动重算目标状态与规划进度；即行动存在里程碑时，里程碑为行动进展的唯一来源。限制：仅 `status=0(草稿)` 或 `status=3(已退回)` 时可编辑里程碑。

------

### 4.10.2 删除里程碑

**接口地址：** `DELETE /profile/career-plans/{planId}/milestones/{milestoneId}`

**路径参数：**

| 参数名      | 类型    | 必填 | 说明      |
| :---------- | :------ | :--- | :-------- |
| planId      | Integer | 是   | 规划 ID   |
| milestoneId | Integer | 是   | 里程碑 ID |

**响应示例：**


```json
{
  "code": 0,
  "message": "删除成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：软删里程碑并清理其成果证明文件（软删 `file_uploads` 记录并删除 OSS 物理文件）；删除后若行动仍存在其他里程碑，按剩余里程碑达成占比重新推导行动状态与完成率并联动重算规划进度。限制：仅 `status=0(草稿)` 或 `status=3(已退回)` 时可删除里程碑。

------

### 4.11 更新个人兴趣标签

**接口地址：** `PUT /profile/interests`

**请求参数：**




| 参数名                       | 类型    | 必填 | 说明                                                         |
| :--------------------------- | :------ | :--- | :----------------------------------------------------------- |
| interests                    | Array   | 是   | 兴趣标签列表                                                 |
| interests[].id               | Integer | 否   | 标签ID（存在则更新，不存在则新增）                           |
| interests[].tagName          | String  | 是   | 标签名称（对应 `user_interests.tag_name`）                   |
| interests[].proficiencyLevel | Integer | 是   | 熟练度：1=入门 2=一般 3=熟练 4=精通（对应 `proficiency_level`） |
| interests[].detailContent    | String  | 否   | 具体内容描述（对应 `detail_content`）                        |
| interests[].isDetail         | Integer | 否   | 0=系统标签 1=用户手动添加，默认1（对应 `is_detail`）         |

**请求示例：**


```json
{
  "interests": [
    {
      "id": 1,
      "tagName": "编程开发",
      "proficiencyLevel": 4,
      "detailContent": "Web前端开发、人工智能应用",
      "isDetail": 1
    },
    {
      "tagName": "数据分析",
      "proficiencyLevel": 2,
      "detailContent": "Python数据分析入门",
      "isDetail": 1
    }
  ]
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "updatedCount": 2,
    "addedCount": 1
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`user_interests` 表，条件唯一索引 `(user_id, tag_name, is_detail)`。

------

### 4.11.1 删除个人兴趣标签

**接口地址：** `DELETE /profile/interests/{id}`

**路径参数：**

| 参数名 | 类型    | 必填 | 说明                                  |
| :----- | :------ | :--- | :------------------------------------ |
| id     | Integer | 是   | 兴趣标签ID（对应 `user_interests.id`） |

**响应示例：**

```json
{
  "code": 0,
  "message": "删除成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**错误码：**

| Code | 说明                 |
| :--- | :------------------- |
| 30001 | 数据不存在（兴趣标签不存在，已删除的标签重复删除也返回此码） |
| 5     | 无访问权限（非本人标签） |

> 业务逻辑：仅允许删除当前登录用户自己的兴趣标签，删除时对 `user_interests` 记录做软删除（`deleted_at` 置为当前时间），配合生成列 `is_deleted_null` 与条件唯一索引 `uk_ui_user_tag`，删除后可重新添加同名标签。系统标签（`is_detail=0`）与用户手动添加标签（`is_detail=1`）均可删除。

------

### 4.12 更新行动状态

**接口地址：** `PUT /profile/career-plans/{planId}/actions/{actionId}/status`

**请求参数：**

| 参数名 | 类型    | 必填 | 说明                       |
| :----- | :------ | :--- | :------------------------- |
| status | Integer | 是   | 0=未开始 1=进行中 2=已完成 |

**响应示例：**


```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "actionId": 21,
    "status": 2,
    "statusLabel": "已完成",
    "completionRate": 100
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 更新 `career_actions.status`，`status=2` 时同步更新 `completed_at` 和 `completion_rate=100`；回退状态（0/1）且行动存在里程碑时，`completion_rate` 按里程碑达成占比回填，不丢失已由里程碑积累的进度。
>
> 注意：行动存在里程碑时，其状态与完成率在里程碑增删改时按达成情况自动推导（见 4.10.1），本接口的手动设置会在下一次里程碑变更时被重新推导覆盖；行动无里程碑时，本接口为状态与完成率的唯一控制入口。

------

### 4.13 上传行动成果

**接口地址：** `POST /profile/career-plans/{planId}/actions/{actionId}/files`

**请求参数：**

| 参数名 | 类型    | 必填 | 说明                                   |
| :----- | :------ | :--- | :------------------------------------- |
| fileId | Integer | 是   | 已上传文件ID（关联 `file_uploads.id`） |

**响应示例：**


```json
{
  "code": 0,
  "message": "上传成功",
  "data": {
    "fileId": 1001,
    "fileName": "导师确认邮件.pdf",
    "fileUrl": "/uploads/..."
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`file_uploads` 表（`biz_type='career_action'`，`biz_id` 关联 `career_actions.id`）。
>
> **删除行动成果**：复用通用附件删除接口 `DELETE /common/files/{fileId}`（2.1.3）。该接口要求规划处于草稿/已退回状态时才能删除绑定到行动的附件（软删 `file_uploads` 记录并删除 OSS 物理文件）；已通过（`status=2`）规划禁止删除附件。

------

### 4.14 添加阶段反思

**接口地址：** `POST /profile/career-plans/{planId}/reflections`

**请求参数：**

| 参数名            | 类型   | 必填 | 说明                                              |
| :---------------- | :----- | :--- | :------------------------------------------------ |
| reflectionContent | String | 是   | 反思内容（`career_reflections.reflection_content`） |

**响应示例：**


```json
{
  "code": 0,
  "message": "添加成功",
  "data": {"reflectionId": 1},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`career_reflections` 表，关联 `career_plans.id`。

------

### 4.15 AI建议一键添加为计划

**接口地址：** `POST /profile/career-plans/ai-add`

**请求参数：**

| 参数名         | 类型    | 必填 | 说明                                                     |
| :------------- | :------ | :--- | :------------------------------------------------------- |
| aiSuggestionId | Integer | 是   | AI建议ID（关联 `improvement_suggestions.id`）            |
| semesterId     | Integer | 是   | 目标学期ID                                               |
| title          | String  | 否   | 规划标题，不传则自动生成                                 |
| requireConfirm | Integer | 否   | 是否由学生确认后生效：0=直接添加 1=返回草稿待确认，默认1 |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "planId": 6,
    "status": 0,
    "statusLabel": "草稿",
    "source": 2,
    "sourceLabel": "AI建议添加",
    "requireConfirm": 1
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：根据 `improvement_suggestions` 内容自动生成 `career_goals` 和 `career_actions`，`career_plans.source=2`、`career_plans.ai_suggestion_id` 记录来源。`requireConfirm=1` 时学生需再次提交确认。

------

### 4.16 档案导出预览

**接口地址：** `GET /profile/export/preview`

**请求参数：** 无

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "sections": [
      {"code": "academicInfo", "name": "学籍信息", "selected": true, "disabled": true},
      {"code": "dimensionScores", "name": "画像分数", "selected": true, "disabled": false},
      {"code": "awards", "name": "获奖记录", "selected": true, "disabled": false},
      {"code": "practices", "name": "实践经历", "selected": true, "disabled": false},
      {"code": "careerPlans", "name": "成长规划", "selected": true, "disabled": false}
    ],
    "dataVersion": "2026-07-01-v3",
    "generatedAt": "2026-07-04T14:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：聚合学生档案各模块，仅包含已审核通过的数据。

------

### 4.17 提交档案导出

**接口地址：** `POST /profile/export`

**请求参数：**

| 参数名   | 类型   | 必填 | 说明                                 |
| :------- | :----- | :--- | :----------------------------------- |
| sections | Array  | 否   | 导出的栏目 code 列表，可选：`academicInfo`/`dimensionScores`/`awards`/`practices`/`careerPlans`；**不传或为空时默认导出全部栏目** |
| fileType | String | 否   | 文件类型：`pdf`（当前版本仅支持 `pdf`，`word` 为后续版本），默认 `pdf` |
| purpose  | String | 否   | 导出用途：`internal`（内部查看，屏幕水印+打印隐藏）/ `external`（外部投递，**默认**，屏幕和打印均无水印） |

**请求示例：**

```json
{
  "sections": ["academicInfo", "dimensionScores", "awards", "practices", "careerPlans"],
  "fileType": "pdf",
  "purpose": "external"
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "导出成功",
  "data": {
    "exportJobId": 50,
    "status": 2,
    "statusLabel": "已完成",
    "fileId": 12345,
    "downloadUrl": "https://student-archives.oss-cn-beijing.aliyuncs.com/student_archive/pdf/2026-07/xxx.pdf?OSSAccessKeyId=...&Expires=...&Signature=...",
    "expireAt": "2026-07-11T14:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> **业务逻辑（V5.7 调整为同步导出）：** 接口同步生成学生成长档案 PDF 并上传 OSS，创建 `export_jobs` 记录（`status=2` 已完成），向 `export_operation_logs` 写入一条 `action=1(创建)` 的审计记录（`export_type='student_archive'`，`file_id` 指向生成的文件），并通过 `user_messages` 通知学生。
>
> **水印策略：**
> - `purpose=external`（默认）：屏幕和打印均不添加水印，适用于投递校外单位/家长等外部场景。
> - `purpose=internal`：屏幕显示水印，打印时自动隐藏。
> - `purpose` 仅支持 `internal` / `external`，其他值返回参数错误。
>
> **下载说明：** 响应中的 `downloadUrl` 为带签名的临时下载链接（有效期由 `oss.url-expire-minutes` 配置，默认 30 分钟）；`expireAt` 为业务有效期（自创建起 7 天）。有效期内均可通过通用文件下载接口 `GET /common/files/{fileId}/download` 重新获取下载链接（每次重新签名）。`fileId` 即生成文件在 `file_uploads` 中的 ID。

------

### 4.18 简历导出预览

**接口地址：** `GET /profile/resume/export/preview`

**请求参数：** 无

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "sections": [
      {"code": "education", "name": "教育背景", "selected": true, "disabled": true},
      {"code": "awards", "name": "获奖情况", "selected": true, "disabled": false},
      {"code": "skills", "name": "技能与兴趣", "selected": true, "disabled": false},
      {"code": "practices", "name": "实践经历", "selected": true, "disabled": false},
      {"code": "certificates", "name": "证书", "selected": true, "disabled": false},
      {"code": "selfEvaluation", "name": "自我评价", "selected": true, "disabled": false}
    ],
    "dataVersion": "v1-20260805",
    "generatedAt": "2026-08-05T19:30:00+08:00"
  },
  "trace_id": "req-20260805-193000-abc123",
  "timestamp": "2026-08-05T19:30:00+08:00"
}
```

> 数据来源：聚合学生学籍、学期成绩、奖项申请、兴趣标签、已审核通过的实践档案及证书、自我评价等数据。

------

### 4.19 提交简历导出

**接口地址：** `POST /profile/resume/export`

**请求参数：**

| 参数名   | 类型   | 必填 | 说明                                                         |
| :------- | :----- | :--- | :----------------------------------------------------------- |
| sections | Array  | 否   | 导出的栏目 code 列表，可选：`education`/`awards`/`skills`/`practices`/`certificates`/`selfEvaluation`；**不传或为空时默认导出全部栏目** |
| fileType | String | 否   | 文件类型：`pdf`（当前版本仅支持 `pdf`），默认 `pdf`          |
| purpose  | String | 否   | 导出用途：`internal`（内部查看，屏幕水印+打印隐藏）/ `external`（外部投递，**默认**，屏幕和打印均无水印） |

**请求示例：**

```json
{
  "sections": ["education", "awards", "skills", "practices", "certificates", "selfEvaluation"],
  "fileType": "pdf",
  "purpose": "external"
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "简历导出任务已创建",
  "data": {
    "exportJobId": 51,
    "status": 2,
    "statusLabel": "已完成",
    "fileId": 12346,
    "downloadUrl": "https://student-archives.oss-cn-beijing.aliyuncs.com/resume_export/pdf/2026-08/xxx.pdf?OSSAccessKeyId=...&Expires=...&Signature=...",
    "expireAt": "2026-08-12T19:30:00+08:00"
  },
  "trace_id": "req-20260805-193000-abc123",
  "timestamp": "2026-08-05T19:30:00+08:00"
}
```

> **业务逻辑：** 接口同步生成个人简历 PDF 并上传 OSS，创建 `export_jobs` 记录（`status=2` 已完成，`export_type='resume'`），向 `export_operation_logs` 写入一条 `action=1(创建)` 的审计记录，并通过 `user_messages` 通知学生。
>
> **水印策略：**
> - `purpose=external`（默认）：屏幕和打印均不添加水印，适用于投递实习公司/HR 等外部场景。
> - `purpose=internal`：屏幕显示水印，打印时自动隐藏。
> - `purpose` 仅支持 `internal` / `external`，其他值返回参数错误。
>
> **下载说明：** 响应中的 `downloadUrl` 为带签名的临时下载链接（有效期由 `oss.url-expire-minutes` 配置，默认 30 分钟）；`expireAt` 为业务有效期（自创建起 7 天）。有效期内均可通过通用文件下载接口 `GET /common/files/{fileId}/download` 重新获取下载链接（每次重新签名）。`fileId` 即生成文件在 `file_uploads` 中的 ID。
>
> **模板配置：** 渲染使用 `export_templates` 表中 `export_type='resume'` 的默认模板；未配置时返回操作失败，提示联系管理员。

------

## 五、消息中心模块

### 5.1 获取消息列表

**接口地址：** `GET /messages`

**请求参数：**




| 参数名     | 类型    | 必填 | 说明                                                         |
| :--------- | :------ | :--- | :----------------------------------------------------------- |
| category   | String  | 否   | 分类：all、system_notice、audit_remind、dynamic_remind、private_message |
| isRead     | Integer | 否   | 状态：0=未读 1=已读，不传为全部（对应 `user_messages.is_read`） |
| isArchived | Integer | 否   | 是否归档：0=未归档 1=已归档，不传则默认0                     |
| keyword    | String  | 否   | 搜索关键词（匹配 `title` 或 `content`）                      |
| page       | Integer | 否   | 页码，默认1                                                  |
| per_page   | Integer | 否   | 每页条数，默认10                                             |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 8,
    "unread": 5,
    "list": [
      {
        "id": 1,
        "category": "audit_remind",
        "categoryLabel": "审批提醒",
        "title": "奖学金申报已通过",
        "content": "您的国家奖学金申请已通过审核。",
        "senderType": 1,
        "senderTypeLabel": "系统",
        "senderName": null,
        "isRead": 0,
        "readAt": null,
        "isImportant": 1,
        "isArchived": 0,
        "archivedAt": null,
        "deadline": null,
        "jumpUrl": "/applications/award/100",
        "sendChannel": "push",
        "relatedType": "award_applications",
        "relatedId": 100,
        "createdAt": "2026-07-04T09:30:00+08:00"
      },
      {
        "id": 2,
        "category": "system_notice",
        "categoryLabel": "系统通知",
        "title": "学期档案申报开始",
        "content": "2023-2024学年第二学期档案申报已开放。",
        "senderType": 1,
        "senderTypeLabel": "系统",
        "isRead": 1,
        "readAt": "2026-07-04T10:00:00+08:00",
        "isImportant": 0,
        "isArchived": 0,
        "archivedAt": null,
        "deadline": "2026-07-10T23:59:59+08:00",
        "jumpUrl": null,
        "sendChannel": "push",
        "relatedType": null,
        "relatedId": null,
        "createdAt": "2026-07-03T08:00:00+08:00"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 10,
      "total": 8,
      "total_pages": 1
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`user_messages` 表，按 `created_at` 倒序。响应字段 `senderType` 对应 `user_messages.sender_type`；`senderName` 由 `sender_id` 关联 `users.name` 得到。

------

### 5.2 单个点击已读

**接口地址：** `PUT /messages/{messageId}/read`

**响应示例：**


```json
{
  "code": 0,
  "message": "标记成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 更新 `user_messages.is_read=1`，`read_at=CURRENT_TIMESTAMP`。

------

### 5.3 全部一键已读

**接口地址：** `PUT /messages/read-all`

**请求参数：**




| 参数名   | 类型   | 必填 | 说明                                                         |
| :------- | :----- | :--- | :----------------------------------------------------------- |
| category | String | 否   | 分类筛选，不传则标记所有未读（system_notice/audit_remind/dynamic_remind/private_message） |

**响应示例：**


```json
{
  "code": 0,
  "message": "标记成功",
  "data": {
    "markedCount": 5
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

------

### 5.4 归档消息

**接口地址：** `PUT /messages/{messageId}/archive`

**响应示例：**


```json
{
  "code": 0,
  "message": "归档成功",
  "data": {
    "messageId": 1,
    "isArchived": 1,
    "archivedAt": "2026-07-04T15:00:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 重要系统消息（`is_important=1`）归档前需二次确认；归档后更新 `user_messages.is_archived=1`、`archived_at=CURRENT_TIMESTAMP`，不做物理删除。

------

### 5.5 取消归档消息

**接口地址：** `PUT /messages/{messageId}/unarchive`

**响应示例：**


```json
{
  "code": 0,
  "message": "取消归档成功",
  "data": {
    "messageId": 1,
    "isArchived": 0,
    "archivedAt": null
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：将 `user_messages.is_archived` 重置为 `0`，`archived_at` 置空。

------

### 5.6 获取消息通知设置

**接口地址：** `GET /messages/settings`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "category": "audit_remind",
      "categoryLabel": "审批提醒",
      "emailEnabled": 1,
      "smsEnabled": 0,
      "pushEnabled": 1
    },
    {
      "category": "system_notice",
      "categoryLabel": "系统通知",
      "emailEnabled": 1,
      "smsEnabled": 0,
      "pushEnabled": 1
    },
    {
      "category": "dynamic_remind",
      "categoryLabel": "动态提醒",
      "emailEnabled": 0,
      "smsEnabled": 0,
      "pushEnabled": 1
    }
  ],
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`notification_settings` 表。

------

### 5.7 更新/新增消息通知设置

**接口地址：** `PUT /messages/settings`

**请求参数：**




| 参数名       | 类型    | 必填 | 说明                                                         |
| :----------- | :------ | :--- | :----------------------------------------------------------- |
| category     | String  | 是   | 通知分类（audit_remind/system_notice/dynamic_remind/private_message） |
| emailEnabled | Integer | 是   | 0=关闭 1=开启邮件通知（对应 `email_enabled`）                |
| smsEnabled   | Integer | 是   | 0=关闭 1=开启短信通知（对应 `sms_enabled`）                  |
| pushEnabled  | Integer | 是   | 0=关闭 1=开启站内推送（对应 `push_enabled`）                 |

**请求示例：**


```json
{
  "category": "audit_remind",
  "emailEnabled": 1,
  "smsEnabled": 0,
  "pushEnabled": 1
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "更新成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 条件唯一索引 `(user_id, category)`，不存在则新增，存在则更新。

------

### 5.8 选择多个已读

**接口地址：** `PUT /messages/batch-read`

**请求参数：**

| 参数名      | 类型  | 必填 | 说明                                     |
| :---------- | :---- | :--- | :--------------------------------------- |
| messageIds | Array | 是   | 待标记已读的消息 ID 列表，1~100 个（对应 `user_messages.id`） |

**请求示例：**

```json
{
  "messageIds": [1, 2, 3]
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "标记成功",
  "data": {
    "markedCount": 3
  },
  "trace_id": "req-20260809-103000-abc123",
  "timestamp": "2026-08-09T10:30:00+08:00"
}
```

> **业务逻辑：**
> - 仅标记**当前登录用户**的消息；非本人、已软删的消息静默跳过，已读消息不重复计入，返回本次实际「未读→已读」条数（`markedCount`）。
> - 幂等：重复调用对已读消息不再计入。
> - 更新 `user_messages.is_read=1`、`read_at=CURRENT_TIMESTAMP`，不做物理删除。

------

### 5.9 删除单条消息

**接口地址：** `DELETE /messages/{messageId}`

**路径参数：**

| 参数名    | 类型 | 必填 | 说明                         |
| :-------- | :--- | :--- | :--------------------------- |
| messageId | Long | 是   | 消息 ID（`user_messages.id`） |

**响应示例：**

```json
{
  "code": 0,
  "message": "删除成功",
  "data": null,
  "trace_id": "req-20260809-103000-abc123",
  "timestamp": "2026-08-09T10:30:00+08:00"
}
```

> **业务逻辑：**
> - 软删除：仅置 `user_messages.deleted_at=当前时间`，配合 `@SQLRestriction("deleted_at IS NULL")` 自动从列表过滤，不做物理删除。
> - 归属/存在校验与 5.2/5.4 一致：消息不存在返回 `30001`，非本人消息返回 `20005 无访问权限`。

**错误码：**

| Code  | 说明           |
| :---- | :------------- |
| 30001 | 消息不存在     |
| 20005 | 无访问权限     |

------

### 5.10 选择多个删除

**接口地址：** `DELETE /messages/batch`

**请求参数：**

| 参数名      | 类型  | 必填 | 说明                                     |
| :---------- | :---- | :--- | :--------------------------------------- |
| messageIds | Array | 是   | 待删除的消息 ID 列表，1~100 个（对应 `user_messages.id`） |

**请求示例：**

```json
{
  "messageIds": [1, 2, 3]
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "删除成功",
  "data": {
    "deletedCount": 3
  },
  "trace_id": "req-20260809-103000-abc123",
  "timestamp": "2026-08-09T10:30:00+08:00"
}
```

> **业务逻辑：**
> - 软删除**当前登录用户**的消息；非本人、已软删的消息静默跳过，返回本次实际删除条数（`deletedCount`）。
> - 幂等：重复调用对已删除消息不再计入。
> - 更新 `user_messages.deleted_at=CURRENT_TIMESTAMP`，不做物理删除。

------

## 六、动态记录模块

> 动态记录即学生的档案/奖项申报记录，数据来源 `archives` / `award_applications` 表。
>
> ⚠️ **接口设计说明：** 下方 `GET /activities/{activityId}`、`DELETE /activities/{activityId}`、`PUT /activities/{activityId}/withdraw` 等接口均需通过查询参数额外传入 `type`（archive/award/career_plan）来区分记录来源表。此设计容易调用出错，**建议后端实现时考虑以下优化方案之一**：
> 1. **路径分拆法：** 将 `type` 放入路径，如 `/activities/archive/{id}`、`/activities/award/{id}`、`/activities/career-plan/{id}`，不同来源使用不同路径前缀
> 2. **自动推断法：** 后端根据 `activityId` 依次查询 `archives`、`award_applications`、`career_plans` 表自动推断类型，前端无需传入 `type`
> 3. **统一路由 + path变量法：** `GET /activities/{type}/{activityId}` 其中 `type` 为路径变量，如 `GET /activities/archive/100`
>
> 推荐方案一或方案三，显式且符合 RESTful 风格。

### 6.1 获取动态记录列表

**接口地址：** `GET /activities`

**请求参数：**




| 参数名      | 类型    | 必填 | 说明                                                         |
| :---------- | :------ | :--- | :----------------------------------------------------------- |
| type        | String  | 否   | 记录类型：archive(档案)/award(奖项)/career_plan(职业规划)，默认全部 |
| archiveType | String  | 否   | 档案/奖项类型编码（如 academic_competition, scholarship 等） |
| status      | Integer | 否   | 状态：0=草稿 1=待审批 2=通过 3=已退回 4=已撤销       |
| semesterId  | Integer | 否   | 学期ID                                                       |
| keyword     | String  | 否   | 搜索关键词（匹配标题）                                       |
| page        | Integer | 否   | 页码，默认1                                                  |
| per_page    | Integer | 否   | 每页条数，默认10                                             |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 15,
    "list": [
      {
        "id": 1,
        "type": "archive",
        "archiveType": "academic_competition",
        "archiveTypeLabel": "学科竞赛",
        "title": "全国大学生数学建模竞赛",
        "content": "学科竞赛申报已提交",
        "status": 1,
        "statusLabel": "待审批",
        "semesterId": 3,
        "semesterName": "2023-2024-1",
        "submitTime": "2026-06-28T14:30:00+08:00",
        "currentVersion": 2,
        "submitCount": 2,
        "canEdit": true,
        "canDelete": true,
        "canWithdraw": false
      },
      {
        "id": 2,
        "type": "award",
        "archiveType": "competition_star",
        "archiveTypeLabel": "竞赛之星",
        "title": "2025年度竞赛之星申报",
        "content": "奖项报名已提交",
        "status": 2,
        "statusLabel": "已通过",
        "semesterId": 3,
        "semesterName": "2023-2024-1",
        "submitTime": "2026-06-20T10:00:00+08:00",
        "currentVersion": 1,
        "submitCount": 1,
        "canEdit": false,
        "canDelete": false,
        "canWithdraw": false
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 10,
      "total": 15,
      "total_pages": 2
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`archives` / `award_applications` / `career_plans` 表聚合查询。

------

### 6.2 获取动态详情

**接口地址：** `GET /activities/{activityId}`

**请求参数：**




| 参数名 | 类型   | 必填 | 说明                                                      |
| :----- | :----- | :--- | :-------------------------------------------------------- |
| type   | String | 是   | 记录类型：archive/award/career_plan（路径参数外额外传入） |

**响应：** 根据 `type` 和具体档案/奖项类型返回对应申报详情，包含完整字段、佐证材料列表、审批历史。

**档案类响应示例（学科竞赛）：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "type": "archive",
    "archiveType": "academic_competition",
    "archiveTypeLabel": "学科竞赛",
    "title": "全国大学生数学建模竞赛",
    "semesterId": 3,
    "semesterName": "2023-2024-1",
    "status": 1,
    "statusLabel": "待审批",
    "submitTime": "2026-06-28T14:30:00+08:00",
    "auditedAt": null,
    "auditorId": null,
    "auditorName": null,
    "rejectedReason": null,
    "currentVersion": 1,
    "submitCount": 1,
    "detail": {
      "competitionName": "全国大学生数学建模竞赛",
      "competitionType": "mathematical_modeling",
      "competitionTypeLabel": "数学建模",
      "awardLevel": "second",
      "awardLevelLabel": "二等奖",
      "obtainedTime": "2025-09-01"
    },
    "evidenceFiles": [
      {"fileId": 10001, "fileName": "获奖证书.pdf", "fileUrl": "/uploads/...", "fileSize": 102400}
    ],
    "approvalHistory": [
      {
        "stepNo": 1,
        "stepName": "辅导员初审",
        "auditorName": "王老师",
        "action": 1,
        "actionLabel": "通过",
        "comment": "材料齐全",
        "completedAt": "2026-07-02T09:00:00+08:00"
      }
    ],
    "versionHistory": [
      {
        "version": 1,
        "title": "全国大学生数学建模竞赛",
        "status": 1,
        "statusLabel": "待审批",
        "createdAt": "2026-06-28T14:30:00+08:00"
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

------

### 6.3 编辑动态记录

**接口地址：** `PUT /activities/{activityId}`

**请求参数：**

| 参数名 | 类型   | 必填 | 说明                                                      |
| :----- | :----- | :--- | :-------------------------------------------------------- |
| `type` | String | 是   | 记录类型：archive/award/career_plan（路径参数外额外传入） |
| （其余参数同对应申报类型的提交字段） | — | — | 按 `type` 对应申报类型的字段传入 |

会创建新版本（`current_version` +1，并写入 `model_versions` 表，`model_type` 对应 `Archive` / `AwardApplication`）。

**响应示例：**


```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "archiveId": 100,
    "status": 1,
    "statusLabel": "待审批",
    "currentVersion": 2,
    "submitCount": 2
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 限制：仅 `status=0(草稿)` 或 `status=3(已退回)` 时可编辑；`status=2(通过)` 和 `status=4(已撤销)` 不可直接编辑，需走更正流程生成新版本。

------

### 6.4 删除动态记录

**接口地址：** `DELETE /activities/{activityId}`

**请求参数：**




| 参数名 | 类型   | 必填 | 说明                    |
| :----- | :----- | :--- | :---------------------- |
| type   | String | 是   | 记录类型：archive/award |

**响应示例：**


```json
{
  "code": 0,
  "message": "删除成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 软删除，更新对应表的 `deleted_at`。限制：仅 `status=0(草稿)` 或 `status=3(已退回)` 时可删除。

------

### 6.5 撤回申报

**接口地址：** `PUT /activities/{activityId}/withdraw`

**请求参数：**




| 参数名 | 类型   | 必填 | 说明                                          |
| :----- | :----- | :--- | :-------------------------------------------- |
| type   | String | 是   | 记录类型：archive/award/career_plan           |

**响应示例：**



```json
{
  "code": 0,
  "message": "撤回成功",
  "data": {
    "status": 0,
    "statusLabel": "草稿"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 限制：仅 `status=1(待审批)` 时可撤回，撤回后状态变为 `0(草稿)`。
>
> **职业规划撤回说明：** 当 `type=career_plan` 时，`activityId` 对应 `career_plans.id`；撤回后 `career_plans.status` 回退为 `0(草稿)`，已生成的待审批任务同步作废。

------

## 七、个人档案信息申报模块

> 所有申报接口共用 `archives` 基表 + 对应扩展表。
> 状态流转：`0=草稿` → `1=待审批` → `2=通过` / `3=已退回` / `4=已撤销`
> 每次提交 `submit_count` +1，创建 `model_versions` 版本记录（`model_type='archive'`，`model_id` 关联 `archives.id`）。
>
> **删除操作入口：** 本模块的申报记录删除统一通过 **动态记录模块（六）`DELETE /activities/{activityId}`** 执行（需额外传入 `type=archive`），详见 [6.4 删除动态记录](#64-删除动态记录)。申报模块自身不提供独立的 DELETE 接口。

> 通用请求字段（所有申报接口均可包含）：
> | 参数名          | 类型    | 必填 | 说明                                                         |
> | :-------------- | :------ | :--- | :----------------------------------------------------------- |
> | participantRole | String  | 否   | 本人角色：leader(负责人)、member(成员)、solo(独立完成)。按档案类型分别存入 `archive_competitions.participant_role` / `archive_researches.participant_role` / `archive_innovations.participant_role` / `archive_social_practices.participant_role`；荣誉证书、奖学金、实习等个人类型无需填写 |
> | certificateNo   | String  | 否   | 证书编号（`archive_certificates.certificate_no`）**注：仅荣誉证书类型与此字段有关，其他档案类型传入此字段将被后端忽略** |
> | issuingUnit     | String  | 否   | 发证单位（`archive_certificates.issuing_unit`）**注：仅荣誉证书类型与此字段有关，其他档案类型传入此字段将被后端忽略** |
> | validUntil      | String  | 否   | 有效期，格式 `YYYY-MM-DD`（`archive_certificates.valid_until`）**注：仅荣誉证书类型与此字段有关，其他档案类型传入此字段将被后端忽略** |

### 7.0 自动保存草稿

**接口地址：** `PUT /applications/{archiveId}/autosave`

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
| :----- | :--- | :--- | :--- |
| （同对应申报类型的提交参数字段） | — | — | 前端可增量保存已填写字段 |

**响应示例：**


```json
{
  "code": 0,
  "message": "草稿已保存",
  "data": {
    "archiveId": 100,
    "status": 0,
    "statusLabel": "草稿",
    "savedAt": "2026-07-04T14:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：仅更新 `status=0(草稿)` 的记录；保存时创建/更新 `model_versions` 草稿版本（`model_type='archive'`）；页面离开前调用此接口防止数据丢失。
>
> **首次草稿创建：** 本接口路径中的 `{archiveId}` 必须已存在。首次创建草稿请使用对应申报类型的 `POST /applications/{type}` 接口并传入 `isDraft=1`，后端会创建 `status=0` 的基表记录并返回 `archiveId`，后续自动保存再调用本接口。

------

### 7.0.1 重复申报检测

**接口地址：** `POST /applications/duplicate-check`

**请求参数：**

| 参数名        | 类型   | 必填 | 说明                     |
| :------------ | :----- | :--- | :----------------------- |
| archiveType   | String | 是   | 档案类型编码             |
| certificateNo | String | 否   | 证书编号                 |
| title         | String | 否   | 申报标题/名称            |
| obtainedTime    | String | 否   | 获得时间，格式 `YYYY-MM-DD` |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "isDuplicate": true,
    "duplicateRecords": [
      {"archiveId": 99, "title": "全国大学生数学建模竞赛", "status": 2, "statusLabel": "已通过", "similarity": 0.92}
    ],
    "suggestion": "检测到疑似重复申报，请确认是否为同一成果。"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`archives` 表，检测规则优先读取 `duplicate_detection_rules` 中当前 `school_id` + `archive_type` 的配置（检测字段、算法、相似度阈值、跨学期/跨类型规则）；未配置时按默认规则（证书编号精确匹配 + 名称/时间加权相似度）计算相似度。

------

### 7.0.2 获取档案评选说明

**接口地址：** `GET /applications/{type}/guide`

**说明：** 获取指定档案类型的评选说明，包含评选标准、材料要求和注意事项等。前端在申报表单上方展示此说明内容。

**路径参数：**

| 参数名 | 类型   | 必填 | 说明                                                   |
| :----- | :----- | :--- | :----------------------------------------------------- |
| type   | String | 是   | 档案类型编码，如 `competition`、`scholarship`、`innovation` 等 |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "type": "competition",
    "typeLabel": "学科竞赛",
    "title": "学科竞赛评选说明",
    "content": "本项用于记录学生在校期间参加的各类学科竞赛获奖情况，包括国家级、省部级、校级等各级竞赛。",
    "requirements": [
      {"field": "competitionName", "label": "竞赛名称", "required": true, "description": "请填写竞赛完整名称，如'全国大学生数学建模竞赛'"},
      {"field": "competitionType", "label": "竞赛类型", "required": true, "description": "选择竞赛所属类型"},
      {"field": "awardLevel", "label": "获奖等级", "required": true, "description": "填写所获奖项等级"},
      {"field": "obtainedTime", "label": "获奖时间", "required": true, "description": "填写证书上的获奖日期"},
      {"field": "evidenceFiles", "label": "佐证材料", "required": true, "description": "须上传获奖证书扫描件或清晰照片，支持 PDF/JPG/PNG 格式，单个文件不超过 10MB"}
    ],
    "notes": [
      "1. 同一竞赛获多项奖请分别申报",
      "2. 佐证材料须包含证书原件扫描件或清晰照片",
      "3. 团队获奖请注明本人角色（负责人/成员）",
      "4. 证书编号选填，有则填写便于查重"
    ],
    "updatedAt": "2026-07-01T00:00:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`archive_type_configs` 表的 `evaluate_desc`/`evaluate_requirements`/`evaluate_notes` 字段，按 `school_id` + `archive_type` 获取当前学校对应档案类型的评选说明。管理端通过 `archive_type_configs` 配置维护。

------

### 7.1 学科竞赛申报

#### 7.1.1 提交/保存学科竞赛申报

**接口地址：** `POST /applications/competition`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID（`archives.semester_id`）                             |
| competitionName | String  | 是   | 竞赛名称（`archive_competitions.competition_name`）          |
| competitionType | String  | 是   | 竞赛类型，字典编码（`archive_competitions.competition_type`） |
| awardLevel      | String  | 是   | 获奖等级，字典编码（`archive_competitions.award_level`）     |
| obtainedTime      | String  | 是   | 获奖时间，格式 `YYYY-MM-DD`（`archives.obtained_at`）           |
| isDraft         | Integer | 否   | 0=提交（status=1） 1=保存草稿（status=0），默认0             |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表（关联 `file_uploads`）                    |

**请求示例：**


```json
{
  "semesterId": 3,
  "competitionName": "全国大学生数学建模竞赛",
  "competitionType": "mathematical_modeling",
  "awardLevel": "second",
  "obtainedTime": "2025-09-01",
  "participantRole": "leader",
  "certificateNo": "CERT-2025-001",
  "issuingUnit": "中国工业与应用数学学会",
  "validUntil": "2028-09-30",
  "isDraft": 0,
  "evidenceFileIds": [10001, 10002]
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "提交成功",
  "data": {
    "archiveId": 100,
    "status": 1,
    "statusLabel": "待审批",
    "currentVersion": 1,
    "submitCount": 1
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

------

### 7.2 奖学金申报

#### 7.2.1 提交/保存奖学金申报

**接口地址：** `POST /applications/scholarship`

**请求参数：**




| 参数名              | 类型    | 必填 | 说明                                                         |
| :------------------ | :------ | :--- | :----------------------------------------------------------- |
| semesterId          | Integer | 是   | 学期ID                                                       |
| scholarshipName     | String  | 是   | 奖学金名称（`archive_scholarships.scholarship_name`）        |
| scholarshipCategory | String  | 是   | 奖学金类别，字典编码（`archive_scholarships.scholarship_category`） |
| awardLevel          | String  | 是   | 获奖等级，字典编码（`archive_scholarships.award_level`）     |
| obtainedTime          | String  | 是   | 获得时间，格式 `YYYY-MM-DD`                                     |
| isDraft             | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds     | Array   | 否   | 佐证材料文件ID列表                                           |

------

### 7.3 创新创业申报

#### 7.3.1 提交/保存创新创业申报

**接口地址：** `POST /applications/innovation`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID                                                       |
| companyName     | String  | 是   | 公司名称（`archive_innovations.company_name`）               |
| industryType    | String  | 是   | 行业类型，字典编码（`archive_innovations.industry_type`）    |
| projectType     | String  | 是   | 公司类型，字典编码（`archive_innovations.project_type`）     |
| registeredTime    | String  | 是   | 注册时间，格式 `YYYY-MM-DD`（`archive_innovations.registered_at`） |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表                                           |

------

### 7.4 学术研究申报

#### 7.4.1 提交/保存学术研究申报

**接口地址：** `POST /applications/research`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID                                                       |
| projectName     | String  | 是   | 项目名称（`archive_researches.project_name`）                |
| projectLevel    | String  | 是   | 项目级别，字典编码（`archive_researches.project_level`）     |
| projectType     | String  | 是   | 项目类型/研究类型（`archive_researches.project_type`）       |
| startDate       | String  | 是   | 开始日期，格式 `YYYY-MM-DD`（`archive_researches.start_date`） |
| endDate         | String  | 是   | 结束日期，格式 `YYYY-MM-DD`（`archive_researches.end_date`） |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表                                           |

> 约束：`end_date > start_date`（数据库 CHECK 约束）。

------

### 7.5 荣誉证书申报

#### 7.5.1 提交/保存荣誉证书申报

**接口地址：** `POST /applications/certificate`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                |
| :-------------- | :------ | :--- | :-------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID                                              |
| certificateType | String  | 是   | 证书类型（`archive_certificates.certificate_type`） |
| certificateName | String  | 是   | 证书名称（`archive_certificates.certificate_name`） |
| obtainedTime      | String  | 是   | 获得时间，格式 `YYYY-MM-DD`                            |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0                            |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表                                  |

------

### 7.6 实习经历申报

#### 7.6.1 提交/保存实习经历申报

**接口地址：** `POST /applications/internship`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID                                                       |
| companyName     | String  | 是   | 实习单位（`archive_internships.company_name`）               |
| location        | String  | 否   | 实习地点（`archive_internships.location`）                   |
| position        | String  | 否   | 实习岗位（`archive_internships.position`）                   |
| startDate       | String  | 是   | 开始时间，格式 `YYYY-MM-DD`（`archive_internships.start_date`） |
| endDate         | String  | 是   | 结束时间，格式 `YYYY-MM-DD`（`archive_internships.end_date`） |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表                                           |

> 约束：`end_date > start_date`。

------

### 7.7 组织履历申报

#### 7.7.1 提交/保存组织履历申报

**接口地址：** `POST /applications/organization`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID                                                       |
| orgLevel        | String  | 否   | 组织级别，字典编码（`archive_organizations.org_level`）      |
| department      | String  | 否   | 所在部门（`archive_organizations.department`）               |
| positionTitle   | String  | 否   | 职位（`archive_organizations.position_title`）               |
| startDate       | String  | 是   | 任职开始，格式 `YYYY-MM-DD`（`archive_organizations.start_date`） |
| endDate         | String  | 是   | 任职结束，格式 `YYYY-MM-DD`（`archive_organizations.end_date`） |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表                                           |

> 约束：`end_date > start_date`。

------

### 7.8 实训项目申报

#### 7.8.1 提交/保存实训项目申报

**接口地址：** `POST /applications/training`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID                                                       |
| projectName     | String  | 是   | 项目名称（`archive_training_projects.project_name`）         |
| projectContent  | String  | 否   | 项目内容（`archive_training_projects.project_content`）      |
| startDate       | String  | 是   | 开始时间，格式 `YYYY-MM-DD`（`archive_training_projects.start_date`） |
| endDate         | String  | 是   | 结束时间，格式 `YYYY-MM-DD`（`archive_training_projects.end_date`） |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表                                           |

> 约束：`end_date > start_date`。

------

### 7.9 社会实践申报

#### 7.9.1 提交/保存社会实践申报

**接口地址：** `POST /applications/practice`

**请求参数：**




| 参数名           | 类型    | 必填 | 说明                                                         |
| :--------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId       | Integer | 是   | 学期ID                                                       |
| activityName     | String  | 是   | 活动名称（`archive_social_practices.activity_name`）         |
| practiceLocation | String  | 否   | 活动地点（`archive_social_practices.practice_location`）     |
| practiceUnit     | String  | 否   | 组织单位（`archive_social_practices.practice_unit`）         |
| startDate        | String  | 是   | 开始时间，格式 `YYYY-MM-DD`（`archive_social_practices.start_date`） |
| endDate          | String  | 是   | 结束时间，格式 `YYYY-MM-DD`（`archive_social_practices.end_date`） |
| volunteerHours   | Decimal | 否   | 志愿时长，默认0（`archive_social_practices.volunteer_hours`） |
| isDraft          | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds  | Array   | 否   | 佐证材料文件ID列表                                           |

> 约束：`end_date > start_date`。

------

### 7.10 图书心得申报

#### 7.10.1 提交/保存图书心得申报

**接口地址：** `POST /applications/book-review`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID                                                       |
| bookName        | String  | 是   | 书名（`archive_book_reviews.book_name`）                     |
| readMonth       | String  | 是   | 阅读时间，格式 `YYYY-MM-DD`（`archive_book_reviews.read_month`） |
| reviewContent   | String  | 是   | 心得体会（`archive_book_reviews.review_content`）            |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表                                           |

------

### 7.11 获取申报版本历史

**接口地址：** `GET /applications/{archiveId}/versions`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "currentVersion": 2,
    "versions": [
      {
        "version": 1,
        "title": "全国大学生数学建模竞赛",
        "status": 3,
        "statusLabel": "已退回",
        "rejectedReason": "缺少获奖证书",
        "createdAt": "2026-06-20T10:00:00+08:00"
      },
      {
        "version": 2,
        "title": "全国大学生数学建模竞赛",
        "status": 1,
        "statusLabel": "待审批",
        "rejectedReason": null,
        "createdAt": "2026-06-28T14:30:00+08:00"
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`model_versions` 表（`model_type='archive'`），按 `version` 倒序。

------

### 7.12 更正已通过申报

**接口地址：** `POST /applications/{archiveId}/correction`

**说明：** 当 `archives.status=2`（已通过）的记录需要修正内容时，学生不可直接编辑原记录，需提交“更正申请”。后端将原记录标记为“待更正”，并生成一条新的 `archives` 记录（版本号 +1），新记录状态为 `1=待审批`，原记录保持不变直至新记录审核通过。

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                                  |
| :-------------- | :------ | :--- | :------------------------------------ |
| correctionReason| String  | 是   | 更正原因说明                          |
| correctedData   | Object  | 是   | 修正后的完整申报数据，结构同原申报类型 |
| evidenceFileIds | Array   | 否   | 补充或替换的佐证材料文件ID列表        |

**请求示例：**


```json
{
  "correctionReason": "获奖等级填写错误，应为国家级一等奖",
  "correctedData": {
    "semesterId": 3,
    "competitionName": "全国大学生数学建模竞赛",
    "competitionType": "national",
    "awardLevel": "first",
    "obtainedTime": "2025-10-15"
  },
  "evidenceFileIds": [10003, 10010]
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "更正申请已提交，等待教师审核",
  "data": {
    "archiveId": 10001,
    "newArchiveId": 10015,
    "oldVersion": 2,
    "newVersion": 3,
    "status": 1,
    "statusLabel": "待审批"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**错误码：**

| Code  | 说明                                     |
| :---- | :--------------------------------------- |
| 40002 | 当前状态不可操作（非已通过状态无法更正） |
| 40009 | 重复提交（该记录已存在待审批的更正申请） |

> 数据来源：`archives`、`model_versions` 表；审核通过后原记录 `status` 更新为 `4=已撤销`，新记录 `status` 更新为 `2=已通过`。
>
> 评分联动：新记录通过后自动触发增量重算，score_calculations 中的 data_source 指向新记录 ID，原记录的评分快照不受影响。
>
> 时间轴联动：原记录对应的时间轴节点自动失效（growth_timelines.status=0），基于新记录生成新时间轴节点。

------

## 八、奖项报名模块

> 所有奖项报名共用 `award_applications` 基表 + 对应扩展表。
> 状态流转同档案申报：`0=草稿` → `1=待审批` → `2=通过` / `3=已退回` / `4=已撤销`
> 通用请求字段（所有奖项报名接口均可包含）：
> | 参数名          | 类型    | 必填 | 说明                                                         |
> | :-------------- | :------ | :--- | :----------------------------------------------------------- |
> | `certificateNo` | String  | 否   | 证书编号（`award_applications.certificate_no`）              |
> | `issuingUnit`   | String  | 否   | 发证/主办单位（`award_applications.issuing_unit`）           |
> | `validUntil`    | String  | 否   | 证书有效期，格式 `YYYY-MM-DD`（`award_applications.valid_until`） |
> | `participantRole`| String  | 否   | 本人角色：leader(负责人)、member(成员)、solo(独立完成)（`award_applications.participant_role`） |

### 8.1 奖项总览统计

**接口地址：** `GET /awards/overview`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "totalSubmissions": 30,
    "pendingReview": 8,
    "approved": 8,
    "newThisSemester": 8,
    "typeDistribution": {
      "competition_star": 8,
      "innovation_star": 5,
      "research_project": 4,
      "software_copyright": 6,
      "published_paper": 7
    },
    "statusDistribution": {
      "draft": 7,
      "pending": 8,
      "approved": 8,
      "rejected": 7
    },
    "semesterTrend": [
      {"semesterId": 1, "semesterName": "2022-2023-1", "count": 5},
      {"semesterId": 2, "semesterName": "2022-2023-2", "count": 8}
    ],
    "recentActivities": [
      {
        "id": 1,
        "title": "全国大学生英语竞赛",
        "applicant": "李四",
        "type": "competition_star",
        "typeLabel": "竞赛之星",
        "submitTime": "2026-06-28",
        "status": 1,
        "statusLabel": "待审批"
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`award_applications` 表聚合统计。

------

### 8.1.1 奖项草稿自动保存

**接口地址：** `PUT /awards/{applicationId}/autosave`

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
| :----- | :--- | :--- | :------------------------------- |
| （同对应奖项类型的提交参数字段） | — | — | 前端可增量保存已填写字段 |

**响应示例：**

```json
{
  "code": 0,
  "message": "草稿已保存",
  "data": {
    "applicationId": 200,
    "status": 0,
    "statusLabel": "草稿",
    "savedAt": "2026-07-04T14:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：仅更新 `status=0(草稿)` 的记录；保存时创建/更新 `model_versions` 草稿版本（`model_type='award_application'`）；页面离开前调用此接口防止数据丢失。首次创建草稿请使用对应奖项类型的 `POST /awards/{type}` 接口并传入 `isDraft=1`。

------

### 8.1.2 奖项重复申报检测

**接口地址：** `POST /awards/duplicate-check`

**请求参数：**

| 参数名        | 类型   | 必填 | 说明                     |
| :------------ | :----- | :--- | :----------------------- |
| awardType     | String | 是   | 奖项类型编码             |
| certificateNo | String | 否   | 证书编号                 |
| title         | String | 否   | 报名标题/名称            |
| participatedTime | String | 否   | 参赛/取得时间，格式 `YYYY-MM-DD` |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "isDuplicate": true,
    "duplicateRecords": [
      {"applicationId": 199, "title": "全国大学生英语竞赛", "status": 2, "statusLabel": "已通过", "similarity": 0.91}
    ],
    "suggestion": "检测到疑似重复报名，请确认是否为同一成果。"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`award_applications` 表，根据证书编号、名称、时间等字段计算相似度。

------

### 8.1.3 获取奖项评选说明

**接口地址：** `GET /awards/{type}/guide`

**说明：** 获取指定奖项类型的评选说明，包含评选标准、申报条件和材料要求等。前端在奖项报名表单上方展示此说明内容。

**路径参数：**

| 参数名 | 类型   | 必填 | 说明                                                         |
| :----- | :----- | :--- | :----------------------------------------------------------- |
| type   | String | 是   | 奖项类型编码，如 `competition_star`、`research_star`、`innovation_star` 等 |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "type": "competition_star",
    "typeLabel": "竞赛之星",
    "title": "竞赛之星评选说明",
    "content": "竞赛之星用于表彰在学科竞赛中取得优异成绩的学生。",
    "requirements": [
      {"field": "competitionName", "label": "竞赛名称", "required": true, "description": "请填写竞赛完整名称"},
      {"field": "competitionLevel", "label": "竞赛级别", "required": true, "description": "选择竞赛的级别（国家级/省部级/校级等）"},
      {"field": "awardLevel", "label": "获奖级别", "required": true, "description": "填写所获奖项等级"},
      {"field": "participatedTime", "label": "参赛时间", "required": false, "description": "填写实际参赛日期"},
      {"field": "evidenceFiles", "label": "佐证材料", "required": true, "description": "须上传获奖证书或参赛证明扫描件"}
    ],
    "notes": [
      "1. 竞赛之星每人每学期限申报一项",
      "2. 佐证材料须包含获奖证书或官方公示截图",
      "3. 团队获奖须注明本人排名"
    ],
    "updatedAt": "2026-07-01T00:00:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`archive_type_configs` 表的 `evaluate_desc`/`evaluate_requirements`/`evaluate_notes` 字段，按 `school_id` + `award_type` 获取当前学校对应奖项类型的评选说明。奖项类型配置通过字典表 `dictionaries` 扩展或复用 `archive_type_configs` 的 `archive_type` 编码管理。

------

### 8.2 竞赛之星报名

#### 8.2.1 提交/保存竞赛之星报名

**接口地址：** `POST /awards/competition-star`

**请求参数：**




| 参数名             | 类型    | 必填 | 说明                           |
| :----------------- | :------ | :--- | :----------------------------- |
| `semesterId`       | Integer | 是   | 学期ID                         |
| `competitionName`  | String  | 是   | 竞赛名称                       |
| `participatedTime`  | String  | 否   | 参赛时间，格式 `YYYY-MM-DD`    |
| `competitionLevel` | String  | 是   | 竞赛级别（字典编码）           |
| `awardLevel`       | String  | 是   | 获奖级别（字典编码）           |
| `certificateNo`    | String  | 否   | 证书编号（`award_applications.certificate_no`） |
| `issuingUnit`      | String  | 否   | 发证/主办单位（`award_applications.issuing_unit`） |
| `validUntil`       | String  | 否   | 证书有效期，格式 `YYYY-MM-DD`（`award_applications.valid_until`） |
| `participantRole`  | String  | 否   | 本人角色：leader(负责人)、member(成员)、solo(独立完成)（`award_applications.participant_role`） |
| `isDraft`          | Integer | 否   | `0`=提交 `1`=保存草稿，默认`0` |
| `evidenceFileIds`  | Array   | 否   | 佐证材料文件ID列表             |

**请求示例：**


```json
{
  "semesterId": 3,
  "competitionName": "全国大学生英语竞赛",
  "participatedTime": "2025-10-15",
  "competitionLevel": "national",
  "awardLevel": "first",
  "isDraft": 0,
  "evidenceFileIds": [10003]
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "提交成功",
  "data": {
    "applicationId": 200,
    "status": 1,
    "statusLabel": "待审批",
    "currentVersion": 1,
    "submitCount": 1
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**错误码：**




| Code  | 说明             |
| :---- | :--------------- |
| 10002 | 必填参数缺失     |
| 10003 | 参数格式错误     |
| 30001 | 学期不存在       |
| 40002 | 当前状态不可操作 |
| 40009 | 重复提交         |





### 8.3 科研之星报名

> 科研之星包含三个子类型（`award_research_stars.primary_category`：`project` / `software_copyright` / `published_paper`），先创建主记录，再添加子项目。

#### 8.3.1 提交/保存科研之星主记录

**接口地址：** `POST /awards/research-star`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                     |
| :-------------- | :------ | :--- | :----------------------- |
| semesterId      | Integer | 是   | 学期ID                   |
| certificateNo   | String  | 否   | 证书编号（`award_applications.certificate_no`） |
| issuingUnit     | String  | 否   | 发证/主办单位（`award_applications.issuing_unit`） |
| validUntil      | String  | 否   | 证书有效期，格式 `YYYY-MM-DD`（`award_applications.valid_until`） |
| participantRole | String  | 否   | 本人角色：leader(负责人)、member(成员)、solo(独立完成)（`award_applications.participant_role`） |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0 |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "applicationId": 201,
    "researchStarId": 50,
    "status": 0,
    "statusLabel": "草稿"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 主记录创建后 `status=0(草稿)`，需至少添加一条子项目后才可提交。

------

#### 8.3.2 添加科研项目

**接口地址：** `POST /awards/research-star/{researchStarId}/projects`

**请求参数：**




| 参数名          | 类型   | 必填 | 说明                                                         |
| :-------------- | :----- | :--- | :----------------------------------------------------------- |
| projectName     | String | 是   | 项目名称（`award_research_projects.project_name`）           |
| projectLevel    | String | 是   | 项目级别，字典编码（`award_research_projects.project_level`） |
| rankTotal       | String | 是   | 排名/总人数，如 `2/5`（`award_research_projects.rank_total`） |
| establishedTime   | String | 是   | 立项时间，格式 `YYYY-MM-DD`（`award_research_projects.established_at`） |
| evidenceFileIds | Array  | 否   | 证明材料文件ID列表                                           |

> 外键约束：`award_research_projects.research_star_id` → `award_research_stars.id`。
> 
> **级联策略：** 按项目约束，关键业务表不采用数据库级 `ON DELETE CASCADE`。删除 `award_research_stars` 主记录时，由应用层进行软删除保护或将 `research_star_id` 置空（`SET NULL`），避免误删导致子记录丢失。

------

#### 8.3.3 添加软件著作权

**接口地址：** `POST /awards/research-star/{researchStarId}/software`

**请求参数：**




| 参数名          | 类型   | 必填 | 说明                                                         |
| :-------------- | :----- | :--- | :----------------------------------------------------------- |
| softwareName    | String | 是   | 软著名称（`award_software_copyrights.software_name`）        |
| rankTotal       | String | 是   | 排名/总人数，如 `1/4`（`award_software_copyrights.rank_total`） |
| approvedTime    | String | 是   | 获批时间，格式 `YYYY-MM-DD`（`award_software_copyrights.approved_at`） |
| evidenceFileIds | Array  | 否   | 证明材料文件ID列表                                           |

------

#### 8.3.4 添加发表论文

**接口地址：** `POST /awards/research-star/{researchStarId}/papers`

**请求参数：**




| 参数名          | 类型   | 必填 | 说明                                                         |
| :-------------- | :----- | :--- | :----------------------------------------------------------- |
| journalName     | String | 是   | 期刊名称（`award_published_papers.journal_name`）            |
| paperTitle      | String | 是   | 论文名称（`award_published_papers.paper_title`）             |
| rankTotal       | String | 是   | 排名/总人数，如 `2/3`（`award_published_papers.rank_total`） |
| publishedTime     | String | 是   | 发表时间，格式 `YYYY-MM-DD`（`award_published_papers.published_at`） |
| evidenceFileIds | Array  | 否   | 证明材料文件ID列表                                           |

------

#### 8.3.5 提交科研之星（确认提交）

**接口地址：** `POST /awards/research-star/{researchStarId}/submit`

**说明：** 当至少添加一条子项目后，调用此接口将主记录状态从 `0=草稿` 更新为 `1=待审批`。

**响应示例：**


```json
{
  "code": 0,
  "message": "提交成功",
  "data": {
    "applicationId": 201,
    "status": 1,
    "statusLabel": "待审批",
    "currentVersion": 1,
    "submitCount": 1
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 校验：`primary_category` 非空（由子项目类型决定），且至少关联一条子记录。

------

### 8.4 双创之星报名

#### 8.4.1 提交/保存双创之星报名

**接口地址：** `POST /awards/innovation-star`

**请求参数：**




| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| semesterId      | Integer | 是   | 学期ID                                                       |
| companyName     | String  | 是   | 公司名称（`award_innovation_stars.company_name`）            |
| industryType    | String  | 是   | 行业类型，字典编码（`award_innovation_stars.industry_type`） |
| applicantRank   | String  | 是   | 申报人排名，如 `1/3`（`award_innovation_stars.applicant_rank`） |
| registeredTime    | String  | 是   | 注册时间，格式 `YYYY-MM-DD`（`award_innovation_stars.registered_at`） |
| certificateNo   | String  | 否   | 证书编号（`award_applications.certificate_no`） |
| issuingUnit     | String  | 否   | 发证/主办单位（`award_applications.issuing_unit`） |
| validUntil      | String  | 否   | 证书有效期，格式 `YYYY-MM-DD`（`award_applications.valid_until`） |
| participantRole | String  | 否   | 本人角色：leader(负责人)、member(成员)、solo(独立完成)（`award_applications.participant_role`） |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0                                     |
| evidenceFileIds | Array   | 否   | 证明材料文件ID列表                                           |

**请求示例：**


```json
{
  "semesterId": 3,
  "companyName": "某某科技有限公司",
  "industryType": "internet",
  "applicantRank": "1/3",
  "registeredTime": "2025-03-15",
  "isDraft": 0,
  "evidenceFileIds": [10004, 10005]
}
```

------

### 8.5 获取奖项版本历史

**接口地址：** `GET /awards/{applicationId}/versions`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "currentVersion": 2,
    "versions": [
      {
        "version": 1,
        "title": "竞赛之星-全国大学生英语竞赛",
        "status": 3,
        "statusLabel": "已退回",
        "rejectedReason": "缺少参赛证明",
        "createdAt": "2026-06-20T10:00:00+08:00"
      },
      {
        "version": 2,
        "title": "竞赛之星-全国大学生英语竞赛",
        "status": 1,
        "statusLabel": "待审批",
        "rejectedReason": null,
        "createdAt": "2026-06-28T14:30:00+08:00"
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`model_versions` 表（`model_type='award_application'`），按 `version` 倒序。

------

## 九、AI 对话模块

### 9.1 创建对话会话

**接口地址：** `POST /ai/conversations`

**请求参数：**




| 参数名  | 类型   | 必填 | 说明                                                  |
| :------ | :----- | :--- | :---------------------------------------------------- |
| title   | String | 否   | 会话标题（`ai_conversations.title`），不传则自动生成  |
| context | Object | 否   | 系统上下文/人设配置JSON（`ai_conversations.context`） |

**请求示例：**


```json
{
  "title": "职业规划咨询",
  "context": {
    "role": "career_advisor",
    "language": "zh"
  }
}
```

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "conversationId": 100,
    "title": "职业规划咨询",
    "status": 1,
    "createdAt": "2026-07-08T10:00:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`ai_conversations` 表，`user_id` 取当前登录用户，`school_id` 取用户所属学校。

------

### 9.2 获取对话会话列表

**接口地址：** `GET /ai/conversations`

**请求参数：**




| 参数名   | 类型    | 必填 | 说明             |
| :------- | :------ | :--- | :--------------- |
| page     | Integer | 否   | 页码，默认1      |
| per_page | Integer | 否   | 每页条数，默认20 |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 15,
    "list": [
      {
        "id": 100,
        "title": "职业规划咨询",
        "status": 1,
        "statusLabel": "正常",
        "lastMessageTime": "2026-07-08T10:30:00+08:00",
        "createdAt": "2026-07-08T10:00:00+08:00"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 15,
      "total_pages": 1
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`ai_conversations` 表，`status=1`，按 `updated_at` 倒序。

------

### 9.3 获取对话消息列表

**接口地址：** `GET /ai/conversations/{conversationId}/messages`

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "conversationId": 100,
    "title": "职业规划咨询",
    "messages": [
      {
        "id": 1,
        "role": "user",
        "content": "请帮我分析我的职业规划",
        "createdAt": "2026-07-08T10:00:00+08:00"
      },
      {
        "id": 2,
        "role": "assistant",
        "content": "根据您的档案信息，我建议...",
        "modelName": "gpt-4",
        "tokenUsage": 256,
        "generationTimeMs": 1200,
        "createdAt": "2026-07-08T10:00:05+08:00"
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`ai_messages` 表，按 `created_at` 正序。

------

### 9.4 发送消息

**接口地址：** `POST /ai/conversations/{conversationId}/messages`

**请求参数：**




| 参数名  | 类型   | 必填 | 说明                 |
| :------ | :----- | :--- | :------------------- |
| content | String | 是   | 消息内容（用户提问） |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "messageId": 3,
    "role": "assistant",
    "content": "根据您的档案信息...",
    "modelName": "gpt-4",
    "tokenUsage": 512,
    "generationTimeMs": 2100,
    "createdAt": "2026-07-08T10:05:00+08:00",
    "suggestedActions": [
      {
        "label": "查看个人档案",
        "jumpUrl": "/profile/info",
        "actionType": "navigate"
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：
>
> 1. 写入用户消息到 `ai_messages`（`role=user`）
> 2. 调用 AI 服务生成回复
> 3. 写入助手消息到 `ai_messages`（`role=assistant`），记录 `model_name`、`token_usage`、`generation_time_ms`
> 4. 更新 `ai_conversations.updated_at` 为最后对话时间
> 5. 同时写入 `ai_generation_logs` 表记录生成日志
> 6. 当 AI 回答涉及系统使用问题（如“如何修改手机号”“如何导出档案”）时，后端应在 `suggestedActions` 中返回对应页面跳转入口；前端优先展示可点击跳转按钮。`suggestedActions` 为后端运行时组装，不写入 `ai_messages` 表。

------

### 9.5 重新生成 AI 消息

**接口地址：** `POST /ai/conversations/{conversationId}/messages/{messageId}/regenerate`

**说明：** 当某条助手消息生成失败或需要重新生成时调用，传入原消息 ID。后端通过 `ai_generation_logs.retry_of` 关联原调用，避免重复扣费。

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "messageId": 4,
    "role": "assistant",
    "content": "根据您的档案信息，我重新建议...",
    "modelName": "gpt-4",
    "modelVersion": "2024-05",
    "tokenUsage": 512,
    "generationTimeMs": 1800,
    "callStatus": 1,
    "isRetry": true,
    "createdAt": "2026-07-08T10:10:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：检查原消息生成状态，若失败则重新调用 AI；新调用记录 `retry_of` 指向原 `ai_generation_logs.id`。

------

### 9.6 获取 AI 辅助建议

**接口地址：** `GET /ai/suggestions`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明                                                         |
| :--------- | :------ | :--- | :----------------------------------------------------------- |
| sourceType | String  | 是   | 来源类型：archive / career_plan / weakness_analysis          |
| sourceId   | Integer | 是   | 来源记录ID                                                   |

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "suggestionId": 10,
        "content": "建议参与导师科研项目，尝试撰写学术论文。",
        "sourceArchives": [
          {"archiveId": 1001, "title": "全国大学生数学建模竞赛"}
        ],
        "aiGenerated": true,
        "aiWarning": "AI辅助生成，请教师复核",
        "teacherAction": 0,
        "teacherActionLabel": "待处理",
        "createdAt": "2026-07-01T10:00:00+08:00"
      }
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`improvement_suggestions`、`ai_generation_logs`、`ai_teacher_feedbacks` 表。其中 `content` 对应 `improvement_suggestions.suggestion_content`。

------

### 9.7 删除对话会话

**接口地址：** `DELETE /ai/conversations/{conversationId}`

**响应示例：**


```json
{
  "code": 0,
  "message": "删除成功",
  "data": null,
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 软删除，更新 `ai_conversations.deleted_at`。级联删除该会话下所有 `ai_messages` 记录（或同样软删除）。

------

## 十、数据字典（接口枚举值）

### 10.1 状态枚举说明

> 不同模块的 `status` 字段语义不同，调用时请务必按所属模块的枚举表解析，禁止混用。

#### 10.1.1 账号状态

适用于 `users.status`。

| 值 | 说明 |
| :- | :--- |
| 0 | 禁用 |
| 1 | 正常 |

#### 10.1.2 角色状态

适用于 `roles.status`。

| 值 | 说明 |
| :- | :--- |
| 0 | 禁用 |
| 1 | 启用 |

#### 10.1.3 业务审批状态

适用于 `archives.status`、`award_applications.status`、`career_plans.status`。

| 值   | 说明   | 状态说明                               |
| :--- | :----- | :------------------------------------- |
| 0    | 草稿   | 学生编辑中，未提交                     |
| 1    | 待审批 | 已提交，等待教师审核                   |
| 2    | 已通过 | 审核通过，已归档                       |
| 3    | 已退回 | 教师退回修改，学生可编辑后重新提交     |
| 4    | 已撤销 | 学生主动撤销或管理员撤销               |

> 注：已严格对齐 To Do List 中的“草稿、待审核、已退回、已通过、已撤销”五种状态。

#### 10.1.4 通用开关状态

适用于 `dictionaries.status`、`evaluation_indicators.status`、`permissions.status` 等。

| 值   | 说明   |
| :--- | :----- |
| 0    | 禁用   |
| 1    | 启用   |

#### 10.1.5 角色范围状态

适用于 `role_scopes.status`。

| 值   | 说明   |
| :--- | :----- |
| 0    | 停用   |
| 1    | 启用   |
| 2    | 过期   |

### 10.2 性别




| 值   | 说明 | 对应表字段     |
| :--- | :--- | :------------- |
| 0    | 未知 | `users.gender` |
| 1    | 男   |                |
| 2    | 女   |                |

### 10.3 审批动作




| 值   | 说明 | 对应表字段          |
| :--- | :--- | :------------------ |
| 1    | 通过 | `audit_logs.action` |
| 2    | 退回 |                     |
| 3    | 撤回 |                     |
| 4    | 转交 |                     |

### 10.4 消息发送渠道




| 值    | 说明     | 对应表字段                   |
| :---- | :------- | :--------------------------- |
| push  | 站内推送 | `user_messages.send_channel` |
| email | 邮件     |                              |
| sms   | 短信     |                              |

### 10.5 消息分类

适用于 `user_messages.category`、`notification_settings.category`。

| 值               | 说明     |
| :--------------- | :------- |
| system_notice    | 系统通知 |
| audit_remind     | 审批提醒 |
| dynamic_remind   | 动态提醒 |
| private_message  | 私信     |

### 10.6 文件分类

> 数据库 `file_uploads.file_category` 仅存储：`certificate` / `photo` / `proof` / `other`。  
> 接口上传参数 `type` 为前端语义值，后端按以下映射写入数据库：

| 接口传入值 | 数据库存储值 | 说明     |
| :---------- | :----------- | :------- |
| certificate | certificate  | 证书     |
| evidence    | proof        | 佐证材料 |
| plan        | other        | 规划文件 |
| avatar      | photo        | 头像     |
| photo       | photo        | 照片     |
| proof       | proof        | 证明材料 |
| other       | other        | 其他     |

### 10.7 成长时间轴事件类型




| 值   | 说明     | 对应表字段                    |
| :--- | :------- | :---------------------------- |
| 1    | 奖项     | `growth_timelines.event_type` |
| 2    | 成绩     |                               |
| 3    | 实践     |                               |
| 4    | 职业规划 |                               |
| 5    | 短板改进 |                               |
| 6    | 能力提升 |                               |

### 10.8 角色层级




| 值   | 说明   | 对应表字段    |
| :--- | :----- | :------------ |
| 0    | 系统   | `roles.level` |
| 1    | 学生   |               |
| 2    | 教师   |               |
| 3    | 辅导员 |               |
| 4    | 系主任 |               |
| 5    | 院长   |               |
| 6    | 校长   |               |
| 7+   | 自定义 |               |

### 10.9 组织范围类型




| 值   | 说明 | 对应表字段                                                  |
| :--- | :--- | :---------------------------------------------------------- |
| 1    | 学校 | `role_scopes.scope_type` / `approval_flow_steps.scope_type` |
| 2    | 学院 |                                                             |
| 3    | 专业 |                                                             |
| 4    | 班级 |                                                             |
| 5    | 课程 |                                                             |
| 6    | 年级 |                                                             |

### 10.10 附件业务类型（`biz_type`）

适用于 `file_uploads.biz_type`（枚举定义见后端 `AttachmentBizTypeEnum`，与 `attachment_relations.biz_type` 同源）。文件先经 `POST /common/upload` 上传暂存（`file_status=1`），由业务提交/导出接口绑定 `biz_type`、`biz_id` 后置 `file_status=2`。前端一般不直接传值，由后端按业务绑定。

| 值              | 说明               | 绑定场景                                        |
| :-------------- | :----------------- | :---------------------------------------------- |
| archive         | 档案               | 档案申报佐证材料                                |
| award           | 奖项               | 奖项申报佐证材料                                |
| career_plan     | 职业规划           | 4.4 提交职业规划时绑定 `evidenceFileIds`        |
| career_action   | 职业规划行动       | 4.13 上传行动成果（V5.7 新增）                  |
| student_archive | 学生成长档案导出   | 4.17 提交档案导出生成文件（V5.7 新增）          |
| announcement    | 公告               | 公告附件                                        |

------

## 十一、登录页公开统计接口

> 提供学生端/管理端登录页品牌区底部统计数字，**免鉴权**（未登录可访问，无需 Bearer Token）。

### 11.1 登录页公开统计概览

**接口地址：** `GET /public/statistics`

**认证方式：** 无（公开接口，已加入免鉴权白名单）

**响应示例：**


```json
{
  "code": 0,
  "message": "success",
  "data": {
    "studentCount": 2840,
    "archiveCount": 12580,
    "pendingApplicationCount": 386,
    "serviceAvailability": "99.9%",
    "systemStability": "99.99%",
    "statTime": "2026-09-01 10:00:00"
  },
  "trace_id": "req-20260901-100000-abc123",
  "timestamp": "2026-09-01T10:00:00+08:00"
}
```

**响应字段：**

| 字段                      | 类型   | 说明                                                                 |
| :------------------------ | :----- | :------------------------------------------------------------------- |
| `studentCount`            | Long   | 在校学生数：COUNT(student_profiles) 全部未软删除行（每名学生一行）   |
| `archiveCount`            | Long   | 档案条目数/学生档案数：COUNT(archives) 全部未软删除档案              |
| `pendingApplicationCount` | Long   | 待审申请数：`archives.status=1` 与 `award_applications.status=1` 之和 |
| `serviceAvailability`     | String | 服务可用率（配置项 `public.stats.service-availability`，默认 99.9%）  |
| `systemStability`         | String | 系统稳定率（配置项 `public.stats.system-stability`，默认 99.99%）     |
| `statTime`                | String | 统计时间（`yyyy-MM-dd HH:mm:ss`）                                    |

> **数据口径：** 统计均为全校有效数据，软删除（`deleted_at IS NULL`）自动排除。`serviceAvailability` / `systemStability` 无法从学生档案库算出真实值，由后端配置项返回（运营可改，前端不再写死）。
>
> **前端映射：** 学生端登录页取 `studentCount` / `archiveCount` / `serviceAvailability`；管理端登录页取 `pendingApplicationCount` / `archiveCount` / `systemStability`。

------

## 附录：教师端核心接口（概要）

> 以下接口由教师端/管理端调用，不在学生端业务范围，但与学生端状态流转紧密相关，故在此列出接口清单，供前后端联调参考。详细参数与响应由教师端接口文档补充。

| 接口地址                                      | 方法   | 说明                                                         |
| :-------------------------------------------- | :----- | :----------------------------------------------------------- |
| `GET /teacher/audits/pending`                 | GET    | 教师待审核列表；按教师授权范围（班级/专业/年级）过滤         |
| `GET /teacher/audits/pending/{taskId}`        | GET    | 待审核详情；同时返回学生填报信息和证明材料                   |
| `POST /teacher/audits/{taskId}/approve`       | POST   | 通过；幂等，重复请求不重复生成结果                           |
| `POST /teacher/audits/{taskId}/reject`        | POST   | 退回修改；必须填写退回原因                                   |
| `POST /teacher/audits/batch/approve`          | POST   | 批量通过；传入待审核任务 ID 数组                             |
| `GET /teacher/audits/history`                 | GET    | 教师/管理端审核记录列表（含历史审核记录）                    |
| `GET /teacher/audits/reject-templates`        | GET    | 常用退回原因模板                                             |
| `POST /teacher/audits/{taskId}/revoke`        | POST   | 管理员撤销错误审核；需填写撤销原因并通知学生                 |
| `POST /teacher/scores/recalculate`            | POST   | 触发指定学生/班级/学期的评分重新计算（教师授权范围内，参数同管理端 `targetType`） |
| `GET /teacher/scores/recalculation-tasks/{id}`| GET    | 查询评分重新计算任务进度                                     |
| `POST /admin/scores/recalculate`              | POST   | 触发全校范围评分重新计算（管理端）                           |
| `GET /admin/scores/recalculation-tasks/{id}`  | GET    | 查询评分重新计算任务进度（管理端）                           |

> 权限控制：教师端所有接口均需校验 `roles` 与 `role_scopes`，学生角色调用返回 `403`。

------

## 附录 B：接口测试交付清单

> 本附录按 To Do List 要求补充学生端核心接口测试项，供前后端联调及验收使用。

### B.1 通用测试检查项

| 检查项 | 说明 | 通过标准 |
| :----- | :--- | :------- |
| 响应格式 | 所有接口返回统一响应体 | `code`/`message`/`data`/`trace_id`/`timestamp` 均存在 |
| 认证校验 | 未携带 Token 或 Token 失效 | 返回 `20001/20002` 及 HTTP `401` |
| 权限校验 | 访问非学生端接口或他人数据 | 返回 `20005` 及 HTTP `403` |
| 幂等控制 | 重复提交相同 `idempotency-key` | 返回首次结果或 `40009` |
| 分页参数 | `page`/`per_page` 边界值 | 默认/最大/越界行为符合全局规范 |

> **幂等语义说明：** 通用规则为「24 小时内同一 `idempotency-key` 重复请求直接返回首次结果」。对于明确定义了「重复提交」语义的端点（如更正申请提交类接口，重复提交返回 `40009 重复提交`），以其显式语义为准；二者为不同场景下的规则，不冲突（见管理端文档「幂等性与防重复提交」）。

### B.2 状态机测试用例

| 用例编号 | 接口 | 前置状态 | 操作 | 期望状态 | 期望错误码 |
| :------- | :--- | :------- | :--- | :------- | :--------- |
| S-001 | `POST /activities` | 草稿 | 提交 | 待审批 | — |
| S-002 | `PUT /activities/{id}/withdraw` | 待审批 | 撤回 | 草稿 | — |
| S-003 | `PUT /activities/{id}/withdraw` | 已通过 | 撤回 | — | `40002` |
| S-004 | `PUT /activities/{id}/withdraw` | `type=career_plan` 且待审批 | 撤回 | 草稿 | — |
| S-005 | `DELETE /activities/{id}` | 草稿/已退回 | 删除 | 软删除 | — |
| S-006 | `DELETE /activities/{id}` | 已通过 | 删除 | — | `40002` |

### B.3 文件与权限测试用例

| 用例编号 | 接口 | 场景 | 期望结果 |
| :------- | :--- | :--- | :------- |
| F-001 | `GET /common/files/{fileId}/download` | 学生下载自己的附件 | 返回文件流 |
| F-002 | `GET /common/files/{fileId}/download` | 学生下载他人附件 | `20005` |
| F-003 | `POST /common/files` | 上传 oversized/格式错误文件 | `10007`/`10006` |

### B.4 成长时间轴测试用例

| 用例编号 | 接口 | 场景 | 期望结果 |
| :------- | :--- | :--- | :------- |
| T-001 | `GET /profile/growth-timeline` | `status=4` 筛选已撤销 | 仅返回已撤销记录 |
| T-002 | `GET /profile/growth-timeline` | `status=5` | 无记录（学生端状态机不存在 `5=已撤销`） |

> **注：** 学生端业务审批状态统一为 `0草稿→1待审批→2通过/3已退回/4已撤销`，测试用例必须按此枚举断言。

