server:
  port: 8089

spring:
  datasource:
    url: jdbc:mysql://8.137.191.26:3306/student_archives?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: StuArch
    password: ZCXCDMLZDNENkYRe
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate    # ← 只验证表结构，不自动建表
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        jdbc.batch_size: 30
        order_inserts: true
        order_updates: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration