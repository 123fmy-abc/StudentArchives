# 学生档案管理系统 - 教师端接口文档（V5.13）

> 基于数据库表结构 V5.6 整理，仅保留教师端接口。教师角色包括辅导员、班主任、专业负责人及具有审批/查看权限的行政教师。
> 更新日期：2026-08-22
>
> 本文档复用《学生端接口文档》中的全局规范、认证接口、通用接口、消息中心接口等共享内容；教师端仅补充教师角色专属接口及特殊说明。

------

## 全局规范

教师端接口复用《学生端接口文档》中的以下规范，本文档不再重复：

- 基础信息（基础 URL、认证方式、统一响应格式）
- 响应体统一结构、分页参数与分页响应
- 幂等性与防重复提交
- 全局错误码体系
- URL 与参数命名规范、状态枚举规范
- 数据范围与权限说明

**教师端补充规范：**

- 教师端接口统一前缀：`/teacher/*`；公共接口复用 `/common/*`、认证复用 `/auth/*`。
- 所有教师端接口必须校验当前用户具有教师角色，并且只能访问其授权数据范围内的学生数据。
- 授权范围由 `role_scopes` 表按 `scope_type`（学校/学院/专业/班级/课程/年级）配置；越权访问统一返回 `20005 无访问权限`。
- 业务审批状态：`0草稿 → 1待审批 → 2通过 / 3已退回 / 4已撤销`。
- 范围类型：`1学校 2学院 3专业 4班级 5课程 6年级`。

**关键权限码：**

| 权限码              | 说明             | 对应接口                          |
| :------------------ | :--------------- | :-------------------------------- |
| `audit:approve`     | 审核通过         | `/teacher/audits/{id}/approve`     |
| `audit:revoke`      | 撤销已审核记录   | `/teacher/audits/{id}/revoke`（**仅管理员角色可用**，普通教师不授予） |
| `export:execute`    | 教师数据导出     | `/teacher/exports/*`              |
| `score:recalculate` | 触发评分重算     | `/teacher/scores/recalculate`     |
| `delegate:manage`   | 管理审批委托     | `/teacher/delegations/*`          |
| `log:view`          | 查看操作日志     | `/teacher/logs`（V5.7 新增）      |
| `statistics:view`   | 统计看板与热力图 | `/teacher/statistics/*`（V5.7 新增，成果热力图） |

> **与需求口径对齐说明：** 已严格对齐 To Do List 中的“草稿、待审核、已退回、已通过、已撤销”五种状态。教师审核时“通过”进入 `2=已通过`，“退回修改”进入 `3=已退回`，学生可编辑后重新提交；`4=已撤销`用于学生主动撤销、管理员撤销或更正流程中原记录被替代后失效。

------

## 一、认证模块

教师端认证接口复用《学生端接口文档》一、认证模块，登录后 `roles` 返回教师相关角色（如 `teacher`、`counselor`、`admin`）。

- 用户登录：`POST /auth/login`
- 获取当前用户信息：`GET /auth/me`
- 退出登录：`POST /auth/logout`
- 修改密码：`PUT /auth/password`
- 刷新访问令牌：`POST /auth/refresh`

> `GET /auth/me` 为教师/学生/管理员共用接口，契约详见《学生端接口文档》1.3。教师/辅导员角色的响应 `data` 中额外包含 `scopes`（授权数据范围，来源 `role_scopes`），返回其可查看/审核的班级、专业等范围。

------

## 二、通用接口

教师端通用接口与学生端一致，详见《学生端接口文档》二、通用接口；教师端权限校验范围为授权范围内的学生数据。

- 文件预览：`GET /common/files/{fileId}/preview`
- 文件下载：`GET /common/files/{fileId}/download`
- 学期下拉选项：`GET /common/semesters`
- 字典数据：`GET /common/dict`
- 指标树查询：`GET /common/indicators`

------

## 三、教师首页模块FMY

### 3.1 教师首页数据概览

**接口地址：** `GET /teacher/dashboard`

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "teacherName": "王老师",
    "teacherNo": "T2023005",
    "currentSemesterId": 3,
    "currentSemesterName": "2023-2024第一学期",
    "scopes": [
      {"scopeType": 4, "scopeId": 10, "scopeName": "计科2301班"},
      {"scopeType": 4, "scopeId": 11, "scopeName": "计科2302班"}
    ],
    "pendingStats": {
      "archivePending": 12,
      "awardPending": 5,
      "careerPlanPending": 3,
      "totalPending": 20
    },
    "todayAudited": 8,
    "recentAudits": [
      {
        "id": 101,
        "type": "archive",
        "archiveType": "academic_competition",
        "title": "全国大学生数学建模竞赛",
        "studentName": "张三",
        "studentNo": "2023001001",
        "action": 1,
        "actionLabel": "通过",
        "auditedAt": "2026-07-04T10:30:00+08:00"
      }
    ],
    "unreadMessageCount": 3
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`role_scopes`、`pending_approvals`、`audit_logs`、`user_messages`。

------

## 四、待审核任务模块LZW

### 4.1 获取待审核列表

**接口地址：** `GET /teacher/audits/pending`

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                                                         |
| :----------- | :------ | :--- | :----------------------------------------------------------- |
| `type`       | String  | 否   | 业务类型：archive/award/career_plan，默认全部                |
| `archiveType`| String  | 否   | 档案/奖项类型编码                                            |
| `scopeType`  | Integer | 否   | 范围类型：1学校 2学院 3专业 4班级 5课程 6年级                |
| `scopeId`    | Integer | 否   | 范围ID，需在教师授权范围内                                   |
| `semesterId` | Integer | 否   | 学期ID                                                       |
| `grade`      | String  | 否   | 年级筛选，如 `2023级`                                        |
| `keyword`    | String  | 否   | 搜索关键词（标题/学生姓名/学号）                             |
| `sortBy`     | String  | 否   | 排序字段：submit_time（默认）                                |
| `sortOrder`  | String  | 否   | asc / desc，默认 asc（先提交先审）                           |
| `page`       | Integer | 否   | 页码，默认1                                                  |
| `per_page`   | Integer | 否   | 每页条数，默认20                                             |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "taskId": 1001,
        "type": "archive",
        "archiveType": "academic_competition",
        "archiveTypeLabel": "学科竞赛",
        "approvableId": 500,
        "title": "全国大学生数学建模竞赛",
        "applicantId": 5,
        "applicantName": "张三",
        "applicantNo": "2023001001",
        "className": "计科2301班",
        "majorName": "计算机科学与技术",
        "semesterId": 3,
        "semesterName": "2023-2024第一学期",
        "submitTime": "2026-07-03T14:30:00+08:00",
        "stepNo": 1,
        "stepName": "辅导员初审",
        "status": 1,
        "statusLabel": "待审批",
        "duplicateCheckStatus": 0,
        "duplicateCheckStatusLabel": "未检测"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 20,
      "total_pages": 1
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`pending_approvals`、`approval_nodes`、`approval_instances`、`archives/award_applications/career_plans`。
> 响应字段映射：`taskId` 对应 `pending_approvals.id`；`approvableId` 对应 `pending_approvals.approvable_id`（即 `archives.id` / `award_applications.id` / `career_plans.id`）；`status` 对应 `pending_approvals.status`。
> 必须按教师 `role_scopes` 范围过滤。

### 4.2 获取待审核详情

**接口地址：** `GET /teacher/audits/pending/{taskId}`

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "taskId": 1001,
    "type": "archive",
    "archiveType": "academic_competition",
    "approvableId": 500,
    "title": "全国大学生数学建模竞赛",
    "applicant": {
      "userId": 5,
      "name": "张三",
      "studentNo": "2023001001",
      "className": "计科2301班",
      "majorName": "计算机科学与技术",
      "grade": "2023级"
    },
    "baseInfo": {
      "semesterId": 3,
      "semesterName": "2023-2024第一学期",
      "obtainTime": "2025-09",
      "certificateNo": "CERT2025001",
      "issuingUnit": "中国工业与应用数学学会",
      "validUntil": "2028-09-01",
      "participantRole": "leader",
      "participantRoleLabel": "负责人"
    },
    "detail": {
      "competitionName": "全国大学生数学建模竞赛",
      "competitionType": "mathematical_modeling",
      "awardLevel": "second",
      "awardLevelLabel": "二等奖"
    },
    "evidenceFiles": [
      {"fileId": 10001, "fileName": "获奖证书.pdf", "fileUrl": "/uploads/2026/07/cert.pdf", "previewUrl": "/preview/10001", "fileSize": 102400}
    ],
    "duplicateInfo": {
      "isDuplicate": false,
      "duplicateRecords": []
    },
    "approvalHistory": [
      {
        "nodeId": 2001,
        "stepNo": 1,
        "stepName": "辅导员初审",
        "auditorName": "王老师",
        "action": 1,
        "actionLabel": "通过",
        "comment": "材料齐全",
        "createdAt": "2026-07-02T09:00:00+08:00"
      }
    ],
    "versionHistory": [
      {"version": 1, "status": 1, "statusLabel": "待审批", "createdAt": "2026-07-03T14:30:00+08:00"}
    ],
    "cursor": {
      "prevTaskId": null,
      "nextTaskId": 1002
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> `cursor.prevTaskId` / `nextTaskId` 用于连续审核上下条导航，按当前筛选条件排序。
>
> `baseInfo` 中字段来源：`certificateNo`/`issuingUnit`/`validUntil` 按档案类型来自 `archive_certificates` 或 `award_applications`；`participantRole` 按档案类型来自 `archive_competitions` / `archive_researches` / `archive_innovations` / `archive_social_practices` 或 `award_applications`。

### 4.3 单个审核通过

**接口地址：** `POST /teacher/audits/{taskId}/approve`

**请求参数：**

| 参数名      | 类型   | 必填 | 说明           |
| :---------- | :----- | :--- | :------------- |
| `comment`   | String | 否   | 审核意见       |
| `nextAuditorId` | Integer | 否 | 指定下一审批人（当流程允许时），对应 `approval_nodes.assigned_auditor_id` |

**响应示例：**

```json
{
  "code": 0,
  "message": "审核通过",
  "data": {
    "taskId": 1001,
    "approvableId": 500,
    "status": 2,
    "statusLabel": "已通过",
    "auditedAt": "2026-07-04T10:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：
> - 校验当前教师是否有该任务审批权限（`pending_approvals.auditor_id` 或角色范围匹配）。
> - 校验任务状态为待审批，避免重复审核。
> - 写入 `audit_logs`（action=1）和 `approval_nodes`。
> - 更新 `archives/award_applications/career_plans` 状态为已通过，触发 `passed_at`、评分异步计算、消息通知。
> - 幂等控制：同一 `taskId` + 教师重复提交返回首次结果或 40009。

### 4.4 单个审核退回

**接口地址：** `POST /teacher/audits/{taskId}/reject`

**请求参数：**

| 参数名         | 类型   | 必填 | 说明                                                         |
| :------------- | :----- | :--- | :----------------------------------------------------------- |
| `comment`      | String | 是   | 退回原因，必填                                               |
| `templateCode` | String | 否   | 退回原因模板编码                                             |
| `rejectToStep` | Integer | 否  | 退回到指定步骤（流程配置允许时），默认退回申请人             |

**响应示例：**

```json
{
  "code": 0,
  "message": "已退回修改",
  "data": {
    "taskId": 1001,
    "approvableId": 500,
    "status": 3,
    "statusLabel": "已退回",
    "returnedAt": "2026-07-04T10:30:00+08:00",
    "rejectedReason": "缺少证书原件扫描件"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：
> - `comment` 必填，否则返回 10002。
> - 可保存到 `audit_comment_templates` 使用频率统计。
> - 状态变为 `3已退回`，更新 `returned_at` 和 `rejected_reason`。
> - 发送消息通知学生。

### 4.5 批量审核通过

**接口地址：** `POST /teacher/audits/batch/approve`

**请求参数：**

| 参数名    | 类型  | 必填 | 说明               |
| :-------- | :---- | :--- | :----------------- |
| `taskIds` | Array | 是   | 待审核任务ID列表   |
| `comment` | String | 否  | 统一审核意见       |

**响应示例：**

```json
{
  "code": 0,
  "message": "批量审核完成",
  "data": {
    "total": 5,
    "success": 5,
    "failed": 0,
    "results": [
      {"taskId": 1001, "success": true, "status": 2},
      {"taskId": 1002, "success": true, "status": 2}
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：
> - 事务控制，任一失败可全部回滚（根据产品需求也可部分成功）。
> - 仅允许批量通过，不允许批量退回（退回需填写原因，建议单独处理）。
> - 需校验所有 taskId 均在教师权限范围内。

### 4.6 撤销已审核记录

**接口地址：** `POST /teacher/audits/{taskId}/revoke`

> 权限码：`audit:revoke`。**仅管理员角色可用**；教师端路径 `/teacher/audits/{taskId}/revoke` 仅作为管理员撤销操作的统一入口，普通教师角色不应被授予 `audit:revoke` 权限。调用时后端必须校验当前用户拥有 `admin` 角色，否则返回 `20005 无访问权限`。

**请求参数：**

| 参数名         | 类型   | 必填 | 说明           |
| :------------- | :----- | :--- | :------------- |
| `revokeReason` | String | 是   | 撤销原因，必填 |

**响应示例：**

```json
{
  "code": 0,
  "message": "撤销成功",
  "data": {
    "taskId": 1001,
    "approvableId": 500,
    "status": 4,
    "statusLabel": "已撤销",
    "revokeReason": "审核有误，需重新提交",
    "revokedAt": "2026-07-04T10:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：
> - 校验原记录状态为已通过。
> - 在 `audit_logs` 写入 action=3 的撤销记录，`revoke_reason` 必填。
> - 触发档案状态回退、评分重算、消息通知学生。

### 4.7 获取常用退回原因模板

**接口地址：** `GET /teacher/audits/reject-templates`

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {"templateCode": "missing_cert", "templateContent": "缺少证书原件扫描件", "usageCount": 156},
    {"templateCode": "info_incomplete", "templateContent": "填报信息不完整，请补充", "usageCount": 89}
  ],
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`audit_comment_templates`，按使用频率或排序返回。

------

## 五、审核历史模块LZW

### 5.1 获取我的审核记录

**接口地址：** `GET /teacher/audits/history`

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                                          |
| :----------- | :------ | :--- | :-------------------------------------------- |
| `type`       | String  | 否   | 业务类型：archive/award/career_plan           |
| `action`     | Integer | 否   | 审核动作：1通过 2退回 3撤销 4转交             |
| `semesterId` | Integer | 否   | 学期ID                                        |
| `startDate`  | String  | 否   | 开始时间，格式 `YYYY-MM-DD`                   |
| `endDate`    | String  | 否   | 结束时间，格式 `YYYY-MM-DD`                   |
| `keyword`    | String  | 否   | 搜索学生姓名/学号/标题                        |
| `page`       | Integer | 否   | 页码，默认1                                   |
| `per_page`   | Integer | 否   | 每页条数，默认20                              |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "auditId": 3001,
        "type": "archive",
        "archiveType": "academic_competition",
        "title": "全国大学生数学建模竞赛",
        "studentName": "张三",
        "studentNo": "2023001001",
        "action": 1,
        "actionLabel": "通过",
        "comment": "材料齐全",
        "auditedAt": "2026-07-04T10:30:00+08:00"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 88,
      "total_pages": 5
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`audit_logs`，按 `auditor_id` 过滤当前教师。响应字段 `action` 对应 `audit_logs.action`，枚举为 `1通过 2退回 3撤销 4转交`。

------

## 六、操作日志查询模块LZW

> 数据来源：`system_logs` 表（由 Flyway V11 迁移直接建表）。写入范围与《管理端接口文档》三、操作日志模块一致，由后端切面统一写入，教师端仅提供只读查询。
> 支撑「日志查看」功能——课任教师/审核员选择时间查看授权范围内的操作记录（学生申报提交、教师审核、导出、评分重算、消息等），实现「选择时间 → 查看记录」的查询交互。
> 权限码：`log:view`。仅返回当前教师自身操作、以及 `role_scopes` 授权范围内学生相关的操作日志，越权范围返回 20005。

### 6.1 查询授权范围内的系统操作日志

**接口地址：** `GET /teacher/logs`

**说明：** 按操作人、操作类型、模块、时间分页查询系统操作日志。数据来自 `system_logs` 表。

**请求参数：**

| 参数名        | 类型    | 必填 | 说明                                                          |
| :------------ | :------ | :--- | :------------------------------------------------------------ |
| `operatorId`  | long    | 否   | 操作人 ID                                                     |
| `action`      | string  | 否   | 操作类型，如 `create`/`update`/`delete`/`audit`/`export`      |
| `module`      | string  | 否   | 操作模块，如 `archive`/`award`/`career_plan`/`score`/`message` |
| `logLevel`    | int     | 否   | 1=普通 2=用户动态 3=审计                                      |
| `startTime`   | string  | 否   | 开始时间，ISO 8601，如 `2026-07-01T00:00:00+08:00`            |
| `endTime`     | string  | 否   | 结束时间，ISO 8601，如 `2026-07-31T23:59:59+08:00`            |
| `relatedType` | string  | 否   | 关联模型类型：`archive`/`award_application`/`career_plan`/`growth_timeline` |
| `relatedId`   | long    | 否   | 关联记录 ID                                                   |
| `grade`       | string  | 否   | 年级筛选，如 `2023级`，按被操作学生所属年级过滤               |
| `keyword`     | string  | 否   | 搜索关键词（学生姓名/学号/标题）                              |
| `page`        | int     | 否   | 页码，默认 1                                                  |
| `per_page`    | int     | 否   | 每页条数，默认 20，最大 100                                   |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 10001,
        "operatorId": 20,
        "operatorName": "王老师",
        "roleId": 2,
        "roleName": "教师",
        "action": "audit",
        "module": "archive",
        "description": "审核通过档案：全国大学生数学建模竞赛",
        "beforeData": null,
        "afterData": { "status": 2, "statusLabel": "已通过" },
        "ipAddress": "10.0.0.8",
        "createdAt": "2026-07-04T10:30:00+08:00"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 88,
      "total_pages": 5
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据范围：默认返回「当前教师自身的操作日志」与「授权范围内学生相关的操作日志」（按 `system_logs.user_id` 或 `related_type` 对应业务对象的组织归属过滤），与教师 `role_scopes` 范围取交集；`grade` 按 `classes.grade` 匹配。越权范围统一返回 `20005`。

------

## 七、学生管理模块LZW

### 7.1 获取授权范围内的学生列表

**接口地址：** `GET /teacher/students`

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                                          |
| :----------- | :------ | :--- | :-------------------------------------------- |
| `scopeType`  | Integer | 否   | 范围类型：1学校 2学院 3专业 4班级 6年级       |
| `scopeId`    | Integer | 否   | 范围ID                                        |
| `semesterId` | Integer | 否   | 学期ID                                        |
| `grade`      | String  | 否   | 年级                                          |
| `keyword`    | String  | 否   | 姓名/学号                                     |
| `page`       | Integer | 否   | 页码，默认1                                   |
| `per_page`   | Integer | 否   | 每页条数，默认20                              |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "userId": 5,
        "studentNo": "2023001001",
        "name": "张三",
        "gender": 1,
        "genderLabel": "男",
        "className": "计科2301班",
        "majorName": "计算机科学与技术",
        "collegeName": "信息工程学院",
        "grade": "2023级",
        "currentGpa": 3.82,
        "approvedArchiveCount": 8,
        "pendingArchiveCount": 2
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 60,
      "total_pages": 3
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 必须按 `role_scopes` 范围过滤，未传 scope 时返回所有授权范围学生。

### 7.2 获取学生申报记录

**接口地址：** `GET /teacher/students/{studentId}/activities`

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                      |
| :----------- | :------ | :--- | :------------------------ |
| `type`       | String  | 否   | archive/award/career_plan |
| `status`     | Integer | 否   | 状态筛选                  |
| `semesterId` | Integer | 否   | 学期ID                    |
| `page`       | Integer | 否   | 页码，默认1               |
| `per_page`   | Integer | 否   | 每页条数，默认10          |

**响应示例：** 参考学生端 `GET /activities`，数据范围受教师授权限制。





------

## 八、职业规划反馈模块FMY

### 8.1 提交职业规划反馈

**接口地址：** `POST /teacher/career-plans/{planId}/feedbacks`

**请求参数：**

| 参数名           | 类型   | 必填 | 说明                        |
| :--------------- | :----- | :--- | :-------------------------- |
| `feedbackContent`| String | 是   | 反馈内容                    |
| `suggestionItems`| Array  | 否   | 建议项列表，存入 `career_plan_feedbacks.suggestion_items` |

**响应示例：**

```json
{
  "code": 0,
  "message": "反馈已提交",
  "data": {
    "feedbackId": 50,
    "createdAt": "2026-07-04T10:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`career_plan_feedbacks`。反馈写入后发送消息通知学生。

------

## 九、AI 辅助评价模块LZW

### 9.1 为指定档案生成 AI 评价建议

**接口地址：** `POST /teacher/ai/generate`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                                                         |
| :-------------- | :------ | :--- | :----------------------------------------------------------- |
| `relatedType`   | String  | 是   | 关联类型：archive/award_application/career_plan/growth_timeline |
| `relatedId`     | Integer | 是   | 关联记录ID                                                   |
| `generationType`| String  | 是   | 生成类型：evaluation/suggestion/summary                      |
| `promptVersion` | String  | 否   | 指定提示词版本                                               |
| `idempotencyKey`| String  | 否   | 幂等键，重复提交返回已生成结果                               |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "generationLogId": 2001,
    "status": 1,
    "statusLabel": "成功",
    "outputContent": "该生在竞赛中表现出较强的建模能力，建议补充项目细节...",
    "modelName": "gpt-4o",
    "modelVersion": "2024-05-13",
    "promptVersion": "v1.2",
    "tokenUsage": 520,
    "generationTimeMs": 3200,
    "sourceArchiveIds": [500, 501]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`ai_generation_logs`。响应字段 `status` 对应表字段 `call_status`，枚举为：`0=失败 1=成功 2=重试`。
> `sourceArchiveIds` 由 `input_data` 解析得到，不在表中单独存储。超时或失败返回明确错误码 8/40001，前端可重新生成。

### 9.2 提交 AI 结果教师反馈

**接口地址：** `POST /teacher/ai/feedbacks`

**请求参数：**

| 参数名           | 类型    | 必填 | 说明                                   |
| :--------------- | :------ | :--- | :------------------------------------- |
| `generationLogId`| Integer | 是   | AI生成记录ID                           |
| `action`         | Integer | 是   | 1=采纳 2=修改后采纳 3=拒绝             |
| `modifiedContent`| String  | 否   | 修改后的最终内容，action=2 时必填      |
| `rejectReason`   | String  | 否   | 拒绝原因，action=3 时必填              |

**响应示例：**

```json
{
  "code": 0,
  "message": "反馈已保存",
  "data": {
    "feedbackId": 3001,
    "action": 2,
    "actionLabel": "修改后采纳",
    "isUsed": true
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`ai_teacher_feedbacks`。AI 结果不能直接作为最终审核结果，仍需走正式审批流程。

### 9.3 获取 AI 生成历史

**接口地址：** `GET /teacher/ai/history`

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                          |
| :----------- | :------ | :--- | :---------------------------- |
| `relatedType`| String  | 否   | 关联类型                      |
| `studentId`  | Integer | 否   | 学生ID                        |
| `page`       | Integer | 否   | 页码，默认1                   |
| `per_page`   | Integer | 否   | 每页条数，默认20              |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "generationLogId": 2001,
        "relatedType": "archive",
        "relatedId": 500,
        "generationType": "evaluation",
        "modelName": "gpt-4o",
        "promptVersion": "v1.2",
        "outputContent": "...",
        "teacherFeedback": {
          "action": 2,
          "modifiedContent": "..."
        },
        "createdAt": "2026-07-04T10:00:00+08:00"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 5,
      "total_pages": 1
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

------

## 十、短板识别与改进建议模块FMY

### 10.1 新增改进建议

**接口地址：** `POST /teacher/students/{studentId}/improvement-suggestions`

**请求参数：**

| 参数名          | 类型   | 必填 | 说明                      |
| :-------------- | :----- | :--- | :------------------------ |
| `weaknessId`    | Integer | 否  | 关联短板ID，对应 `improvement_suggestions.weakness_id` |
| `suggestionType`| String  | 是  | 建议类型，对应 `improvement_suggestions.suggestion_type` |
| `content`       | String  | 是  | 建议内容，对应 `improvement_suggestions.suggestion_content` |
| `relatedGoalId` | Integer | 否  | 关联目标ID，对应 `improvement_suggestions.related_goal_id` |

**响应示例：**

```json
{
  "code": 0,
  "message": "建议已添加",
  "data": {
    "suggestionId": 100,
    "createdAt": "2026-07-04T10:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`improvement_suggestions`。





## 十一、审批委托模块FMY

> 教师因出差、请假等原因无法处理审批任务时，可将自己的审批权限临时委托给其他教师。委托期间受托人代为处理委托范围内的待审批任务，到期后权限自动归还。
>
> 数据来源：`approval_delegations` 表。委托仅对 `approval_flow_steps.allow_delegate=1` 的审批节点生效。
> 权限码：`delegate:manage` — 管理自己的审批委托（创建/取消）。

### 15.1 获取我的审批委托列表

**接口地址：** `GET /teacher/delegations`

**说明：** 返回当前教师创建的委托记录（作为委托人）和接收的委托记录（作为受托人）。

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                                            |
| :----------- | :------ | :--- | :---------------------------------------------- |
| `direction`  | String  | 否   | 筛选方向：`delegator`（我委托的）/ `delegatee`（委托给我的），默认全部 |
| `status`     | Integer | 否   | 状态筛选：0=待生效 1=生效中 2=已过期 3=已取消   |
| `page`       | Integer | 否   | 页码，默认1                                     |
| `per_page`   | Integer | 否   | 每页条数，默认10                                |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "delegationId": 1,
        "delegator": {"userId": 20, "name": "王老师", "userNo": "T2023005"},
        "delegatee": {"userId": 21, "name": "李老师", "userNo": "T2023010"},
        "role": {"roleId": 3, "roleName": "辅导员"},
        "scopeType": 4,
        "scopeTypeLabel": "班级",
        "scopeId": 10,
        "scopeName": "计科2301班",
        "startTime": "2026-07-10T00:00:00+08:00",
        "endTime": "2026-07-20T23:59:59+08:00",
        "reason": "外出培训一周",
        "status": 1,
        "statusLabel": "生效中",
        "createdAt": "2026-07-09T10:00:00+08:00"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 10,
      "total": 3,
      "total_pages": 1
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

### 15.2 创建审批委托

**接口地址：** `POST /teacher/delegations`

**说明：** 将当前教师的部分或全部审批权限委托给其他教师。
> 校验规则：
> - 受托人必须有教师角色（`roles.is_auditor=1`）。
> - 委托时段不得与已有生效中的委托记录重叠。
> - 委托人只能委托自己拥有审批权限的角色和范围。
> - 委托的审批节点必须 `allow_delegate=1`，否则该节点不支持委托。
> - `startTime` 必须晚于当前时间，`endTime` 必须晚于 `startTime`，最长委托期为 180 天。

**请求参数：**

| 参数名        | 类型    | 必填 | 说明                                                         |
| :------------ | :------ | :--- | :----------------------------------------------------------- |
| `delegateeId` | Integer | 是   | 受托人用户ID                                                 |
| `roleId`      | Integer | 否   | 委托角色范围，不传则委托所有角色                             |
| `scopeType`   | Integer | 否   | 范围类型：2=学院 3=专业 4=班级，不传则委托全部范围           |
| `scopeId`     | Integer | 否   | 范围ID，不传则委托该类型下所有范围                           |
| `startTime`   | String  | 是   | 委托开始时间，ISO 8601 格式                                  |
| `endTime`     | String  | 是   | 委托结束时间，ISO 8601 格式                                  |
| `reason`      | String  | 否   | 委托原因（建议填写，便于审计追溯）                           |

**请求示例：**

```json
{
  "delegateeId": 21,
  "roleId": 3,
  "scopeType": 4,
  "scopeId": 10,
  "startTime": "2026-07-10T00:00:00+08:00",
  "endTime": "2026-07-20T23:59:59+08:00",
  "reason": "外出培训一周"
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "委托创建成功",
  "data": {
    "delegationId": 1,
    "delegateeName": "李老师",
    "status": 0,
    "statusLabel": "待生效",
    "startTime": "2026-07-10T00:00:00+08:00",
    "endTime": "2026-07-20T23:59:59+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

### 15.3 取消审批委托

**接口地址：** `PUT /teacher/delegations/{delegationId}/cancel`

**说明：** 取消一条生效中或待生效的委托记录。已过期或已取消的委托不可重复取消。
> 取消后，该委托对应的待审批任务按原审批人重新分配。受托人正在处理的审批任务不受影响（已接单的继续由受托人完成）。

**请求参数：**

| 参数名         | 类型   | 必填 | 说明           |
| :------------- | :----- | :--- | :------------- |
| `cancelReason` | String | 否   | 取消原因       |

**请求示例：**

```json
{
  "cancelReason": "提前返回，可自行处理审批"
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "委托已取消",
  "data": {
    "delegationId": 1,
    "status": 3,
    "statusLabel": "已取消",
    "cancelledAt": "2026-07-15T10:00:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

**错误码：**

| Code  | 说明                                 |
| :---- | :----------------------------------- |
| 40002 | 当前状态不可取消（已过期/已取消的委托） |
| 30001 | 委托记录不存在                       |
| 20005 | 无权操作（非本人创建的委托）         |

------

## 十二、已复用模块

### 学生管理模块

**获取学生档案详情**

**接口地址：** `GET /teacher/students/{studentId}/profile`

**响应示例：** 参考学生端 `GET /profile/info`，但教师视角增加 `isInScope` 标记。若学生不在教师授权范围内，返回 20005。

> **实现说明**：复用 Fmy `ProfileService.getProfileInfo(studentId)`（学生端 `GET /profile/info` 同一服务）组装档案主体（学籍/联系信息/画像分数/兴趣标签/学期成绩/个人奖项/短板分析等），教师侧仅增加 `isInScope` 授权校验（`role_scopes` 范围匹配），越权返回 `20005`。



 **获取学生成长时间轴**

**接口地址：** `GET /teacher/students/{studentId}/growth-timeline`

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                                                         |
| :----------- | :------ | :--- | :----------------------------------------------------------- |
| `semesterId` | Integer | 否   | 学期ID筛选                                                   |
| `eventType`  | Integer | 否   | 事件类型：1=奖项 2=成绩 3=实践 4=职业规划 5=短板改进 6=能力提升 |
| `status`     | Integer | 否   | 审核状态筛选                                                 |

**响应示例：** 参考学生端 `GET /profile/growth-timeline`。

> **实现说明**：复用 Fmy `ProfileService.getGrowthTimeline(studentId, semesterId, eventType, status, viewType)`（学生端 4.2 同一服务），以目标学生 userId 调用；教师侧先做 `role_scopes` 范围校验，越权返回 `20005`。



 **获取学生职业规划详情**

**接口地址：** `GET /teacher/students/{studentId}/career-plans/{planId}`

**响应示例：** 参考学生端 `GET /profile/career-plans/{id}`。

> **实现说明**：复用 Fmy `ProfileCareerPlanService.getPlanDetail(studentId, planId)`（学生端 4.6 同一服务），以目标学生 userId 调用；教师侧先做 `role_scopes` 范围校验，越权返回 `20005`。



### 消息中心模块

教师端消息接口与学生端结构一致，仅接口路径前缀为 `/teacher/*`，返回教师相关消息。详见《学生端接口文档》五、消息中心模块。

- 获取消息列表：`GET /teacher/messages`
- 标记消息已读：`PUT /teacher/messages/{messageId}/read`
- 归档消息：`PUT /teacher/messages/{messageId}/archive`

> 消息触发点：学生提交申报、审核结果反馈、计划反馈、导出任务完成等。
>
> **实现说明**：消息按 `user_messages.user_id` 归属，教师/学生同为 `users` 记录，无学生特有逻辑，因此本模块三个接口直接复用学生端 `MessageService`（`MessageController` 同名方法），仅 URL 前缀不同，请求/响应 DTO（`MessageListResponse` / `MessageArchiveResponse`）与参数校验完全一致。若教师端后续需要取消归档/删除/通知设置等操作，可同样复用学生端 5.5/5.6/5.7/5.9/5.10 对应服务。



### 个人中心模块

> **获取个人信息**：不再提供独立接口（原 14.1 `GET /teacher/profile` 已删除），统一复用 `GET /auth/me`（见一、认证模块，`AuthService#getCurrentUser`）获取当前登录教师信息——`users` / `user_contact_infos` / `user_roles` / `role_scopes`。

 **更新个人联系信息**

**接口地址：** `PUT /teacher/profile/contact`

**请求参数：**

| 参数名            | 类型   | 必填 | 说明                                                             |
| :---------------- | :----- | :--- | :--------------------------------------------------------------- |
| email             | String | 是   | 邮箱（写入 `user_contact_infos.email`，空串表示清空）            |
| phone             | String | 是   | 手机号（写入 `user_contact_infos.phone`，空串表示清空）          |
| address           | String | 是   | 通讯地址（写入 `user_contact_infos.address`，空串表示清空）      |
| emergencyName     | String | 是   | 紧急联系人姓名（写入 `user_contact_infos.emergency_name`，空串表示清空） |
| emergencyRelation | String | 是   | 与紧急联系人关系（写入 `user_contact_infos.emergency_relation`，空串表示清空） |
| emergencyPhone    | String | 是   | 紧急联系人电话（写入 `user_contact_infos.emergency_phone`，空串表示清空） |

> 全量更新语义（与学生端 4.1.1 一致）：六个字段均必填，缺失任一字段返回参数校验错误；传空字符串表示清空对应字段。

**响应示例：**

```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "email": "wang@demo.edu.cn",
    "phone": "13800000020",
    "avatar": "/uploads/avatar/xxx.jpg",
    "address": "北京市海淀区学院路xx号",
    "emergencyName": "王建军",
    "emergencyRelation": "父亲",
    "emergencyPhone": "13900139000"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`user_contact_infos` 表。读写策略与学生端一致：`users.email/phone/avatar` 仅由外部系统同步写入，应用层只读；本接口写入 `user_contact_infos` 表。
> **头像字段不在此接口维护**：本请求体不包含 `avatar`，头像更新复用通用上传头像接口 `POST /common/upload/avatar`（直接写入 `user_contact_infos.avatar`，与学生端一致）。
> **实现说明**：本接口复用学生端 `PUT /profile/contact`（4.1.1）的服务与请求/响应 DTO（`ProfileService.updateContact`），数据表同为 `user_contact_infos`，仅 URL 前缀不同。



### 数据导出模块

**获取可导出模板列表**

**接口地址：** `GET /teacher/exports/templates`

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "templateId": 1,
      "templateName": "班级档案汇总表",
      "exportType": "student_archive",
      "exportTypeLabel": "学生成长档案",
      "scopeType": 4,
      "scopeTypeLabel": "班级"
    }
  ],
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`export_templates`。仅返回当前登录用户所属学校的**启用**模板（`status=1`），按更新时间倒序；`templateId` 对应 `export_templates.id`，与 12.2 提交导出的请求字段对称。
>
> **实现说明**：复用 `export_templates` 查询逻辑（参考管理端 5.3 的学校过滤口径，但不要求 `export:template:manage`），由 `TeacherExportService` 返回精简字段；权限码 `export:execute`，越权返回 `20005`。

 **提交导出任务**

**接口地址：** `POST /teacher/exports`

**请求参数（复用管理端 5.11「一键导出学生档案」请求契约 `ArchiveExportRequest`）：**

| 参数名            | 类型    | 必填 | 说明                                                         |
| :---------------- | :------ | :--- | :----------------------------------------------------------- |
| `semesterId`      | Integer | 否   | 学期ID；为空时取当前学期                                     |
| `scopeType`       | Integer | 是   | 范围类型：1学校 2学院 3专业 4班级 6年级                      |
| `scopeId`         | Integer | 否   | 范围ID；scopeType=2/3/4 时必填，scopeType=1/6 时可为空       |
| `grade`           | String  | 否   | 年级（scopeType=6 时必填，如 `2023`）                        |
| `fileType`        | String  | 是   | 文件类型：`pdf` / `xlsx`                                     |
| `templateId`      | Integer | 否   | 导出模板ID（对应 12.1 的 `templateId`）；为空时使用学校默认学生档案模板 |
| `sections`        | Array   | 否   | 档案组成部分：`basic_info`/`academicInfo`/`dimensionScores`/`awards`/`practices`/`careerPlans`；为空时导出全部 |
| `archiveStatus`   | Integer | 否   | 档案状态筛选：1在读 2休学 3毕业 4退学；为空时导出全部        |
| `purpose`         | String  | 否   | 用途：`internal`（默认，PDF 加水印）/ `external`（无水印）   |
| `includeMetadata` | Boolean | 否   | 是否包含数据版本与字段说明信息，默认 true                    |

**请求示例：**

```json
{
  "semesterId": 3,
  "scopeType": 4,
  "scopeId": 10,
  "fileType": "pdf",
  "templateId": 1,
  "sections": ["academicInfo", "awards"],
  "purpose": "internal",
  "includeMetadata": true
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "导出任务已创建",
  "data": {
    "jobId": 80,
    "exportType": "student_archive",
    "status": 0,
    "statusLabel": "待处理",
    "estimatedSeconds": 60
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 业务逻辑：创建 `export_jobs` 后台任务（`filter_conditions` 快照自动记录本次请求的完整筛选条件），异步生成 PDF（逐学生渲染后合并）/ Excel，完成后写入 `file_uploads` 并通知用户下载；任务完成后经 12.3 任务列表获取下载链接。
>
> **实现说明**：复用管理端 `AdminExportService.submitArchiveExportByTeacher`（「一键导出学生档案」引擎，与管理端 5.11 `POST /admin/exports/archives` 同款契约与异步执行路径）；权限码 `export:execute`，且请求范围（`scopeType`/`scopeId`）必须在教师 `role_scopes` 授权范围内（学校级授权覆盖校内全部范围，学院/专业/班级按同类型匹配），越权返回 `20005`。`isAnonymized` 匿名化研究导出（`POST /admin/exports/research`）仍仅限管理端，教师端不开放。



 **获取导出任务列表**

**接口地址：** `GET /teacher/exports`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明                      |
| :--------- | :------ | :--- | :------------------------ |
| `page`     | Integer | 否   | 页码，默认1               |
| `per_page` | Integer | 否   | 每页条数，默认10，最大100 |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "exportJobId": 80,
        "templateName": "班级档案汇总表",
        "exportType": "student_archive",
        "status": 2,
        "statusLabel": "完成",
        "totalCount": 35,
        "successCount": 35,
        "downloadUrl": "/common/files/20001/download",
        "expireAt": "2026-07-11T14:30:00+08:00",
        "createdAt": "2026-07-04T10:00:00+08:00"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 10,
      "total": 5,
      "total_pages": 1
    }
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`export_jobs`、`export_templates`、`file_uploads`。`exportJobId` 对应 `export_jobs.id`；`templateName` 由 `template_id` → `export_templates.template_name` 解析（模板已删除时为 null）；`downloadUrl` 指向通用下载端点 `GET /common/files/{fileId}/download`（统一权限/有效期校验并补写下载审计），仅任务完成（status=2）且有生成文件时返回。
>
> **实现说明**：按 `export_jobs.operator_id` 过滤当前教师的任务，按任务 ID 倒序分页；软删除（`deleted_at`）任务自动过滤；权限码 `export:execute`，越权返回 `20005`。



 **删除导出任务**

**接口地址：** `DELETE /teacher/exports/{jobId}`

**路径参数：**

| 参数名  | 类型    | 必填 | 说明                           |
| :------ | :------ | :--- | :----------------------------- |
| `jobId` | Integer | 是   | 导出任务ID（`export_jobs.id`） |

**响应示例：**

```json
{
  "code": 0,
  "message": "删除成功",
  "data": {
    "jobId": 80,
    "deletedAt": "2026-07-04T10:30:00+08:00"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`export_jobs`、`export_operation_logs`。删除时写入 `export_operation_logs` 审计（`action=3`，表示删除）。
>
> **实现说明**：软删除 `export_jobs`（`deleted_at` 置位，任务从 12.3 列表自动消失，不物理删除 OSS 文件——下载链接本身 7 天过期）；仅本人创建的任务可删除，管理员可删除任意任务，非本人且非管理员返回 `20005 无访问权限`；任务不存在返回 `30001`；权限码 `export:execute`。



### 统计看板模块

**教师范围统计看板**

**接口地址：** `GET /teacher/statistics/dashboard`

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                                    |
| :----------- | :------ | :--- | :-------------------------------------- |
| `scopeType`  | Integer | 否   | 范围类型：1学校 2学院 3专业 4班级 6年级 |
| `scopeId`    | Integer | 否   | 范围ID                                  |
| `semesterId` | Integer | 否   | 学期ID，默认当前学期                    |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "semesterId": 3,
    "scopeName": "计科2301班",
    "studentCount": 35,
    "submittedCount": 30,
    "approvedCount": 25,
    "pendingCount": 3,
    "rejectedCount": 2,
    "averageGpa": 3.45,
    "dimensionAvgScores": [
      {"dimensionCode": "academic", "dimensionName": "学业成绩", "avgScore": 85.6},
      {"dimensionCode": "competition", "dimensionName": "竞赛实践", "avgScore": 72.3}
    ],
    "archiveTypeDistribution": [
      {"archiveType": "academic_competition", "count": 12},
      {"archiveType": "scholarship", "count": 8}
    ]
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`archives`、`award_applications`、`portrait_evaluation_scores`、`semester_gpa_summaries`。大数据量时走缓存，禁止返回写死数据。
>
> **实现说明**：复用 Fmy `AdminStatisticsService` 的聚合与缓存读取基础（`org_archive_summaries` 快照 + `statistics_cache`，与管理端 16.1/16.2 统计同源），教师 wrapper 按 `role_scopes` 限定 `scopeType`/`scopeId` 传入（学校级授权覆盖全校范围，学院/专业/班级按同类型匹配），越权返回 `20005`；审批状态计数（`submittedCount`/`approvedCount`/`pendingCount`/`rejectedCount`）为教师侧新增，按 `archives`/`award_applications`/`career_plans` 状态补充。
>
> **缓存说明：** 本接口优先读取 L1/L2 缓存（详见《学生档案系统表》`scheduled_tasks` 设计说明 - 统计缓存刷新策略）。响应头部增加 `X-Cache-Hit: L1` / `X-Cache-Hit: L2` / `X-Cache-Hit: MISS` 标记。缓存未命中时降级为单班级直接查询，全校/全学院范围聚合若缓存未命中则返回空数据集并在响应中标记 `cache_hit: false`，避免慢 SQL 打库。



**评分重新计算（教师权限范围内）**

**接口地址：** `POST /teacher/scores/recalculate`

**说明：** 教师触发其授权范围内指定学生、班级或学期的评分重算。任务进入 `score_recalculation_tasks` 异步执行，接口立即返回任务 ID。

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                                                         |
| :----------- | :------ | :--- | :----------------------------------------------------------- |
| `targetType` | Integer | 是   | 重算范围：1=指定学生 2=指定班级 3=指定学期，对应 `score_recalculation_tasks.task_type` |
| `targetId`   | Integer | 否   | 对应范围 ID；`targetType=1` 时为学生 ID，`targetType=2` 时为班级 ID，`targetType=3` 时可与 `semesterId` 一致或留空 |
| `semesterId` | Integer | 是   | 学期 ID                                                      |

**响应示例：**

```json
{
  "code": 0,
  "message": "评分重算任务已创建",
  "data": {
    "taskId": 50001,
    "targetType": 2,
    "targetId": 10,
    "semesterId": 3,
    "status": 0,
    "statusLabel": "排队中"
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 批量评分计算放入队列异步执行，完成后通过消息中心通知。
>
> **`targetType` 枚举映射（与 `score_recalculation_tasks.task_type` 保持一致）：**
>
> | 接口传入值 | 数据库存储值 | 说明     |
> | :--------- | :----------- | :------- |
> | `1`        | `1`          | 指定学生 |
> | `2`        | `2`          | 指定班级 |
> | `3`        | `3`          | 指定学期 |
>
> **范围枚举对齐说明：** 教师端仅支持触发其 `role_scopes` 授权范围内的学生、班级、学期评分重算；超出授权范围返回 `20005 无访问权限`。如需重新计算全校范围或指定专业，请使用管理端 `POST /admin/scores/recalculate`。
>
> **实现说明**：复用 Fmy `AdminScoreService.triggerRecalculate` 的异步引擎（`executeAsync`，任务进入 `score_recalculation_tasks` 独立事务执行，与管理端 2.1 同一路径），教师 wrapper 先做 `role_scopes` 范围校验并限制 `targetType` 仅 1/2/3，越权返回 `20005`。



 **查询评分重算任务进度**

**接口地址：** `GET /teacher/scores/recalculation-tasks/{taskId}`

**说明：** 查询教师触发的评分重算任务的执行状态、进度和结果摘要。

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 50001,
    "targetType": 2,
    "targetId": 10,
    "semesterId": 3,
    "status": 1,
    "progress": 85,
    "successCount": 42,
    "failCount": 0,
    "startedAt": "2026-07-25T10:35:02+08:00",
    "completedAt": null,
    "errorMessage": null
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`score_recalculation_tasks` 表。`status` 枚举：0=排队中 1=执行中 2=完成 3=失败。
>
> **实现说明**：复用 Fmy `AdminScoreService.getRecalculationTask(taskId)` 查询逻辑（与管理端 2.2 同一服务）；教师侧补充任务归属校验（仅本人触发的任务，`triggered_by`），非本人任务返回 `20005`。

 

**班级/专业级成果热力图**

**接口地址：** `GET /teacher/statistics/heatmap`

**说明：** 返回「成果热力图」渲染数据：以教师授权范围内的班级（或专业/学院）为行、以该年级 8 个学期节点（大一上 → 大四下）为列，聚合展示奖项/实践/成绩密度矩阵。数值统一归一化为 0-100，便于热力图着色；同时返回 `rawValues` 原始值供 Tooltip 展示。

**请求参数：**

| 参数         | 类型   | 必填 | 说明                                                         |
| :----------- | :----- | :--- | :----------------------------------------------------------- |
| `metric`     | string | 是   | 指标：`gpa`（成绩）/ `award`（奖项）/ `practice`（实践）/ `archive`（档案量） |
| `grade`      | string | 否   | 年级，如 `2023级`；用于推导 8 个学期节点，不传取教师授权范围内主要年级 |
| `scopeType`  | int    | 否   | 行维度：2=学院 3=专业 4=班级；不传默认按班级维度返回         |
| `scopeId`    | long   | 否   | 上级组织 ID，返回其下各组织行（下钻）；不传返回该维度教师授权范围内全部组织 |
| `semesterId` | long   | 否   | 学期 ID，传则仅返回单学期列（行 × 1 列）；不传返回该年级完整 8 个学期节点 |

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "metric": "award",
    "grade": "2023级",
    "semesters": [
      {"semesterId": 1, "semesterName": "2023-2024第一学期", "stageLabel": "大一上"},
      {"semesterId": 2, "semesterName": "2023-2024第二学期", "stageLabel": "大一下"},
      {"semesterId": 3, "semesterName": "2024-2025第一学期", "stageLabel": "大二上"},
      {"semesterId": 4, "semesterName": "2024-2025第二学期", "stageLabel": "大二下"},
      {"semesterId": 5, "semesterName": "2025-2026第一学期", "stageLabel": "大三上"},
      {"semesterId": 6, "semesterName": "2025-2026第二学期", "stageLabel": "大三下"},
      {"semesterId": 7, "semesterName": "2026-2027第一学期", "stageLabel": "大四上"},
      {"semesterId": 8, "semesterName": "2026-2027第二学期", "stageLabel": "大四下"}
    ],
    "rows": [
      {
        "orgId": 10,
        "orgName": "计科2301班",
        "values": [0, 20, 35, 52, 68, 80, 91, 100],
        "rawValues": [0, 2, 5, 8, 11, 14, 16, 18],
        "total": 74
      },
      {
        "orgId": 11,
        "orgName": "计科2302班",
        "values": [0, 12, 28, 45, 60, 72, 85, 93],
        "rawValues": [0, 1, 4, 7, 10, 12, 15, 17],
        "total": 66
      }
    ],
    "maxValue": 100,
    "minValue": 0
  },
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`archives`、`award_applications`、`semester_gpa_summaries`、`portrait_evaluation_scores` 实时聚合；大数据量时优先读 `org_archive_summaries`（`org_type=2/3/4` 快照）+ `statistics_cache`，缓存策略与 11.1 统计看板一致（`X-Cache-Hit` 标记）。
> 行维度按教师 `role_scopes` 授权范围过滤，越权范围返回 `20005`。归一化公式：`value = (raw - min) / (max - min) * 100`，分母为该年级该指标全校最大值；单学期为空返回 `0`。
> `stageLabel`（大一上/下 … 大四上/下）由学期在年级 8 节点中的序号推导（第 1/2 学期 → 大一上/下，……，第 7/8 学期 → 大四上/下），供前端列头直接展示。
>
> **实现说明**：复用 Fmy `AdminStatisticsService.heatmap`（管理端 16.3 同一引擎，`HeatmapResponse`/`HeatmapRow`/`HeatmapSemesterItem` DTO 直接对齐，`orgType`/`orgId`/`grade` 参数天然支持行维度与下钻），教师 wrapper 按 `role_scopes` 校验并限定 `orgType`/`orgId`，越权返回 `20005`；`metric` 取 gpa/award/practice/archive 子集。



### 短板识别与改进建议模块

 **获取学生短板分析列表**

**接口地址：** `GET /teacher/students/{studentId}/weaknesses`

**响应示例：**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "weaknessType": "科研创新",
      "weaknessDesc": "科研项目经历较少，缺乏论文发表",
      "severityLevel": 3,
      "source": 1,
      "sourceLabel": "系统分析",
      "isRead": 1,
      "createdAt": "2026-06-01T10:00:00+08:00"
    }
  ],
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

> 数据来源：`weakness_analyses`。
>
> **实现说明**：复用 Fmy `WeaknessAnalysisRepository.findByUserIdOrderByCreatedAtDesc(userId)` 查询 + `ProfileService.toWeaknessItem` 映射（学生端 4.1 个人档案中短板分析同源）；`source` 对应 `weakness_analyses.source`（1=系统分析），`sourceLabel` 由字典映射补充；教师侧先做 `role_scopes` 范围校验，越权返回 `20005`。

## 附录 A：教师端接口权限对照

| 接口路径 | 所需权限 | 说明 |
| :------- | :------- | :--- |
| `GET /teacher/dashboard` | `dashboard:view` | 教师首页 |
| `GET /teacher/audits/pending` | `audit:pending` | 待审核列表 |
| `POST /teacher/audits/{id}/approve` | `audit:approve` | 单个审核 |
| `POST /teacher/audits/batch/approve` | `audit:batch` | 批量审核 |
| `POST /teacher/audits/{id}/revoke` | `audit:revoke` | 撤销审核 |
| `GET /teacher/students` | `student:view` | 学生列表 |
| `GET /teacher/students/{id}/profile` | `student:view` | 学生详情 |
| `POST /teacher/ai/generate` | `ai:invoke` | AI 生成 |
| `POST /teacher/scores/recalculate` | `score:recalculate` | 评分重算 |
| `GET /teacher/exports/templates` | `export:execute` | 可导出模板列表（V5.11 新增实现） |
| `POST /teacher/exports` | `export:execute` | 数据导出（提交导出任务） |
| `GET /teacher/exports` | `export:execute` | 导出任务列表（V5.11 新增实现） |
| `DELETE /teacher/exports/{jobId}` | `export:execute` | 删除导出任务（V5.11 新增实现） |
| `GET /teacher/logs` | `log:view` | 系统操作日志查询（V5.7 新增） |
| `GET /teacher/statistics/heatmap` | `statistics:view` | 成果热力图（V5.7 新增） |

------

## 附录 B：关键业务校验规则

1. **数据范围校验**：所有教师端查询必须附加 `class_id/major_id/college_id IN (SELECT scope_id FROM role_scopes WHERE user_id = current_user_id AND status = 1)`。
2. **状态机校验**：审核通过/退回必须在待审批状态下进行，已通过不可重复通过，已撤销不可再审核。
3. **幂等控制**：审核接口必须对同一 `taskId` 或同一 `idempotency-key` 去重。
4. **退回必填原因**：`reject` 接口 `comment` 必填。
5. **文件下载权限**：教师只能下载其授权范围内学生的附件，越权返回 20005。
6. **AI 结果隔离**：AI 输出必须先经教师反馈（`ai_teacher_feedbacks`），不能直接写入档案正式字段。

------

## 附录 C：接口测试交付清单

> 本附录按 To Do List 要求补充教师端核心接口测试项，重点覆盖数据范围、审核状态机、评分重算权限边界。

### C.1 通用测试检查项

| 检查项 | 说明 | 通过标准 |
| :----- | :--- | :------- |
| 角色校验 | 非教师角色调用教师端接口 | 返回 `20005` 及 HTTP `403` |
| 数据范围 | 查询学生数据 | 仅返回 `role_scopes` 授权范围内的记录 |
| 跨范围访问 | 访问非授权班级/专业学生数据 | 返回 `20005` |
| 幂等控制 | 同一 `taskId` 重复审核 | 返回首次结果或 `40009` |

### C.2 审核状态机测试用例

| 用例编号 | 接口 | 前置状态 | 操作 | 期望结果 | 期望错误码 |
| :------- | :--- | :------- | :--- | :------- | :--------- |
| T-001 | `POST /teacher/audits/{id}/approve` | 待审批 | 通过 | 状态=2，消息通知 | — |
| T-002 | `POST /teacher/audits/{id}/reject` | 待审批 | 退回 | 状态=3，必填原因 | — |
| T-003 | `POST /teacher/audits/{id}/approve` | 已通过 | 重复通过 | — | `40002`/`40009` |
| T-004 | `POST /teacher/audits/{id}/revoke` | 已通过 | 普通教师调用 | — | `20005` |
| T-005 | `POST /teacher/audits/{id}/revoke` | 已通过 | 管理员调用 | 状态=4，撤销原因记录 | — |

### C.3 评分重算范围测试用例

| 用例编号 | 接口 | 参数 | 场景 | 期望结果 |
| :------- | :--- | :--- | :--- | :------- |
| R-001 | `POST /teacher/scores/recalculate` | `targetType=1`, 授权范围内学生 | 教师重算指定学生 | 任务创建成功 |
| R-002 | `POST /teacher/scores/recalculate` | `targetType=1`, 非授权学生 | 越权重算 | `20005` |
| R-003 | `POST /teacher/scores/recalculate` | `targetType=2`, 授权班级 | 教师重算班级 | 任务创建成功 |
| R-004 | `POST /teacher/scores/recalculate` | `targetType=3`, 当前学期 | 教师重算学期 | 任务创建成功 |
| R-005 | `POST /teacher/scores/recalculate` | `targetType=4/5` | 教师触发全量/专业 | `20005` 或 `10004` |

### C.4 数据导出测试用例

| 用例编号 | 接口 | 场景 | 期望结果 |
| :------- | :--- | :--- | :------- |
| E-001 | `POST /teacher/exports` | 导出授权班级数据 | 生成下载链接 |
| E-002 | `POST /teacher/exports` | 导出非授权班级数据 | `20005` |

### C.5 操作日志查询测试用例

| 用例编号 | 接口 | 参数 | 场景 | 期望结果 |
| :------- | :--- | :--- | :--- | :------- |
| L-001 | `GET /teacher/logs` | `startTime`/`endTime` | 按时间选择查看授权范围操作日志 | 返回该时间段内本人操作 + 授权学生相关操作 |
| L-002 | `GET /teacher/logs` | 授权班级学生操作 | 查看学生申报提交/审核日志 | 仅返回 `role_scopes` 范围内记录 |
| L-003 | `GET /teacher/logs` | 访问非授权班级学生日志 | 越权查看 | `20005` |

### C.6 成果热力图测试用例

| 用例编号 | 接口 | 参数 | 场景 | 期望结果 |
| :------- | :--- | :--- | :--- | :------- |
| H-001 | `GET /teacher/statistics/heatmap` | `metric=award`，授权班级 | 查询班级成果热力图 | 返回 8 个学期节点，`values` 归一化 0-100 |
| H-002 | `GET /teacher/statistics/heatmap` | 访问非授权班级/专业 | 越权查询 | `20005` |
| H-003 | `GET /teacher/statistics/heatmap` | `scopeType=4` 下钻 | 下钻班级明细 | 返回班级行、`rawValues` 与 `total` |

> **注：** 教师端 `audit:revoke` 权限仅授予管理员角色，测试时需分别构造普通教师账号与管理员账号验证权限边界。
