一、基础架构与权限体系
多角色权限管控
搭建学生、教师、辅导员、管理员四类角色体系，全接口增加权限校验，不依赖前端控制；学生仅可查看自身档案，教师仅能访问授权班级 / 专业数据，管理员拥有全量管理权限。
文件下载接口同步校验权限，无权限统一返回 403；管理员全部操作强制留存日志。
全链路操作日志
覆盖登录、申报提交 / 修改 / 删除、审核、评分计算、数据导出等全部关键行为；记录操作人、角色、时间、IP、操作类型、业务编号、修改前后核心字段。
日志普通用户不可删除，提供按用户、时间、操作类型检索接口。
二、核心 P0 级后端功能（最高优先级）
1. 多级指标与自动评分模块
数据表：搭建支持一 / 二 / 三级的指标配置表，存储指标名称、编码、父级、权重、计分规则、启用状态、版本号。
接口：指标树查询、管理员指标配置更新接口；自动评分计算接口。
数据规则：每次计分永久留存评分结果、计算时间、规则版本、数据来源；指标更新不覆盖历史分数，支持按学生 / 班级 / 学期批量重算评分。
2. 档案申报与附件管理
数据表：统一档案记录表、档案附件表；业务状态分为草稿、待审核、已退回、已通过、已撤销，后端拦截非法状态流转。
数据留存：记录每次提交、退回、修改、通过时间，保存表单历史修改版本；通过证书编号、名称、时间做重复申报校验提醒。
文件管控：校验文件格式、大小、数量；已通过档案学生无法直接删改，更正时生成新版本并保留原始记录。
3. 教师审核业务逻辑
接口：教师待审核列表（按教师管辖班级做数据隔离）、单条 / 批量通过、退回接口；支持管理员撤销错误审核并记录撤销原因。
数据留存：每条审核记录绑定审核教师、时间、意见、结果；拦截重复审核请求避免重复数据；审核完成自动推送消息通知学生。
4. 消息中心基础能力
自动触发消息场景：申报提交、审核通过、退回、成长计划教师反馈；消息绑定业务编号与前端跳转地址，记录已读、归档时间；重要系统消息禁止彻底删除。
三、P1 级后端完善功能（核心增值模块）
1. AI 辅助评价能力
完整记录 AI 调用日志：学生记录编号、模型名 / 版本、提示词版本、调用时间、输出内容、耗时；AI 输出绑定对应档案材料。
防重复机制：同一份请求重复提交不重复扣费、重复生成；AI 异常返回明确报错，支持重试。
结果管控：AI 内容仅作辅助，不能直接作为终审结果；记录教师对 AI 内容采纳 / 修改 / 拒绝状态及修改后最终文本。
2. 成长规划管理
数据表：成长目标、行动、里程碑、证明材料、阶段反思、教师反馈独立建表；记录计划创建、修改、完成、反馈时间。
数据隔离：已结束学期计划永久保留历史版本；提供成长计划完成进度统计接口。
3. 成长时间轴数据聚合
统一聚合已通过档案、成长计划、教师反馈等数据生成标准化时间轴事件；保证同一业务仅生成一条时间节点；提供按时间、事件类型、审核状态筛选查询接口。
4. 学生档案概览与 PDF 导出
数据分层：学籍只读信息与学生自主填写信息分表存储；提供学生档案汇总查询接口。
导出限制：PDF 仅读取已审核通过数据；生成限时有效下载链接，每次导出行为写入操作日志。
5. 统计看板接口
通用统计接口，支持学期、专业、班级、数据类型、状态多维度筛选；返回分项数量、总数、统计时间；正式环境禁用固定演示数据，大数据量统计增加缓存优化。
四、P2 级后端运维与扩展需求
1. 批量数据导出
支持按学期、专业、班级导出档案记录、指标得分、审核记录、AI 调用日志、成长计划多类数据；科研导出自动匿名化（编号替换姓名、学号）。
导出文件附带字段说明、数据版本；大批量导出采用后台异步任务，完成后消息通知；导出操作写入日志，普通师生无导出入口权限。
2. 测试、交付与运维配套
提供登录、申报、审核、评分、AI 调用、导出全套接口测试支撑；覆盖越权访问、重复提交、大文件、异常格式、AI 超时限流等异常场景。
交付输出完整数据库字段说明、接口文档；提供系统部署、数据备份恢复方案；上线记录版本号、更新时间、变更内容。
五、通用后端约束规范
区分演示数据与正式业务数据，正式环境禁止硬编码演示数据；
所有配置类参数（指标、权重、计分规则）不硬编码，全部由后端数据库提供；
已审核生效数据不允许直接删除 / 覆盖，变更均生成新版本留存历史；
所有长时间、大批量操作（统计、导出）做性能优化，增加缓存、异步任务机制；
文件、附件、下载链路全程绑定权限校验，防止越权查看他人材料