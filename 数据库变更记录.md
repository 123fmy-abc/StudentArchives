# 数据库修改记录 - users

> 修改人标识：`yueye12`（来自 git config user.name）

---

## 修改记录

### 记录 1：字段添加

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-06-11 |
| 修改人 | yueye12 |
| 变更类型 | 字段添加 |
| 变更字段 | nickname |
| 变更原因 | 新增用户昵称功能 |

**SQL 语句：**
```sql
ALTER TABLE `users` ADD COLUMN `nickname` varchar(50) NOT NULL DEFAULT '' COMMENT '用户昵称' AFTER `username`;
```

**回滚 SQL：**
```sql
ALTER TABLE `users` DROP COLUMN `nickname`;
```

---

### 记录 2：字段修改

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-06-10 |
| 修改人 | yueye12 |
| 变更类型 | 字段修改 |
| 变更字段 | status |
| 变更原因 | 状态字段长度不足以支持新的状态类型 |

**SQL 语句：**
```sql
ALTER TABLE `users` MODIFY COLUMN `status` tinyint(4) NOT NULL DEFAULT '0' COMMENT '用户状态：0-正常 1-禁用 2-冻结';
```

**回滚 SQL：**
```sql
ALTER TABLE `users` MODIFY COLUMN `status` tinyint(2) NOT NULL DEFAULT '0' COMMENT '用户状态：0-正常 1-禁用';
```

---

### 记录 3：字段删除

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-06-05 |
| 修改人 | yueye12 |
| 变更类型 | 字段删除 |
| 变更字段 | old_field |
| 变更原因 | 废弃字段，已无业务使用 |

**SQL 语句：**
```sql
ALTER TABLE `users` DROP COLUMN `old_field`;
```

**回滚 SQL：**
```sql
ALTER TABLE `users` ADD COLUMN `old_field` varchar(255) NOT NULL DEFAULT '' COMMENT '已废弃' AFTER `id`;
```

---

### 记录 4：数据库删除（全库）

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-05-20 |
| 修改人 | yueye12 |
| 变更类型 | 数据库删除 |
| 变更说明 | 删除测试数据库 test_db_v1 |

**SQL 语句：**
```sql
DROP DATABASE IF EXISTS `test_db_v1`;
```

**回滚方案：**
从备份文件 `/data/backup/test_db_v1_20260520.sql` 恢复

---

### 记录 5：新增 teacher_courses 表

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-07-10 |
| 修改人 | 123fmy-abc |
| 变更类型 | 表新增 |
| 变更对象 | teacher_courses |
| 变更原因 | 课任教师需要关联授课班级和课程 |

**SQL 语句：**
```sql
CREATE TABLE teacher_courses (
    id              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    school_id       BIGINT UNSIGNED NOT NULL COMMENT '关联 schools.id',
    teacher_id      BIGINT UNSIGNED NOT NULL COMMENT '教师用户ID，关联 users.id',
    class_id        BIGINT UNSIGNED NOT NULL COMMENT '关联 classes.id',
    course_code     VARCHAR(50)     NOT NULL COMMENT '课程编码',
    course_name     VARCHAR(255)    NOT NULL COMMENT '课程名称',
    semester_id     BIGINT UNSIGNED NOT NULL COMMENT '关联 semesters.id',
    is_primary      TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '0=兼任 1=主职',
    status          TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '0=禁用 1=启用',
    created_at      TIMESTAMP       NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at      TIMESTAMP       NULL DEFAULT NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uk_teacher_courses (teacher_id, class_id, course_code, semester_id),
    INDEX idx_teacher_courses_teacher (teacher_id),
    INDEX idx_teacher_courses_class (class_id),
    INDEX idx_teacher_courses_semester (semester_id),
    INDEX idx_teacher_courses_course (course_code),
    CONSTRAINT fk_tc_teacher FOREIGN KEY (teacher_id) REFERENCES users(id),
    CONSTRAINT fk_tc_class FOREIGN KEY (class_id) REFERENCES classes(id),
    CONSTRAINT fk_tc_semester FOREIGN KEY (semester_id) REFERENCES semesters(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='教师授课关系表';
```

**回滚 SQL：**
```sql
DROP TABLE IF EXISTS teacher_courses;
```

---

### 记录 6：role_scopes.scope_type 增加课程维度

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-07-10 |
| 修改人 | 123fmy-abc |
| 变更类型 | 字段修改 |
| 变更字段 | scope_type |
| 变更原因 | 支持按课程授权，scope_type 增加 5=课程 |

**SQL 语句：**
```sql
ALTER TABLE role_scopes
MODIFY COLUMN scope_type TINYINT UNSIGNED NOT NULL COMMENT '范围类型：1=学校 2=学院 3=专业 4=班级 5=课程';
```

**回滚 SQL：**
```sql
ALTER TABLE role_scopes
MODIFY COLUMN scope_type TINYINT UNSIGNED NOT NULL COMMENT '范围类型：1=学校 2=学院 3=专业 4=班级';
```

---

### 记录 7：新增审核员角色

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-07-10 |
| 修改人 | 123fmy-abc |
| 变更类型 | 数据新增 |
| 变更对象 | roles 表插入 auditor 角色 |
| 变更原因 | 脑图中存在独立的审核员角色，需在系统中内置 |

**SQL 语句：**
```sql
INSERT INTO roles (name, code, description, level, is_system, is_auditor, scope_types, max_scope_count, status, created_at, updated_at)
VALUES ('审核员', 'auditor', '独立审核员角色，可审核档案、奖项、职业规划等', 2, 1, 1, '[1,2,3,4,5]', 0, 1, NOW(), NOW());
```

**回滚 SQL：**
```sql
DELETE FROM roles WHERE code = 'auditor';
```

---

### 记录 8：新增教师端权限编码

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-07-10 |
| 修改人 | 123fmy-abc |
| 变更类型 | 数据新增 |
| 变更对象 | permissions 表插入教师端权限 |
| 变更原因 | 教师端需要成果热力图、改进建议等功能权限 |

**SQL 语句：**
```sql
-- 教师工作台主菜单
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('教师工作台', 'teacher:workspace', 1, NULL, 50, 1, NOW(), NOW());

-- 子菜单：我的班级、成果热力图、学生画像、改进建议、成绩分析、成长时间轴
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('我的班级', 'teacher:class', 1, @workspace_id, 1, 1, NOW(), NOW());
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('成果热力图', 'teacher:heatmap', 1, @workspace_id, 2, 1, NOW(), NOW());
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('学生画像', 'teacher:portrait', 1, @workspace_id, 3, 1, NOW(), NOW());
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('改进建议', 'teacher:suggestion', 1, @workspace_id, 4, 1, NOW(), NOW());
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('成绩分析', 'teacher:grade:analysis', 1, @workspace_id, 5, 1, NOW(), NOW());
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('成长时间轴', 'teacher:timeline', 1, @workspace_id, 6, 1, NOW(), NOW());

-- 按钮级：导出数据、发送提醒、添加评语
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('导出数据', 'teacher:export', 2, @workspace_id, 1, 1, NOW(), NOW());
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('发送提醒', 'teacher:notify', 2, @workspace_id, 2, 1, NOW(), NOW());
INSERT INTO permissions (name, code, type, parent_id, sort, status, created_at, updated_at)
VALUES ('添加评语', 'teacher:comment', 2, @workspace_id, 3, 1, NOW(), NOW());
```

**回滚 SQL：**
```sql
DELETE FROM permissions WHERE code LIKE 'teacher:%';
```

---

### 记录 9：archives 增加 course_code 字段

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-07-10 |
| 修改人 | 123fmy-abc |
| 变更类型 | 字段添加 |
| 变更字段 | course_code |
| 变更原因 | 档案按需关联课程 |

**SQL 语句：**
```sql
ALTER TABLE archives
ADD COLUMN course_code VARCHAR(50) NULL DEFAULT NULL COMMENT '关联课程编码' AFTER semester_id;
```

**回滚 SQL：**
```sql
ALTER TABLE archives DROP COLUMN course_code;
```

---

### 记录 10：新增 teacher_class_relations 视图

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-07-10 |
| 修改人 | 123fmy-abc |
| 变更类型 | 视图新增 |
| 变更对象 | teacher_class_relations |
| 变更原因 | 方便查询教师与班级、课程、专业、学院之间的关联关系 |

**SQL 语句：**
```sql
CREATE OR REPLACE VIEW teacher_class_relations AS
SELECT
    tc.id               AS relation_id,
    tc.teacher_id       AS teacher_id,
    u.name              AS teacher_name,
    u.user_no           AS teacher_no,
    tc.class_id         AS class_id,
    c.name              AS class_name,
    c.grade             AS class_grade,
    m.id                AS major_id,
    m.name              AS major_name,
    col.id              AS college_id,
    col.name            AS college_name,
    tc.course_code      AS course_code,
    tc.course_name      AS course_name,
    tc.semester_id      AS semester_id,
    s.name              AS semester_name,
    tc.is_primary       AS is_primary,
    tc.status           AS status,
    tc.school_id        AS school_id
FROM teacher_courses tc
JOIN users u ON u.id = tc.teacher_id AND u.deleted_at IS NULL
JOIN classes c ON c.id = tc.class_id AND c.deleted_at IS NULL
JOIN majors m ON m.id = c.major_id AND m.deleted_at IS NULL
JOIN colleges col ON col.id = m.college_id AND col.deleted_at IS NULL
JOIN semesters s ON s.id = tc.semester_id AND s.deleted_at IS NULL;
```

**回滚 SQL：**
```sql
DROP VIEW IF EXISTS teacher_class_relations;
```

---

## 汇总

| 序号 | 日期 | 类型 | 字段/对象 | 修改人 |
|------|------|------|-----------|--------|
| 1 | 2026-06-11 | 字段添加 | nickname | yueye12 |
| 2 | 2026-06-10 | 字段修改 | status | yueye12 |
| 3 | 2026-06-05 | 字段删除 | old_field | yueye12 |
| 4 | 2026-05-20 | 数据库删除 | test_db_v1 | yueye12 |
| 5 | 2026-07-10 | 表新增 | teacher_courses | 123fmy-abc |
| 6 | 2026-07-10 | 字段修改 | role_scopes.scope_type | 123fmy-abc |
| 7 | 2026-07-10 | 数据新增 | roles (auditor) | 123fmy-abc |
| 8 | 2026-07-10 | 数据新增 | permissions (teacher:*) | 123fmy-abc |
| 9 | 2026-07-10 | 字段添加 | archives.course_code | 123fmy-abc |
| 10 | 2026-07-10 | 视图新增 | teacher_class_relations | 123fmy-abc |
