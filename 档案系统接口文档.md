我来根据数据库表结构重新整理接口文档，补充AI对话、用户导入、成绩导入等功能。

---

# 学生档案管理系统 - 完整接口文档（V4.1）

> 基于数据库表结构 V5.0 整理  
> 更新日期：2026-07-08

---

## 基础信息

| 项目         | 说明                                           |
| ------------ | ---------------------------------------------- |
| 基础URL      | `http://111.228.56.207:8089/api`               |
| 请求格式     | JSON                                           |
| 认证方式     | Bearer Token (JWT)                             |
| 统一响应格式 | `{code: 200, message: "success", data: {...}}` |

---

## 全局规范

### 1. 响应体统一结构

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "trace_id": "req-20260704-143025-abc123",
  "timestamp": "2026-07-04T14:30:25+08:00"
}
```

### 2. 分页参数（通用）

| 参数         | 类型   | 必填 | 说明                            |
| ------------ | ------ | ---- | ------------------------------- |
| `page`       | int    | 否   | 页码，默认 `1`                  |
| `per_page`   | int    | 否   | 每页条数，默认 `20`，最大 `100` |
| `sort_by`    | string | 否   | 排序字段                        |
| `sort_order` | string | 否   | `asc` / `desc`，默认 `desc`     |

### 3. 分页响应（通用）

```json
{
  "code": 0,
  "data": {
    "list": [],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 156,
      "total_pages": 8
    }
  }
}
```

### 4. 全局错误码

#### 4.1通用响应码

| Code | 含义         | 说明               |
| ---- | ------------ | ------------------ |
| 0    | 成功         | 接口执行成功       |
| 1    | 系统异常     | 未知异常           |
| 2    | 数据不存在   | 查询结果为空       |
| 3    | 数据已存在   | 唯一性校验失败     |
| 4    | 操作失败     | 通用失败           |
| 5    | 禁止操作     | 当前状态不允许执行 |
| 6    | 请求过于频繁 | 限流               |
| 7    | 服务暂不可用 | 系统维护           |
| 8    | 网络异常     | 第三方服务异常     |
| 9    | 并发冲突     | 乐观锁冲突         |

------

#### 4.2参数相关（10000段）

| Code  | 含义         |
| ----- | ------------ |
| 10001 | 参数错误     |
| 10002 | 必填参数缺失 |
| 10003 | 参数格式错误 |
| 10004 | 参数超出范围 |
| 10005 | 非法参数     |
| 10006 | 文件格式错误 |
| 10007 | 文件过大     |
| 10008 | 上传失败     |

------

#### 4.3认证授权（20000段）

| Code  | 含义       |
| ----- | ---------- |
| 20001 | 未登录     |
| 20002 | Token失效  |
| 20003 | Token错误  |
| 20004 | 登录已过期 |
| 20005 | 无访问权限 |
| 20006 | 账号被禁用 |
| 20007 | 账号被冻结 |
| 20008 | 密码错误   |

示例：

```
{    "code": 20001,    "msg": "请先登录",    "data": null,    "success": false}
```

------

#### 4.4数据相关（30000段）

| Code  | 含义         |
| ----- | ------------ |
| 30001 | 数据不存在   |
| 30002 | 数据已删除   |
| 30003 | 数据重复     |
| 30004 | 数据状态异常 |
| 30005 | 数据已锁定   |
| 30006 | 数据关联存在 |
| 30007 | 数据校验失败 |
| 30008 | 数据版本冲突 |

------

#### 4.5 业务相关（40000段）

| Code  | 含义             |
| ----- | ---------------- |
| 40001 | 操作失败         |
| 40002 | 当前状态不可操作 |
| 40003 | 审批未通过       |
| 40004 | 库存不足         |
| 40005 | 金额超限         |
| 40006 | 超出配额         |
| 40007 | 已提交审核       |
| 40008 | 已完成不可修改   |
| 40009 | 重复提交         |
| 40010 | 超出业务规则限制 |

------

#### 4.6 第三方服务（50000段）

| Code  | 含义           |
| ----- | -------------- |
| 50001 | 微信接口异常   |
| 50002 | 支付宝接口异常 |
| 50003 | 短信发送失败   |
| 50004 | 邮件发送失败   |
| 50005 | OSS上传失败    |
| 50006 | Redis连接失败  |
| 50007 | MQ消息发送失败 |
| 50008 | 第三方接口超时 |

------

#### 4.7 数据库异常（60000段）

| Code  | 含义           |
| ----- | -------------- |
| 60001 | 数据库连接失败 |
| 60002 | SQL执行失败    |
| 60003 | 事务提交失败   |
| 60004 | 事务回滚       |
| 60005 | 唯一索引冲突   |
| 60006 | 外键约束失败   |
| 60007 | 死锁异常       |
| 60008 | 数据库超时     |

------

#### 4.8 系统异常（90000段）

| Code  | 含义           |
| ----- | -------------- |
| 90001 | 系统异常       |
| 90002 | 未知错误       |
| 90003 | 程序运行异常   |
| 90004 | 服务繁忙       |
| 90005 | 系统维护中     |
| 90006 | 配置错误       |
| 90007 | 文件读写失败   |
| 90008 | 服务器内部错误 |

## 二、认证模块

### 2.1 用户登录（获取JWT Token）

**接口地址：** `POST /auth/login`

**请求参数：**

| 参数名   | 类型   | 必填 | 说明                          |
| -------- | ------ | ---- | ----------------------------- |
| userNo   | String | 是   | 学号/工号/管理员工号          |
| password | String | 是   | 密码（明文传输，后端Bcrypt校验） |

**请求示例：**
```json
{
  "userNo": "2023001001",
  "password": "123456"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIs...",
    "tokenType": "Bearer",
    "expiresIn": 86400,
    "refreshToken": "eyJhbGciOiJIUzI1NiIs...",
    "user": {
      "userId": 5,
      "userNo": "2023001001",
      "name": "赵小明",
      "email": "zhaoxm@demo.edu.cn",
      "gender": 1,
      "genderLabel": "男",
      "schoolId": 1,
      "schoolName": "示例大学",
      "roles": ["student"],
      "roleNames": ["学生"],
      "avatar": null
    }
  }
}
```

**错误码：**

| Code  | 说明     |
| ----- | -------- |
| 20001 | 未登录   |
| 20008 | 密码错误 |
| 20006 | 账号被禁用 |

---

### 2.2 获取当前用户信息

**接口地址：** `GET /auth/me`

**请求头：** `Authorization: Bearer {accessToken}`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "userId": 5,
    "userNo": "2023001001",
    "name": "赵小明",
    "email": "zhaoxm@demo.edu.cn",
    "phone": "13800000005",
    "gender": 1,
    "genderLabel": "男",
    "schoolId": 1,
    "schoolName": "示例大学",
    "roles": ["student"],
    "roleNames": ["学生"],
    "permissions": ["archive:my", "archive:audit", "grade:view"],
    "avatar": null
  }
}
```

---

---

### 2.3 退出登录

**接口地址：** `POST /auth/logout`

**请求头：** `Authorization: Bearer {accessToken}`

**请求参数：**

| 参数名 | 类型   | 必填 | 说明                          |
| ------ | ------ | ---- | ----------------------------- |
| all    | Boolean | 否   | 是否退出所有设备，默认false   |

**请求示例：**
```json
{
  "all": true
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "退出成功"
}
```

---

### 2.4 修改密码

**接口地址：** `PUT /auth/password`

**请求头：** `Authorization: Bearer {accessToken}`

**请求参数：**

| 参数名      | 类型   | 必填 | 说明                         |
| ----------- | ------ | ---- | ---------------------------- |
| oldPassword | String | 是   | 原密码                       |
| newPassword | String | 是   | 新密码（后端Bcrypt哈希加密） |

**请求示例：**
```json
{
  "oldPassword": "123456",
  "newPassword": "NewPass@888"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "密码修改成功"
}
```

**错误码：**

| Code  | 说明       |
| ----- | ---------- |
| 20008 | 原密码错误 |

---

### 2.5 请求重置密码（发送邮件）

**接口地址：** `POST /auth/password/reset`

**请求参数：**

| 参数名 | 类型   | 必填 | 说明                         |
| ------ | ------ | ---- | ---------------------------- |
| email  | String | 是   | 注册邮箱，用于接收重置链接 |

**请求示例：**
```json
{
  "email": "zhaoxm@demo.edu.cn"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "重置密码邮件已发送，请检查您的邮箱"
}
```

**错误码：**

| Code  | 说明             |
| ----- | ---------------- |
| 30001 | 邮箱未注册       |
| 50004 | 邮件发送失败     |

---

### 2.6 确认重置密码

**接口地址：** `POST /auth/password/reset/confirm`

**请求参数：**

| 参数名      | 类型   | 必填 | 说明                     |
| ----------- | ------ | ---- | ------------------------ |
| token       | String | 是   | 邮件中的重置令牌         |
| email       | String | 是   | 注册邮箱                 |
| newPassword | String | 是   | 新密码（Bcrypt哈希加密） |

**请求示例：**
```json
{
  "token": "abc123def456",
  "email": "zhaoxm@demo.edu.cn",
  "newPassword": "NewPass@888"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "密码重置成功"
}
```

**错误码：**

| Code  | 说明               |
| ----- | ------------------ |
| 20003 | 重置令牌无效或已过期 |
| 30001 | 邮箱未注册         |

---

### 2.7 发送邮箱验证码

**接口地址：** `POST /auth/email/verification/send`

**请求头：** `Authorization: Bearer {accessToken}`

**请求参数：**

| 参数名 | 类型   | 必填 | 说明                         |
| ------ | ------ | ---- | ---------------------------- |
| email  | String | 否   | 目标邮箱，默认使用注册邮箱 |

**请求示例：**
```json
{
  "email": "newemail@demo.edu.cn"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "验证码已发送"
}
```

**错误码：**

| Code  | 说明           |
| ----- | -------------- |
| 50004 | 邮件发送失败   |

---

### 2.8 验证邮箱

**接口地址：** `POST /auth/email/verification/verify`

**请求头：** `Authorization: Bearer {accessToken}`

**请求参数：**

| 参数名 | 类型   | 必填 | 说明     |
| ------ | ------ | ---- | -------- |
| email  | String | 是   | 邮箱地址 |
| code   | String | 是   | 验证码   |

**请求示例：**
```json
{
  "email": "newemail@demo.edu.cn",
  "code": "888888"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "邮箱验证成功"
}
```

---

## 三、通用接口

### 3.1 文件上传接口

**接口地址：** `POST /common/upload`

**请求参数：**

| 参数名 | 类型   | 必填 | 说明                                                         |
| ------ | ------ | ---- | ------------------------------------------------------------ |
| file   | File   | 是   | 文件对象                                                     |
| type   | String | 是   | 文件类型：evidence(佐证材料)、certificate(证书)、plan(规划文件) |
| module | String | 是   | 所属模块：competition、scholarship、practice、research等     |

**响应示例：**
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "fileId": 10001,
    "fileName": "certificate.jpg",
    "fileUrl": "/uploads/2026/07/certificate.jpg",
    "fileSize": 204800,
    "fileType": "image/jpeg"
  }
}
```

---

### 3.2 学期下拉选项接口

**接口地址：** `GET /common/semesters`

**响应示例：**
```json
{
  "code": 200,
  "data": [
    {"value": 1, "label": "2022-2023第一学期", "name": "2022-2023-1"},
    {"value": 2, "label": "2022-2023第二学期", "name": "2022-2023-2"},
    {"value": 3, "label": "2022-2023第三学期", "name": "2022-2023-3"}
  ]
}
```

---

### 3.3 字典数据接口（下拉选项）

**接口地址：** `GET /common/dict`

**请求参数：**

| 参数名   | 类型   | 必填 | 说明     |
| -------- | ------ | ---- | -------- |
| dictType | String | 是   | 字典类型 |

**支持的字典类型：**
- `competition_level` - 竞赛级别
- `award_level` - 获奖等级
- `scholarship_level` - 奖学金级别
- `scholarship_award_level` - 奖学金获奖等级
- `company_type` - 公司类型
- `industry_type` - 行业类型
- `research_type` - 研究类型
- `project_level` - 项目级别
- `organization_level` - 组织级别
- `archive_category` - 档案分类
- `message_category` - 消息分类
- `apply_status` - 申报状态

**响应示例：**
```json
{
  "code": 200,
  "data": [
    {"value": "national", "label": "国家级", "sort": 1},
    {"value": "provincial", "label": "省部级", "sort": 2},
    {"value": "school", "label": "校级", "sort": 3},
    {"value": "college", "label": "院级", "sort": 4}
  ]
}
```

---

## 四、首页模块

### 4.1 首页数据概览

**接口地址：** `GET /home/dashboard`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "studentName": "张三",
    "major": "计算机科学与技术",
    "className": "计科2401班",
    "studentNo": "2024060001",
    "currentDate": "2026年7月7日星期一",
    "applicationTotal": 12,
    "approvedCount": 8,
    "pendingCount": 3,
    "gpa": 3.82,
    "dimensionScores": {
      "academic": {"score": 88, "trend": "+7", "targetGap": 4},
      "competition": {"score": 75, "trend": "+7", "targetGap": 15},
      "research": {"score": 60, "trend": "+5", "targetGap": 20},
      "socialWork": {"score": 85, "trend": "+3", "targetGap": 5},
      "comprehensive": {"score": 80, "trend": "+4", "targetGap": 8}
    },
    "radarChart": {
      "current": [88, 75, 60, 85, 80],
      "target": [92, 90, 80, 90, 88],
      "previous": [81, 68, 55, 82, 76]
    },
    "quickEntries": [
      {"name": "成长时间轴", "icon": "timeline", "path": "/growth-timeline", "recent": true},
      {"name": "奖学金", "icon": "scholarship", "path": "/applications?tab=scholarship", "recent": true},
      {"name": "社会实践", "icon": "practice", "path": "/applications?tab=practice", "recent": true},
      {"name": "学科竞赛", "icon": "competition", "path": "/applications?tab=competition", "recent": false}
    ],
    "recentActivities": [
      {
        "id": "act_001",
        "title": "学科竞赛申报已提交",
        "time": "2026-06-28 14:30",
        "type": "submitted"
      }
    ]
  }
}
```

---

## 五、个人中心模块

### 5.1 获取个人档案信息

**接口地址：** `GET /profile/info`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "basicInfo": {
      "name": "张三",
      "studentNo": "2024060001",
      "grade": "2024级",
      "major": "计算机科学与技术",
      "className": "计科2401班",
      "email": "zhangsan@edu.cn",
      "phone": "13800138000",
      "gender": 1,
      "genderLabel": "男",
      "avatar": "/uploads/avatar/xxx.jpg",
      "politicalStatus": "共青团员",
      "volunteerHours": "120.00",
      "languageAbility": "英语（CET-6）、日语（N3）",
      "hobbies": "篮球、跑步"
    },
    "dimensionProfile": {
      "academic": {"score": 88, "label": "学业成绩"},
      "competition": {"score": 65, "label": "竞赛实践"},
      "research": {"score": 60, "label": "科研创新"},
      "socialWork": {"score": 85, "label": "社会工作"},
      "comprehensive": {"score": 80, "label": "综合素质"}
    },
    "interests": [
      {
        "tagName": "编程开发",
        "detailContent": "Web 前端开发、人工智能应用",
        "proficiencyLevel": 3,
        "proficiencyLabel": "熟练",
        "weight": 15
      },
      {
        "tagName": "语言能力",
        "detailContent": "英语（CET-6）、日语（N3）",
        "proficiencyLevel": 2,
        "proficiencyLabel": "良好",
        "weight": 8
      }
    ],
    "semesterGrades": [
      {
        "semester": "2022-2023-1",
        "semesterName": "2022-2023第一学期",
        "courseCount": 8,
        "gpa": 3.2,
        "averageScore": 85.6,
        "rankInClass": 5,
        "rankInMajor": 12
      }
    ],
    "personalAwards": [
      {
        "awardName": "全国大学生数学建模竞赛",
        "awardLevel": "省级",
        "awardGrade": "二等奖",
        "awardTime": "2025-09"
      }
    ]
  }
}
```

---

### 5.2 获取成长时间轴

**接口地址：** `GET /profile/growth-timeline`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "summary": {
      "experiences": 5,
      "skills": 10,
      "averageGrowth": "71%",
      "potential": "∞"
    },
    "timeline": [
      {
        "id": 1,
        "semesterId": 4,
        "semesterName": "第4学期",
        "eventTime": "2025-07-05",
        "eventName": "暑期社会实践",
        "content": "参与乡村支教项目，学会了与不同背景的人沟通。",
        "eventType": 3,
        "eventTypeLabel": "实践",
        "abilityData": {
          "沟通能力": 80,
          "责任心": 85
        },
        "tags": ["实践", "公益"],
        "coverImage": "/uploads/cover/xxx.jpg",
        "sourceId": 100,
        "sourceType": "archive_social_practices"
      }
    ]
  }
}
```

---

### 5.3 获取职业规划列表

**接口地址：** `GET /profile/career-plan`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "plans": [
      {
        "id": 1,
        "semesterId": 3,
        "semesterName": "2023-2024-1",
        "title": "大二学年成长规划",
        "submittedAt": "2025-09-15",
        "status": 2,
        "statusLabel": "已通过",
        "fileUrl": "/uploads/plan_001.pdf",
        "currentVersion": 2,
        "submitCount": 2
      }
    ],
    "weaknessAnalysis": [
      {
        "category": "科研创新",
        "weakness": "科研项目经历较少，缺乏论文发表",
        "suggestion": "建议参与导师科研项目，尝试撰写学术论文",
        "progress": 45
      }
    ]
  }
}
```

---

### 5.4 新增/提交职业规划

**接口地址：** `POST /profile/career-plan`

**请求参数：**

| 参数名      | 类型    | 必填 | 说明       |
| ----------- | ------- | ---- | ---------- |
| semesterId  | Integer | 是   | 学期ID     |
| title       | String  | 是   | 规划标题   |
| content     | String  | 是   | 规划内容   |
| requirement | String  | 否   | 要求/目标  |
| fileId      | Integer | 否   | 规划文件ID |

**请求示例：**
```json
{
  "semesterId": 3,
  "title": "大二学年成长规划",
  "content": "本学年计划...",
  "requirement": "GPA达到3.5以上",
  "fileId": 10001
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "提交成功",
  "data": {
    "planId": 3,
    "status": 1,
    "statusLabel": "待审批"
  }
}
```

---

### 5.5 下载职业规划文件

**接口地址：** `GET /profile/career-plan/{planId}/download`

**响应：** 文件流

---

### 5.6 更新个人兴趣标签

**接口地址：** `PUT /profile/interests`

**请求参数：**

| 参数名                       | 类型    | 必填 | 说明                             |
| ---------------------------- | ------- | ---- | -------------------------------- |
| interests                    | Array   | 是   | 兴趣标签列表                     |
| interests[].tagName          | String  | 是   | 标签名称                         |
| interests[].proficiencyLevel | Integer | 是   | 熟练度：1入门 2一般 3熟练 4精通  |
| interests[].detailContent    | String  | 否   | 具体内容描述                     |
| interests[].isDetail         | Integer | 否   | 0=系统标签 1=用户手动添加，默认1 |

---

## 六、消息中心模块

### 6.1 获取消息列表

**接口地址：** `GET /messages`

**请求参数：**

| 参数名   | 类型    | 必填 | 说明                                                         |
| -------- | ------- | ---- | ------------------------------------------------------------ |
| category | String  | 否   | 分类：all、system_notice、audit_remind、dynamic_remind、private_message |
| isRead   | Integer | 否   | 状态：0=未读 1=已读，不传为全部                              |
| keyword  | String  | 否   | 搜索关键词                                                   |
| page     | Integer | 否   | 页码，默认1                                                  |
| pageSize | Integer | 否   | 每页条数，默认10                                             |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 8,
    "unread": 5,
    "list": [
      {
        "id": 1,
        "category": "audit_remind",
        "categoryLabel": "审批提醒",
        "title": "奖学金申报已通过",
        "content": "您的国家奖学金申请已通过审核。",
        "senderType": 1,
        "senderTypeLabel": "系统",
        "isRead": 0,
        "readAt": null,
        "sendChannel": "push",
        "relatedType": "award_applications",
        "relatedId": 100,
        "createdAt": "2026-07-04 09:30"
      }
    ]
  }
}
```

---

### 6.2 标记消息已读

**接口地址：** `PUT /messages/{messageId}/read`

**批量标记：** `PUT /messages/read-all`

**响应示例：**
```json
{
  "code": 200,
  "message": "标记成功"
}
```

---

### 6.3 删除消息

**接口地址：** `DELETE /messages/{messageId}`

**响应示例：**
```json
{
  "code": 200,
  "message": "删除成功"
}
```

---

### 6.4 获取消息通知设置

**接口地址：** `GET /messages/settings`

**响应示例：**
```json
{
  "code": 200,
  "data": [
    {
      "category": "audit_remind",
      "emailEnabled": 1,
      "smsEnabled": 0,
      "pushEnabled": 1
    },
    {
      "category": "system_notice",
      "emailEnabled": 1,
      "smsEnabled": 0,
      "pushEnabled": 1
    }
  ]
}
```

---

### 6.5 更新消息通知设置

**接口地址：** `PUT /messages/settings`

**请求参数：**

| 参数名       | 类型    | 必填 | 说明                  |
| ------------ | ------- | ---- | --------------------- |
| category     | String  | 是   | 通知分类              |
| emailEnabled | Integer | 是   | 0=关闭 1=开启邮件通知 |
| smsEnabled   | Integer | 是   | 0=关闭 1=开启短信通知 |
| pushEnabled  | Integer | 是   | 0=关闭 1=开启站内推送 |

---

## 七、动态记录模块

### 7.1 获取动态记录列表

**接口地址：** `GET /activities`

**请求参数：**

| 参数名   | 类型    | 必填 | 说明                                         |
| -------- | ------- | ---- | -------------------------------------------- |
| status   | Integer | 否   | 状态：0=草稿 1=待审批 2=通过 3=驳回 4=需修改 |
| keyword  | String  | 否   | 搜索关键词                                   |
| page     | Integer | 否   | 页码，默认1                                  |
| pageSize | Integer | 否   | 每页条数，默认10                             |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 15,
    "list": [
      {
        "id": 1,
        "content": "学科竞赛申报已提交",
        "status": 1,
        "statusLabel": "待审批",
        "archiveType": "academic_competition",
        "archiveTypeLabel": "学科竞赛",
        "title": "全国大学生数学建模竞赛",
        "submitTime": "2026-06-28 14:30",
        "canEdit": true,
        "canDelete": true
      }
    ]
  }
}
```

---

### 7.2 获取动态详情

**接口地址：** `GET /activities/{activityId}`

**响应：** 根据 `archive_type` 返回对应申报详情

---

### 7.3 编辑动态记录

**接口地址：** `PUT /activities/{activityId}`

**请求参数：** 同对应申报类型的提交参数，会创建新版本

---

### 7.4 删除动态记录

**接口地址：** `DELETE /activities/{activityId}`

**响应示例：**
```json
{
  "code": 200,
  "message": "删除成功"
}
```

---

## 八、个人档案信息申报模块

> 所有申报接口共用 `archives` 基表 + 对应扩展表  
> 状态流转：0=草稿 → 1=待审批 → 2=通过/3=驳回/4=需修改  
> 每次提交 `submit_count` +1，创建 `archive_versions` 版本记录

### 8.1 学科竞赛申报

#### 7.1.1 提交/保存学科竞赛申报

**接口地址：** `POST /applications/competition`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                     |
| --------------- | ------- | ---- | ------------------------ |
| semesterId      | Integer | 是   | 学期ID                   |
| competitionName | String  | 是   | 竞赛名称                 |
| competitionType | String  | 是   | 竞赛类型（字典编码）     |
| awardLevel      | String  | 是   | 获奖等级（字典编码）     |
| obtainTime      | String  | 是   | 获奖时间，格式 `YYYY-MM` |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0 |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表       |

**请求示例：**
```json
{
  "semesterId": 3,
  "competitionName": "全国大学生数学建模竞赛",
  "competitionType": "mathematical_modeling",
  "awardLevel": "second",
  "obtainTime": "2025-09",
  "isDraft": 0,
  "evidenceFileIds": [10001, 10002]
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "提交成功",
  "data": {
    "archiveId": 100,
    "status": 1,
    "statusLabel": "待审批",
    "currentVersion": 1,
    "submitCount": 1
  }
}
```

---

### 8.2 奖学金申报

#### 7.2.1 提交/保存奖学金申报

**接口地址：** `POST /applications/scholarship`

**请求参数：**

| 参数名              | 类型    | 必填 | 说明                     |
| ------------------- | ------- | ---- | ------------------------ |
| semesterId          | Integer | 是   | 学期ID                   |
| scholarshipName     | String  | 是   | 奖学金名称               |
| scholarshipCategory | String  | 是   | 奖学金类别（字典编码）   |
| awardLevel          | String  | 是   | 获奖等级（字典编码）     |
| obtainTime          | String  | 是   | 获得时间，格式 `YYYY-MM` |
| isDraft             | Integer | 否   | 0=提交 1=保存草稿，默认0 |
| evidenceFileIds     | Array   | 否   | 佐证材料文件ID列表       |

---

### 8.3 创新创业申报

#### 7.3.1 提交/保存创新创业申报

**接口地址：** `POST /applications/innovation`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                        |
| --------------- | ------- | ---- | --------------------------- |
| semesterId      | Integer | 是   | 学期ID                      |
| companyName     | String  | 是   | 公司名称                    |
| industryType    | String  | 是   | 行业类型（字典编码）        |
| projectType     | String  | 是   | 公司类型（字典编码）        |
| teamRole        | String  | 是   | 团队角色                    |
| registerTime    | String  | 是   | 注册时间，格式 `YYYY-MM-DD` |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0    |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表          |

---

### 8.4 学术研究申报

#### 7.4.1 提交/保存学术研究申报

**接口地址：** `POST /applications/research`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                     |
| --------------- | ------- | ---- | ------------------------ |
| semesterId      | Integer | 是   | 学期ID                   |
| projectName     | String  | 是   | 项目名称                 |
| projectLevel    | String  | 是   | 项目级别（字典编码）     |
| projectType     | String  | 是   | 项目类型/研究类型        |
| teamRole        | String  | 是   | 团队角色                 |
| projectTime     | String  | 是   | 项目时间，格式 `YYYY-MM` |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0 |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表       |

---

### 8.5 荣誉证书申报

#### 7.5.1 提交/保存荣誉证书申报

**接口地址：** `POST /applications/certificate`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                     |
| --------------- | ------- | ---- | ------------------------ |
| semesterId      | Integer | 是   | 学期ID                   |
| certificateType | String  | 是   | 证书类型                 |
| certificateName | String  | 是   | 证书名称                 |
| obtainTime      | String  | 是   | 获得时间，格式 `YYYY-MM` |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0 |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表       |

---

### 8.6 实习经历申报

#### 7.6.1 提交/保存实习经历申报

**接口地址：** `POST /applications/internship`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                        |
| --------------- | ------- | ---- | --------------------------- |
| semesterId      | Integer | 是   | 学期ID                      |
| companyName     | String  | 是   | 实习单位                    |
| location        | String  | 是   | 实习地点                    |
| position        | String  | 是   | 实习岗位                    |
| startDate       | String  | 是   | 开始时间，格式 `YYYY-MM-DD` |
| endDate         | String  | 是   | 结束时间，格式 `YYYY-MM-DD` |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0    |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表          |

---

### 8.7 组织履历申报

#### 7.7.1 提交/保存组织履历申报

**接口地址：** `POST /applications/organization`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                        |
| --------------- | ------- | ---- | --------------------------- |
| semesterId      | Integer | 是   | 学期ID                      |
| orgLevel        | String  | 是   | 组织级别                    |
| department      | String  | 是   | 部门                        |
| positionTitle   | String  | 是   | 职位                        |
| startDate       | String  | 是   | 任职开始，格式 `YYYY-MM-DD` |
| endDate         | String  | 是   | 任职结束，格式 `YYYY-MM-DD` |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0    |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表          |

---

### 8.8 实训项目申报

#### 7.8.1 提交/保存实训项目申报

**接口地址：** `POST /applications/training`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                        |
| --------------- | ------- | ---- | --------------------------- |
| semesterId      | Integer | 是   | 学期ID                      |
| projectName     | String  | 是   | 项目名称                    |
| projectContent  | String  | 是   | 项目内容                    |
| startDate       | String  | 是   | 开始时间，格式 `YYYY-MM-DD` |
| endDate         | String  | 是   | 结束时间，格式 `YYYY-MM-DD` |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0    |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表          |

---

### 8.9 社会实践申报

#### 7.9.1 提交/保存社会实践申报

**接口地址：** `POST /applications/practice`

**请求参数：**

| 参数名           | 类型    | 必填 | 说明                        |
| ---------------- | ------- | ---- | --------------------------- |
| semesterId       | Integer | 是   | 学期ID                      |
| activityName     | String  | 是   | 活动名称                    |
| practiceLocation | String  | 是   | 活动地点                    |
| practiceUnit     | String  | 是   | 组织单位                    |
| startDate        | String  | 是   | 开始时间，格式 `YYYY-MM-DD` |
| endDate          | String  | 是   | 结束时间，格式 `YYYY-MM-DD` |
| volunteerHours   | Decimal | 否   | 志愿时长，默认0             |
| isDraft          | Integer | 否   | 0=提交 1=保存草稿，默认0    |
| evidenceFileIds  | Array   | 否   | 佐证材料文件ID列表          |

---

### 8.10 图书心得申报

#### 7.10.1 提交/保存图书心得申报

**接口地址：** `POST /applications/book-review`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                     |
| --------------- | ------- | ---- | ------------------------ |
| semesterId      | Integer | 是   | 学期ID                   |
| bookName        | String  | 是   | 书名                     |
| readMonth       | String  | 是   | 阅读时间，格式 `YYYY-MM` |
| reviewContent   | String  | 是   | 心得体会                 |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0 |
| evidenceFileIds | Array   | 否   | 佐证材料文件ID列表       |

---

### 8.11 获取申报版本历史

**接口地址：** `GET /applications/{archiveId}/versions`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "currentVersion": 2,
    "versions": [
      {
        "version": 1,
        "title": "全国大学生数学建模竞赛",
        "status": 3,
        "statusLabel": "已驳回",
        "rejectedReason": "缺少获奖证书",
        "createdAt": "2026-06-20 10:00"
      },
      {
        "version": 2,
        "title": "全国大学生数学建模竞赛",
        "status": 1,
        "statusLabel": "待审批",
        "createdAt": "2026-06-28 14:30"
      }
    ]
  }
}
```

---

## 九、奖项报名模块

> 所有奖项报名共用 `award_applications` 基表 + 对应扩展表  
> 状态流转同档案申报：0=草稿 → 1=待审批 → 2=通过/3=驳回/4=需修改

### 9.1 奖项总览统计

**接口地址：** `GET /awards/overview`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "totalSubmissions": 30,
    "pendingReview": 8,
    "approved": 8,
    "newThisSemester": 8,
    "typeDistribution": {
      "competition_star": 8,
      "innovation_star": 5,
      "research_project": 4,
      "research_soft": 6,
      "research_paper": 7
    },
    "statusDistribution": {
      "draft": 7,
      "pending": 8,
      "approved": 8,
      "rejected": 7
    },
    "semesterTrend": [
      {"semesterId": 1, "semesterName": "2022-2023-1", "count": 5},
      {"semesterId": 2, "semesterName": "2022-2023-2", "count": 8}
    ],
    "recentActivities": [
      {
        "id": 1,
        "title": "全国大学生英语竞赛",
        "applicant": "李四",
        "type": "competition_star",
        "typeLabel": "竞赛之星",
        "submitTime": "2026-06-28",
        "status": 1
      }
    ]
  }
}
```

---

### 9.2 竞赛之星报名

#### 8.2.1 提交/保存竞赛之星报名

**接口地址：** `POST /awards/competition-star`

**请求参数：**

| 参数名           | 类型    | 必填 | 说明                        |
| ---------------- | ------- | ---- | --------------------------- |
| semesterId       | Integer | 是   | 学期ID                      |
| competitionName  | String  | 是   | 竞赛名称                    |
| participateTime  | String  | 是   | 参赛时间，格式 `YYYY-MM-DD` |
| competitionLevel | String  | 是   | 竞赛级别（字典编码）        |
| awardLevel       | String  | 是   | 获奖级别（字典编码）        |
| isDraft          | Integer | 否   | 0=提交 1=保存草稿，默认0    |
| evidenceFileIds  | Array   | 否   | 佐证材料文件ID列表          |

---

### 9.3 科研之星报名

> 科研之星包含三个子类型，先创建主记录，再添加子项目

#### 8.3.1 提交/保存科研之星主记录

**接口地址：** `POST /awards/research-star`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明                     |
| ---------- | ------- | ---- | ------------------------ |
| semesterId | Integer | 是   | 学期ID                   |
| isDraft    | Integer | 否   | 0=提交 1=保存草稿，默认0 |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "applicationId": 200,
    "researchStarId": 50
  }
}
```

---

#### 8.3.2 添加科研项目

**接口地址：** `POST /awards/research-star/{researchStarId}/projects`

**请求参数：**

| 参数名          | 类型   | 必填 | 说明                        |
| --------------- | ------ | ---- | --------------------------- |
| projectName     | String | 是   | 项目名称                    |
| projectLevel    | String | 是   | 项目级别（字典编码）        |
| rankTotal       | String | 是   | 排名/总人数，如 `2/5`       |
| establishTime   | String | 是   | 立项时间，格式 `YYYY-MM-DD` |
| evidenceFileIds | Array  | 否   | 证明材料文件ID列表          |

---

#### 8.3.3 添加软件著作权

**接口地址：** `POST /awards/research-star/{researchStarId}/software`

**请求参数：**

| 参数名          | 类型   | 必填 | 说明                        |
| --------------- | ------ | ---- | --------------------------- |
| softwareName    | String | 是   | 软著名称                    |
| issuingUnit     | String | 是   | 颁发单位                    |
| rankTotal       | String | 是   | 排名/总人数，如 `1/4`       |
| approvalTime    | String | 是   | 获批时间，格式 `YYYY-MM-DD` |
| evidenceFileIds | Array  | 否   | 证明材料文件ID列表          |

---

#### 8.3.4 添加发表论文

**接口地址：** `POST /awards/research-star/{researchStarId}/papers`

**请求参数：**

| 参数名          | 类型   | 必填 | 说明                        |
| --------------- | ------ | ---- | --------------------------- |
| journalName     | String | 是   | 期刊名称                    |
| paperTitle      | String | 是   | 论文名称                    |
| rankTotal       | String | 是   | 排名/总人数，如 `2/3`       |
| publishTime     | String | 是   | 发表时间，格式 `YYYY-MM-DD` |
| evidenceFileIds | Array  | 否   | 证明材料文件ID列表          |

---

### 9.4 双创之星报名

#### 8.4.1 提交/保存双创之星报名

**接口地址：** `POST /awards/innovation-star`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                        |
| --------------- | ------- | ---- | --------------------------- |
| semesterId      | Integer | 是   | 学期ID                      |
| companyName     | String  | 是   | 公司名称                    |
| industryType    | String  | 是   | 行业类型（字典编码）        |
| applicantRank   | String  | 是   | 申报人排名，如 `1/3`        |
| registerTime    | String  | 是   | 注册时间，格式 `YYYY-MM-DD` |
| isDraft         | Integer | 否   | 0=提交 1=保存草稿，默认0    |
| evidenceFileIds | Array   | 否   | 证明材料文件ID列表          |

---



---

## 十、审批流程配置模块（高校自定义）

---

### 1、审批流程模板管理

#### 10.1 获取审批流程列表

**接口地址：** `GET /admin/approval-flows`

**请求参数：**

| 参数名            | 类型    | 必填 | 说明                                          |
| ----------------- | ------- | ---- | --------------------------------------------- |
| schoolId          | Integer | 否   | 学校ID（超管可跨校查询，校管只能查本校）      |
| applicableType    | String  | 否   | 适用类型：Archive/AwardApplication/CareerPlan |
| applicableSubType | String  | 否   | 适用子类型（如 competition_star）             |
| status            | Integer | 否   | 0=禁用 1=启用                                 |
| page              | Integer | 否   | 页码，默认1                                   |
| pageSize          | Integer | 否   | 每页条数，默认10                              |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 5,
    "list": [
      {
        "id": 1,
        "schoolId": 1,
        "schoolName": "某某大学",
        "flowName": "学科竞赛审批流程",
        "applicableType": "Archive",
        "applicableSubType": "academic_competition",
        "applicableTypeLabel": "档案-学科竞赛",
        "version": 1,
        "isDefault": 1,
        "status": 1,
        "statusLabel": "启用",
        "totalSteps": 3,
        "createdByName": "系统管理员",
        "createdAt": "2026-01-15 10:00:00",
        "updatedAt": "2026-01-15 10:00:00"
      },
      {
        "id": 2,
        "schoolId": 1,
        "flowName": "奖学金审批流程",
        "applicableType": "Archive",
        "applicableSubType": "scholarship",
        "applicableTypeLabel": "档案-奖学金",
        "version": 2,
        "isDefault": 1,
        "status": 1,
        "statusLabel": "启用",
        "totalSteps": 4,
        "copiedFrom": 1,
        "createdByName": "管理员A",
        "createdAt": "2026-03-20 14:30:00",
        "updatedAt": "2026-03-20 14:30:00"
      }
    ]
  }
}
```

---

#### 10.2 获取单个审批流程详情

**接口地址：** `GET /admin/approval-flows/{flowId}`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "schoolId": 1,
    "flowName": "学科竞赛审批流程",
    "applicableType": "Archive",
    "applicableSubType": "academic_competition",
    "version": 1,
    "isDefault": 1,
    "status": 1,
    "createdBy": 1,
    "createdByName": "系统管理员",
    "createdAt": "2026-01-15 10:00:00",
    "steps": [
      {
        "id": 1,
        "stepNo": 1,
        "stepName": "辅导员初审",
        "roleId": 3,
        "roleName": "辅导员",
        "roleCode": "counselor",
        "scopeType": 4,
        "scopeTypeLabel": "班级",
        "scopeRule": "student_class",
        "autoAssign": 1,
        "allowDelegate": 1,
        "allowSkip": 0,
        "allowDesignateNext": 0,
        "timeoutHours": 48,
        "rejectAction": "end",
        "rejectToStep": null,
        "sort": 0
      },
      {
        "id": 2,
        "stepNo": 2,
        "stepName": "系主任复审",
        "roleId": 4,
        "roleName": "系主任",
        "roleCode": "department_head",
        "scopeType": 3,
        "scopeTypeLabel": "专业",
        "scopeRule": "student_major",
        "autoAssign": 1,
        "allowDelegate": 1,
        "allowSkip": 0,
        "allowDesignateNext": 1,
        "timeoutHours": 72,
        "rejectAction": "return",
        "rejectToStep": 1,
        "sort": 0
      },
      {
        "id": 3,
        "stepNo": 3,
        "stepName": "院长终审",
        "roleId": 5,
        "roleName": "院长",
        "roleCode": "college_dean",
        "scopeType": 2,
        "scopeTypeLabel": "学院",
        "scopeRule": "student_college",
        "autoAssign": 1,
        "allowDelegate": 0,
        "allowSkip": 0,
        "allowDesignateNext": 0,
        "timeoutHours": 48,
        "rejectAction": "return",
        "rejectToStep": 2,
        "sort": 0
      }
    ]
  }
}
```

---

#### 10.3 创建审批流程

**接口地址：** `POST /admin/approval-flows`

**请求参数：**

| 参数名            | 类型    | 必填 | 说明                                          |
| ----------------- | ------- | ---- | --------------------------------------------- |
| schoolId          | Integer | 是   | 学校ID                                        |
| flowName          | String  | 是   | 流程名称                                      |
| applicableType    | String  | 是   | 适用类型：Archive/AwardApplication/CareerPlan |
| applicableSubType | String  | 否   | 适用子类型（如 academic_competition）         |
| isDefault         | Integer | 否   | 0=非默认 1=默认流程，默认0                    |
| steps             | Array   | 是   | 审批步骤列表                                  |

**steps 子对象：**

| 参数名             | 类型    | 必填 | 说明                                                         |
| ------------------ | ------- | ---- | ------------------------------------------------------------ |
| stepNo             | Integer | 是   | 第几步（从1开始）                                            |
| stepName           | String  | 是   | 步骤名称                                                     |
| roleId             | Integer | 是   | 该节点要求的角色ID                                           |
| scopeType          | Integer | 是   | 范围类型：1=学校 2=学院 3=专业 4=班级                        |
| scopeRule          | String  | 是   | 范围规则：student_class/student_major/student_college/school |
| autoAssign         | Integer | 否   | 1=自动分配 0=手动，默认1                                     |
| allowDelegate      | Integer | 否   | 1=允许委托 0=不允许，默认0                                   |
| allowSkip          | Integer | 否   | 1=允许跳过 0=不允许，默认0                                   |
| allowDesignateNext | Integer | 否   | 1=允许指定下一审批人 0=不允许，默认0                         |
| timeoutHours       | Integer | 否   | 超时小时数，默认48                                           |
| rejectAction       | String  | 否   | 驳回动作：end(结束)/return(退回)，默认end                    |
| rejectToStep       | Integer | 否   | 驳回后退回步骤（rejectAction=return时必填）                  |
| sort               | Integer | 否   | 排序，默认0                                                  |

**请求示例：**
```json
{
  "schoolId": 1,
  "flowName": "学科竞赛审批流程",
  "applicableType": "Archive",
  "applicableSubType": "academic_competition",
  "isDefault": 1,
  "steps": [
    {
      "stepNo": 1,
      "stepName": "辅导员初审",
      "roleId": 3,
      "scopeType": 4,
      "scopeRule": "student_class",
      "autoAssign": 1,
      "allowDelegate": 1,
      "timeoutHours": 48,
      "rejectAction": "end"
    },
    {
      "stepNo": 2,
      "stepName": "系主任复审",
      "roleId": 4,
      "scopeType": 3,
      "scopeRule": "student_major",
      "autoAssign": 1,
      "allowDelegate": 1,
      "allowDesignateNext": 1,
      "timeoutHours": 72,
      "rejectAction": "return",
      "rejectToStep": 1
    },
    {
      "stepNo": 3,
      "stepName": "院长终审",
      "roleId": 5,
      "scopeType": 2,
      "scopeRule": "student_college",
      "autoAssign": 1,
      "timeoutHours": 48,
      "rejectAction": "return",
      "rejectToStep": 2
    }
  ]
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "flowId": 10,
    "version": 1,
    "status": 1
  }
}
```

---

#### 10.4 修改审批流程（创建新版本）

**接口地址：** `POST /admin/approval-flows/{flowId}/new-version`

> 修改流程时创建新版本，不影响进行中的审批实例

**请求参数：** 同创建审批流程

**响应示例：**
```json
{
  "code": 200,
  "message": "新版本创建成功",
  "data": {
    "flowId": 11,
    "version": 2,
    "copiedFrom": 10
  }
}
```

---

#### 10.5 复制审批流程

**接口地址：** `POST /admin/approval-flows/{flowId}/copy`

**请求参数：**

| 参数名   | 类型   | 必填 | 说明                       |
| -------- | ------ | ---- | -------------------------- |
| flowName | String | 否   | 新流程名称，默认"xxx-复制" |

**响应示例：**
```json
{
  "code": 200,
  "message": "复制成功",
  "data": {
    "flowId": 12,
    "version": 1,
    "copiedFrom": 10
  }
}
```

---

#### 10.6 更新审批流程状态

**接口地址：** `PUT /admin/approval-flows/{flowId}/status`

**请求参数：**

| 参数名 | 类型    | 必填 | 说明          |
| ------ | ------- | ---- | ------------- |
| status | Integer | 是   | 0=禁用 1=启用 |

---

#### 10.7 设置默认流程

**接口地址：** `PUT /admin/approval-flows/{flowId}/set-default`

> 设置后，同学校同类型的其他流程 is_default 自动变为 0

---

#### 10.8 删除审批流程

**接口地址：** `DELETE /admin/approval-flows/{flowId}`

> 软删除，有进行中的实例时不可删除

---

### 2、审批流程步骤管理

#### 10.1 获取流程步骤列表

**接口地址：** `GET /admin/approval-flows/{flowId}/steps`

---

#### 10.2 添加流程步骤

**接口地址：** `POST /admin/approval-flows/{flowId}/steps`

**请求参数：** 同创建流程时的 steps 子对象

---

#### 10.3 更新流程步骤

**接口地址：** `PUT /admin/approval-flows/{flowId}/steps/{stepId}`

---

#### 10.4 删除流程步骤

**接口地址：** `DELETE /admin/approval-flows/{flowId}/steps/{stepId}`

> 删除后需重新调整后续步骤的 stepNo

---

#### 10.5 调整步骤顺序

**接口地址：** `PUT /admin/approval-flows/{flowId}/steps/reorder`

**请求参数：**

| 参数名  | 类型  | 必填 | 说明                     |
| ------- | ----- | ---- | ------------------------ |
| stepIds | Array | 是   | 按新顺序排列的步骤ID列表 |

---

### 3、审批实例管理

#### 10.1 获取审批实例列表

**接口地址：** `GET /admin/approval-instances`

**请求参数：**

| 参数名         | 类型    | 必填 | 说明                                          |
| -------------- | ------- | ---- | --------------------------------------------- |
| approvableType | String  | 否   | 模型类型：Archive/AwardApplication/CareerPlan |
| status         | Integer | 否   | 1=审批中 2=已通过 3=已驳回 4=已撤回 5=需修改  |
| applicantId    | Integer | 否   | 申请人ID                                      |
| startDate      | String  | 否   | 开始日期                                      |
| endDate        | String  | 否   | 结束日期                                      |
| page           | Integer | 否   | 页码，默认1                                   |
| pageSize       | Integer | 否   | 每页条数，默认10                              |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 50,
    "list": [
      {
        "id": 100,
        "approvableType": "Archive",
        "approvableId": 50,
        "applicableSubType": "academic_competition",
        "flowId": 1,
        "flowVersion": 1,
        "flowName": "学科竞赛审批流程",
        "applicantId": 1001,
        "applicantName": "张三",
        "applicantNo": "2024060001",
        "currentStep": 2,
        "totalSteps": 3,
        "status": 1,
        "statusLabel": "审批中",
        "createdAt": "2026-07-01 10:00:00",
        "completedAt": null,
        "nodes": [
          {
            "stepNo": 1,
            "stepName": "辅导员初审",
            "assignedAuditorName": "王老师",
            "action": 1,
            "actionLabel": "通过",
            "comment": "材料齐全",
            "completedAt": "2026-07-02 09:00:00"
          },
          {
            "stepNo": 2,
            "stepName": "系主任复审",
            "assignedAuditorName": "李老师",
            "action": null,
            "startedAt": "2026-07-02 09:00:00"
          }
        ]
      }
    ]
  }
}
```

---

#### 10.2 获取审批实例详情

**接口地址：** `GET /admin/approval-instances/{instanceId}`

---

#### 10.3 强制终止审批实例

**接口地址：** `PUT /admin/approval-instances/{instanceId}/terminate`

**请求参数：**

| 参数名 | 类型   | 必填 | 说明     |
| ------ | ------ | ---- | -------- |
| reason | String | 是   | 终止原因 |

---

### 4、审批委托管理

#### 10.1 创建审批委托

**接口地址：** `POST /approval/delegations`

**请求参数：**

| 参数名      | 类型    | 必填 | 说明                                      |
| ----------- | ------- | ---- | ----------------------------------------- |
| delegateeId | Integer | 是   | 受托人ID                                  |
| roleId      | Integer | 否   | 委托的角色范围                            |
| scopeType   | Integer | 否   | 委托范围类型：1=学校 2=学院 3=专业 4=班级 |
| scopeId     | Integer | 否   | 委托范围ID                                |
| flowId      | Integer | 否   | 指定委托某个流程（null=全部）             |
| startTime   | String  | 是   | 委托开始时间，格式 `YYYY-MM-DD HH:mm:ss`  |
| endTime     | String  | 是   | 委托结束时间，格式 `YYYY-MM-DD HH:mm:ss`  |
| reason      | String  | 否   | 委托原因                                  |

**请求示例：**
```json
{
  "delegateeId": 200,
  "roleId": 3,
  "scopeType": 4,
  "scopeId": 10,
  "startTime": "2026-07-10 00:00:00",
  "endTime": "2026-07-20 23:59:59",
  "reason": "出差委托"
}
```

---

#### 10.2 获取审批委托列表

**接口地址：** `GET /approval/delegations`

**请求参数：**

| 参数名 | 类型    | 必填 | 说明                       |
| ------ | ------- | ---- | -------------------------- |
| status | Integer | 否   | 0=已取消 1=生效中 2=已过期 |
| page   | Integer | 否   | 页码，默认1                |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 3,
    "list": [
      {
        "id": 1,
        "delegateeId": 200,
        "delegateeName": "赵老师",
        "roleName": "辅导员",
        "scopeType": 4,
        "scopeTypeLabel": "班级",
        "scopeName": "计科2401班",
        "flowName": null,
        "startTime": "2026-07-10 00:00:00",
        "endTime": "2026-07-20 23:59:59",
        "reason": "出差委托",
        "status": 1,
        "statusLabel": "生效中",
        "createdAt": "2026-07-08 10:00:00"
      }
    ]
  }
}
```

---

#### 10.3 取消审批委托

**接口地址：** `PUT /approval/delegations/{delegationId}/cancel`

---

### 5、待审批任务（审批人视角）

#### 10.1 获取我的待审批列表

**接口地址：** `GET /approval/my-pending`

**请求参数：**

| 参数名   | 类型    | 必填 | 说明             |
| -------- | ------- | ---- | ---------------- |
| page     | Integer | 否   | 页码，默认1      |
| pageSize | Integer | 否   | 每页条数，默认10 |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 5,
    "list": [
      {
        "pendingId": 1,
        "nodeId": 50,
        "instanceId": 100,
        "approvableType": "Archive",
        "approvableId": 50,
        "categoryLabel": "学科竞赛",
        "title": "全国大学生数学建模竞赛",
        "applicantName": "张三",
        "applicantNo": "2024060001",
        "content": "申请登记全国大学生数学建模竞赛参赛信息",
        "submitTime": "2026-07-01 10:00:00",
        "stepNo": 2,
        "stepName": "系主任复审",
        "roleName": "系主任",
        "status": 1,
        "statusLabel": "待审批",
        "timeoutAt": "2026-07-04 10:00:00"
      }
    ]
  }
}
```

---

#### 10.2 获取待审批详情

**接口地址：** `GET /approval/my-pending/{pendingId}`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "pendingId": 1,
    "nodeId": 50,
    "applicationId": 50,
    "approvableType": "Archive",
    "applicableSubType": "academic_competition",
    "categoryLabel": "学科竞赛",
    "title": "全国大学生数学建模竞赛",
    "applicantName": "张三",
    "applicantNo": "2024060001",
    "applicantClass": "计科2401班",
    "applicantMajor": "计算机科学与技术",
    "submitTime": "2026-07-01 10:00:00",
    "content": "申请登记全国大学生数学建模竞赛参赛信息",
    "detail": {
      "semesterId": 3,
      "semesterName": "2023-2024-1",
      "competitionName": "全国大学生数学建模竞赛",
      "competitionType": "mathematical_modeling",
      "awardLevel": "second",
      "obtainTime": "2025-09",
      "evidenceFiles": [
        {"fileId": 10001, "fileName": "获奖证书.pdf", "fileUrl": "/uploads/..."}
      ]
    },
    "approvalHistory": [
      {
        "stepNo": 1,
        "stepName": "辅导员初审",
        "auditorName": "王老师",
        "action": 1,
        "actionLabel": "通过",
        "comment": "材料齐全，同意上报",
        "completedAt": "2026-07-02 09:00:00"
      }
    ],
    "currentStep": {
      "stepNo": 2,
      "stepName": "系主任复审",
      "allowDelegate": 1,
      "allowSkip": 0,
      "allowDesignateNext": 1,
      "timeoutHours": 72,
      "rejectAction": "return",
      "rejectToStep": 1
    }
  }
}
```

---

#### 10.3 审批操作

**接口地址：** `POST /approval/my-pending/{pendingId}/action`

**请求参数：**

| 参数名        | 类型    | 必填 | 说明                                       |
| ------------- | ------- | ---- | ------------------------------------------ |
| action        | Integer | 是   | 1=通过 2=驳回 3=转交 4=跳过                |
| comment       | String  | 否   | 审批意见                                   |
| nextAuditorId | Integer | 否   | 指定下一审批人ID（allowDesignateNext=1时） |
| nextNodeId    | Integer | 否   | 动态指定下一节点ID（打破预设流程）         |

**请求示例（通过）：**
```json
{
  "action": 1,
  "comment": "材料齐全，审核通过"
}
```

**请求示例（驳回）：**
```json
{
  "action": 2,
  "comment": "缺少获奖证书原件扫描件"
}
```

**请求示例（转交）：**
```json
{
  "action": 3,
  "comment": "请张主任协助审核",
  "nextAuditorId": 300
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "审批成功",
  "data": {
    "nodeId": 50,
    "action": 1,
    "nextStep": {
      "stepNo": 3,
      "stepName": "院长终审",
      "assignedAuditorName": "刘院长"
    }
  }
}
```

## 十一、AI 对话模块

### 11.1 创建对话会话

**接口地址：** `POST /ai/conversations`

**请求参数：**

| 参数名  | 类型   | 必填 | 说明                     |
| ------- | ------ | ---- | ------------------------ |
| title   | String | 否   | 会话标题，不传则自动生成 |
| context | Object | 否   | 系统上下文/人设配置JSON  |

**请求示例：**
```json
{
  "title": "职业规划咨询",
  "context": {
    "role": "career_advisor",
    "language": "zh"
  }
}
```

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "conversationId": 100,
    "title": "职业规划咨询",
    "createdAt": "2026-07-08 10:00:00"
  }
}
```

---

### 11.2 获取对话会话列表

**接口地址：** `GET /ai/conversations`

**请求参数：**

| 参数名   | 类型    | 必填 | 说明             |
| -------- | ------- | ---- | ---------------- |
| page     | Integer | 否   | 页码，默认1      |
| pageSize | Integer | 否   | 每页条数，默认20 |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 15,
    "list": [
      {
        "id": 100,
        "title": "职业规划咨询",
        "status": 1,
        "lastMessageTime": "2026-07-08 10:30:00",
        "createdAt": "2026-07-08 10:00:00"
      }
    ]
  }
}
```

---

### 11.3 获取对话消息列表

**接口地址：** `GET /ai/conversations/{conversationId}/messages`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "conversationId": 100,
    "messages": [
      {
        "id": 1,
        "role": "user",
        "content": "请帮我分析我的职业规划",
        "createdAt": "2026-07-08 10:00:00"
      },
      {
        "id": 2,
        "role": "assistant",
        "content": "根据您的档案信息，我建议...",
        "modelName": "gpt-4",
        "tokenUsage": 256,
        "generationTimeMs": 1200,
        "createdAt": "2026-07-08 10:00:05"
      }
    ]
  }
}
```

---

### 11.4 发送消息

**接口地址：** `POST /ai/conversations/{conversationId}/messages`

**请求参数：**

| 参数名  | 类型   | 必填 | 说明     |
| ------- | ------ | ---- | -------- |
| content | String | 是   | 消息内容 |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "messageId": 3,
    "role": "assistant",
    "content": "根据您的档案信息...",
    "modelName": "gpt-4",
    "tokenUsage": 512,
    "generationTimeMs": 2100,
    "createdAt": "2026-07-08 10:05:00"
  }
}
```

---

### 11.5 删除对话会话

**接口地址：** `DELETE /ai/conversations/{conversationId}`

---

## 十二、系统管理模块（管理员）

### 12.1 用户管理

#### 11.1.1 获取用户列表

**接口地址：** `GET /admin/users`

**请求参数：**

| 参数名    | 类型    | 必填 | 说明                    |
| --------- | ------- | ---- | ----------------------- |
| keyword   | String  | 否   | 搜索关键词（姓名/学号） |
| schoolId  | Integer | 否   | 学校ID                  |
| collegeId | Integer | 否   | 学院ID                  |
| majorId   | Integer | 否   | 专业ID                  |
| classId   | Integer | 否   | 班级ID                  |
| status    | Integer | 否   | 状态：0=禁用 1=正常     |
| page      | Integer | 否   | 页码，默认1             |
| pageSize  | Integer | 否   | 每页条数，默认20        |

---

#### 11.1.2 新增用户

**接口地址：** `POST /admin/users`

**请求参数：**

| 参数名          | 类型    | 必填 | 说明                     |
| --------------- | ------- | ---- | ------------------------ |
| schoolId        | Integer | 是   | 学校ID                   |
| userNo          | String  | 是   | 学号/工号                |
| name            | String  | 是   | 姓名                     |
| gender          | Integer | 否   | 0=未知 1=男 2=女         |
| email           | String  | 否   | 邮箱                     |
| phone           | String  | 否   | 电话                     |
| password        | String  | 是   | 密码（明文，后端Bcrypt） |
| roleIds         | Array   | 否   | 角色ID列表               |
| classId         | Integer | 否   | 班级ID（学生必填）       |
| politicalStatus | String  | 否   | 政治面貌                 |

---

#### 11.1.3 更新用户

**接口地址：** `PUT /admin/users/{userId}`

---

#### 11.1.4 删除用户（软删除）

**接口地址：** `DELETE /admin/users/{userId}`

---

#### 11.1.5 批量导入用户

**接口地址：** `POST /admin/users/import`

**请求参数：**

| 参数名   | 类型    | 必填 | 说明               |
| -------- | ------- | ---- | ------------------ |
| file     | File    | 是   | Excel文件（.xlsx） |
| schoolId | Integer | 是   | 目标学校ID         |

**Excel模板字段：**

| 列名             | 必填 | 说明                     |
| ---------------- | ---- | ------------------------ |
| user_no          | 是   | 学号/工号                |
| name             | 是   | 姓名                     |
| gender           | 否   | 男/女/未知               |
| email            | 否   | 邮箱                     |
| phone            | 否   | 电话                     |
| password         | 是   | 初始密码                 |
| role_codes       | 否   | 角色编码，多个用逗号分隔 |
| college_code     | 否   | 学院编码                 |
| major_code       | 否   | 专业编码                 |
| class_name       | 否   | 班级名称                 |
| grade            | 否   | 年级，如 2024级          |
| political_status | 否   | 政治面貌                 |

**响应示例：**
```json
{
  "code": 200,
  "message": "导入成功",
  "data": {
    "totalCount": 100,
    "successCount": 98,
    "failCount": 2,
    "failDetails": [
      {"row": 5, "userNo": "2024060005", "reason": "学号已存在"},
      {"row": 12, "userNo": "2024060012", "reason": "邮箱格式错误"}
    ]
  }
}
```

---

#### 11.1.6 下载导入模板

**接口地址：** `GET /admin/users/import-template`

**响应：** Excel模板文件下载

---

### 12.2 成绩管理

#### 11.2.1 获取成绩列表

**接口地址：** `GET /admin/grades`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明             |
| ---------- | ------- | ---- | ---------------- |
| userId     | Integer | 否   | 学生ID           |
| semesterId | Integer | 否   | 学期ID           |
| keyword    | String  | 否   | 课程名称关键词   |
| page       | Integer | 否   | 页码，默认1      |
| pageSize   | Integer | 否   | 每页条数，默认20 |

---

#### 11.2.2 新增成绩记录

**接口地址：** `POST /admin/grades`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明              |
| ---------- | ------- | ---- | ----------------- |
| userId     | Integer | 是   | 学生ID            |
| semesterId | Integer | 是   | 学期ID            |
| courseCode | String  | 否   | 课程编码          |
| courseName | String  | 是   | 课程名称          |
| attemptNo  | Integer | 否   | 修读次数，默认1   |
| score      | Decimal | 否   | 期末成绩（0-100） |
| gpa        | Decimal | 否   | 绩点（0-5.00）    |
| credit     | Decimal | 否   | 学分              |

---

#### 11.2.3 批量导入成绩

**接口地址：** `POST /admin/grades/import`

**请求参数：**

| 参数名     | 类型    | 必填 | 说明               |
| ---------- | ------- | ---- | ------------------ |
| file       | File    | 是   | Excel文件（.xlsx） |
| semesterId | Integer | 是   | 目标学期ID         |

**Excel模板字段：**

| 列名        | 必填 | 说明            |
| ----------- | ---- | --------------- |
| user_no     | 是   | 学号            |
| course_code | 否   | 课程编码        |
| course_name | 是   | 课程名称        |
| attempt_no  | 否   | 修读次数，默认1 |
| score       | 否   | 期末成绩        |
| gpa         | 否   | 绩点            |
| credit      | 否   | 学分            |

**响应示例：**
```json
{
  "code": 200,
  "message": "导入成功",
  "data": {
    "importLogId": 50,
    "totalCount": 500,
    "successCount": 495,
    "failCount": 5,
    "failDetails": [
      {"row": 10, "userNo": "2024060010", "courseName": "高等数学", "reason": "学号不存在"},
      {"row": 25, "userNo": "2024060025", "courseName": "英语", "reason": "成绩超出范围(0-100)"}
    ]
  }
}
```

---

#### 11.2.4 获取导入历史

**接口地址：** `GET /admin/grades/import-logs`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 10,
    "list": [
      {
        "id": 50,
        "operatorName": "管理员",
        "semesterName": "2023-2024-1",
        "totalCount": 500,
        "successCount": 495,
        "failCount": 5,
        "importStatus": 1,
        "statusLabel": "已完成",
        "startedAt": "2026-07-08 09:00:00",
        "completedAt": "2026-07-08 09:05:00"
      }
    ]
  }
}
```

---

#### 11.2.5 下载成绩导入模板

**接口地址：** `GET /admin/grades/import-template`

**响应：** Excel模板文件下载

---

### 12.3 组织架构管理

#### 11.3.1 学校管理

| 接口                  | 方法   | 说明         |
| --------------------- | ------ | ------------ |
| `/admin/schools`      | GET    | 获取学校列表 |
| `/admin/schools`      | POST   | 新增学校     |
| `/admin/schools/{id}` | PUT    | 更新学校     |
| `/admin/schools/{id}` | DELETE | 删除学校     |

---

#### 11.3.2 学院管理

| 接口                   | 方法   | 说明         |
| ---------------------- | ------ | ------------ |
| `/admin/colleges`      | GET    | 获取学院列表 |
| `/admin/colleges`      | POST   | 新增学院     |
| `/admin/colleges/{id}` | PUT    | 更新学院     |
| `/admin/colleges/{id}` | DELETE | 删除学院     |

---

#### 11.3.3 专业管理

| 接口                 | 方法   | 说明         |
| -------------------- | ------ | ------------ |
| `/admin/majors`      | GET    | 获取专业列表 |
| `/admin/majors`      | POST   | 新增专业     |
| `/admin/majors/{id}` | PUT    | 更新专业     |
| `/admin/majors/{id}` | DELETE | 删除专业     |

---

#### 11.3.4 班级管理

| 接口                  | 方法   | 说明         |
| --------------------- | ------ | ------------ |
| `/admin/classes`      | GET    | 获取班级列表 |
| `/admin/classes`      | POST   | 新增班级     |
| `/admin/classes/{id}` | PUT    | 更新班级     |
| `/admin/classes/{id}` | DELETE | 删除班级     |

---

### 12.4 角色权限管理

#### 11.4.1 角色管理

| 接口                            | 方法   | 说明                            |
| ------------------------------- | ------ | ------------------------------- |
| `/admin/roles`                  | GET    | 获取角色列表                    |
| `/admin/roles`                  | POST   | 新增角色                        |
| `/admin/roles/{id}`             | PUT    | 更新角色                        |
| `/admin/roles/{id}`             | DELETE | 删除角色（is_system=1不可删除） |
| `/admin/roles/{id}/permissions` | GET    | 获取角色权限                    |
| `/admin/roles/{id}/permissions` | PUT    | 更新角色权限                    |

---

#### 11.4.2 权限管理

| 接口                      | 方法   | 说明       |
| ------------------------- | ------ | ---------- |
| `/admin/permissions`      | GET    | 获取权限树 |
| `/admin/permissions`      | POST   | 新增权限   |
| `/admin/permissions/{id}` | PUT    | 更新权限   |
| `/admin/permissions/{id}` | DELETE | 删除权限   |

---

### 12.5 审批流程配置

#### 11.5.1 流程管理

| 接口                               | 方法 | 说明         |
| ---------------------------------- | ---- | ------------ |
| `/admin/approval-flows`            | GET  | 获取流程列表 |
| `/admin/approval-flows`            | POST | 新增流程     |
| `/admin/approval-flows/{id}`       | PUT  | 更新流程     |
| `/admin/approval-flows/{id}/copy`  | POST | 复制流程     |
| `/admin/approval-flows/{id}/steps` | GET  | 获取流程步骤 |
| `/admin/approval-flows/{id}/steps` | POST | 配置流程步骤 |

---

### 12.6 系统配置

#### 11.6.1 系统设置

| 接口              | 方法 | 说明         |
| ----------------- | ---- | ------------ |
| `/admin/settings` | GET  | 获取系统配置 |
| `/admin/settings` | PUT  | 更新系统配置 |

---

#### 11.6.2 数据字典

| 接口                       | 方法   | 说明         |
| -------------------------- | ------ | ------------ |
| `/admin/dictionaries`      | GET    | 获取字典列表 |
| `/admin/dictionaries`      | POST   | 新增字典项   |
| `/admin/dictionaries/{id}` | PUT    | 更新字典项   |
| `/admin/dictionaries/{id}` | DELETE | 删除字典项   |

---

#### 11.6.3 公告管理

| 接口                        | 方法   | 说明         |
| --------------------------- | ------ | ------------ |
| `/admin/announcements`      | GET    | 获取公告列表 |
| `/admin/announcements`      | POST   | 发布公告     |
| `/admin/announcements/{id}` | PUT    | 更新公告     |
| `/admin/announcements/{id}` | DELETE | 删除公告     |

---

### 12.7 系统日志

#### 11.7.1 操作日志

**接口地址：** `GET /admin/logs/system`

**请求参数：**

| 参数名    | 类型    | 必填 | 说明                             |
| --------- | ------- | ---- | -------------------------------- |
| module    | String  | 否   | 操作模块                         |
| logLevel  | Integer | 否   | 1=普通日志 2=用户动态 3=审计日志 |
| startDate | String  | 否   | 开始日期                         |
| endDate   | String  | 否   | 结束日期                         |
| page      | Integer | 否   | 页码，默认1                      |
| pageSize  | Integer | 否   | 每页条数，默认20                 |

---

#### 11.7.2 登录日志

**接口地址：** `GET /admin/logs/login`

**请求参数：**

| 参数名      | 类型    | 必填 | 说明             |
| ----------- | ------- | ---- | ---------------- |
| userId      | Integer | 否   | 用户ID           |
| loginStatus | Integer | 否   | 0=失败 1=成功    |
| startDate   | String  | 否   | 开始日期         |
| endDate     | String  | 否   | 结束日期         |
| page        | Integer | 否   | 页码，默认1      |
| pageSize    | Integer | 否   | 每页条数，默认20 |



---

## 十三、数据字典（接口枚举值）

### 14.1 通用状态

| 值   | 说明                       |
| ---- | -------------------------- |
| 0    | 禁用/草稿/失败/未读        |
| 1    | 启用/正常/待审批/成功/已读 |
| 2    | 已通过/已过期              |
| 3    | 已驳回                     |
| 4    | 需修改                     |

### 14.2 性别

| 值   | 说明 |
| ---- | ---- |
| 0    | 未知 |
| 1    | 男   |
| 2    | 女   |

### 14.3 审批动作

| 值   | 说明 |
| ---- | ---- |
| 1    | 通过 |
| 2    | 驳回 |
| 3    | 转交 |
| 4    | 跳过 |

### 14.4 消息发送渠道

| 值    | 说明     |
| ----- | -------- |
| push  | 站内推送 |
| email | 邮件     |
| sms   | 短信     |

### 14.5 文件分类

| 值          | 说明     |
| ----------- | -------- |
| certificate | 证书     |
| photo       | 照片     |
| proof       | 证明材料 |
| other       | 其他     |

---

