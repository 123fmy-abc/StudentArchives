# 数据库修改记录 - users

### 记录 1：字段删除

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-07-29 |
| 修改人 | fmy |
| 变更类型 | 字段删除 |
| 变更字段 | remember_token |
| 变更原因 | remember_token 列已不再使用（记住我功能使用 JWT RefreshToken 实现），通过 Flyway V14 迁移清理 |

**SQL 语句：**
```sql
ALTER TABLE `users` DROP COLUMN `remember_token`;
```

**回滚 SQL：**
```sql
ALTER TABLE `users` ADD COLUMN `remember_token` VARCHAR(100) NULL DEFAULT NULL COMMENT '记住我令牌' AFTER `password`;
```

---

### 记录 2：字段添加

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-08-01 |
| 修改人 | fmy |
| 变更类型 | 字段添加 |
| 变更字段 | career_actions.completed_at |
| 变更原因 | 对齐《学生端接口文档》4.12「更新行动状态」：`status=2` 时需同步更新 `completed_at`，原 V8 建表缺少该列，通过 Flyway V18 迁移补齐 |

**SQL 语句：**
```sql
ALTER TABLE `career_actions`
    ADD COLUMN `completed_at` DATETIME NULL DEFAULT NULL COMMENT '完成时间（status=2 时由应用层写入）' AFTER `completion_rate`;
```

**回滚 SQL：**
```sql
ALTER TABLE `career_actions` DROP COLUMN `completed_at`;
```

---

### 记录 3：字段修改

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-08-05 |
| 修改人 | fmy |
| 变更类型 | 字段修改 |
| 变更字段 | export_jobs.template_id |
| 变更原因 | 学生端档案导出（POST /profile/export）不涉及导出模板，原 V12 建表将 `template_id` 定义为 `NOT NULL`，而 `ProfileExportService` 创建任务时未赋值，导致 INSERT 触发非空约束（`DataIntegrityViolationException`，报"数据校验失败，请检查提交内容"）。通过 Flyway V19 迁移改为可空 |

**SQL 语句：**
```sql
ALTER TABLE `export_jobs`
    MODIFY COLUMN `template_id` BIGINT UNSIGNED NULL DEFAULT NULL COMMENT '关联 export_templates.id（学生端档案导出不涉及模板，可空）';
```

**回滚 SQL：**
```sql
ALTER TABLE `export_jobs`
    MODIFY COLUMN `template_id` BIGINT UNSIGNED NOT NULL COMMENT '关联 export_templates.id';
```

---

### 记录 4：应用层字段扩展（无表结构变更）

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-08-05 |
| 修改人 | fmy |
| 变更类型 | 应用层字段扩展 |
| 变更字段 | `export_jobs.filter_conditions`、`export_operation_logs.filter_conditions` |
| 变更原因 | 导出接口新增 `purpose` 用途参数（`internal`/`external`），用于区分内部查看与外部投递场景的水印策略；该参数以 JSON 快照形式写入既有 `filter_conditions` 字段，未新增数据库列 |

**SQL 语句：** 无（应用层变更，无需 DDL）

**回滚 SQL：** 无

---

### 记录 5：字段添加

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-08-06 |
| 修改人 | fmy |
| 变更类型 | 字段添加 |
| 变更字段 | `majors.degree_type`、`student_profiles.student_status` |
| 变更原因 | 支持简历/档案展示学生的学历层次（专科/本科/硕士/博士/博士后）与学生状态（在校生/应届毕业生/已毕业）。学历层次由专业决定，学生状态由学生自行维护 |

**SQL 语句：**
```sql
ALTER TABLE `majors`
    ADD COLUMN `degree_type` VARCHAR(30) NULL DEFAULT NULL COMMENT '学历层次：associate专科/undergraduate本科/master硕士/doctor博士/postdoctor博士后' AFTER `code`;

ALTER TABLE `student_profiles`
    ADD COLUMN `student_status` VARCHAR(30) NULL DEFAULT NULL COMMENT '学生状态：current在校生/fresh_graduate应届毕业生/graduated已毕业' AFTER `political_status`;
```

**回滚 SQL：**
```sql
ALTER TABLE `majors` DROP COLUMN `degree_type`;
ALTER TABLE `student_profiles` DROP COLUMN `student_status`;
```

---

### 记录 6：字段添加

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-08-11 |
| 修改人 | fmy |
| 变更类型 | 字段添加 |
| 变更字段 | `evaluation_indicators.description` |
| 变更原因 | 对齐《管理端接口文档》一、指标配置模块创建/更新接口（1.2/1.3）的 `description` 参数：管理端指标配置支持填写指标说明，原 V1 建表缺少该列，通过 Flyway V24 迁移补齐 |

**SQL 语句：**
```sql
ALTER TABLE `evaluation_indicators`
    ADD COLUMN `description` VARCHAR(500) NULL DEFAULT NULL COMMENT '指标说明' AFTER `weight`;
```

**回滚 SQL：**
```sql
ALTER TABLE `evaluation_indicators` DROP COLUMN `description`;
```

---

### 记录 7：字段添加

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-08-11 |
| 修改人 | fmy |
| 变更类型 | 字段添加 |
| 变更字段 | `indicator_rule_versions.tree_snapshot` |
| 变更原因 | 支持按历史版本查询指标树：`evaluation_indicators.version` 仅记录“当前版本号戳记”，发布时被整体覆盖；`indicator_versions` 仅快照 weight/scoring_rule，无法还原完整历史指标树。本列在每次发布时写入完整指标树 JSON（含结构字段），供学生端 `GET /common/indicators?versionId=xx` 精确回溯历史版本，也作为评分计算的权威树快照。通过 Flyway V25 迁移补齐 |

**SQL 语句：**
```sql
ALTER TABLE `indicator_rule_versions`
    ADD COLUMN `tree_snapshot` JSON NULL COMMENT '发布时点的完整指标树快照（JSON，含指标结构字段），供按历史版本查询/评分追溯' AFTER `effective_at`;
```

**回滚 SQL：**
```sql
ALTER TABLE `indicator_rule_versions` DROP COLUMN `tree_snapshot`;
```

---

### 记录 8：字段添加

| 项目 | 内容 |
|------|------|
| 修改日期 | 2026-08-11 |
| 修改人 | fmy |
| 变更类型 | 字段添加 |
| 变更字段 | `indicator_rule_versions.semester_id`（含 `(school_id, semester_id)` 索引） |
| 变更原因 | 支持管理端按学期过滤指标树/规则版本：指标配置为学校维度，发布版本此前无学期归属，`GET /admin/indicators/tree` 的 `semesterId` 仅做存在性校验。本列记录每次发布版本归属的学期（`null`=不限定学期），按学期查询指标树时取该学期最近发布的版本快照，未发布过的学期回退当前草稿树。通过 Flyway V26 迁移补齐 |

**SQL 语句：**
```sql
ALTER TABLE `indicator_rule_versions`
    ADD COLUMN `semester_id` BIGINT NULL COMMENT '发布版本归属学期（semesters.id，null=不限定学期）' AFTER `school_id`;
CREATE INDEX idx_indicator_rule_versions_school_semester
    ON `indicator_rule_versions` (`school_id`, `semester_id`);
```

**回滚 SQL：**
```sql
ALTER TABLE `indicator_rule_versions` DROP INDEX `idx_indicator_rule_versions_school_semester`;
ALTER TABLE `indicator_rule_versions` DROP COLUMN `semester_id`;
```

---

## 汇总

| 序号 | 日期 | 类型 | 字段/对象 | 修改人 |
|------|------|------|-----------|--------|
| 1 | 2026-07-29 | 字段删除 | users.remember_token | fmy |
| 2 | 2026-08-01 | 字段添加 | career_actions.completed_at | fmy |
| 3 | 2026-08-05 | 字段修改 | export_jobs.template_id | fmy |
| 4 | 2026-08-05 | 应用层字段扩展 | export_jobs.filter_conditions / export_operation_logs.filter_conditions | fmy |
| 5 | 2026-08-06 | 字段添加 | majors.degree_type / student_profiles.student_status | fmy |
| 6 | 2026-08-11 | 字段添加 | evaluation_indicators.description | fmy |
| 7 | 2026-08-11 | 字段添加 | indicator_rule_versions.tree_snapshot | fmy |
| 8 | 2026-08-11 | 字段添加 | indicator_rule_versions.semester_id | fmy |
